problems:
- code: P01
  name_ko: 발행 데이터 없음 (no_result)
  severity: MAJOR
  family: pipeline
  hook: result_parse
  automation_level: human_approval
  one_line_action: 후보 소진인지, 일시 수집 공백인지, 품질·중복 guard 차단인지 구분 후 reason 세분화, 제한된 재시도만 적용
  target_files:
  - path: dispatcher.py
    hint: no_result 반환 경로(디스패처 result_parse 단계), _fetch_for_blog 계열 가드 로직
  - path: config/blogs.d/*.yaml
    hint: 해당 블로그의 fetch_sources, schedule 정의
  verify:
  - GET /api/registry?blog_id={blog_id}에서 해당 problem_id 상태 확인
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - no_result
  - no_data
  - fetch_error
  - escalation
  - no_topic
  - no_topics
  - no_trade_data
  - no_subscription_data
  threshold: consecutive:3
  alert_template: '⚠️ [MAJOR] 발행 데이터 없음

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    연속 실패: {consecutive}회

    조치: {action}'
  action: 데이터 수집 소스/API 상태 확인 후 재발행
  playbook_ref: ERROR_PLAYBOOKS.md#p01
  summary_human: '발행 데이터 없음 (no_result): 후보 소진인지, 일시 수집 공백인지, 품질·중복 guard 차단인지 구분 후 reason 세분화, 제한된 재시도만 적용.'
  summary_llm: '{"problem_id": "P01", "name_ko": "발행 데이터 없음 (no_result)", "severity": "MAJOR", "family": "pipeline", "one_line_action": "후보 소진인지, 일시 수집 공백인지, 품질·중복 guard 차단인지 구분 후 reason 세분화, 제한된 재시도만 적용", "needs_human": false, "target_files": [{"path": "dispatcher.py", "hint": "no_result 반환 경로(디스패처 result_parse 단계), _fetch_for_blog 계열 가드 로직"}, {"path": "config/blogs.d/*.yaml", "hint": "해당 블로그의 fetch_sources, schedule 정의"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P02
  name_ko: 콘텐츠 생성 실패 (no_content)
  severity: MAJOR
  family: pipeline
  hook: result_parse
  automation_level: human_approval
  one_line_action: 원문 수집 실패, LLM 응답 공백, 콘텐츠 후처리 실패 중 실패 지점별로 오류 전파·fallback·입력 검증 보완
  target_files:
  - path: dispatcher.py
    hint: result_parse 단계, no_content reason 처리
  - path: shared/ai_writer.py
    hint: GPT 호출·응답 검증 로직
  - path: pipelines/*/pipeline.py
    hint: 각 파이프라인 generate/run 함수, 제목/본문 생성 부분
  verify:
  - GET /api/registry?blog_id={blog_id}에서 해당 problem_id 상태 확인
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - no_content
  - write_error
  - publish_error
  - publish
  - generation_failed
  - prompt_not_found
  - publish_failed
  - write_failed
  threshold: consecutive:3
  alert_template: '⚠️ [MAJOR] 콘텐츠 생성 실패

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    연속 실패: {consecutive}회

    조치: {action}'
  action: 프롬프트/모델 설정 점검 후 콘텐츠 재생성 — 최초 1회는 `python3 dispatcher.py {blog_id}` 수동 재시도 (transient 여부 확인), 재발 시 logs/scheduler.log에서 {blog_id} 실패 stage 확인
  playbook_ref: ERROR_PLAYBOOKS.md#p02
  summary_human: '콘텐츠 생성 실패 (no_content): 원문 수집 실패, LLM 응답 공백, 콘텐츠 후처리 실패 중 실패 지점별로 오류 전파·fallback·입력 검증 보완.'
  summary_llm: '{"problem_id": "P02", "name_ko": "콘텐츠 생성 실패 (no_content)", "severity": "MAJOR", "family": "pipeline", "one_line_action": "원문 수집 실패, LLM 응답 공백, 콘텐츠 후처리 실패 중 실패 지점별로 오류 전파·fallback·입력 검증 보완", "needs_human": false, "target_files": [{"path": "dispatcher.py", "hint": "result_parse 단계, no_content reason 처리"}, {"path": "shared/ai_writer.py", "hint": "GPT 호출·응답 검증 로직"}, {"path": "pipelines/*/pipeline.py", "hint": "각 파이프라인 generate/run 함수, 제목/본문 생성 부분"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P03
  name_ko: 유사 제목 중복 (similar_title)
  severity: MAJOR
  family: pipeline
  hook: result_parse
  automation_level: human_approval
  one_line_action: 실제 제목 유사도·비교 대상 범위·언어/템플릿 오탐 확인 후 유사도 비교의 입력 정규화 또는 후보 선택 보정
  target_files:
  - path: shared/content_store.py
    hint: title_similar_exists(), SequenceMatcher 80% 임계값 비교 로직
  - path: dispatcher.py
    hint: similar_title reason 처리 경로
  verify:
  - GET /api/registry?blog_id={blog_id}에서 해당 problem_id 상태 확인
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - similar_title
  threshold: consecutive:3
  alert_template: '⚠️ [MAJOR] 발행 불가 — 유사 제목 중복

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    연속 실패: {consecutive}회

    조치: {action}'
  action: 키워드별 제목 변형 다양화 검토
  playbook_ref: ERROR_PLAYBOOKS.md#p03
  summary_human: '유사 제목 중복 (similar_title): 실제 제목 유사도·비교 대상 범위·언어/템플릿 오탐 확인 후 유사도 비교의 입력 정규화 또는 후보 선택 보정.'
  summary_llm: '{"problem_id": "P03", "name_ko": "유사 제목 중복 (similar_title)", "severity": "MAJOR", "family": "pipeline", "one_line_action": "실제 제목 유사도·비교 대상 범위·언어/템플릿 오탐 확인 후 유사도 비교의 입력 정규화 또는 후보 선택 보정", "needs_human": false, "target_files": [{"path": "shared/content_store.py", "hint": "title_similar_exists(), SequenceMatcher 80% 임계값 비교 로직"}, {"path": "dispatcher.py", "hint": "similar_title reason 처리 경로"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P04
  name_ko: 배포 실패 (deploy_error)
  severity: CRITICAL
  family: deploy
  hook: post_deploy
  automation_level: human_approval
  one_line_action: 빌드·배포 명령의 실제 실패 지점만 수정; 잘못된 환경변수 참조·경로를 최소 수정
  target_files:
  - path: shared/publishers/deploy.py
    hint: _deploy_site_inner(), wrangler 호출·환경변수 구성
  - path: dispatcher.py
    hint: _build_and_deploy_central(), 배포 락·에러 전파
  - path: wrangler.toml
    hint: Workers 블로그 설정 (health/pet/kitchen/beauty/camping/baby)
  verify:
  - '로컬 Hugo 빌드: HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes hugo --gc --minify --source {site_path}'
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - deploy_error
  - deploy
  threshold: always
  alert_template: '🚨 [CRITICAL] 배포 실패

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    조치: {action}'
  action: wrangler 배포 로그 확인 후 재배포
  playbook_ref: ERROR_PLAYBOOKS.md#p04
  summary_human: '배포 실패 (deploy_error): 빌드·배포 명령의 실제 실패 지점만 수정; 잘못된 환경변수 참조·경로를 최소 수정.'
  summary_llm: '{"problem_id": "P04", "name_ko": "배포 실패 (deploy_error)", "severity": "CRITICAL", "family": "deploy", "one_line_action": "빌드·배포 명령의 실제 실패 지점만 수정; 잘못된 환경변수 참조·경로를 최소 수정", "needs_human": false, "target_files": [{"path": "shared/publishers/deploy.py", "hint": "_deploy_site_inner(), wrangler 호출·환경변수 구성"}, {"path": "dispatcher.py", "hint": "_build_and_deploy_central(), 배포 락·에러 전파"}, {"path": "wrangler.toml", "hint": "Workers 블로그 설정 (health/pet/kitchen/beauty/camping/baby)"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P05
  name_ko: Hugo 빌드 실패
  severity: CRITICAL
  family: deploy
  hook: post_deploy
  automation_level: human_approval
  one_line_action: 테마/경로/Front Matter 문법을 원인 파일에 한정해 수정
  target_files:
  - path: HUGO_THEMESDIR 환경변수 (/Users/twinssn/Projects/shared-themes)
    hint: Hugo 테마 경로 설정
  - path: content/posts/*/index.md
    hint: .failed 포스트의 frontmatter YAML 문법 확인 (컬론 뒤 공백, 따옴표 등)
  - path: hugo.toml
    hint: 테마 설정, params.toml 연동
  verify:
  - '로컬 Hugo 빌드: HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes hugo --gc --minify --source {site_path} (에러 0건 확인)'
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - hugo_build_failed
  - build_failed
  threshold: always
  alert_template: '🚨 [CRITICAL] Hugo 빌드 실패

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    조치: {action}'
  action: Hugo 테마/themesDir 점검 후 재빌드
  playbook_ref: ERROR_PLAYBOOKS.md#p05
  summary_human: 'Hugo 빌드 실패: 테마/경로/Front Matter 문법을 원인 파일에 한정해 수정.'
  summary_llm: '{"problem_id": "P05", "name_ko": "Hugo 빌드 실패", "severity": "CRITICAL", "family": "deploy", "one_line_action": "테마/경로/Front Matter 문법을 원인 파일에 한정해 수정", "needs_human": false, "target_files": [{"path": "HUGO_THEMESDIR 환경변수 (/Users/twinssn/Projects/shared-themes)", "hint": "Hugo 테마 경로 설정"}, {"path": "content/posts/*/index.md", "hint": ".failed 포스트의 frontmatter YAML 문법 확인 (컬론 뒤 공백, 따옴표 등)"}, {"path": "hugo.toml", "hint": "테마 설정, params.toml 연동"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P06
  name_ko: 썸네일 404 (broken_featureimage)
  severity: CRITICAL
  family: publish
  hook: post_publish
  automation_level: human_approval
  one_line_action: 잘못된 참조 또는 누락된 업로드만 복구; 기존 미디어를 삭제하지 않음 — batch_thumbnails.py로 썸네일 생성 후 R2 업로드
  target_files:
  - path: scripts/batch_thumbnails.py
    hint: --slug 인자로 썸네일 생성·R2 업로드·frontmatter 업데이트 실행
  - path: shared/thumbnail_generator/generator.py
    hint: 썸네일 이미지 생성 로직
  - path: shared/r2_uploader.py
    hint: Cloudflare R2 S3 호환 업로드
  - path: shared/publishers/hugo_writer.py
    hint: featureimage frontmatter 업데이트
  - path: content/posts/{slug}/index.md
    hint: featureimage 필드가 가리키는 URL 확인 (R2 pub-<hash>.r2.dev/webp 여부)
  verify:
  - featureimage URL HTTP HEAD/GET 200 확인
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - broken_featureimage
  threshold: always
  alert_template: '🚨 [CRITICAL] 썸네일 404

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    조치: {action}'
  action: batch_thumbnails.py로 썸네일 생성 후 R2 업로드
  playbook_ref: ERROR_PLAYBOOKS.md#p06
  summary_human: '썸네일 404 (broken_featureimage): 잘못된 참조 또는 누락된 업로드만 복구; 기존 미디어를 삭제하지 않음 — batch_thumbnails.py로 썸네일 생성 후 R2 업로드.'
  summary_llm: '{"problem_id": "P06", "name_ko": "썸네일 404 (broken_featureimage)", "severity": "CRITICAL", "family": "publish", "one_line_action": "잘못된 참조 또는 누락된 업로드만 복구; 기존 미디어를 삭제하지 않음 — batch_thumbnails.py로 썸네일 생성 후 R2 업로드", "needs_human": false, "target_files": [{"path": "scripts/batch_thumbnails.py", "hint": "--slug 인자로 썸네일 생성·R2 업로드·frontmatter 업데이트 실행"}, {"path": "shared/thumbnail_generator/generator.py", "hint": "썸네일 이미지 생성 로직"}, {"path": "shared/r2_uploader.py", "hint": "Cloudflare R2 S3 호환 업로드"}, {"path": "shared/publishers/hugo_writer.py", "hint": "featureimage frontmatter 업데이트"}, {"path": "content/posts/{slug}/index.md", "hint": "featureimage 필드가 가리키는 URL 확인 (R2 pub-<hash>.r2.dev/webp 여부)"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P07
  name_ko: CJK 누수
  severity: CRITICAL
  family: content
  hook: post_generate
  automation_level: full_auto
  one_line_action: 스캐너(scan_multilingual_leak.py)로 확인 후 프롬프트/후처리/차단 로직 수정; 감지기가 맞는지 샘플 확인
  target_files:
  - path: shared/ai_writer.py
    hint: GPT 응답 후처리·CJK 필터 로직
  - path: shared/problem_detectors.py
    hint: CJK 누수 탐지 함수 (프롬프트 패턴 매칭)
  - path: scripts/scan_multilingual_leak.py
    hint: 다국어 누수 스캔 도구
  - path: config/prompts.yaml
    hint: AI 글쓰기 프롬프트 — CJK 차단 지시문 확인
  verify:
  - 오염·정상 fixture 모두 검사 (스캔 스크립트 실행)
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - cjk_leak
  threshold: always
  alert_template: '🚨 [CRITICAL] 콘텐츠 오염 — CJK 누수 감지

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    감지 패턴: {pattern}

    조치: {action}'
  action: scan_multilingual_leak.py로 확인 후 재생성
  playbook_ref: ERROR_PLAYBOOKS.md#p07
  summary_human: 'CJK 누수: 스캐너(scan_multilingual_leak.py)로 확인 후 프롬프트/후처리/차단 로직 수정; 감지기가 맞는지 샘플 확인.'
  summary_llm: '{"problem_id": "P07", "name_ko": "CJK 누수", "severity": "CRITICAL", "family": "content", "one_line_action": "스캐너(scan_multilingual_leak.py)로 확인 후 프롬프트/후처리/차단 로직 수정; 감지기가 맞는지 샘플 확인", "needs_human": false, "target_files": [{"path": "shared/ai_writer.py", "hint": "GPT 응답 후처리·CJK 필터 로직"}, {"path": "shared/problem_detectors.py", "hint": "CJK 누수 탐지 함수 (프롬프트 패턴 매칭)"}, {"path": "scripts/scan_multilingual_leak.py", "hint": "다국어 누수 스캔 도구"}, {"path": "config/prompts.yaml", "hint": "AI 글쓰기 프롬프트 — CJK 차단 지시문 확인"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P08
  name_ko: LLM/CoT 누수
  severity: CRITICAL
  family: content
  hook: post_generate
  automation_level: full_auto
  one_line_action: 원문 노출을 막는 후처리·프롬프트 경계 보완; 추론 본문을 저장하지 않음
  target_files:
  - path: shared/ai_writer.py
    hint: LLM 응답에서 CoT/추론 본문 필터링 로직
  - path: shared/problem_detectors.py
    hint: CoT 누수 탐지 함수
  - path: config/prompts.yaml
    hint: AI 프롬프트 — 추론 과정 출력 방지 지시문 확인
  verify:
  - 안전·정상 fixture 테스트 (누수 패턴 포함/미포함)
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - llm_cot_leak
  - cot_leak
  threshold: always
  alert_template: '🚨 [CRITICAL] 콘텐츠 오염 — LLM/CoT 누수 감지

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    감지 패턴: {pattern}

    조치: {action}'
  action: scan_multilingual_leak.py로 확인 후 재생성
  playbook_ref: ERROR_PLAYBOOKS.md#p08
  summary_human: 'LLM/CoT 누수: 원문 노출을 막는 후처리·프롬프트 경계 보완; 추론 본문을 저장하지 않음.'
  summary_llm: '{"problem_id": "P08", "name_ko": "LLM/CoT 누수", "severity": "CRITICAL", "family": "content", "one_line_action": "원문 노출을 막는 후처리·프롬프트 경계 보완; 추론 본문을 저장하지 않음", "needs_human": false, "target_files": [{"path": "shared/ai_writer.py", "hint": "LLM 응답에서 CoT/추론 본문 필터링 로직"}, {"path": "shared/problem_detectors.py", "hint": "CoT 누수 탐지 함수"}, {"path": "config/prompts.yaml", "hint": "AI 프롬프트 — 추론 과정 출력 방지 지시문 확인"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P09
  name_ko: 이미지 URL 토큰 반복
  severity: CRITICAL
  family: content
  hook: post_generate
  automation_level: full_auto
  one_line_action: URL 토큰 분리·중복 제거 로직을 수정하되 이미지 정책을 우회하지 않음
  target_files:
  - path: shared/publishers/hugo_writer.py
    hint: IMAGE-GUARD, sanitize_featureimage_url(), 이미지 URL 삽입 로직
  - path: shared/problem_detectors.py
    hint: image_url_repeat 탐지 함수 (토큰 반복 패턴 매칭)
  verify:
  - 반복·정상 URL fixture 검사
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - image_url_repeat
  threshold: always
  alert_template: '🚨 [CRITICAL] 콘텐츠 오염 — 이미지 URL 토큰 반복

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    감지 패턴: {pattern}

    조치: {action}'
  action: 이미지 URL 토큰 반복 제거 후 재생성
  playbook_ref: ERROR_PLAYBOOKS.md#p09
  summary_human: '이미지 URL 토큰 반복: URL 토큰 분리·중복 제거 로직을 수정하되 이미지 정책을 우회하지 않음.'
  summary_llm: '{"problem_id": "P09", "name_ko": "이미지 URL 토큰 반복", "severity": "CRITICAL", "family": "content", "one_line_action": "URL 토큰 분리·중복 제거 로직을 수정하되 이미지 정책을 우회하지 않음", "needs_human": false, "target_files": [{"path": "shared/publishers/hugo_writer.py", "hint": "IMAGE-GUARD, sanitize_featureimage_url(), 이미지 URL 삽입 로직"}, {"path": "shared/problem_detectors.py", "hint": "image_url_repeat 탐지 함수 (토큰 반복 패턴 매칭)"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P10
  name_ko: 제목 템플릿 패턴 (title_blocked)
  severity: MAJOR
  family: pipeline
  hook: result_parse
  automation_level: human_approval
  one_line_action: 제목 생성 규칙·정규식만 최소 수정; 금칙어 완화는 승인 필요
  target_files:
  - path: pipelines/*/pipeline.py
    hint: 제목 생성 함수, 블랙키워드/차단 패턴 정의
  - path: shared/content_store.py
    hint: title_blocked 관련 가드 로직 (블로그/템플릿 패턴 차단)
  verify:
  - 차단·통과 제목 fixture 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - title_blocked
  threshold: consecutive:3
  alert_template: '⚠️ [MAJOR] 발행 불가 — 제목 템플릿/차단 키워드

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    연속 실패: {consecutive}회

    조치: {action}'
  action: 제목 템플릿/블랙키워드 점검
  playbook_ref: ERROR_PLAYBOOKS.md#p10
  summary_human: '제목 템플릿 패턴 (title_blocked): 제목 생성 규칙·정규식만 최소 수정; 금칙어 완화는 승인 필요.'
  summary_llm: '{"problem_id": "P10", "name_ko": "제목 템플릿 패턴 (title_blocked)", "severity": "MAJOR", "family": "pipeline", "one_line_action": "제목 생성 규칙·정규식만 최소 수정; 금칙어 완화는 승인 필요", "needs_human": false, "target_files": [{"path": "pipelines/*/pipeline.py", "hint": "제목 생성 함수, 블랙키워드/차단 패턴 정의"}, {"path": "shared/content_store.py", "hint": "title_blocked 관련 가드 로직 (블로그/템플릿 패턴 차단)"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P11
  name_ko: 제목 재생성 실패
  severity: MAJOR
  family: pipeline
  hook: result_parse
  automation_level: human_approval
  one_line_action: bounded retry와 실패 사유 전파 보완; 무한 재시도 금지
  target_files:
  - path: pipelines/*/pipeline.py
    hint: 제목 재생성 함수, retry budget, fallback 체인
  - path: shared/ai_writer.py
    hint: 제목 생성용 GPT 호출·응답 검증
  verify:
  - retry budget 테스트 (mock 응답)
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - title_regenerate_failed
  threshold: consecutive:3
  alert_template: '⚠️ [MAJOR] 발행 불가 — 제목 재생성 실패

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    연속 실패: {consecutive}회

    조치: {action}'
  action: 제목 생성 프롬프트/모델 확인 후 재생성
  playbook_ref: ERROR_PLAYBOOKS.md#p11
  summary_human: '제목 재생성 실패: bounded retry와 실패 사유 전파 보완; 무한 재시도 금지.'
  summary_llm: '{"problem_id": "P11", "name_ko": "제목 재생성 실패", "severity": "MAJOR", "family": "pipeline", "one_line_action": "bounded retry와 실패 사유 전파 보완; 무한 재시도 금지", "needs_human": false, "target_files": [{"path": "pipelines/*/pipeline.py", "hint": "제목 재생성 함수, retry budget, fallback 체인"}, {"path": "shared/ai_writer.py", "hint": "제목 생성용 GPT 호출·응답 검증"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P12
  name_ko: 콘텐츠 품질 게이트 차단
  severity: MAJOR
  family: pipeline
  hook: result_parse
  automation_level: human_approval
  one_line_action: 생성·후처리 품질 보완 또는 검사기 결함 수정; 품질 게이트 비활성화 금지
  target_files:
  - path: pipelines/*/pipeline.py
    hint: quality gate 체크 함수, 콘텐츠 검증 단계
  - path: shared/problem_detectors.py
    hint: content_quality_gate / language_error 탐지 함수
  verify:
  - 실패·정상 콘텐츠 fixture 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - content_quality_gate
  - language_error
  threshold: consecutive:3
  alert_template: '⚠️ [MAJOR] 발행 불가 — 콘텐츠 품질 게이트

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    연속 실패: {consecutive}회

    조치: {action}'
  action: 콘텐츠 품질 게이트 통과 기준 확인
  playbook_ref: ERROR_PLAYBOOKS.md#p12
  summary_human: '콘텐츠 품질 게이트 차단: 생성·후처리 품질 보완 또는 검사기 결함 수정; 품질 게이트 비활성화 금지.'
  summary_llm: '{"problem_id": "P12", "name_ko": "콘텐츠 품질 게이트 차단", "severity": "MAJOR", "family": "pipeline", "one_line_action": "생성·후처리 품질 보완 또는 검사기 결함 수정; 품질 게이트 비활성화 금지", "needs_human": false, "target_files": [{"path": "pipelines/*/pipeline.py", "hint": "quality gate 체크 함수, 콘텐츠 검증 단계"}, {"path": "shared/problem_detectors.py", "hint": "content_quality_gate / language_error 탐지 함수"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P13
  name_ko: API 차단 (rate_limited)
  severity: MAJOR
  family: pipeline
  hook: result_parse
  automation_level: human_approval
  one_line_action: backoff·호출 간격·캐시·작업 큐를 보완; 할당량 증가·키 교체는 승인 필요
  target_files:
  - path: shared/ai_writer.py
    hint: LLM 호출 backoff·rate limit 처리, 모델별 재시도 로직
  - path: shared/coupang_senior.py
    hint: Coupang API 호출·rate limit 처리 (해당 파이프라인 경우)
  verify:
  - rate-limit mock, 재시도 상한 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - rate_limited
  threshold: consecutive:3
  alert_template: '⚠️ [MAJOR] API 차단

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    연속 실패: {consecutive}회

    조치: {action}'
  action: API 할당량 초기화 후 재시도
  playbook_ref: ERROR_PLAYBOOKS.md#p13
  summary_human: 'API 차단 (rate_limited): backoff·호출 간격·캐시·작업 큐를 보완; 할당량 증가·키 교체는 승인 필요.'
  summary_llm: '{"problem_id": "P13", "name_ko": "API 차단 (rate_limited)", "severity": "MAJOR", "family": "pipeline", "one_line_action": "backoff·호출 간격·캐시·작업 큐를 보완; 할당량 증가·키 교체는 승인 필요", "needs_human": false, "target_files": [{"path": "shared/ai_writer.py", "hint": "LLM 호출 backoff·rate limit 처리, 모델별 재시도 로직"}, {"path": "shared/coupang_senior.py", "hint": "Coupang API 호출·rate limit 처리 (해당 파이프라인 경우)"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P14
  name_ko: 수집/품질 게이트 실패
  severity: MAJOR
  family: pipeline
  hook: result_parse
  automation_level: human_approval
  one_line_action: 소스·필터를 정확히 보정; 실패 reason을 구체화; 기준 완화는 승인 필요
  target_files:
  - path: pipelines/*/pipeline.py
    hint: 데이터 수집·필터 단계, 후보 유효성 체크, 차단 사유 구체화
  - path: config/blogs.d/*.yaml
    hint: 해당 블로그의 fetch_sources, 필터 설정
  verify:
  - source fixture + guard 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - no_keyword
  - collect_error
  - insufficient_products
  - irrelevant_products
  - low_relevance
  threshold: consecutive:3
  alert_template: '⚠️ [MAJOR] 발행 불가 — 수집/품질 게이트

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    연속 실패: {consecutive}회

    조치: {action}'
  action: '수집 소스/필터 기준 점검 — irrelevant_products 반복 시 키워드 오염 의심: `python3 -c "import sys; sys.path.insert(0, ''.''); from pipelines.curation.keywords import get_keywords; [print(k) for k in get_keywords(''{blog_id}'')]"` 로 정크 여부 확인 (read-time 필터가 KEYWORD_MAP 오염은 자동 차단)'
  playbook_ref: ERROR_PLAYBOOKS.md#p14
  summary_human: '수집/품질 게이트 실패: 소스·필터를 정확히 보정; 실패 reason을 구체화; 기준 완화는 승인 필요.'
  summary_llm: '{"problem_id": "P14", "name_ko": "수집/품질 게이트 실패", "severity": "MAJOR", "family": "pipeline", "one_line_action": "소스·필터를 정확히 보정; 실패 reason을 구체화; 기준 완화는 승인 필요", "needs_human": false, "target_files": [{"path": "pipelines/*/pipeline.py", "hint": "데이터 수집·필터 단계, 후보 유효성 체크, 차단 사유 구체화"}, {"path": "config/blogs.d/*.yaml", "hint": "해당 블로그의 fetch_sources, 필터 설정"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P15
  name_ko: 발행 후 검증 실패
  severity: MAJOR
  family: validation
  hook: post_validate
  automation_level: human_approval
  one_line_action: 검사 항목·라이브 HTML·생성 파일·검사기 자체 오류를 분리; 실제 결함은 콘텐츠·템플릿에 최소 수정, 검사기 오탐은 검사기만 수정
  target_files:
  - path: shared/problem_detectors.py
    hint: 발행 후 검증 함수, 검사 항목별 탐지 로직
  - path: shared/publishers/hugo_writer.py
    hint: Hugo frontmatter 정리·검증 단계
  - path: content/posts/{slug}/index.md
    hint: 실제 생성된 포스트 파일 — 검증 실패 항목 확인
  verify:
  - 단위 fixture + 라이브 샘플 확인
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - validation
  - validation_failed
  threshold: consecutive:3
  alert_template: '⚠️ [MAJOR] 발행 후 검증 실패

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    연속 실패: {consecutive}회

    조치: {action}'
  action: 검증 항목별 문제 수정 후 재발행
  playbook_ref: ERROR_PLAYBOOKS.md#p15
  summary_human: '발행 후 검증 실패: 검사 항목·라이브 HTML·생성 파일·검사기 자체 오류를 분리; 실제 결함은 콘텐츠·템플릿에 최소 수정, 검사기 오탐은 검사기만 수정.'
  summary_llm: '{"problem_id": "P15", "name_ko": "발행 후 검증 실패", "severity": "MAJOR", "family": "validation", "one_line_action": "검사 항목·라이브 HTML·생성 파일·검사기 자체 오류를 분리; 실제 결함은 콘텐츠·템플릿에 최소 수정, 검사기 오탐은 검사기만 수정", "needs_human": false, "target_files": [{"path": "shared/problem_detectors.py", "hint": "발행 후 검증 함수, 검사 항목별 탐지 로직"}, {"path": "shared/publishers/hugo_writer.py", "hint": "Hugo frontmatter 정리·검증 단계"}, {"path": "content/posts/{slug}/index.md", "hint": "실제 생성된 포스트 파일 — 검증 실패 항목 확인"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P16
  name_ko: 중복 slug/source_id
  severity: MINOR
  family: pipeline
  hook: result_parse
  automation_level: human_approval
  one_line_action: 재고 선택·상태 기록·idempotency를 수정; 중복 guard 우회·기존 글 삭제 금지
  target_files:
  - path: pipelines/*/pipeline.py
    hint: slug/source_id 생성·중복 체크 로직, 상태 기록
  - path: content.db
    hint: publish_ledger 테이블의 slug/source_id 중복 확인 (SQLite 직접 조회)
  verify:
  - 동시 실행·중복 fixture 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - duplicate_slug
  - duplicate_source_id
  threshold: quiet
  alert_template: 'ℹ️ [MINOR] 중복 slug/source_id

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}'
  action: slug/source_id 중복 제거 후 재발행
  playbook_ref: ERROR_PLAYBOOKS.md#p16
  summary_human: '중복 slug/source_id: 재고 선택·상태 기록·idempotency를 수정; 중복 guard 우회·기존 글 삭제 금지.'
  summary_llm: '{"problem_id": "P16", "name_ko": "중복 slug/source_id", "severity": "MINOR", "family": "pipeline", "one_line_action": "재고 선택·상태 기록·idempotency를 수정; 중복 guard 우회·기존 글 삭제 금지", "needs_human": false, "target_files": [{"path": "pipelines/*/pipeline.py", "hint": "slug/source_id 생성·중복 체크 로직, 상태 기록"}, {"path": "content.db", "hint": "publish_ledger 테이블의 slug/source_id 중복 확인 (SQLite 직접 조회)"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P17
  name_ko: 일일 할당량 도달
  severity: MINOR
  family: scheduler
  hook: result_parse
  automation_level: full_auto
  one_line_action: 정상 상태면 suppressed; 집계 버그만 수정; quota를 오류처럼 재시도하지 않음
  target_files:
  - path: dispatcher.py
    hint: quota_met/daily_quota 처리, suppressed 분기 로직
  - path: config/blogs.d/*.yaml
    hint: 해당 블로그의 daily_quota 정의
  verify:
  - timezone·경계 시각 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - quota_met
  - quota_exceeded
  - daily_quota_exceeded
  - daily_quota
  - duplicate_title
  - daily_quota_reached
  threshold: quiet
  alert_template: 'ℹ️ [MINOR] 일일 할당량 도달

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}'
  action: 일일 할당량 초기화 대기
  playbook_ref: ERROR_PLAYBOOKS.md#p17
  summary_human: '일일 할당량 도달: 정상 상태면 suppressed; 집계 버그만 수정; quota를 오류처럼 재시도하지 않음.'
  summary_llm: '{"problem_id": "P17", "name_ko": "일일 할당량 도달", "severity": "MINOR", "family": "scheduler", "one_line_action": "정상 상태면 suppressed; 집계 버그만 수정; quota를 오류처럼 재시도하지 않음", "needs_human": false, "target_files": [{"path": "dispatcher.py", "hint": "quota_met/daily_quota 처리, suppressed 분기 로직"}, {"path": "config/blogs.d/*.yaml", "hint": "해당 블로그의 daily_quota 정의"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P18
  name_ko: 동시 실행 방지
  severity: MINOR
  family: scheduler
  hook: result_parse
  automation_level: full_auto
  one_line_action: stale lock 여부를 증거로 확인한 뒤 정리 로직 보완; 활성 작업 종료는 승인
  target_files:
  - path: dispatcher.py
    hint: already_running 처리, lock 파일 확인·정리 로직
  - path: /tmp/.lock_{blog_id}
    hint: 파이프라인 동시 실행 방지 락 파일 (존재 시 stale 여부 확인)
  verify:
  - 정상 lock·stale lock 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - already_running
  threshold: quiet
  alert_template: 'ℹ️ [MINOR] 동시 실행 방지

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}'
  action: 이전 실행 종료 확인 후 재실행
  playbook_ref: ERROR_PLAYBOOKS.md#p18
  summary_human: '동시 실행 방지: stale lock 여부를 증거로 확인한 뒤 정리 로직 보완; 활성 작업 종료는 승인.'
  summary_llm: '{"problem_id": "P18", "name_ko": "동시 실행 방지", "severity": "MINOR", "family": "scheduler", "one_line_action": "stale lock 여부를 증거로 확인한 뒤 정리 로직 보완; 활성 작업 종료는 승인", "needs_human": false, "target_files": [{"path": "dispatcher.py", "hint": "already_running 처리, lock 파일 확인·정리 로직"}, {"path": "/tmp/.lock_{blog_id}", "hint": "파이프라인 동시 실행 방지 락 파일 (존재 시 stale 여부 확인)"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P19
  name_ko: 오래된/만료 데이터
  severity: MINOR
  family: validation
  hook: post_validate
  automation_level: human_approval
  one_line_action: 데이터 유효기간·발행 일정·외부 이벤트 종료일 확인; 실제 stale이면 갱신 작업으로 큐화; 대량 갱신·삭제는 승인
  target_files:
  - path: shared/problem_detectors.py
    hint: stale/expired 탐지 함수, 데이터 유효기간 체크
  - path: pipelines/*/pipeline.py
    hint: 데이터 소스 갱신 로직, 만료 데이터 처리
  verify:
  - 새 데이터·날짜 경계 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - stale
  - stale_data
  - event_expired
  - expired_service
  threshold: quiet
  alert_template: 'ℹ️ [MINOR] 오래된/만료 데이터

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}'
  action: 오래된 데이터 갱신/제거
  playbook_ref: ERROR_PLAYBOOKS.md#p19
  summary_human: '오래된/만료 데이터: 데이터 유효기간·발행 일정·외부 이벤트 종료일 확인; 실제 stale이면 갱신 작업으로 큐화; 대량 갱신·삭제는 승인.'
  summary_llm: '{"problem_id": "P19", "name_ko": "오래된/만료 데이터", "severity": "MINOR", "family": "validation", "one_line_action": "데이터 유효기간·발행 일정·외부 이벤트 종료일 확인; 실제 stale이면 갱신 작업으로 큐화; 대량 갱신·삭제는 승인", "needs_human": false, "target_files": [{"path": "shared/problem_detectors.py", "hint": "stale/expired 탐지 함수, 데이터 유효기간 체크"}, {"path": "pipelines/*/pipeline.py", "hint": "데이터 소스 갱신 로직, 만료 데이터 처리"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P20
  name_ko: subprocess 에러
  severity: MAJOR
  family: pipeline
  hook: result_parse
  automation_level: human_approval
  one_line_action: 입력 인자·결과 계약·정리를 수정; shell 문자열 조합·광범위 kill 금지
  target_files:
  - path: dispatcher.py
    hint: STAP/TAP subprocess 호출부 (_run_stap, _run_tap 등), exit code·stdout/stderr 수집
  - path: logs/
    hint: subprocess 실행 로그 (stap_timeout, tap_timeout 등)
  - path: /Users/twinssn/Projects/STAP
    hint: STAP subprocess 프로젝트 (별도 venv, 별도 프로세스)
  - path: /Users/twinssn/Projects/TAP
    hint: TAP subprocess 프로젝트
  verify:
  - subprocess mock·exit code 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - stap_timeout
  - tap_timeout
  - stap_subprocess_error
  - stap_no_output
  - stap_error
  - tap_error
  - stap_not_found
  - tap_not_found
  - tap_subprocess_error
  - dispatch_returned_none
  threshold: consecutive:3
  alert_template: '⚠️ [MAJOR] subprocess 실행 에러

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    연속 실패: {consecutive}회

    조치: {action}'
  action: STAP/TAP subprocess 로그 확인 후 재실행
  playbook_ref: ERROR_PLAYBOOKS.md#p20
  summary_human: 'subprocess 에러: 입력 인자·결과 계약·정리를 수정; shell 문자열 조합·광범위 kill 금지.'
  summary_llm: '{"problem_id": "P20", "name_ko": "subprocess 에러", "severity": "MAJOR", "family": "pipeline", "one_line_action": "입력 인자·결과 계약·정리를 수정; shell 문자열 조합·광범위 kill 금지", "needs_human": false, "target_files": [{"path": "dispatcher.py", "hint": "STAP/TAP subprocess 호출부 (_run_stap, _run_tap 등), exit code·stdout/stderr 수집"}, {"path": "logs/", "hint": "subprocess 실행 로그 (stap_timeout, tap_timeout 등)"}, {"path": "/Users/twinssn/Projects/STAP", "hint": "STAP subprocess 프로젝트 (별도 venv, 별도 프로세스)"}, {"path": "/Users/twinssn/Projects/TAP", "hint": "TAP subprocess 프로젝트"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P21
  name_ko: 설정 오류 (config_error)
  severity: MAJOR
  family: pipeline
  hook: result_parse
  automation_level: full_auto
  one_line_action: 스키마 검증·명확한 오류 메시지·누락 기본값 보완; 비밀값을 로그·대시보드에 저장 금지
  target_files:
  - path: config/blogs.d/*.yaml
    hint: 블로그 정의 YAML — id, pipeline, platform, schedule, site_path 등 스키마 확인
  - path: config/blogs.yaml
    hint: 마스터 스케줄·설정 (blogs.d와 병합됨)
  - path: dispatcher.py
    hint: 블로그 설정 로드·스키마 검증, unknown_pipeline/unknown_blog_id/inactive 처리
  verify:
  - 유효·누락·잘못된 YAML fixture 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - config_error
  - unknown_pipeline
  - unknown_blog_id
  - inactive
  - unknown_stap_blog
  - config
  - pipeline
  threshold: consecutive:3
  alert_template: '⚠️ [MAJOR] 설정 오류

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    연속 실패: {consecutive}회

    조치: {action}'
  action: 블로그 설정(blogs.d) 확인 후 수정
  playbook_ref: ERROR_PLAYBOOKS.md#p21
  summary_human: '설정 오류 (config_error): 스키마 검증·명확한 오류 메시지·누락 기본값 보완; 비밀값을 로그·대시보드에 저장 금지.'
  summary_llm: '{"problem_id": "P21", "name_ko": "설정 오류 (config_error)", "severity": "MAJOR", "family": "pipeline", "one_line_action": "스키마 검증·명확한 오류 메시지·누락 기본값 보완; 비밀값을 로그·대시보드에 저장 금지", "needs_human": false, "target_files": [{"path": "config/blogs.d/*.yaml", "hint": "블로그 정의 YAML — id, pipeline, platform, schedule, site_path 등 스키마 확인"}, {"path": "config/blogs.yaml", "hint": "마스터 스케줄·설정 (blogs.d와 병합됨)"}, {"path": "dispatcher.py", "hint": "블로그 설정 로드·스키마 검증, unknown_pipeline/unknown_blog_id/inactive 처리"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P22
  name_ko: LLM 폴백 체인 전체 실패
  severity: MAJOR
  family: pipeline
  hook: result_parse
  automation_level: human_approval
  one_line_action: 모델 호출 실패 전파·fallback 순서·입력 제한 보정; 모델 계정·결제·키 변경은 승인
  target_files:
  - path: shared/ai_writer.py
    hint: LLM fallback chain — config/models.yaml tier_order 순차 시도 로직
  - path: config/models.yaml
    hint: LLM provider 모델 목록, tier_order, thinking 모드 설정
  - path: api_keys.yaml
    hint: API 키 (gitignore됨) — provider 키 존재 여부 확인
  verify:
  - 각 fallback mock 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - llm_fallback_exhausted
  threshold: consecutive:3
  alert_template: '⚠️ [MAJOR] LLM 폴백 체인 전체 실패

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    연속 실패: {consecutive}회

    조치: {action}'
  action: 모델 폴백 체인/provider 키 점검
  playbook_ref: ERROR_PLAYBOOKS.md#p22
  summary_human: 'LLM 폴백 체인 전체 실패: 모델 호출 실패 전파·fallback 순서·입력 제한 보정; 모델 계정·결제·키 변경은 승인.'
  summary_llm: '{"problem_id": "P22", "name_ko": "LLM 폴백 체인 전체 실패", "severity": "MAJOR", "family": "pipeline", "one_line_action": "모델 호출 실패 전파·fallback 순서·입력 제한 보정; 모델 계정·결제·키 변경은 승인", "needs_human": false, "target_files": [{"path": "shared/ai_writer.py", "hint": "LLM fallback chain — config/models.yaml tier_order 순차 시도 로직"}, {"path": "config/models.yaml", "hint": "LLM provider 모델 목록, tier_order, thinking 모드 설정"}, {"path": "api_keys.yaml", "hint": "API 키 (gitignore됨) — provider 키 존재 여부 확인"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P23
  name_ko: 이미지 URL 길이 초과
  severity: MINOR
  family: content
  hook: post_generate
  automation_level: full_auto
  one_line_action: URL 생성기·서명 파라미터·이미지 호스트 정책 확인; 길이 제한을 무시하지 않고 URL 축약 또는 업로드 경로 수정
  target_files:
  - path: shared/publishers/hugo_writer.py
    hint: sanitize_featureimage_url() — max_len 파라미터, URL 축약 로직
  - path: shared/image_handler.py
    hint: 이미지 URL 생성·서명 파라미터 처리
  verify:
  - 최대 길이 fixture, 실제 URL 요청 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - image_url_length
  threshold: quiet
  alert_template: 'ℹ️ [MINOR] 이미지 URL 길이 초과

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}'
  action: 이미지 URL 길이 축소
  playbook_ref: ERROR_PLAYBOOKS.md#p23
  summary_human: '이미지 URL 길이 초과: URL 생성기·서명 파라미터·이미지 호스트 정책 확인; 길이 제한을 무시하지 않고 URL 축약 또는 업로드 경로 수정.'
  summary_llm: '{"problem_id": "P23", "name_ko": "이미지 URL 길이 초과", "severity": "MINOR", "family": "content", "one_line_action": "URL 생성기·서명 파라미터·이미지 호스트 정책 확인; 길이 제한을 무시하지 않고 URL 축약 또는 업로드 경로 수정", "needs_human": false, "target_files": [{"path": "shared/publishers/hugo_writer.py", "hint": "sanitize_featureimage_url() — max_len 파라미터, URL 축약 로직"}, {"path": "shared/image_handler.py", "hint": "이미지 URL 생성·서명 파라미터 처리"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P24
  name_ko: 검증 함수 자체 결함
  severity: MINOR
  family: validation
  hook: post_validate
  automation_level: full_auto
  one_line_action: 오류가 콘텐츠가 아닌 검사기의 DB·정규식·입력 형태·예외 처리에서 왔는지 확인; 검사 기준 승격·완화는 승인
  target_files:
  - path: shared/problem_detectors.py
    hint: validation_defect 탐지 함수, 검증 로직 자체
  - path: ops_dashboard/checks/
    hint: 각종 검사기 함수 (표준.py, content_integrity.py, maintenance.py 등)
  verify:
  - 실패 재현 + 정상/오탐 fixture 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - validation_defect
  threshold: quiet
  alert_template: 'ℹ️ [MINOR] 검증 함수 자체 결함

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}'
  action: 검증 함수 자체 점검/수정
  playbook_ref: ERROR_PLAYBOOKS.md#p24
  summary_human: '검증 함수 자체 결함: 오류가 콘텐츠가 아닌 검사기의 DB·정규식·입력 형태·예외 처리에서 왔는지 확인; 검사 기준 승격·완화는 승인.'
  summary_llm: '{"problem_id": "P24", "name_ko": "검증 함수 자체 결함", "severity": "MINOR", "family": "validation", "one_line_action": "오류가 콘텐츠가 아닌 검사기의 DB·정규식·입력 형태·예외 처리에서 왔는지 확인; 검사 기준 승격·완화는 승인", "needs_human": false, "target_files": [{"path": "shared/problem_detectors.py", "hint": "validation_defect 탐지 함수, 검증 로직 자체"}, {"path": "ops_dashboard/checks/", "hint": "각종 검사기 함수 (표준.py, content_integrity.py, maintenance.py 등)"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P25
  name_ko: Scheduler timeout
  severity: CRITICAL
  family: scheduler
  hook: scheduler
  automation_level: human_approval
  one_line_action: 병목 단계를 분리하고 단계별 제한·정리·결과 전파를 보완; timeout 상향만으로 해결하지 않음
  target_files:
  - path: scheduler.py
    hint: 스케줄러 루프, 600초 타임아웃 제한, subprocess 실행 관리
  - path: dispatcher.py
    hint: 파이프라인 실행 진입점, subprocess 호출·시간 측정
  verify:
  - 제한 시간 fixture, 하위 프로세스 정리 테스트, 제한 dry run
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - scheduler_timeout
  - timeout_600s
  - pipeline_timeout_300s
  - pipeline_timeout_600s
  - pipeline_timeout_900s
  threshold: always
  alert_template: '[CRITICAL] Scheduler timeout

    blog: {blog_id}

    problem: {problem_id}

    stage: {phase}

    action: {action}'
  action: 'Review the blocked subprocess and its external request timeouts — preflight 확인: `python3 -c "import sys; sys.path.insert(0, ''.''); from dispatcher import preflight_check; preflight_check(''{blog_id}'')"` (60s 초과 시 corpus cap/샘플링 로그 확인)'
  playbook_ref: ERROR_PLAYBOOKS.md#p25
  summary_human: 'Scheduler timeout: 병목 단계를 분리하고 단계별 제한·정리·결과 전파를 보완; timeout 상향만으로 해결하지 않음.'
  summary_llm: '{"problem_id": "P25", "name_ko": "Scheduler timeout", "severity": "CRITICAL", "family": "scheduler", "one_line_action": "병목 단계를 분리하고 단계별 제한·정리·결과 전파를 보완; timeout 상향만으로 해결하지 않음", "needs_human": false, "target_files": [{"path": "scheduler.py", "hint": "스케줄러 루프, 600초 타임아웃 제한, subprocess 실행 관리"}, {"path": "dispatcher.py", "hint": "파이프라인 실행 진입점, subprocess 호출·시간 측정"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P26
  name_ko: Source unavailable
  severity: MAJOR
  family: fetch
  hook: data_fetch
  automation_level: human_approval
  one_line_action: 캐시 우선, bounded retry, 구체적 reason·HTTP 상태 기록; 데이터 고갈과 혼동하지 않음
  target_files:
  - path: shared/r2_uploader.py
    hint: Cloudflare R2 S3 호환 업로드 — 소스 접근 실패 시 탐지
  - path: shared/blogger_publisher.py
    hint: Google Blogger API 호출·오류 처리
  - path: pipelines/*/fetcher.py
    hint: 데이터 소스 fetch 함수 (API/DB/파일 접근)
  verify:
  - 응답 실패/복구 fixture 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - source_unavailable
  - data_fetch_error
  threshold: consecutive:3
  alert_template: '[MAJOR] Source unavailable

    blog: {blog_id}

    problem: {problem_id}

    stage: {phase}

    action: {action}'
  action: Check source API, database, or source file availability before retrying.
  playbook_ref: ERROR_PLAYBOOKS.md#p26
  summary_human: 'Source unavailable: 캐시 우선, bounded retry, 구체적 reason·HTTP 상태 기록; 데이터 고갈과 혼동하지 않음.'
  summary_llm: '{"problem_id": "P26", "name_ko": "Source unavailable", "severity": "MAJOR", "family": "fetch", "one_line_action": "캐시 우선, bounded retry, 구체적 reason·HTTP 상태 기록; 데이터 고갈과 혼동하지 않음", "needs_human": false, "target_files": [{"path": "shared/r2_uploader.py", "hint": "Cloudflare R2 S3 호환 업로드 — 소스 접근 실패 시 탐지"}, {"path": "shared/blogger_publisher.py", "hint": "Google Blogger API 호출·오류 처리"}, {"path": "pipelines/*/fetcher.py", "hint": "데이터 소스 fetch 함수 (API/DB/파일 접근)"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P27
  name_ko: Source exhausted
  severity: MAJOR
  family: pipeline
  hook: result_parse
  automation_level: human_approval
  one_line_action: 재고 갱신 작업 제안; 오탐이면 후보 SQL·필터 보정; 기준 완화·과거 글 재사용은 승인
  target_files:
  - path: pipelines/*/pipeline.py
    hint: 후보 재고 관리, 미발행 유효 후보 수 체크, 제외 규칙
  - path: pipelines/*/data_collector.py
    hint: get_unused_data() 등 후보 수집 함수 (sector/index/krx 등)
  verify:
  - 후보 수·중복 제외 fixture 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - source_exhausted
  - no_eligible_source
  threshold: consecutive:3
  alert_template: '[MAJOR] Source exhausted

    blog: {blog_id}

    problem: {problem_id}

    stage: {phase}

    action: {action}'
  action: Refresh the source inventory or relax an eligible-item guard.
  playbook_ref: ERROR_PLAYBOOKS.md#p27
  summary_human: 'Source exhausted: 재고 갱신 작업 제안; 오탐이면 후보 SQL·필터 보정; 기준 완화·과거 글 재사용은 승인.'
  summary_llm: '{"problem_id": "P27", "name_ko": "Source exhausted", "severity": "MAJOR", "family": "pipeline", "one_line_action": "재고 갱신 작업 제안; 오탐이면 후보 SQL·필터 보정; 기준 완화·과거 글 재사용은 승인", "needs_human": false, "target_files": [{"path": "pipelines/*/pipeline.py", "hint": "후보 재고 관리, 미발행 유효 후보 수 체크, 제외 규칙"}, {"path": "pipelines/*/data_collector.py", "hint": "get_unused_data() 등 후보 수집 함수 (sector/index/krx 등)"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P28
  name_ko: Invalid pipeline result contract
  severity: MAJOR
  family: pipeline
  hook: result_parse
  automation_level: human_approval
  one_line_action: 반환 타입·필드 검증, 오류를 성공으로 덮지 않는 전파 수정; 예외를 빈 성공 결과로 변환 금지
  target_files:
  - path: dispatcher.py
    hint: result_parse 단계 — 파이프라인 반환값 스키마 검증, success 필드 전파
  - path: pipelines/*/pipeline.py
    hint: run() 함수 반환 타입·필드 (dict, success, reason 등 표준 결과 스키마)
  verify:
  - dict/non-dict/success:false fixture 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - invalid_result_contract
  - non_dict_result
  threshold: consecutive:3
  alert_template: '[MAJOR] Invalid result contract

    blog: {blog_id}

    problem: {problem_id}

    stage: {phase}

    action: {action}'
  action: Update the pipeline to return the standard result dictionary.
  playbook_ref: ERROR_PLAYBOOKS.md#p28
  summary_human: 'Invalid pipeline result contract: 반환 타입·필드 검증, 오류를 성공으로 덮지 않는 전파 수정; 예외를 빈 성공 결과로 변환 금지.'
  summary_llm: '{"problem_id": "P28", "name_ko": "Invalid pipeline result contract", "severity": "MAJOR", "family": "pipeline", "one_line_action": "반환 타입·필드 검증, 오류를 성공으로 덮지 않는 전파 수정; 예외를 빈 성공 결과로 변환 금지", "needs_human": false, "target_files": [{"path": "dispatcher.py", "hint": "result_parse 단계 — 파이프라인 반환값 스키마 검증, success 필드 전파"}, {"path": "pipelines/*/pipeline.py", "hint": "run() 함수 반환 타입·필드 (dict, success, reason 등 표준 결과 스키마)"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P29
  name_ko: Publisher schema mismatch
  severity: CRITICAL
  family: publish
  hook: publish
  automation_level: full_auto
  one_line_action: 입력 정규화 또는 명시적 검증을 추가; 임의 문자열 변환으로 데이터 의미를 잃지 않음
  target_files:
  - path: shared/publishers/hugo_writer.py
    hint: Hugo frontmatter 생성·타입 변환·스키마 검증
  - path: shared/publishers/blogger_writer.py
    hint: Blogger API post 생성·필드 타입 처리
  - path: shared/publishers/wordpress_writer.py
    hint: WordPress XML-RPC post 생성·필드 타입 처리
  verify:
  - dict/list/string/None fixture, Hugo build 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - frontmatter_schema_error
  - publisher_schema_error
  threshold: always
  alert_template: '[CRITICAL] Publisher schema mismatch

    blog: {blog_id}

    problem: {problem_id}

    stage: {phase}

    action: {action}'
  action: Normalize the content field type before creating front matter.
  playbook_ref: ERROR_PLAYBOOKS.md#p29
  summary_human: 'Publisher schema mismatch: 입력 정규화 또는 명시적 검증을 추가; 임의 문자열 변환으로 데이터 의미를 잃지 않음.'
  summary_llm: '{"problem_id": "P29", "name_ko": "Publisher schema mismatch", "severity": "CRITICAL", "family": "publish", "one_line_action": "입력 정규화 또는 명시적 검증을 추가; 임의 문자열 변환으로 데이터 의미를 잃지 않음", "needs_human": false, "target_files": [{"path": "shared/publishers/hugo_writer.py", "hint": "Hugo frontmatter 생성·타입 변환·스키마 검증"}, {"path": "shared/publishers/blogger_writer.py", "hint": "Blogger API post 생성·필드 타입 처리"}, {"path": "shared/publishers/wordpress_writer.py", "hint": "WordPress XML-RPC post 생성·필드 타입 처리"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P30
  name_ko: Content generation failure
  severity: MAJOR
  family: generation
  hook: content_generation
  automation_level: human_approval
  one_line_action: 입력 검증·fallback·명확한 실패 구분; 실제 재생성·발행은 승인
  target_files:
  - path: shared/ai_writer.py
    hint: GPT 호출·응답 검증·실패 구분, 모델별 오류 처리
  - path: pipelines/*/pipeline.py
    hint: 콘텐츠 생성 진입점, generate_*_article() 함수, fallback 호출
  - path: config/prompts.yaml
    hint: AI 글쓰기 프롬프트 — 입력 크기·프롬프트 구조 확인
  verify:
  - provider mock + 정상 생성 fixture 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - content_generation_error
  - ai_generation_error
  threshold: consecutive:3
  alert_template: '[MAJOR] Content generation failure

    blog: {blog_id}

    problem: {problem_id}

    stage: {phase}

    action: {action}'
  action: Check the model response, prompt, and fallback chain before retrying.
  playbook_ref: ERROR_PLAYBOOKS.md#p30
  summary_human: 'Content generation failure: 입력 검증·fallback·명확한 실패 구분; 실제 재생성·발행은 승인.'
  summary_llm: '{"problem_id": "P30", "name_ko": "Content generation failure", "severity": "MAJOR", "family": "generation", "one_line_action": "입력 검증·fallback·명확한 실패 구분; 실제 재생성·발행은 승인", "needs_human": false, "target_files": [{"path": "shared/ai_writer.py", "hint": "GPT 호출·응답 검증·실패 구분, 모델별 오류 처리"}, {"path": "pipelines/*/pipeline.py", "hint": "콘텐츠 생성 진입점, generate_*_article() 함수, fallback 호출"}, {"path": "config/prompts.yaml", "hint": "AI 글쓰기 프롬프트 — 입력 크기·프롬프트 구조 확인"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P31
  name_ko: Telegram delivery failure
  severity: MAJOR
  family: notification
  hook: notification
  automation_level: human_approval
  one_line_action: 감사 레코드, HTTP 상태, bot 권한, chat ID, 메시지 길이·parse mode 확인; token·chat 변경은 승인
  target_files:
  - path: shared/telegram_notifier.py
    hint: Telegram Bot API 호출, HTTP 상태·에러 처리, 메시지 길이·parse mode
  - path: api_keys.yaml
    hint: Telegram bot token (gitignore됨) — 유효 여부 확인
  verify:
  - 전송 성공/4xx/5xx mock, 정제 테스트
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - telegram_delivery_error
  threshold: consecutive:3
  alert_template: '[MAJOR] Telegram delivery failure

    blog: {blog_id}

    problem: {problem_id}

    stage: {phase}

    action: {action}'
  action: Check bot credentials, chat permissions, and Telegram API availability.
  playbook_ref: ERROR_PLAYBOOKS.md#p31
  summary_human: 'Telegram delivery failure: 감사 레코드, HTTP 상태, bot 권한, chat ID, 메시지 길이·parse mode 확인; token·chat 변경은 승인.'
  summary_llm: '{"problem_id": "P31", "name_ko": "Telegram delivery failure", "severity": "MAJOR", "family": "notification", "one_line_action": "감사 레코드, HTTP 상태, bot 권한, chat ID, 메시지 길이·parse mode 확인; token·chat 변경은 승인", "needs_human": false, "target_files": [{"path": "shared/telegram_notifier.py", "hint": "Telegram Bot API 호출, HTTP 상태·에러 처리, 메시지 길이·parse mode"}, {"path": "api_keys.yaml", "hint": "Telegram bot token (gitignore됨) — 유효 여부 확인"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P32
  name_ko: 빈 본문 배포 (empty_content_deployed)
  severity: MAJOR
  family: deploy
  hook: post_deploy
  automation_level: human_approval
  one_line_action: publish_log 최근 글의 public HTML 본문 단어수 200 미만 탐지 → topic 재생성 + 재배포. 가드(ETAP-GUARD)는 _write_hugo_post_etap 진입부에 적용 완료.
  target_files:
  - path: shared/publishers/hugo_writer.py
    hint: _write_hugo_post_etap() ETAP-GUARD (빈 content/최소 단어수 차단)
  - path: ops_dashboard/checks/content_integrity.py
    hint: detect_empty_content_deployed() — public HTML 본문 단어수 감지 함수 (구현 예정)
  - path: data/travel-en.db
    hint: publish_log 테이블 — 최근 글의 slug/topic_id/blog_id 조회
  verify:
  - 로컬 Hugo 빌드: HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes hugo --gc --minify --source {site_path}
  - POST /api/run-checks?blog_id={blog_id} 재검사 트리거
  needs_human: false
  reason_keys:
  - empty_content_deployed
  threshold: always
  alert_template: '⚠️ [MAJOR] 빈 본문 배포 감지

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}

    영향 글: {matched}

    조치: {action}'
  action: publish_log 확인 후 topic 재생성 + 재배포
  playbook_ref: ERROR_PLAYBOOKS.md#p32
  summary_human: '빈 본문 배포 (empty_content_deployed): publish_log 최근 글의 public HTML 본문 단어수 200 미만 탐지 → topic 재생성 + 재배포. 가드(ETAP-GUARD)는 _write_hugo_post_etap 진입부에 적용 완료..'
  summary_llm: '{"problem_id": "P32", "name_ko": "빈 본문 배포 (empty_content_deployed)", "severity": "MAJOR", "family": "deploy", "one_line_action": "publish_log 최근 글의 public HTML 본문 단어수 200 미만 탐지 → topic 재생성 + 재배포. 가드(ETAP-GUARD)는 _write_hugo_post_etap 진입부에 적용 완료.", "needs_human": false, "target_files": [{"path": "shared/publishers/hugo_writer.py", "hint": "_write_hugo_post_etap() ETAP-GUARD (빈 content/최소 단어수 차단)"}, {"path": "ops_dashboard/checks/content_integrity.py", "hint": "detect_empty_content_deployed() — public HTML 본문 단어수 감지 함수 (구현 예정)"}, {"path": "data/travel-en.db", "hint": "publish_log 테이블 — 최근 글의 slug/topic_id/blog_id 조회"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: unknown_failure
  name_ko: 미등록 실패 사유
  severity: MINOR
  family: pipeline
  hook: result_parse
  automation_level: human_approval
  one_line_action: dispatcher 로그에서 실패 원인 확인
  target_files: []
  verify: []
  needs_human: false
  reason_keys: []
  threshold: quiet
  alert_template: 'ℹ️ [MINOR] 미등록 실패 사유

    블로그: {blog_id}

    문제: {problem_id} — {name_ko}

    감지 단계: {phase}'
  action: dispatcher 로그에서 실패 원인 확인
  playbook_ref: ''
  summary_human: '미등록 실패 사유: dispatcher 로그에서 실패 원인 확인.'
  summary_llm: '{"problem_id": "unknown_failure", "name_ko": "미등록 실패 사유", "severity": "MINOR", "family": "pipeline", "one_line_action": "dispatcher 로그에서 실패 원인 확인", "needs_human": false, "target_files": [], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영"}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P35
  name_ko: CUAP 형식/톤 위반 (라벨덤프·반말체)
  severity: MAJOR
  family: content_quality
  hook: content_check
  automation_level: human_approval
  one_line_action: 'CUAP 포스트 본문의 라벨 덤프(가격:/장점: 등) 및 반말체(~다) 비율 점검 → ~입니다/~습니다 체로 재생성'
  target_files:
    - path: ops_dashboard/checks/content_integrity.py
      hint: check_c10 (c10_cuap_format_tone) — CUAP 전용 라벨덤프+톤 비율 검사
    - path: pipelines/curation/writer.py
      hint: ":467 라벨 표기 금지, :486 ~입니다/~습니다 통일 지시 준수 확인"
  verify:
    - CUAP 블로그 content/ 스캔 시 라벨덤프 0건
    - check_results c10_cuap_format_tone status=pass
  needs_human: false
  reason_keys: []
  threshold: quiet
  alert_template: '⚠️ [MAJOR] CUAP 형식/톤 위반

    블로그: {blog_id}

    문제: P35 — {name_ko}

    세부: {detail}'
  action: CUAP 라벨덤프/반말체 포스트 재생성 (~입니다 체)
  playbook_ref: ERROR_PLAYBOOKS.md#p35
  summary_human: 'CUAP 형식/톤 위반(P35): 라벨 덤프(가격:/장점: 등) 또는 반말체(~다) 비율 초과 발견 → writer 지시 준수하여 ~입니다/~습니다 체로 재생성.'
  summary_llm: '{"problem_id": "P35", "name_ko": "CUAP 형식/톤 위반 (라벨덤프·반말체)", "severity": "MAJOR", "family": "content_quality", "one_line_action": "CUAP 포스트 본문의 라벨 덤프 및 반말체 비율 점검 → ~입니다/~습니다 체로 재생성", "needs_human": false, "target_files": [{"path": "ops_dashboard/checks/content_integrity.py", "hint": "check_c10 (c10_cuap_format_tone) — CUAP 전용 검사"}, {"path": "pipelines/curation/writer.py", "hint": "라벨 표기 금지 + ~입니다 통일 지시"}], "how_to_add": "config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복."}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가. code 미중복. 저장 후 프로세스 리로드/재기동 시 자동 반영'
- code: P36
  name_ko: 할루시네이션·비공식 가격 표기
  severity: CRITICAL
  family: content_quality
  hook: content_check
  automation_level: human_approval
  one_line_action: 가격·재고 정보 원본 출처 일치 여부, 환율·세금 추정치 표기 검증 → 출처 기반 재생성
  target_files:
    - path: ops_dashboard/checks/content_integrity.py
      hint: check_hallucination_price() 구현 — 쿠팡/공식 API 가격 대비 차이 검출
    - path: pipelines/curation/writer.py
      hint: 가격 생성 시 출처 인용 강제
  verify:
    - 게시물 가격 출처 일치율 100%
    - check_results hallucination_price status=pass
  needs_human: false
  reason_keys:
    - hallucination
    - price_mismatch
  threshold: always
  alert_template: '🚨 [CRITICAL] 할루시네이션/가격 왜곡

    블로그: {blog_id}

    문제: P36 — {name_ko}

    글: {matched}'
  action: 원본 재수집 및 가격·정보 출처 기반 재생성
  playbook_ref: ERROR_PLAYBOOKS.md#p36
  summary_human: '할루시네이션·비공식 가격 표기(P36): 가격/재고가 원본과 불일치 → 출처 재수집 후 재생성.'
  summary_llm: '{"problem_id":"P36","name_ko":"할루시네이션·비공식 가격 표기","severity":"CRITICAL","family":"content_quality","one_line_action":"가격·재고 원본 일치 검증 → 출처 기반 재생성","needs_human":false}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가.'
- code: P37
  name_ko: 지역/시군구 불일치
  severity: CRITICAL
  family: content_quality
  hook: content_check
  automation_level: human_approval
  one_line_action: frontmatter region과 본문 언급 시군구, slug 일치 검증 → 불일치 시 재생성
  target_files:
    - path: ops_dashboard/checks/content_integrity.py
      hint: check_region_mismatch() — frontmatter region vs 본문 시군구 추출 비교
    - path: pipelines/rap/pipeline.py
      hint: region 추출 및 slug 생성 로직
  verify:
    - region 일치율 100%
    - check_results region_mismatch status=pass
  needs_human: false
  reason_keys:
    - region_mismatch
    - district_mismatch
  threshold: always
  alert_template: '🚨 [CRITICAL] 지역 불일치

    블로그: {blog_id}

    문제: P37 — {name_ko}

    글: {matched}'
  action: region 정보 재확인 후 재생성
  playbook_ref: ERROR_PLAYBOOKS.md#p37
  summary_human: '지역/시군구 불일치(P37): frontmatter region과 본문 불일치 → 재생성.'
  summary_llm: '{"problem_id":"P37","name_ko":"지역/시군구 불일치","severity":"CRITICAL","family":"content_quality","one_line_action":"region 일치 검증 → 재생성","needs_human":false}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가.'
- code: P38
  name_ko: 구조 위반 H2 부족
  severity: MAJOR
  family: content_quality
  hook: content_check
  automation_level: human_approval
  one_line_action: 본문 H2 개수 3개 미만 검출 → 구조 재생성
  target_files:
    - path: ops_dashboard/checks/content_integrity.py
      hint: check_h2_count() — H2 개수 검증
    - path: shared/publishers/hugo_writer.py
      hint: H2 강제 삽입 가드
  verify:
    - H2 개수 >=3
    - check_results h2_count status=pass
  needs_human: false
  reason_keys:
    - h2_insufficient
  threshold: always
  alert_template: '⚠️ [MAJOR] 구조 위반 H2 부족

    블로그: {blog_id}

    문제: P38 — {name_ko}

    글: {matched}'
  action: 구조 재생성 및 H2 3개 이상 보장
  playbook_ref: ERROR_PLAYBOOKS.md#p38
  summary_human: '구조 위반 H2 부족(P38): H2 3개 미만 → 재생성.'
  summary_llm: '{"problem_id":"P38","name_ko":"구조 위반 H2 부족","severity":"MAJOR","family":"content_quality","one_line_action":"H2 개수 검증 → 재생성","needs_human":false}'
  how_to_add: 'config/problems.yaml 의 problems: 아래에 동일 형식으로 항목 추가.'

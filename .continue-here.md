# Continue Here — 세션 핸드오프

> 작성: 2026-09-05 19:10 KST | 세션: CAP/SEAP/RAP 품질 감사·수정 + senior-blogger Blogger API 백필 + rap4 no_trade_data 트리아지

## Current Position

5000 중앙 컨트롤 repo. 이번 세션은 GSD phase가 아닌 **운영/품질 세션**이었음 (CAP phase-01은 9/4 완료).

- 세션 주제: CAP 8블로그 감사·phase-01 → m0313 3건 → SEAP+RAP 감사·근본 해결 → 대시보드 체크 등록 → senior-blogger API 백필
- 관련 worklog: `.planning/worklog/WL-20260905-seaprap-quality-audit-fixes.md`
- worklog에 이번 세션 전체 기록 완료 상태 (갱신분까지 포함)

## Completed This Session

### 1. CAP phase-01 (9/4 완료, 커밋 7a21b7a/73d6ba93a/32479fa2c)
- 제목 잘림(validators 트렁케이션 제거), desc ÷36 정합, EV 스키마 프롬프트 6개, 체크박스 13건, ev baseURL, 로더 2중 6사이트, 재배포 완료

### 2. m0313 3건 (커밋 bfb92d8cc)
- ①GV60/i4 EV 스키마 수정+재배포 ②compare 2시리즈 484→520만원 수정+재배포 ③P02 토픽 소진 근본 해결(topic_manager fallback 콤보 필터 + reset_skip_no_data)
- W5 게이트 regex 버그 수정(deploy.py:150 중첩 대괄호)

### 3. rap4 P14 (커밋 5aaa346f6)
- pipeline.py:103 LIMIT 200→2000

### 4. SEAP+RAP 감사+근본 해결 (커밋 03415dcc5→5728c9f0d)
- A1/A2 게이트(validators _check_rap), 관악 이중 미매칭 게이트, 로더 6사이트, desc 오염 792건 백필, CoT 4건 폐기, 관악 폐기, 재배포 6+3+6건 전부 rc=0
- 잔존 위험 R1~R5 해소(_headers s-maxage=3600 등)

### 5. 스킬+룩북 (커밋 ecbfb89a8)
- cross-branch-content-audit 스킬 + ERROR_LOOKBOOK.md 섹션 10 (ERR-023~031)

### 6. 대시보드 체크 3종 등록 (커밋 2d26722e4)
- desc_pollution.py, rap_region.py, render.py 3c 로더 카운트 + pick/rank 예외 _ADSENSE_EXCEPTIONS. 스모크 전부 pass.

### 7. senior-blogger Blogger API 작업 (커밋 2d26722e4→0b4be6fb3→648d91f8a)
- **searchDescription 오판 정정**: Blogger API는 미지원 필드. 테마가 본문 자동 추출 렌더 — 파이프라인 수정 불필요
- **체크박스 [ ] 백필**: 전수 772포스트 중 81건/481개 → 0개 (파괴적 4단계, 백업 완료)
- **재발방지 이중화**: writer.py:328 프롬프트(소프트) + validators.py:_check_senior CRITICAL 게이트(하드, 정규식 `( ^|\W)\[( |x|X)\]`)
- **스킬 작성**: `~/.config/opencode/skills/blogger-api-publishing/SKILL.md` — pickle 토큰 인증(비밀번호 불필요), blog ID 매핑 3개, enum/502/sleep gotchas, searchDescription 미신화, 백필 4단계 패턴

### 8. rap4-hugo P01 no_trade_data 트리아지 (커밋 6eaddd1e4 + a070eee35)
- **알림**: rap4-hugo fetcher "실거래가 0건: 새샘마을3단지 세종시 전세" 3회 연속(11:50/14:50/18:00), stage=no_trade_data
- **근본 원인**: rap_data_sync.py SYNC_REGIONS 58개에 세종 등 11개 법정동 부재 → rents 커버리지 구멍. 세종 rents 0건 + trades 202604만료(3개월 폴백 윈도 밖) → no_trade_data → 키워드 자동 비활성화. 영향: active 키워드 1,124건(세종/수영구/대구동구/달서구/남동구/광산구/대전동구/유성구/수원권선구/진천군/아산시)
- **수정 [PRODUCTION]**: SYNC_REGIONS 58→69개 (+11: 36110 세종, 26410 수영구, 27140 대구동구, 27290 달서구, 28200 남동구, 29200 광산구, 30140 대전동구, 30200 유성구, 41113 수원권선구, 43750 진천군, 44200 아산시)
- **즉시 수집**: rents +2,367건/trades +2,073건 (파괴 4단계, 로그 기록). 광산구(29200)만 API totalCount=0 — 공공데이터 원천 부재, 데이터 생길 때까지 정상 실패
- **키워드 복구**: 오늘 자동 비활성화 6건 active 복구(새샘3 전세+실거래/호려울7 전세+실거래/탕정 전세+실거래/첫마을3 실거래)
- **검증 [검증됨]**: 세종 rents 276건/새샘3단지 2건. dispatcher rap4-hugo 재실행 → article_id 13056(대전 유성구 전월세), 배포 rc=0, 라이브 https://rent.informationhot.kr/posts/대전-유성구-전월세-실거래가-분석-및-시장-정보/
- **트리아지 문서**: `.planning/triage/20260905--rap4-no-trade-data-rents-coverage.md` + INDEX.md prepend. 교훈: 신규 지역 키워드 대량 추가 시 SYNC_REGIONS 대조 필수

## Remaining Work

- 없음 — 세션 과제 전부 완료. 새 알림(P02/P04 등) 도착 시 publish-error-triage 스킬로 대응.
- 다음 스케줄 사이클(21:00) rap4-hugo 발행 성공 여부 모니터링 권장 (SYNC_REGIONS 수정 후 첫 자동 사이클).

## Decisions

1. **pick/rank AdSense 8772 유지** — informationhot.kr 도메인이나 8772 사용(2026-09-04 사용자 결정, 라이브 백지 없음 3개월+, 문서화된 예외)
2. **관악드림타운 글 폐기** — 강남구 데이터로 쓰인 글, 지역명 교체 불가. 삭제+재배포로 404 처리 완료
3. **CoT 릭 4건 폐기** — rap2 2건+rap5 2건, 백업 후 rm
4. **75-2.html 빈 desc 방치** — 사용자 결정 "그냥 놔두자" (9/4 삼중발행 유물)
5. **senior-blogger 테마 수정 취소** — 사용자 결정 "테마 수정은 필요없어. seo만 잘되면 된다" — desc는 테마 자동 렌더로 정상 판명되어 불필요해짐
6. **Blogger 로그인 포기, API로 전환** — 비밀번호 7회 거부(hugh7973 4+twinssn 3). API 토큰(blogger_token.pickle)으로 전 작업 수행 — 스킬로 문서화

## Blockers

없음.

## Bugs & Issues Discovered

### 해결 완료
1. **CAP 제목 잘림 67.5%** — 증상: 제목 35자 트렁케이션. 원인: validators.py:86-91 소프트 트렁케이션. 해결: 제거+테스트 수정. 완료.
2. **desc 수치 불일치 91%** — 증상: desc '월 유지비' vs 본문 총비용. 원인: pipeline.py 계산식 (tax+ins+fuel)/12. 해결: three_year_total_cost/36 + '월 환산' 라벨. 완료.
3. **EV에 내연 스키마** — 증상: EV 글에 km/l·배기량·유류비. 원인: post_type 프롬프트에 EV 조건 규칙 부재. 해결: 6개 프롬프트에 조건부 블록. 완료.
4. **[ ] 체크박스 릭 (car)** — 원인: 프롬프트 13건 '[ ]' 예시. 해결: 대시 목록 교체+common 규칙. 완료.
5. **topic_manager KeyError '_reuse'** — 원인: .format()이 {_reuse} 만남. 해결: .replace() 체인 (커밋 32479fa2c). 완료.
6. **W5 게이트 regex 오판** — 증상: 이미지 있는데 0장 차단. 원인: `!\[[^\]]*\](` 중첩 대괄호 미매칭. 해결: `!\[[^\n]*](`. 완료.
7. **rap4 P14 no_keyword** — 원인: LIMIT 200 + 지역 가드. 해결: LIMIT 2000. 완료.
8. **rap desc 오염 792건** — 원인: 후처리 related 블록 복사(8/1 이전 유물). 해결: 백필 전멸+A2 게이트. 완료.
9. **관악드림타운 지역 왜곡** — 원인: find_lawd_cd 미매칭→랜덤 강남구 fallback. 해결: 글 폐기+이중 미매칭 게이트. 완료.
10. **CoT 릭 4건** — 원인: 8/3 이전 발행 유물. 해결: 폐기. 완료.
11. **senior-blogger [ ] 릭 81건/481개** — 증상: `<li>[ ] text</li>` 리터럴 렌더. 원인: writer.py L328 '[ ]' 예시 지시 + Blogger 체크박스 변환 불가. 해결: ①프롬프트 수정 ②81건 백필 0개 ③_check_senior CRITICAL 게이트. 완료.
12. **searchDescription 오판** — 증상: "None 10/10" 결함 보고. 원인: API 미지원 필드 측정. 해결: 실증 테스트로 정정 — 테마 자동 렌더 정상. 완료(오판 정정).
13. **rap4 no_trade_data (P01)** — 증상: 세종 새샘마을3단지 실거래가 0건 3회 연속. 원인: SYNC_REGIONS 58개에 세종 등 11개 법정동 부재 → rents 수집 구멍 + trades 202604만료 → 키워드 자동 비활성화(1,124건 영향). 해결: SYNC_REGIONS +11 지역, 즉시 수집(rents +2,367/trades +2,073), 비활성화 6건 복구. 광산구는 원천 데이터 부재(정상 실패). 발행 성공(article_id 13056). 완료.

### 미해결 — 다음 세션 이관 없음 (전부 방치/완료 결정)
- 75-2.html 빈 desc 1건 — 사용자 방치 결정. 복구 불필요.

## Environment Notes

- **Blogger API**: blogger_token.pickle(5000 루트) — 자동 refresh+재저장. 비밀번호 로그인 불필요(스킬 참조)
- **Blogger 토큰 blog 접근**: hugh79757 OAuth 앱 — 시니어(2.techpawz.com), 여행(travel.rotcha.kr), 65세(65.informationhot.kr) 등 14개 접근 가능
- **minify grep 함정**: Hugo/Blogger 라이브 HTML grep 시 속성 따옴표 제거됨 — `name=description` 형태로 검색
- **wrangler 배포**: dispatcher.py 경유. 직접 실행 금지. CLOUDFLARE_API_TOKEN 제거 자동 처리
- **elbow 캐시**: 6사이트 `_headers`로 /posts/* s-maxage=3600 — 삭제 글 1시간 내 자연 404
- **스킬 2종 이번 세션 신설**: `blogger-api-publishing`, `cross-branch-content-audit` (~/.config/opencode/skills/)
- **grep 셸 주의**: 대괄호 리터럴 grep 시 `\[ \]` 이스케이프 필요
- **rap4 발행 구조**: _pick_keyword(LIMIT 2000, Q-D 30일 district 가드) → BLOG_STRATEGY(rap4='trade') → _fetch_rents_from_db rents 우선 → 0건이면 trades 폴백 → no_trade_data 시 키워드 자동 inactive
- **rap.db**: rents 이제 61지역 78,494건(76,127+2,367). 광산구(29200)는 공공데이터 원천 부재 — totalCount=0 정상 실패
- **SYNC_REGIONS 교훈**: 키워드 풀 생성 시점과 수집 커버리지가 독립적 — 신규 지역 키워드 대량 추가 시 SYNC_REGIONS 대조 필수 (트리아지 문서 기록)

## Resume Instructions

1. **세션 재개 시**: 이 파일 읽고 → worklog `.planning/worklog/WL-20260905-seaprap-quality-audit-fixes.md` 확인
2. **새 알림 대응**: publish-error-triage 스킬 로드 → P 코드 매핑 → 트리아지
3. **Blogger 작업**: `blogger-api-publishing` 스킬 로드 — pickle 토큰으로 즉시 작업 가능
4. **라이브 검증 curl 패턴**:
   ```bash
   curl -s "https://2.techpawz.com/2026/09/blog-post_05.html" | grep -oE "<meta[^>]*name='description'[^>]*/?>"
   curl -s {url} | grep -c '\[ \]'  # 0이어야 정상
   ```
5. **마지막 커밋 확인**: `git log --oneline -1` → a070eee35 (triage: rap4 no_trade_data rents coverage entry)

## Uncommitted Changes

- `.planning/worklog/WL-20260905-seaprap-quality-audit-fixes.md` (M) — handoff 직전 마지막 세이션 기록 완료분. WIP 커밋 대상
- `ops_dashboard/checks/content_quality_live.py` (M) — **이번 세션 아님**: P36/37/38 체크 suggestions 필드 추가 잔여 (이전 세션 eb8926435 작업 연장선)
- `data/analytics_status.json` (M) — 런타임 산출물, 커밋 불가
- untracked: 임시 이미지/output/lock 파일 — 런타임 산출물, 커밋 불가
- **스킬 2종**은 ~/.config/opencode/skills/ — git 밖, 로컬 보존됨

> 본 파일은 WIP 커밋으로 저장됨.

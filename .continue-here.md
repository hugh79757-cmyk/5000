# Work Handoff — 2026-07-26

## Current Position
**Active Phase:** Phase 45 — TAP Blog Meta-Response Detection & Prevention (Wave 3 complete, Wave 4-6 pending)
**Project:** 5000 Central Control Pipeline
**Branch:** main (7 commits ahead of origin)

---

## Completed This Session

### Phase 45: TAP Blog Meta-Response Prevention (4-Layer Defense)
| Wave | Task | Status | Files Modified |
|------|------|--------|----------------|
| 1 | Validator Foundation — 11 meta-patterns, `_is_meta_response()`, `validate_content()` returns `is_meta_response` | ✅ | `core/validators.py`, `core/__init__.py`, `tests/test_validators.py` |
| 2 | AI Writer Prompt Hardening — `ANTI-META (CRITICAL)` section added to system prompt | ✅ | `core/ai_writer.py` |
| 3 | AI Writer Retry Logic — `generate_full_content(max_retries=2)` with meta-check loop, logging, `RuntimeError` on exhaustion | ✅ | `core/ai_writer.py` |
| 4 | Pipeline Retry + Telegram Alert | ⏳ Pending | `app.py` |
| 5 | Tests & Dry-Run (30 gens) | ⏳ Pending | `tests/test_meta_response.py` |
| 6 | Production Verification | ⏳ Pending | — |

### TAP Blogger Migration (Separate Quick Task)
- Moved `tap-blogger` from standalone TAP (`app.py` → Blogger) to 5000 `travel` pipeline
- Updated `config/blogs.d/tap.yaml`: pipeline `tap` → `travel`, added `blogger_blog_id`
- Added `tap-blogger` to `BLOG_FETCH_MAP` (camping:heritage:festival = 0.4:0.4:0.2)
- Added prompt mappings in `pipelines/travel/writer.py`
- Verified: `python dispatcher.py tap-blogger` → published to `travel.rotcha.kr`

### Triage Entry Created
- `.planning/triage/20260726--tap-meta-response-block-fix.md` — root cause & 4-layer defense documented
- `.planning/triage/INDEX.md` updated

---

## Remaining Work

### Phase 45 (Waves 4-6)
1. **Wave 4** — `app.py` `run_publish()` wrap AI gen + validation in 3-attempt retry, call `send_error()` on final failure
2. **Wave 5** — `tests/test_meta_response.py` with `TestAIWriterRetry`, `TestPipelineRetry`, dry-run 30 generations
3. **Wave 6** — Live publish verification, monitor 3 scheduled runs for <5% retry rate

### Phase 47 (Discuss-Phase — 5 Undecided Items)
- A: BLOCK threshold (1 deletion = block vs threshold)
- B: Telegram alert on BLOCK vs stdout only
- C: `ref_docs` manifest location (AGENTS.md vs separate file)
- D: Verification script location (scripts/ vs gsd-sdk vs plan-phase)
- E: Pre-commit hook vs plan-phase duplication

### Config Cleanup
Modified YAML files in `config/blogs.d/` need review (10 files changed)

---

## Decisions

| Decision | Rationale |
|----------|-----------|
| 11 regex patterns for meta-detection | Covers 4 observed meta-patterns (apology, duplicate mention, structure outline, rewrite offer) + 7 common AI signatures |
| `max_retries=2` in AI Writer | 3 total attempts (1 initial + 2 retries) — balances cost vs reliability |
| Extend `test_validators.py` not new file | Keeps regression suite together; existing tests unchanged |
| Phase 45 before Phase 47 | Meta-response bug is production-blocking; Phase 47 is defensive infrastructure |

---

## Blockers
- None — all dependencies available (OpenAI API, TAP env, Telegram notifier)

---

## Bugs & Issues Discovered

### 1. TAP Blogger Published AI Meta-Response
- **증상**: `travel.rotcha.kr` 포스트에 "죄송합니다. 제가 이미 동일한 요청에 대해 블로그 글을 작성하여 전달드렸습니다..." 등 AI 대화형 응답이 여행 콘텐츠 대신 발행됨 (2026-07-25)
- **원인**: 
  - gpt-4o-mini가 프롬프트를 대화 연속으로 해석해 "이미 답변함" 응답 생성
  - 시스템 프롬프트에 대화형 응답 금지 규칙 없음
  - 검증기(`core/validators.py`)가 길이/H2/H3만 체크, 메타 응답 탐지 로직 없음
  - `generate_full_content()` 재시도 로직 없음 — 1회 호출 후 검증 통과 시 바로 발행
- **해결책**: 4계층 방어 구현 (Phase 45 Waves 1-3 완료)
  - 프롬프트: `ANTI-META (CRITICAL)` 5개 금지 카테고리 명시
  - 검증기: 11개 정규식 + `_is_meta_response()` + `validate_content()` `is_meta_response` 플래그
  - AI 라이터: `max_retries=2` 재시도 루프, 메타 탐지 시 로깅 후 재시도
  - 파이프라인: 3회 전체 재시도 + 텔레그램 알림 (Wave 4 예정)
- **미해결**: Wave 4-6 (파이프라인 재시도, 통합 테스트, 실전 검증)

### 2. `generate_full_content` 메서드가 클래스 밖에서 정의됨
- **증상**: `AIWriter` 인스턴스에 `generate_full_content` 메서드 없음 (인덴트 누락)
- **원인**: 코드 편집 중 `def generate_full_content`가 클래스 바깥으로 빠져나감
- **해결책**: `core/ai_writer.py` 라인 59 인덴트 수정 (4칸 들여쓰기)
- **미해결**: 해결됨

---

## Environment Notes
- **TAP venv**: `/Users/twinssn/Projects/TAP/venv/bin/python` — `OPENAI_API_KEY` 환경변수 필요
- **Scheduler**: `com.5000.scheduler.plist` (launchd) active; `com.tap.scheduler.plist.disabled` 비활성화됨
- **5000 Dispatcher**: `python dispatcher.py {blog_id}` — 배포 포함 전체 파이프라인 실행
- **Config**: `config/blogs.d/tap.yaml` — TAP 6개 블로그 설정 (Blogger 1 + Hugo 5)

---

## Resume Instructions

```bash
# 1. Restore TAP environment
cd /Users/twinssn/Projects/TAP
source venv/bin/activate  # or use full path: /Users/twinssn/Projects/TAP/venv/bin/python

# 2. Continue Phase 45 Wave 4 (Pipeline Retry)
# Edit app.py run_publish() — wrap ai_write + validate in 3-attempt loop
# Add: from shared.telegram_notifier import send_error

# 3. Run tests
cd /Users/twinssn/Projects/TAP
python -m pytest tests/test_validators.py -xvs
python -m pytest tests/test_core.py -xvs -k "not database"

# 4. Dry-run 30 generations (costs ~$0.30)
python -c "
from core.ai_writer import AIWriter
w = AIWriter()
for cat in ['캠핑', '문화유산', '축제']:
    for i in range(10):
        # call generate_full_content with real API
        pass
"

# 5. Live test
cd /Users/twinssn/Projects/TAP && python app.py run
# Check travel.rotcha.kr for clean content
```

---

## Uncommitted Changes

### Modified Files (15)
```
M .DS_Store
M .planning/ROADMAP.md
M .planning/triage/INDEX.md
M AGENTS.md
M config/blogs.d/cap.yaml
M config/blogs.d/cuap.yaml
M config/blogs.d/etap.yaml
M config/blogs.d/manual_blog_for_backup.yaml
M config/blogs.d/rap.yaml
M config/blogs.d/seap.yaml
M config/blogs.d/stap.yaml
M config/blogs.d/tap.yaml
M data/cooldown.json
M data/failure_count.json
M pipelines/travel/pipeline.py
M pipelines/travel/writer.py
```

### New Files (4)
```
?? .planning/phases/45-tap-blog-meta-response-detection-prevention/
?? .planning/quick/20260726-tap-blogger-migration/
?? .planning/triage/20260726--tap-meta-response-block-fix.md
?? update_prompts.py
```
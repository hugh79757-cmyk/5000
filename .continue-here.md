# .continue-here.md — Work Handoff

**Timestamp**: 2026-08-23 저녁 KST
**Branch**: `main` (HEAD: a0d60b92f)
**Session 주제**: Track C 후속 — schema_registry/GA4 프로비저닝 → 스케줄러 사고 트리아지·복구 → Lookbook → 자율수정 → ETAP 전수 감사·표준화·배포

---

## Current Position

GSD Phase 체계와 별개의 운영 태스크 연속 수행 세션. 마지막 완료 태스크 = **ETAP_BORDER_REMOVAL_AND_FULL_DEPLOY** (36사이트 배포까지 완료). 활성 GSD phase: STATE.md 기준 M5 대기 + Phase 52 Wave 5. 이번 세션은 phase 워크플로 밖 ops 태스크 라인.

## Completed This Session (시간순)

1. **schema_registry DB+뷰+스케줄** (`7c96e87f2`, 선행 `e1acc419e`): ops.db schema_registry 테이블(85행), /schema 뷰, classify_standardization(내구신호 64/12/9), scheduler 09:00 훅, 테스트 6건
2. **GA4 프로비저닝** (`06b0ff3e9` + cuap c12ef45 + interior 5641429 + hotissue 0fa0f30): account 321003076, 5 속성 생성(car G-RPZN2K1ZJR/golf G-KPP1QLTFE2/homeappliance G-8TT26J1BZT/massage G-TYKN76B4PM/bike G-LPQ1CG44JJ), 단일소스 원칙(hugo.toml services 유지), scripts/ga4_provision.py
3. **스케줄러 재기동+harvest 검증**: launchctl kickstart PID 53558 → harvest 로그 부재 발견 → 침묵사 트리아지
4. **Expander 격리** (`f4040a234`): keyword_expander subprocess+heartbeat poll+killpg deadline(CAR 패턴), watchdog 재로딩
5. **main 병합** (`9b09c9f28`): stash→merge track-c-cap-standardization --no-ff→stash pop
6. **Universal Error Lookbook** (`9bbedc9c2`): docs/lookbook/ERROR_LOOKBOOK.md ERR-001~020 + /lookbook 라우트 + Appendix C deprecation 노트
7. **자율수정**: 대시보드 재기동(PID 16053:5060), c08 캐시문구 중복 버그 수정(`578a49c54`), P04 burst=게이트 정상 분류, 6블로그 디스패치 정지 조사
8. **CAR refresh 복구** (`d6f2c73ea`): daily_refresh ModuleNotFoundError 수정(-m 실행), 수동 refresh로 pending 44→269, 6블로그 게이트 healthy 해제
9. **ETAP 감사 3종**: visual audit(36곳, 타이틀 빈값 10·ads.txt 34누락·날짜미렌더 33), conformance V2(13패밀리), family-API mapping(travel-en.db 단일, 소실 소스 3건)
10. **ETAP bulk fix**: 타이틀 10곳 주입, showDate=true ×36, ads.txt ×36, 커밋 27 repos
11. **공급망**: recovery plan 6파일, viator_api 삭제+DATA_SOURCE_REGISTRY (`a0d60b92f`), TM 미마운트 SKIP
12. **NO-GIT 9곳 git init** + 전수빌드 33/33 OK
13. **nightlife 핫픽스**: background.html border 제거+배포, 라이브 검증 완료
14. **border 일괄 제거+전체 배포**: 35곳 sed(정밀 패턴), 빌드 36/36, 커밋 35, **배포 36/36 DEPLOYED**, 라이브 7/7 검증

## Remaining Work / 승인대기

1. **CUAP_FINAL_CLOSURE PART_A/B/D** — 08-24 06:00+ : logs/harvest_20260824.log 존재, keyword_pool 생성, api_call_log ≤8/hr, expander <1800s, worklog WL-20260824 업데이트 후 커밋(전부 ✅일 때만)
2. **F5 DB 오염 정리**: check_results detail에 ' (캐시: 24h 내 실행됨)' ~50회 누적 행 UPDATE — 승인 필요
3. **6블로그(compare/deal/ev/guide/hotissue/tco) 다음 스케줄 틱에서 실제 발행 재개 확인** — gate는 healthy 해제됨
4. **Airalo 파트너 피드 URL 재요청**(외부 커뮤니케이션) + visa collector 신규 빌드(~80줄) + Omio 원격화 + michelin Kaggle loader — supply chain plan #1~#4
5. **TM 마운트 후 Archive/Airalo XML 수동 스캔** — /tmp/timemachine_source_scan.md 절차 참조
6. **deal/tco 소스 수급**(promotions=0/resale_prices=0) — 재발 방지
7. harvester limiter 맹목 문제(api_call_log 미참조) — 수정 제안만 문서화됨
8. watersports/nature 토픽부족 알림의 대시보드 미노출 갭 — no_keyword 대시보드 반영 별도 과제

## Decisions (이번 세션)

- GA4 삽입 단일소스 = hugo.toml [services.googleAnalytics] 유지 + extend-head gtag 제거 (태스크 지시와 반대였으나 이중주입 해소 위해 검증된 방향 채택)
- hotissue(PaperMod) 활성 partial = extend-head.html 하이픈 — empirical marker test로 입증 (Blowfish와 반대)
- classify_standardization은 내구 신호만 사용(64/12/9) — 감사 스냅샷 54/22와 차이는 H2 샘플링 미재현 때문, 문서화
- launchd 관리 프로세스는 nohup 금지 — kickstart/bootstrap 사용
- expander deadline 1800s(env EXPANDER_TIMEOUT_SEC), heartbeat 30s poll
- ETAP 시각 결함 수정은 사이트 자체 오버라이드 레이어(background.html)에서, 아바타 border-emerald-200 등 의도 디자인 보존

## Bugs & Issues Discovered

| 증상 | 원인 | 해결 | 미해결 |
|---|---|---|---|
| 스케줄러 02:00~06:59 침묵 | keyword_expander in-process 블록(DNS 추정) + watchdog 미로딩 | subprocess 격리+f4040a234, watchdog bootstrap | 근본 syscall 미확인(추정 단계) |
| 03:00 harvest 미실행·keyword_pool 부재 | 위 침묵사의 결과 | 스케줄러 정상화 — 08-24 03:00 첫 검증 대기 | 내일 확인 필요 |
| CAR 6블로그 08-16부터 발행 0 | daily_refresh가 직접 스크립트 실행→ModuleNotFoundError 'shared' | `-m` 실행으로 수정+d6f2c73ea, 수동 refresh +225 topics | deal/tco 소스 고갈 잔존 |
| c08 detail '(캐시…)' 무한 누적 | cached branch가 매 실행 detail append | content_integrity.py replace-then-append +테스트(578a49c54) | DB 기존 오염 행 UPDATE 승인대기 |
| GA4 이중삽입 | 네이티브 config+extend-head 수동 블록 공존 | 네이티브 유지+수동 제거 | — |
| zsh 단어분할로 title 잘림 | unquoted 문자열 배열 확장 | 개별 sed로 재실행, 최종 grep 검증 | — |
| border sed 미매칭 | 클래스 사이 px-8 py-10 존재 | 전체 문자열 정밀 sed | — |
| wrangler lock timeout 경고 1건(visafree~walking 사이) | deploy lock 60s 경합 추정 | 라이브 재검증으로 실반영 소명 | 원인 로그 미보존 |
| daily_refresh --help가 실실행 | argparse 부재(__main__ 가드 없음) | 없음 | dry-run 플래그 추가 과제 |

## Environment Notes

- Hugo 빌드 필수: `HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes`
- ETAP 배포 절차: `export CLOUDFLARE_API_TOKEN=$(env -u CLOUDFLARE_API_TOKEN wrangler auth token | tail -1)` → `deploy_site(dir,blog_id)` from shared/publishers/deploy.py — dispatcher는 5000 관리 블로그용
- GA4 Admin API: token `/Users/twinssn/Projects/blogdex/credentials/token_2_informationhot.json`, account 321003076
- 대시보드 API 테스트: `create_app().test_client()` + load_dotenv('ops_dashboard/.env') auth=(OPS_USER,OPS_PASSWORD); 모듈레벨 app 없음
- curl UA 필수(python urllib 403), minify 후 gtag 더블쿼터
- BSD sed -i '', zsh 배열 확장 위험
- ETAP 사이트들은 각각 별도 git repo(36개, NO-GIT 9곳은 init 완료)
- 시스템 TZ=+07, sqlite datetime('now')는 UTC

## Resume Instructions

1. 08-24 06:00 이후: `ls logs/harvest_20260824.log && sqlite3 data/curation.db "SELECT COUNT(*) FROM keyword_pool"` → CUAP_FINAL_CLOSURE PART_A/B/D 진행(worklog WL-20260824-cuap-final-closure.md 체크리스트 따름)
2. `sqlite3 ops_dashboard/ops.db "SELECT blog_id,state,available_count FROM pipeline_availability WHERE state!='healthy'"` → 6블로그 발행 재개 확인
3. 승인받은 것부터: F5 오염 UPDATE, Airalo 피드 요청, visa collector 빌드

## Uncommitted Changes

- 5000 repo: ~195 dirty 파일(이전 세션~현재 누적 — dispatcher.py, ops_dashboard checks/standard.py·fix_history.py, pipelines/etap/* writer/pipeline 다수, docs/superpowers specs, logs/destructive_2026-08-23.log 등). 이번 세션 신규분은 커밋 완료(a0d60b92f까지). pause-work 워크플로에 따라 wip 커밋 예정.
- ETAP 36 사이트 repo: border 제거 커밋 완료(클린)
- /tmp 산출물 다수(리부트 시 소멸 — 중요 문서는 docs/ 또는 worklog에 있음)

---
*다음 세션 시작 시: 이 파일 읽고 "Resume Instructions"부터.*

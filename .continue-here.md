# .continue-here.md — 2026-09-07 18:40

## Current Position

운영 세션 (GSD phase 아님). 2026-09-06~09-07 publish-error triage 배치 전부 종결. 모든 alert REAL/오탐/자가치유 분류 완료, REAL 문제 전부 FIX + 배포 + 문서화 완료. 세션 종료 지점.

## Completed This Session

### Publish-error triage (9/6-9/7 alert 배치)
1. **STAP stock writer SyntaxError fix** — writer.py:962 nested quote 제거. ast/py_compile 검증. (commit 안 됨 — STAP repo)
2. **STAP `** ` title 오염 fix** — 5 writer(iapo/finance/dividend/stock/etf) 파서 `TITLE: ** ` 정리 + 라이브 2건(ipo/finance) 수정 배포. pytest 29 passed.
3. **golf-hugo 공구 오염 fix** — `드라이버` 단독 키워드 제거(골프드라이버로) 3파일(pipeline.py allowed/blocked, keywords.py, keyword_expander.py) + naver_trending golf 214→5행 purge. 실코드 재현: 공구 2종 차단/골프 2종 생존.
4. **ETAP 토픽 리필 +704행** — flights 0→200(발행재개)/dining 9→97/esim 6→8/citytours 39→126/nomad 35→115/multiday 50→98/nightlife 49→50/cruise 57→66/watersports 68→160/extreme 73→167. 정크 47 + esim 중복slug 9 삭제. 백업 bak_topic_refill_20260906211044.
5. **ETAP H2-GUARD 영어 바이패스** — hugo_writer.py `_has_hangul` 감지로 영어 H2 강등 해제 + post_processor.py affiliate-btn/card 마크업 + 라이브 14블로그 강등 H2 4,196 복원 + Book Now 2,573 버튼화 + Pages 2회차 재배포. watersports 라이브 H2 7개 확인.
6. **airlines/layover 토픽 정화** — airlines 가짜 `Aero {Country} {code} Airways` 296행 DELETE + 실데이터 26행 리셋(283→26), layover 데이터0 도시 111행 DELETE + city_aliases 3행 교정(150→39). 가짜 publish_log 13행 DELETE(로그오염 — mark_published_by_id가 데이터부족 시에도 INSERT).
7. **Travelpayouts 항공권 CTA 구현** — airlines_pipeline._add_product_cards 전면 교체(TRAVELPAYOUTS_MARKER→허브탐색→flight_prices 최저가 3노선 jetradar 링크) + insert_product_cards additive 파라미터(card_title/btn_text, 호출자 45곳 무영향). 스킬 생성 `~/.config/opencode/skills/travelpayouts-affiliate-links/SKILL.md`. Westjet 발행 성공 + 라이브 jetradar 3노선(LAX→LAS/PHX/SFO) marker=513970 확인.
8. **travel1-hugo duplicate_source_id fix** — fetcher.py fetch_festival() published_ids에 stap_content.db 필터 추가(content.db만 보던 것). 시흥시 글 발행 성공.
9. **compare/ev no_topics 해결** — 원인: 단종 차량 토픽 무한순환(reset_skip_no_data 무조건 리셋 × build_input 시판필터 차단). FIX daily_refresh.py 리셋 조건에 시판 trims EXISTS 검사. car.db 1,569행 skip 마킹. ev는 90일 콤보가드 정상 대기.
10. **carisyou 후속모델 미스캔 교정** — scan_new_cars 커서 결함(scan_log 12293 우선 → 7831+ 실모델 18대 영구 미스캔) → cars.MAX 고정 교정. 일회성 재스캔: cars 9대 등록(아르카나/GV60/일렉트리파이드GV70/iX3/EX90/A6/코나2027/코나HEV2027) + trims 751 UPDATE/39 신규 + topics 98 INSERT(ev 48/pick 26/rank 24).
11. **신차 이미지 눈검수 (CAP SOP)** — 검수 페이지 scripts/image_review_new_cars_20260907.html → 사용자 ID 10개 → car_images 11행 DELETE(같은URL 기존행 1 포함) + blocked_images 615행(재수집 차단). 잔여 77개.
12. **rap4 지역왜곡 오탐 확정** — 벨섬시티 글 본문 수원 25회/권선구 15회 포함, 라이브 200.

### 자가치유/오탐 분류 (조치 불필요)
- travel-hugo P01(13:00 성공), health-hugo P16/P14(발행 성공, `건강하게` 0.50 경계 반복), rap-hugo no_trade_data(3개월 필터 설계), travel4-hugo(07:19 성공 리셋), stock-hugo(카카오뱅크 발행), sector-hugo(07:31 성공), laptop-hugo(07:47 성공), rap3-hugo Jaccard 가드 정상, BC 큐(13:26 자가회복).

## Remaining Work

1. **내일 06:30 daily_refresh 정상 순환 확인** — 새 커서 로직(cars.MAX 기준) 첫 자동 실행. scan_log에 cars.MAX+1 스캔 기록 확인.
2. **compare/ev 내일 발행 모니터링** — compare는 오늘 daily cooldown → 내일 스케줄 자동 재시도. select_topic 재현은 통과(1288/1061/4999).
3. **잔여 77 이미지 추가검수** — 사용자 전달 대기. 검수 페이지 유지됨.
4. **airlines remaining 26 (~5일) / layover 39 (~8일)** — 추후 재리필 필요. airlines는 ref_airlines 기반 재생성 스크립트 검토.
5. **nightlife/cruise/esim 소스 한계** — Viator cats 확장 필요(nightlife +1만 가능했음).
6. **nomad 기존 slug 중복 43건** — refill 이전 분, duplicate_slug 에러 가능 — 모니터링.
7. **airlines/layover 토픽 오염 원인(생성 스크립트) 미추적** — 재발 가능.
8. **content.db ↔ car.db ledger_sync 지연** — ev 4/21 vs 9/6. 콤보가드 불일치 시 duplicate_source_id 가능.
9. **단종 표기 근본 원인** — carisyou 원본 표기(2026년형 부분변경 출시로 구형 단종). 버그 아님, trims 갱신 시 개선 reset이 자동 복귀.
10. **ETAP 신규 발행글 H2+버튼 확인** — 발행 시점 적용이므로 다음 스케줄 발행물 확인.
11. **S02 quality gate 경고 (airlines 템플릿 구조)** — 매발행 반복 예상, writer 프롬프트 다양화 검토.

## Decisions

- golf 라이브 공구 포스트 2건 유지 (SEO 트래픽 잠재력 — 사용자 결정)
- carisyou 미등록 9대(링컨/미니/BYD/GMC/애스턴마틴/램/포드)는 AUTO_REGISTER_BRANDS 의도적 큐레이션 외 — 유지
- Travelpayouts 스킬 신규 생성 (~/.config/opencode/skills/travelpayouts-affiliate-links/) — jetradar/aviasales 링크 포맷, 마커 513970
- STAP 배포 시 `-t PaperMod` 금지 — hugo.toml theme=blowfish + themesDir(shared-themes). flag 없이 `hugo --gc --minify --source <site>`
- 긴급배포 필요시 수동 `deploy_site(site_path, cf_project, 'pages')` — token 추출 관례: `export CLOUDFLARE_API_TOKEN=$(env -u CLOUDFLARE_API_TOKEN wrangler auth token 2>/dev/null | tail -1)`

## Blockers

없음.

## Bugs & Issues Discovered

| # | 증상 | 원인 | 해결 | 미해결 |
|---|------|------|------|--------|
| 1 | stock-hugo SyntaxError 5연속 | writer.py:962 nested quote | 제거, py_compile OK | — |
| 2 | ipo/finance title `** ` prefix | 파서가 `TITLE: ** ` 미처리 | 5 writer 정규식 정리+배포 | — |
| 3 | golf-hugo 공구 포스트 | `드라이버` 단독 키워드가 Coupang 공구 검색 | 3파일 정정+DB purge | 다른 블로그 오염 가능성 미점검 |
| 4 | ETAP 영어 H2 전부 strong 강등 | H2-GUARD 패턴 한국어 전용 | 한글 감지 바이패스 | — |
| 5 | airlines 제휴링크 0개 | `city LIKE '%IATA%'` 매칭불가 설계버그 | 전면 교체(jetradar) | — |
| 6 | airlines 가짜항공사 토픽 | synthetic `Aero X code Airways` 생성 | 296행 purge+26 리셋 | 생성 스크립트 원천 미추적 |
| 7 | travel1 duplicate_source_id 5연속 | fetcher content.db만 조회, publisher는 stap_content.db | 필터 추가 | — |
| 8 | compare no_topics 4/5 | 단종 토픽 무한순환 | reset 시판검사+1,569 skip | carisyou 단종표기 자체는 정상 |
| 9 | carisyou 후속모델 미스캔 | scan_log 커서가 빈페이지대역 우선 | cars.MAX 고정 | — |
| 10 | 신차 쓰레기 이미지 | carisyou 동일 쓰레기가 모델명으로 | 11행 DELETE+blocked 615 | 잔여 77 검수 대기 |
| 11 | mark_published_by_id 로그오염 | 데이터부족 스킵도 publish_log INSERT | 13행 DELETE | 설계는 유지(무한방지 목적) |

## Environment Notes

- travel-en.db 백업: bak_topic_refill_20260906211044, bak_airlines_layover_20260907104717. car.db: bak_discontinued_cleanup_20260907144338
- 파괴적 로그: logs/destructive_2026-09-06.log (3엔트리), logs/destructive_2026-09-07.log (5엔트리)
- select_topic 재현 시 `conn.row_factory = sqlite3.Row` 필수 (topic_manager가 t["id"] 인덱싱)
- 테스트 pre-existing 실패: test_pipeline_skeleton(_normalize_result reason), C06 ImportError — git stash로 원인 확인됨. OPS_TEST_MODE=1로 단독 실행 시 대부분 통과
- 검수 페이지: scripts/image_review_new_cars_20260907.html (77 잔여)
- tmp 스크립트 유지: scripts/tmp_h2_restore.py, scripts/tmp_btn_class.py (재사용 가능성)
- STAP repo (/Users/twinssn/Projects/STAP) 변경분 5 writer + 2 content — STAP repo에서 커밋 필요

## Resume Instructions

1. `python3 scripts/tmp_refill_precount.py` 없음 — remaining 확인은 SQL: `SELECT COUNT(*) FROM {tbl} WHERE exhausted=0 AND id NOT IN (SELECT topic_id FROM publish_log WHERE blog_id=? AND topic_id IS NOT NULL)`
2. 내일 오전: scheduler.log에서 06:30 daily_refresh 순환 확인 → scan_log 조회 → compare/ev 발행 결과 확인
3. alert 배치 수신 시 triage 스킬(publish-error-triage) 적용 — INDEX.md 최신 엔트리가 본 세션 기록
4. 모든 worklog는 `.planning/worklog/WL-2026090*.md`, INDEX는 `.planning/triage/INDEX.md` 최상단

## Uncommitted Changes (5000)

**수정 (M):** .planning/STATE.md, .planning/triage/INDEX.md, .planning/worklog/WL-20260905-seaprap-quality-audit-fixes.md, config/blogs.d/manual_blog_for_backup.yaml, data/analytics_status.json, pipelines/car/daily_refresh.py, pipelines/curation/{keyword_expander,keywords,pipeline}.py, pipelines/etap/{airlines_pipeline,post_processor}.py, pipelines/travel/fetcher.py, shared/publishers/hugo_writer.py

**신규 (worklog):** WL-20260906-{etap-topic-refill,golf-driver-contamination-fix,stap-publish-error-triage}.md, WL-20260907-{airlines-layover-topic-purge,compare-ev-no-topics-discontinued,etap-h2-affiliate-btn}.md, logs/destructive_2026-09-{06,07}.log

**커밋 금지 (런타임 산출물):** output/image_cache/, output/images/*, data/.lock_*, ops_dashboard/ops.db-{shm,wal}, *.png 스크린샷, scripts/stap_quality_audit.py(정체불명) — git add 시 선택적으로: `git add .planning/ logs/destructive_2026-09-06.log logs/destructive_2026-09-07.log pipelines/ shared/ config/`

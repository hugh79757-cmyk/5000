# .continue-here.md — 세션 핸드오프 (2026-09-03 17:00경)

## Current Position

**Phase 77 (analytics 셀프 개선 루프) — 완료 + etap 확장 조사 중간에 일시 정지.**
- `.planning/phases/phase-77-analytics-self-improvement/` — CONTEXT/PLAN/RUNBOOK-enable/ACTIVATION/VERIFICATION 전부 완료 (5/5 PASS)
- **개선 루프 활성 상태**: 스케줄러 PID 45719, `PERF_SIGNALS=1` plist 적용 완료 (15:03 watcher 자동 활성화, `logs/perf_activate_20260903.log` 마지막 줄 "=== 개선 루프 활성화 완료 ===")

## Completed This Session

1. **Phase 77 W1-3 전체 구현 + 검증 + 커밋**:
   - W1 `d97e9b868`: `shared/performance_signals.py` (83줄, gsc_keywords 클릭≥1 90일 윈도우 → data/keyword_performance.json 원자적 쓰기) + collect_analytics.sh non-fatal 훅
   - W2 `0d730ce7e`: curation `_select_keyword` boost (`_boost_by_signals` 순수함수 + `_apply_perf_signal_boost` env 게이트) + tests/test_performance_signals.py (9 테스트)
   - W3 `74f2dc45a`: RUNBOOK-enable.md + STATE 갱신 (`4cc4ac50e`) + VERIFICATION.md (`aac967d15`, 5/5 PASS)
2. **GSC 90일 백필** `ef0c0a37b`: backfill_gsc.py — 90/90일 완료 (06-02~08-31, keywords 10,738 / pages 35,437 / clicks 429, 281.6분)
3. **게시물 상태 리포트** `2cee710b9`: post_status_report.py — 3781편 5분류 (15일 기준 top=0, low=24, zero=158, unmatched=3599 — 매칭률 제한 원인: 403 무데이터 21도메인)
4. **travel 지역 boost** `c16d6ce24`: fetcher.py `_perf_boosted_sigungu` (신호 지역 가중 3배, 광역시도명→시군구 확장, FIVEK_ROOT 절대경로) + tests/test_travel_perf_boost.py (5 테스트)
5. **PERF_BLOG_ID 주입** `72d4eb933`: travel `_run_single` 시작부 env 주입 1줄
6. **개선 루프 활성화**: watcher (activate_perf_loop.sh)가 백필 종료 감지 → 추출기 재생성 (37패턴) → plist PERF_SIGNALS=1 → launchd reload (PID 33969 → KeepAlive 재시작으로 45719)
7. **rotcha 메뉴얼 블로그 제외 확정** (사용자 결정): hotissue/compare/ev/tco/guide/deal 전부 car 파이프라인 → published_url 오염 368건 복구 스킵, dryrun 스크립트 폐기

## Remaining Work

1. **etap boost 확장 — 결론 도출 직전에 정지** (사용자 "추가해줘" 승인 후 조사):
   - **핵심 발견**: etap 36 블로그는 **클릭 신호 0행** (90일 gsc_daily_summary: escape-hugo 0클릭/439노출, cruise-hugo 0/200이 최대). keyword_performance.json은 `clicks>=1` 게이트라 **etap에 boost 코드를 넣어도 전부 passthrough — 발동 불가**
   - etap 개입 지점은 파악 완료: `pipelines/etap/topic_manager.py:pick_topic_by_id(topic_table, blog_id)` (priority DESC 정렬로 단일 토픽 선택, publish_log 중복 제외, deals origin 필터) — boost를 넣는다면 이 함수의 ORDER BY에 신호 매칭 가중치 추가가 최소 변경점
   - **다음 세션 결정 사항**: (a) 클릭 기준 완화 — 노출 기준으로 게이트 낮춰 escape-hugo(439노출) 등에 신호 부여, (b) etap 코드 삽입 보류 — 신호 생길 때까지 관찰, (c) 지금 삽입해도 대기 상태로 두기 (향후 첫 클릭 발생 시 자동 발동). 사용자에게 3옵션 제시 후 결정 필요
2. **첫 boost 실측 대기**: travel3-hugo 다음 발행 (07:06 스케줄)에서 첫 `_perf_boosted_sigungu` 발동 — 대전 5구 가중 확인
3. **가동 후 검증** (활성화 후 1~2시간 경과 시): 게이트 4종 로그 (scheduler_stdout.log perf-boost 라인), curation publish_log 키워드 대조, 회귀 테스트 (tests/test_travel_perf_boost.py + tests/test_performance_signals.py + tests/curation 19F/142P baseline)
4. **16:06 스케줄러 자동 재시작 원인 추적 보류**: S01/S02 게이트 WARNING 연쇄 (best-toys/best-unisex-clothing.rotcha.kr, ratio=0.0000/sim=1.0000) 중 KeepAlive 재시작 1회 — 재발 시 원인 추적. Traceback 없음

## Decisions

- **rotcha 계열(hotissue/compare/ev/tco/guide/deal)은 개선 루프 제외** — 사용자 지시 "핫이슈 롯차등 메뉴얼 블로그는 포함시키지 않는다" (전부 car 파이프라인)
- **개선 루프 활성화는 옵션 2 (watcher 자동)** — 백필 완료 감지 후 자동 plist+reload, 사람 개입 최소화
- **오염 published_url 368건 복구는 스킵** — 전부 rotcha 계열이고 slug NULL이라 content.db만으로 재구성 불가 (Hugo 디스크 파일 근사 매칭은 36.4%만 성공, TV 방송물 220건은 파일 자체 소실)
- **boost 게이트 구조 공통 패턴 확정**: env PERF_SIGNALS 체크 → blog_id별 신호 로드 → 토큰/지역 매칭 → 가중 증폭/정렬 → 예외 시 passthrough (기존 동작 완전 보존)

## Blockers

- 없음. etap 확장은 블로커가 아니라 결정 대기 상태 (위 Remaining Work #1)

## Bugs & Issues Discovered

1. **기존 수집기 결함 — collect_gsc(days=1)이 days 무시** (analytics_collector.py:635-644)
   - 증상: gsc_pages가 08-15부터만 존재, 6h마다 같은 날 4번 중복 수집
   - 원인: 항상 `today-3` 하루치만 조회
   - 해결: backfill_gsc.py로 90일 백필 완료. **본 수집기 days 파라미터는 미수정** — 향후 개선 후보
   - 미해결: daily 수집기가 여전히 하루치만 조회 (신규 데이터는 정상 유입되므로 긴급 아님)
2. **16:06 스케줄러 자동 재시작**
   - 증상: S01 Uniqueness gate FAILED (ratio=0.0000, max_sim=1.0000) + S02 gate FAILED (sim=1.0000) WARNING 연쇄 후 프로세스 종료 → launchd KeepAlive 재기동
   - 원인: 미확인 (Traceback 없음 — WARNING 후 정상 종료로 보임)
   - 해결: 없음 — 1회 발생, 재발 시 추적
   - 미해결: 예 — 재발 여부 관찰 필요
3. **GA4 미수집** — ga4_pages에 4월 데이터만 존재, 현재 미수집. 체류시간/참여 기반 학습 불가 (GSC+AdSense만 사용)

## Environment Notes

- 스케줄러: launchd `com.5000.scheduler.plist` — **PERF_SIGNALS=1 EnvironmentVariables 추가됨** (백업: `.bak_perf_20260903`)
- 백업 plist 원복 시 boost가 조용히 OFF 되므로 주의
- keyword_performance.json: 37패턴 (90일 기준) — 6h마다 collect_analytics.sh 말미에서 자동 재생성
- `PERF_SIGNALS=0` env로 즉시 무력화 가능 (재시작 불필요, 기본값 OFF 구조)
- travel fetcher는 cwd를 TAP_ROOT로 chdir — 신호 파일 경로는 FIVEK_ROOT 기준 절대경로로 해결됨 (c16d6ce24)
- GSC API: 73사이트 3계정 OAuth, 16사이트 403 (타계정 소유 — curation 계열 대부분이 여기라 신호 불가)

## Resume Instructions

1. `cd /Users/twinssn/Projects/5000`
2. 컨텍스트 복구: 이 파일 + `.planning/phases/phase-77-analytics-self-improvement/` (CONTEXT.md, VERIFICATION.md, ACTIVATION.md)
3. 활성 상태 확인: `ps aux | grep "[s]cheduler.py"` + `tail -1 logs/perf_activate_20260903.log` + `ps eww <PID> | grep -o PERF_SIGNALS=1`
4. 첫 boost 실측 (travel3 07:06 발행 후): `grep "perf-boost" logs/scheduler_stdout.log | tail -5`
5. etap 확장 결정 필요 시: 위 Remaining Work #1의 3옵션 사용자에게 제시
6. 회귀 확인: `OPS_TEST_MODE=1 python3 -m pytest tests/test_travel_perf_boost.py tests/test_performance_signals.py -q` (14 passed 기대) + `tests/curation` (19F/142P baseline)

## Uncommitted Changes

**주의: 병렬 세션 작업이 섞여 있음 — 이 세션 것이 아님:**
- `pipelines/etap/*_pipeline.py` 24개 + image_fetcher.py — **병렬 세션 "etap-image-fetcher-city-pollution-batch-fix"** (triage `.planning/triage/20260903--etap-image-fetcher-city-pollution-batch-fix.md`, status: resolved, Hugo 빌드+wrangler 3배포 검증 완료) — **미커밋 상태, 병렬 세션이 커밋 예정이었는지 확인 필요**
- `pipelines/curation/pipeline.py` camping-hugo allowed 목록 확장 — 병렬 세션 작업으로 추정 (이 세션은 boost만 커밋함, golf fix `3ca9f463d`가 마지막 curation 커밋)
- `docs/lookbook/ERROR_LOOKBOOK.md`, `.planning/triage/INDEX.md`, `data/analytics_status.json` — 병렬 세션/스케줄러 산출물
- `.planning/worklog/WL-20260903-scheduler-interval-skip-fix.md` (untracked) — 다른 세션 작업
- 이 세션 작업은 **전부 커밋 완료** — 마지막: `72d4eb933`

**WIP 커밋 시 주의: `git add -A` 금지 — 병렬 세션 파일이 섞임. 이 세션 파일만 선택적으로 커밋할 것 (이번엔 추가 커밋 없음 — 전부 커밋됨).**

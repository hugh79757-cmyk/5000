# Continue Here — 5000

**Last session:** 2026-07-02 (약 30분)
**Status:** Phase 10 (Blowfish Shortcodes) — 배포 완료, toggle off 상태. Phase 11 (coupang import fix) — 완료.

---

## Current Phase: Phase 11 — Coupang Import Fix ✅ (완료)

### Goal
`content_enhancer.py:_insert_coupang()`가 `shared.coupang_car`에서 `inject_coupang`을 import 못 하는 버그 수정. rank-hugo 발행 실패 원인 제거.

### What Was Done

| Commit | What |
|--------|------|
| `9c5a3e56d` | `inject_coupang()` 함수를 `coupang_car.py`와 `coupang_senior.py`에 추가 |

### Files Changed
- `shared/coupang_car.py` — `inject_coupang(body_md, segment, fuel_type, blog_cfg)` 추가
- `shared/coupang_senior.py` — `inject_coupang(body_md, segment, fuel_type, blog_cfg)` 추가
- `.planning/phases/11-coupang-import-fix/11-01-PLAN.md` — 플랜 문서

### Root Cause
`coupang_car.py`와 `coupang_senior.py`에는 `CoupangCar`/`CoupangSenior` 클래스만 있고 standalone `inject_coupang` 함수가 없었음. `content_enhancer.py`가 이 함수를 import 하려다 실패.

### Impact
- rank-hugo (`07:22` 발행)이 매일 아침 실패
- catchup이 3회 더 재시도 → 총 4회 Telegram 오류 메시지
- 다른 car 블로그(pick-hugo 등)도 동일한 오류 발생 가능

---

## Previous: Phase 10 — Blowfish Shortcode Engagement (HOLD)

### shortcodes_enabled: false (CURRENT)
Config in `config/blogs.d/tap.yaml` line 37: `shortcodes_enabled: false`
- hugo_writer.py skips ALL shortcode post-processors
- pipeline.py skips `<!-- CHART: -->` injection

### To Re-enable
Change `shortcodes_enabled: false` → `true` in `config/blogs.d/tap.yaml` line 37.

### Enhancement Proposals
📄 **`.planning/phases/10-blowfish-engagement-optimization/10-IMPROVEMENT-PROPOSALS.md`**
- 8 items across P1~P4 priority

---

## Uncommitted Changes (stale — not related to this session)

```
 M config/blogs.d/cap.yaml
 M coverage.xml
 M pipelines/curation/keywords.py
?? .coverage
?? data/cooldown.json
```

---

## Resume Instructions

```bash
cd /Users/twinssn/Projects/5000

# State check
cat .continue-here.md

# Phase 10 shortcodes toggle
# Edit config/blogs.d/tap.yaml → shortcodes_enabled: true/false

# Phase 11 fix already deployed — verify next publish
cat .planning/phases/11-coupang-import-fix/11-01-PLAN.md
```

# Continue Here — Pipeline Error Fixes + Kuta-hugo Thumbnail Fix

**Created:** 2026-07-11
**Phase:** Triage — Pipeline error fixes + kuta-hugo broken thumbnail
**Status:** ✅ All fixes implemented, kuta-hugo deploy pending

---

## Current Position

Two triage sessions on 2026-07-11:

1. **Session 1 (committed)**: Pipeline error fixes for 6 blogs + branch documentation
2. **Session 2 (current)**: kuta-hugo broken thumbnail fix + auto-publisher investigation + GSD docs update

## Session 2 — Completed Today

### 1. kuta-hugo `broken_featureimage` — Thumbnail Fix
- **Problem**: `featureimage` pointed to `img-kuta.informationhot.kr/images/kuta/{slug}/thumbnail.webp` (WordPress-only domain, 404)
- **Root Cause**: Post published outside 5000 pipeline → `batch_thumbnails.py` never ran
- **Fix**: `batch_thumbnails.py --slug "20260617-212002-부산-동래구-카페-추천-핫플레이스-5곳"` executed
  - `thumbnail_generator/generator.py` → generated thumbnail
  - `r2_uploader.py` → uploaded to R2
  - `hugo_writer.py` → updated `featureimage` to correct R2 URL
- **Verification**: R2 URL HTTP 200 confirmed

### 2. Auto-publisher Thumbnail Investigation
- Found: `md-editor-nicegui/core/publish/auto_scheduler.py` (launchd `kr.informationhot.auto_scheduler`, 30-min interval)
- **6 Hugo jobs** configured in `scheduler_jobs.json`: kuta, informationhot, rotcha, biz.techpawz, Issue.techpawz, techpawz
- ✅ blogsmith-output posts auto-generate **gradient overlay thumbnails** via `make_thumb()` → `thumbnail_gen.py`
- Flow: `auto_scheduler.py` → `make_thumb()` → R2 upload → `_add_cover_image()` → `_convert_md_paths()` → `wrangler pages deploy`
- ⚠️ 5000-pipeline posts require manual `batch_thumbnails.py` run
- Key files: `hugo.py` (R2 upload/cover/convert), `thumbnail_gen.py` (gradient), `config.py` (site config)

### 3. GSD Documentation Update
| Document | Change |
|----------|--------|
| `AGENTS.md` | Added `broken_featureimage` incident + error pattern row + key files (batch_thumbnails, thumbnail_generator, r2_uploader, md-editor-nicegui refs) |
| `.planning/triage/20260711--kuta-hugo-broken-thumbnail.md` | New triage entry with full investigation |
| `.planning/triage/INDEX.md` | Added 2026-07-11 entry |
| `.planning/STATE.md` | Quick tasks updated, last_updated → 2026-07-11 |
| `.planning/ROADMAP.md` | last_updated refreshed |

## Session 1 — Completed Earlier (committed)

### Pipeline Error Fixes (6 blogs)
| Blog | Error | Fix | File |
|------|-------|-----|------|
| travel2-hugo | `no_result` (10x) | `days=14→7` | `pipelines/travel/pipeline.py:247` |
| sector-hugo | `no_content` (23x) | `lookback_days=7→3` | `STAP/pipelines/sector/pipeline.py:337` |
| appliance-hugo | `similar_title` | SequenceMatcher 80% threshold | `shared/content_store.py:228` |
| rap5-hugo | `keyword_coverage` | Added to `TECH_IDENTIFIERS` | `shared/post_validator.py:157` |
| pet-hugo | `low_relevance` | Threshold override 0.55 | `shared/relevance_scorer.py:15` |
| kitchen/health | `irrelevant_products` | "가벼운" removed from all 12 blogs | `pipelines/curation/keywords.py` |
| CUAP (10 blogs) | lead font-size | `.lead{font-size:1rem}` + deploy | CSS |

### Branch Documentation (6 files in `docs/branches/`)
- TAP, STAP, RAP, CUAP, CAP, SEAP — centralized in 5000

---

## Remaining Work

### [HIGH] kuta-hugo Deploy
- kuta-hugo rebuilt (`hugo --gc --minify`) but **wrangler deploy not run**
- Frontmatter has correct R2 URL for `featureimage`
- Command: `cd /Users/twinssn/Projects/kuta-hugo && wrangler pages deploy public --project-name kuta-hugo --commit-dirty=true`

### Monitoring (24h)
- Confirm travel2-hugo publishes successfully (was failing 10x)
- Confirm sector-hugo publishes successfully (was failing 23x)
- Confirm kitchen-hugo/health-hugo no longer quarantine
- Confirm rap5-hugo passes keyword_coverage
- Confirm pet-hugo passes low_relevance

---

## Decisions

1. **Two thumbnail systems**: 5000 uses `batch_thumbnails.py` (image-based, manual run), blogsmith auto-publisher uses gradient overlay via `thumbnail_gen.py` (automatic). Important architectural distinction — documented in AGENTS.md.
2. **"가벼운" removal (not replacement)**: Removed generic keyword entirely rather than guessing category-specific replacements. Safer approach.
3. **SequenceMatcher over prefix LIKE**: Replaced SQL `LIKE '%core%'` with Python SequenceMatcher 80% ratio. More accurate similarity detection.
4. **Conservative lookback reduction**: travel2-hugo 14→7, sector-hugo 7→3. Tuned per pipeline data availability.

---

## Blockers

None.

---

## Resume Instructions

### Deploy kuta-hugo thumbnail fix (HIGHEST PRIORITY)
```bash
cd /Users/twinssn/Projects/kuta-hugo
hugo --gc --minify   # already done, re-run if public/ is stale
wrangler pages deploy public --project-name kuta-hugo --commit-dirty=true
```

### Check pipeline fix results
```bash
python3 -c "
import sqlite3
db = sqlite3.connect('data/content.db')
for blog in ['travel2-hugo', 'sector-hugo', 'appliance-hugo', 'rap5-hugo', 'pet-hugo', 'kitchen-hugo', 'health-hugo']:
    rows = db.execute('SELECT created_at, status, error FROM publish_log WHERE blog_id=? ORDER BY created_at DESC LIMIT 3', (blog,)).fetchall()
    print(f'\n=== {blog} ===')
    for r in rows:
        print(f'  {r[0]} | {r[1]} | {r[2]}')
"
```

### Manual pipeline tests
```bash
python dispatcher.py travel2-hugo   # test travel fix
python dispatcher.py rap5-hugo      # test rap5 fix
python dispatcher.py pet-hugo       # test pet fix
python dispatcher.py kitchen-hugo   # test kitchen fix
```

---

## Uncommitted Changes

```
 M AGENTS.md                                    ← incidents + error pattern + key files
 M .planning/ROADMAP.md                         ← last_updated refresh
 M .planning/STATE.md                           ← quick tasks + last_updated
 M .planning/triage/INDEX.md                    ← new triage entry
?? .planning/triage/20260711--kuta-hugo-broken-thumbnail.md  ← NEW triage
 M pipelines/curation/keywords.py               ← "가벼운" removed
 M pipelines/curation/pipeline.py               ← SequenceMatcher fix
 M shared/content_store.py                      ← title_similar_exists()
 M shared/post_validator.py                     ← TECH_IDENTIFIERS + rap5
 M shared/relevance_scorer.py                   ← pet-hugo threshold 0.55
 M shared/publishers/hugo_writer.py             ← IMAGE-GUARD, sanitize
 M pipelines/travel/pipeline.py                 ← days=14→7
?? docs/branches/                               ← 6 branch docs
?? scripts/batch_thumbnails.py                  ← thumbnail batch generator
?? scripts/resume_backfill.py
?? scripts/fix_remaining_posts.py
?? shared/thumbnail_generator/                  ← thumbnail generation module
?? .planning/triage/*.md                        ← other triage entries


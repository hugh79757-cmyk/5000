# .continue-here.md — Handoff

**Generated:** 2026-08-31 11:00 KST (session pause)
**Branch:** main (HEAD 033ee5e2d, 1 ahead of origin/main before push)
**Active work:** Fleet hardening continues — best-* fleet stabilization + publish-error triage

---

## Current Position

No formal GSD phase in progress. Continuous fleet audit + hardening track (publish-error P01/P02/P25, keyword/prompt/dashboard purge, Phase 75 best fleet, daytrips quality, AdSense compliance). Latest session focused on best-* fleet post-deploy stabilization and rap4-hugo P25 recurring timeout.

Latest commit: `033ee5e2d fix(dispatcher): daemon worker threads for 150s pipeline timeout` (includes best fleet filters + title diversification + AdSense slot). Pending push of this commit only.

---

## Completed This Session

1. **best-* fleet never-publish root cause** — `CATEGORY_FILTERS`/`RELEVANCE_CONFIG` missing `best-*` entries → score 0.0 < 0.75 → P14/P36 4/5 consecutive. Added `best-kitchen/beauty/baby` entries to `pipelines/curation/pipeline.py:243` and `shared/relevance_scorer.py`, fixed `best_categories.py` 1020→1013 (live probe 11/20 주방용품), purged polluted keywords (12 bad + health junk), cleared `curation.db`. Published best-kitchen 09:25/09:46 (quota 2/2), best-beauty 09:26, best-baby 09:34 — all wrangler ~24-28s, live 200.

2. **best-* title diversification** — all best titles were `베스트 1-5위` single pattern → `similar_title` block on second run (세타필 vs 센카). Added 5 templates to `shared/title_templates.py` (best_ranking/comparison/budget/situation/spec), `BLOG_TEMPLATE_OVERRIDES` for best-* prefer best_*, `_classify_title` detects 베스트 1~5위, updated `pipelines/curation/writer.py` 베스트 prompt with 제목 다양화 필수 (판매량/브랜드/가격/스펙 angle rotate, 앞2단어 겹침 금지). Verified compile + 5 variants distinct. Quota bypass publish for best-baby 10:10:39 리벤스베베앙 물티슈 베스트 … 4719 chars 28.6s.

3. **AdSense slot update** — snippet `ca-pub-6677996696534146 slot 4009356716 auto` (informationhot family). Audited 3 best blogs: all had topSlot/inArticleSlot 2195212287, extend-head hyphen, site.Params PASS, slot 4009356716 not found. Updated `config/_default/params.toml` topSlot 2195212287→4009356716 in all 3 via parallel workers, deployed (kitchen 24.5s, beauty 26.7s, baby 23.8s), verified live HTML `data-ad-slot 4009356716 auto` + fluid in-article.

4. **rap4-hugo P25 recurring analysis** — gsd quick 20260830 fix covered tour/michelin (150s/300s) but not rap4 (separate RAP pipeline). Measured timeout budget 150+180+300=630>600, but real cause non-daemon ThreadPoolExecutor worker surviving to scheduler 600s kill + duplicate dispatch 09:23. Previous daemon attempt used Python 3.13 API (`_initializer/_initargs`) incompatible with 3.14 (`_create_worker_context/weakref`).

5. **rap4 daemon fix (re-fix)** — patched `dispatcher.py:1810` to `_DaemonThreadPoolExecutor` with daemon=True, 3.14-compatible `_adjust_thread_count` (weakref + _create_worker_context + _threads_queues). First run 10:29 failed AttributeError, repatched 10:30 OK. Subsequent rap4 runs 10:30:57 and 10:35:14 both `published` (미사강변아란티움, 서광등촌마을아파트). Fleet-wide fix — all blogs share same dispatcher path.

---

## Remaining Work

- **Push pending** — commit 033ee5e2d not yet pushed (see Resume Instructions).
- **Dashboard P25 open** — `publish_error_events` rap4-hugo P25 `fb78bfd2` state=open 02:33 UTC timeout 600s still open despite 10:35 success (manual dispatcher → monitor auto-close not triggered by manual run). Will auto-close on next scheduler success cycle; or manual close.
- **ETAP 36 per-blog repos uncommitted** — params.toml TOC false + single.html header-ad removal deployed live but per-blog git repos still dirty (from earlier session, unchanged).
- **deals-hugo disabled** — 134/134 origins exhausted, needs new flight_prices origins before re-enable.
- **Scheduler duplicate dispatch** — 09:23 double Queue executing for rap4 still unpatched (daemon mitigates but not root). If recurs, fix scheduler.py re-entrancy.
- **Best fleet verification** — best-kitchen quota full 2/2, beauty/baby 1 each; next day quota resets. Verify best-* titles diversify across days (template picker).

---

## Decisions

- **Best title diversification via templates** — not prompt-only rotation; persisted in `BLOG_TEMPLATE_OVERRIDES` to guarantee distinct angles.
- **Daemon threads over scheduler timeout bump** — raising scheduler 600→900 masks leak; daemon fix is root (fleet-wide 59 historical 600s kills share same defect).
- **TopSlot slot 4009356716** — Display auto slot replaces generic 2195212287 for 3 best blogs; inArticleSlot stays 2195212287 fluid.
- **Quota bypass via runtime patch** — `shared/quota_bypass.py` (daily_quota=999 patch) keeps duplicate guards; used for best-baby manual run.

---

## Blockers

- None blocking. P25 recurrence mitigated; dashboard open status cosmetic pending auto-close.

---

## Bugs & Issues Discovered

1. **symptom:** best-* fleet 0 posts despite live 200, scheduler P14 low_relevance 4/5 consecutive, avg=0.00
   **cause:** `CATEGORY_FILTERS` missing `best-*` keys → `allowed=[]` → score_products 0.0 < threshold 0.75, no fallback for is_best.
   **fix:** added best-kitchen/beauty/baby to CATEGORY_FILTERS + RELEVANCE_CONFIG, fixed category_id 1020→1013.
   **unresolved:** no — published verified.

2. **symptom:** best-beauty second run `similar_title` block (세타필·일리윤 vs 센카 퍼펙트) despite different products
   **cause:** single template `베스트 1-5위` pattern → SequenceMatcher 80% duplicate within 14 days same blog_id.
   **fix:** 5 best templates + BLOG_TEMPLATE_OVERRIDES + writer prompt diversification.
   **unresolved:** no — next run will pick different template.

3. **symptom:** rap4-hugo P25 timeout 600s recurred after 150s guard commit (09:23 survive to 600s, 11:22 pipeline_timeout_150s)
   **cause:** ThreadPoolExecutor worker non-daemon → `result(timeout=150)` return does not kill process; interpreter waits for live thread. 3.14 API mismatch (`_initializer` missing) caused AttributeError on first daemon attempt.
   **fix:** `_DaemonThreadPoolExecutor` daemon=True with 3.14-compatible weakref/_create_worker_context/_threads_queues; py_compile OK, subsequent runs published 10:30/10:35.
   **unresolved:** no for timeout; duplicate dispatch remains.

4. **symptom:** AdSense slot 4009356716 not found in best-* blogs (grep 0)
   **cause:** blogs used generic topSlot 2195212287.
   **fix:** updated topSlot to 4009356716 in 3 params.toml, deployed, verified live.
   **unresolved:** no.

5. **symptom:** dashboard P25 open persists after manual success
   **cause:** `publish_error_events` auto-close triggers on scheduler cycle, not manual dispatcher run; occurrence_count=1 vs scheduler consecutive 1/5 separate.
   **fix:** none yet — will auto-close next scheduler success; or manual UPDATE state=closed.
   **unresolved:** cosmetic, pending next cycle.

---

## Environment Notes

- Python 3.14, ThreadPoolExecutor 3.14 internal API uses `_create_worker_context()` not `_initializer` — daemon subclass must match.
- Deploy via `dispatcher.py {blog_id}` ONLY; Hugo builds `HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes hugo --gc --minify`; Workers deploy `wrangler deploy --config wrangler.toml`.
- Ops dashboard DB `ops_dashboard/ops.db` tables `publish_error_events`/`daily_summary_events` etc. (no `events` table). P25 = scheduler timeout 600s.
- Scheduler timeout 600s at `scheduler.py:402`, pipeline guard 150s at `dispatcher.py:1835`, Hugo 180s, Wrangler 300s — budget 630 > 600 but daemon fix avoids reaching scheduler kill.
- Best fleet sites: `/Users/twinssn/Projects/cuap/best-*-hugo` (external to 5000), Workers type, daily_quota 2.

---

## Resume Instructions

1. `git log --oneline -5` — confirm 033ee5e2d at HEAD.
2. `git status --short` — should show only runtime locks/ops.db-shm/wal/logs.
3. `git push` — push 033ee5e2d (1 commit ahead).
4. Verify rap4 dashboard close: `sqlite3 ops_dashboard/ops.db "SELECT state FROM publish_error_events WHERE blog_id='rap4-hugo' AND problem_id='P25' ORDER BY occurred_at DESC LIMIT 1"` — expect `closed` after next scheduler run; if still open, manual: `sqlite3 ops_dashboard/ops.db "UPDATE publish_error_events SET state='closed', resolved_at=datetime('now') WHERE event_id='fb78bfd2-839b-4e3d-8073-b2e49a80ee9f'"`.
5. Verify best live: `curl -s https://best-kitchen.informationhot.kr/ | grep -o 'data-ad-slot=\"[^\"]*\"' | head`
6. Next day quota: scheduler will auto-publish best-*; check `sqlite3 data/content.db "SELECT blog_id,COUNT(*) FROM publish_ledger WHERE date(created_at)=date('now') AND status='published' GROUP BY blog_id"`.

---

## Uncommitted Changes (at pause)

- `data/.lock_best-*-hugo` (3 runtime locks — DO NOT COMMIT)
- `ops_dashboard/ops.db-shm`, `ops_dashboard/ops.db-wal` (runtime — DO NOT COMMIT)
- `ops_dashboard/logs/` (transient)
- ETAP 36 blog repos: params.toml + single.html dirty per-repo (deployed live, uncommitted)
- 5000 repo: 1 commit ahead (033ee5e2d) not yet pushed — needs `git push`

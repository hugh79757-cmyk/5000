# .continue-here.md — 2026-09-11 15:05

## Current Position

운영 세션 (GSD phase 아님). 오전 핸드오프(13:40) 이어: LLM 체인 수리, ev kona/k8 발행+이미지, FIX C/C-2, 연비 fuel_nm 오매칭 FIX — 전부 커밋·push 완료.
ev-hugo: ev_analysis 풀 정상 소진 — 게이트(시판×연비) 통과 25대 전원 90일 콤보가드. 해제: 모델Y 11/8 최초, 11/25~ 대량(프리우스/EV3/EV9/모델3 등).
kona(5062)/k8(5063) 14:19~14:32 발행 완료, 배포 미실행(BUG-11) — 다음 발행 시 자동 배포.
사용자 결정 (c): ev에 타 post_type 추가 — 조사 완료(Resume #2), 구현 대기 (최우선).

## Completed This Session

### 1. LLM 체인 수리 (92e7534d6)
- opencode-zen 3티어(bigpickle/deepseek-free/mimo-free) enabled:false — 400 'Model is unavailable' 3회 재시도로 예산 소진. opencode 에이전트 전용으로 유지
- ai_writer.py: 요청 timeout 30→60s(L53), CHAIN_TIME_BUDGET 120→300s(L247)
- 검증: 단답 생성 5.8s / ev 실프롬프트 13s groq-gpt120b 2269자 — 이후 chain_timeout 재발 0

### 2. ev kona/k8 발행 + 이미지 (GV60/iX3는 오전 세션)
- k8(5063) 14:19, kona(5062) 14:32 발행 (article 13738=코나). --dry-run이 실발행 주의(BUG-10)
- scripts/verify_car_images.py 신규(verify_ev_images_20260911.py 일반화, car_id 인자): kona 5장(16922~26)/k8 4장(16940,16942~44) R2 업로드+verified=1, 실패 1장(16941 download timeout)
- 두 글 본문 첫 H2 후 이미지 삽입 + publish_log r2_url 갱신
- 배포: 수동 deploy_site 실패(BUG-11) → 다음 발행 시 자동 배포로 대체(사용자 확인)

### 3. FIX C + C-2 (bddd78aa4, 500a662e7)
- replenish_topics need 계산: raw pending → 시판 trim 보유(usable) pending 교체
- C-2: ev_cars/unpop_ev 풀 + ev_analysis usable 카운트에 lookup_fuel_efficiency 게이트 연결
- 검증: ev usable 2→11(raw 46→55), hotissue 27/rank 12/compare 25 정상 보충, 신규 pending 전원 게이트 통과
- 오전 수동 INSERT ioniq5/6 잔재 → skip_no_data 전환(연비 부재, 정상 차단)

### 4. 연비 fuel_nm 오매칭 FIX (data_builder.py, 커밋 완료)
- 원인: 연식 DESC 정렬 → 코나 일렉트릭(N-Line) 2024 '전기 4.7' 행이 하이브리드 2023 '휘발유 19.8'보다 먼저 채택. K8은 동명 EV형 없어 무사
- 패치: fuel_type→fuel_nm 우선정렬(전기→전기, 가솔린·하이브리드→휘발유, 디젤→경유, LPG→LPG) + fuel_type 옵션 인자 + LIMIT 5→15. 일치 행 없으면 기존 순서 유지(회귀 0)
- 검증: kona_hev 2025/2027 19.8 복원, k8 18.1·santafe 14.4·sonata 13.5·GV60 4.5·iX3 4.1·EV3 5.4·EV9 4.2·모델Y 4.9 불변, ioniq5/6 None 유지

### 5. ev_analysis 소진 확정 (정상 동작, 14:55)
- 게이트 통과 25대 전원 최근 30일 내 ev 발행 + 90일 콤보가드 → 선택 가능 0

## Remaining Work

1. **ev-hugo 타 post_type 추가 (사용자 결정 (c), 최우선)** — ev.rotcha.kr 반영. 후보 top5_rank/persona_pick. 구현: ev-hugo.yaml post_type 리스트화 + 신규 타입 topics 시드(daily_refresh SITE_POST_TYPE 확장 또는 수동 INSERT) + 발행 검증
2. **kona 글(13738) 연비 4.7→19.8 수정** — 미배포 상태라 지금이 창구. 재생성 vs 본문 수동 수정 결정 대기
3. **kona/k8 배포** — 다음 ev 발행 시 자동 (타입 추가가 시점을 당김)
4. 대시보드 older stale 116건 + fresh 재검사 (ev CQ03 포함)
5. 신차 이미지 미검수 471장 — python3 scripts/verify_car_images.py <car_id> 반복 실행으로 처리
6. 유료 API 키 제거 (env.common) — 사용자 직접 진행 예정. default(deepseek-v4-flash)·upstage-pro4 유료 여부 확인
7. 이월: travel* CQ03 과다 4블로그 / tco·pet 13:5x chain_timeout(LLM 수리 전 발생, 재발 모니터)

## Decisions

- ev 소진 대응: (c) 타 post_type 추가. 콤보가드 90일 유지(완화 안 함)
- opencode-zen: 글쓰기 체인 제외(opencode 에이전트 전용)
- LLM: 요청 60s/체인 예산 300s — 관찰 후 조정
- 배포: 수동 deploy 중단, 발행 시 자동 배포로 통일
- zen 제거 후 체인: groq 중심 + gemini-lite 폴백(새 GEMINI 키 정상작동)

## Bugs & Issues Discovered

### BUG-9: 연비 fuel_nm 오매칭 (P-data)
- 코나 하이브리드 글 '연비 4.7 km/L'(실제 19.8 km/L). 패치·검증 완료. kona 글 본문 수정 대기(Remaining #2)

### BUG-10: dispatcher --dry-run 실발행
- --dry-run이 select+생성까지 실행(topics published 전환). dry-run 후 topics/publish_log 확인 필수, 소진 검증 용도 부적합

### BUG-11: deploy_site 수동 호출 cf_project 미확인
- 플레이스홀더 문자열 전달로 wrangler rc=1. 수동 배포 시 blogs.d 설정 또는 wrangler pages project list로 프로젝트명 확인 후 호출

### 이월: BUG-2 잔여(신차 이미지 471장), BUG-6(travel CQ03 과다), BUG-8(car.db 06:30 lock 모니터)

## Environment Notes

- 오전 핸드오프 유지 + LLM timeout 60s/예산 300s, zen 제외, gemini-2.5/3.5-flash dead(3a6d1cb97)
- scheduler pid 762(13:46 재기동, 원인 미상 — 구 pid 74612), dashboard pid 784
- 백업 파일: models.yaml.bak_zenoff_* / ai_writer.py.bak_budget_* / daily_refresh.py.bak_fixc_* / data_builder.py.bak_fuelnm_* — 안정화 후 정리
- STAP 함정 유지: 진짜 sector DB = /Users/twinssn/Projects/STAP/data/stap_content.db (5000/data/stap_content.db는 사본)

## Resume Instructions

# 1. 환경 확인
ps aux | grep -E "scheduler.py|ops_dashboard" | grep -v grep

# 2. post_type 추가 구현 재료 (조사 — 결과 보고 구현)
cat config/blogs.d/ev-hugo.yaml
sqlite3 data/car.db "SELECT post_type, status, COUNT(*) FROM topics WHERE site_id='ev' GROUP BY post_type, status;"
sqlite3 data/car.db "SELECT id, site_id, car_id, competitor_car_id, post_type, priority, status FROM topics WHERE post_type IN ('top5_rank','persona_pick') AND status='pending' LIMIT 6;"
sed -n "$(grep -n 'def build_top5_rank_input' pipelines/car/data_builder.py | cut -d: -f1),+18p" pipelines/car/data_builder.py

# 3. 타입 추가 후 발행 확인 (dry-run 주의 BUG-10 — 상태 확인 필수)
python3 dispatcher.py ev-hugo 2>&1 | tail -5

# 4. kona 글 연비 수정 (Remaining #2)
grep -n "4\.7" "/Users/twinssn/Projects/cap/ev-hugo/content/posts/코나-하이브리드-연비-47kml-연간-585만원-유류비-내연기관-평균-대비-얼마나-아끼나/index.md" | head -8

# 5. 대시보드
export $(grep -E "^OPS_" .env | xargs) && curl -s -u "$OPS_USER:$OPS_PASSWORD" http://127.0.0.1:5060/api/attention | python3 -m json.tool | head -30

## Uncommitted Changes

- 없음 (본 세션 변경 전부 커밋·push 완료). untracked: .bak_* 4종(정리 대상), data/.lock_*, naver_captcha*, output/image_cache 등 — 커밋 불필요

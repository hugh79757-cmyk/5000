# Work Handoff — 2026-07-26 (Late Session)

## Current Position
**Active Phase:** Phase 51 — Image URL Token Repetition Bug Fix ✅ Complete
**Project:** 5000 Central Control Pipeline
**Branch:** main (5 commits ahead of origin)
**Last Commit:** `83e1346d0` — wip: save before pause — Phase 51 complete, ROADMAP updated, handoff created

---

## Completed This Session

### Phase 51: Image URL Token Repetition Bug Fix (5 Steps)

| Step | Description | Status | Verification |
|------|-------------|--------|-------------|
| 1 | 25개 Hugo 블로그(6000+ post) 전수 스캔 | ✅ | 1건 감염 발견: health-hugo 10888자 URL (gLozv0 반복 x수백) |
| 2 | 감염 파일 수정 | ✅ | 반복 패턴 제거 + `)` 이후 쓰레기 4994자 제거 → 정상 251자 URL. HTTP HEAD 200 확인 |
| 3 | Pipeline 예방 코드 | ✅ | `_fix_repeated_image_urls()` in `hugo_writer.py` + `writer.py`. 4자+ 5회+ 연속 반복 감지 → 스트립 |
| 4 | Batch fix script | ✅ | `scripts/fix_repeated_image_urls.py` with `--dry-run`, `--blogs`, `--backup-dir` |
| 5 | 테스트 검증 | ✅ | 63/63 통과 (1건 pre-existing failure unrelated) |

### Files Modified
- **`shared/publishers/hugo_writer.py`** — Added `_fix_repeated_image_urls()` + call in `_clean_body()` (all Hugo pipelines)
- **`pipelines/curation/writer.py`** — Added `_fix_repeated_image_urls()` + call in `_sanitize_body()` (curation pipeline only)
- **`scripts/fix_repeated_image_urls.py`** — NEW batch fix script
- **`health-hugo`** (cuap submodule) — Fixed infected post (10888→251 chars)
- **`.planning/ROADMAP.md`** — Phase 51 entry added, orphaned Phase 49 text removed from Phase 50

---

## Remaining Work (from ROADMAP)

| Phase | Status | Priority | Notes |
|-------|--------|----------|-------|
| Phase 44 Full Audit | 🟡 In Progress (44-01~03 done, 44-04~05 pending) | 🟡 MEDIUM | "좋은" 금지어, H2/H3 구조 강제, 브랜드 키워드 등 미달 |
| Phase 46 | 📋 Reserved | — | — |
| Phase 45 TAP Meta-Response | 📋 Planned | 🔴 HIGH | Validator + prompt hardening + retry — TAP core 모듈에서 일부 구현됨, pipeline-level retry/alert 미완 |

---

## Decisions
1. **Detection threshold:** 4자 이상 substring이 5회 이상 연속 반복 → URL 비정상으로 판정
2. **Fix strategy:** 반복 시작 위치(position)까지 URL 자르고, 남은 부분 폐기. HTTP HEAD 검증 대신 자르기 적용 (원본 URL 유효성은 쿠팡 CDN 특성상 동적이므로 매번 확인 불필요)
3. **Dual layer protection:** `_sanitize_body()` (curation writer) + `_clean_body()` (hugo_writer) 두 단계에서 검사 — LLM 생성 직후 + Hugo 쓰기 직전
4. **Batch script:** 5000 repo 내 scripts/ 에 배치, submodule(cuap/CAP)의 content 파일은 5000 스크립트가 직접 수정

## Blockers
- **None** — Phase 51 완료. 다음 Phase 시작에 대한 blocker 없음.

## Bugs & Issues Discovered

### Phase 51: 이미지 URL 토큰 반복 버그
- **증상:** health-hugo 포스트에서 쿠팡 이미지 URL 마지막 부분이 `gLozv0gLozv0gLozv0...` 로 수백 회 반복 (URL 10888자)
- **원인:** LLM(GPT) 생성 시 token repetition(stutter) 현상 — 이미지 URL 끝부분에서 특정 토큰 `gLozv0`을 계속 반복
- **해결책:** `_fix_repeated_image_urls()` 함수로 4자+ 5회+ 반복 패턴 감지 후 반복 시작점까지 URL 자름. 파이프라인 두 단계에 적용
- **미해결:** ❌ 해결 완료. 단, LLM repetition은 근본적으로 모델 특성이므로 향후 다른 패턴의 repetition이 발생할 가능성은 있음

## Environment Notes
- **macOS** (darwin), Python 3.14
- Hugo sites at `/Users/twinssn/Projects/cuap/*-hugo/` (10 blogs)
- CAP sites at `/Users/twinssn/Projects/CAP/*-hugo/` (6 blogs)
- All cuap repos are git submodules under `/Users/twinssn/Projects/cuap/`
- Pipeline test: `python3 -m pytest tests/ -x -q` (63/63 pass, 1 pre-existing failure `test_blocks_baby_diaper`)

## Resume Instructions

```bash
# 1. Check current state
cd /Users/twinssn/Projects/5000
git log --oneline -5
git status --short

# 2. If continuing next phase (e.g., Phase 45 TAP Meta-Response):
#    Read ROADMAP.md for phase details
cat .planning/ROADMAP.md | tail -100

# 3. Run tests to confirm baseline
python3 -m pytest tests/ -x -q -k "not test_blocks_baby_diaper and not test_each_blog_has_60_to_200_keywords"

# 4. Start new phase:
#    Phase 44 (Full Audit) — continue 44-04/44-05
#    Phase 45 (TAP Meta-Response) — implement pipeline retry + Telegram alert in app.py
```

## Uncommitted Changes
```
 M data/cooldown.json           # Runtime pipeline data (auto-updated, unrelated to phases)
 M data/failure_count.json      # Runtime pipeline data (auto-updated, unrelated to phases)
```
**Note:** These are runtime data files updated by scheduler execution. Not related to any phase work. Safe to ignore or commit as-is.

## Git Status
- 5 commits ahead of origin/main (all pushed: `git log @{u}..HEAD` shows 0 unpushed)
- Remote tracking up to date

# .continue-here.md — Handoff

**Generated:** 2026-08-31 (session pause)
**Branch:** main (up to date with origin/main)
**Active work:** Fleet quality hardening (publish-error triage, keyword/prompt purge, AdSense compliance, daytrips quality floor, P04 flicker gate, Phase 75 best-* fleet)

---

## Current Position

No formal GSD phase in progress. Work is a continuous "fleet audit + hardening" track that started from publish-error triage (P01/P02/P25) and expanded to 4 dashboard-missed classes (duplicate images, prompt leak, AdSense, readability), then to keyword/prompt/dashboard simplification, Phase 75 (best-seller standalone fleet), and daytrips quality+revenue upgrade.

Latest concrete deliverables all committed+pushed except per-blog ETAP git repos (see Uncommitted Changes).

---

## Completed This Session

1. **Fleet audit (4 classes)** — duplicate ETAP images (global dedup + page random), prompt leak (C04 pattern extension + 5 CUAP posts cleaned), AdSense (5000 clean; external per-site unverified), readability (ETAP TOC finding → later REVERTED, see Bugs).
2. **Publish-error fixes** — P15 false-positive gate (WARNING skip), travel3 P01 food-place guard, sector P02 date-aware title dedup, tour/michelin P25 (dispatcher ThreadPoolExecutor 150s + michelin 8→3 images), fitness/kitchen P36/P14 keyword purge, kitchen 다기능 vague-title guard, daytrips P04 flicker (false positive), deals P01 (134/134 exhausted → disabled in etap.yaml).
3. **Quick task keyword-prompt-dashboard** — T1 keywords 3193→2293 (900 purge across 16 blogs), T2 12 hardcoded price examples purged across 8 writers, T3 dashboard 9-family simplification applied (open 416→143).
4. **Phase 75 best-* fleet** — best_collector + best_categories (1020/1010/1011) + bidirectional entity linker + writer 베스트 branch + pipeline dispatch + coupang_client path fix + 3 Hugo Workers sites (best-kitchen/beauty/baby) deployed live, DNS best-*.informationhot.kr 200.
5. **P25 root cause re-fix** — ThreadPoolExecutor `with` shutdown(wait=True) defeated timeout; fixed shutdown(wait=False) + `_terminate_pipeline_children()` kills hugo/wrangler on timeout to release deploy lock.
6. **Daytrips quality+revenue** — writer 7-H2/1200-1800 structure, self-correct loop, quality-fail topic exhausted (stop token-waste loop), inline Viator deep links + Trip.com CTA, etap_affiliate.generate_trip_city_link(). Floor raised 3H2/600w → 5H2/1000w.
7. **AdSense compliance emergency** — discovered earlier readability audit wrongly set 36 ETAP blogs `showTableOfContents = true` (violates ADSENSE-GUIDE.md §2/§8 금기#1 → all ads unfilled). Reverted to false + removed header in-article ad. 36/36 redeployed.
8. **P04 flicker gate** — dispatcher.py:1872 now gates on `_is_site_live`; if site 200, skip CRITICAL + auto-close.
9. **Coupang skill** — created `/Users/twinssn/.config/opencode/skills/coupang-affiliate-api/SKILL.md` (HMAC, two endpoint forms + 404 gotcha, rate-limit, envelope, BEST_CATEGORY_MAP, Viator/Trip deep links). No literal keys by design.

---

## Remaining Work

- **ETAP 36 per-blog repos uncommitted** — the params.toml TOC revert + single.html header-ad removal are deployed (live) but the 36 individual git repos have uncommitted working trees (no top-level repo). Need `git commit` in each CUAP/ETAP blog repo for traceability.
- **deals-hugo re-enable** — disabled (134/134 valid origins exhausted, 196 invalid). Needs new flight_prices origins seeded (currently 10 US origins only) before re-enabling in etap.yaml.
- **dispatcher `_build_and_deploy_central` transient-False root cause** — P04 gate masks the flicker but the underlying transient-False (lock/preflight/hugo) is untraced. Optional: log stderr to deploy.log on that path.
- **Dashboard remaining noise** — P08 CoT open (88) downgrade to quiet; test-blog pollution filter applied but historical rows remain.
- **Phase 75 best-* verification** — human-verify live bestcategories probe + entity count >0 on best-*.informationhot.kr.

---

## Decisions

- **TOC must stay FALSE on ETAP** (ADSENSE-GUIDE.md §2/§8) — readability audit's true-setting was a revenue-breaking mistake, reverted.
- **Keyword purge principle** — a keyword must be a substring of its blog's `CATEGORY_FILTERS.allowed` or it is dead (causes P36/P14).
- **Quality-fail topics marked exhausted** — stops infinite token-waste regeneration loop.
- **Best fleet = separate blogs (Workers)** not sections — Pages limit 100 hit, used Workers (no 500-build limit).
- **No literal API keys in skills** — env-only by design.

---

## Blockers

- None blocking. deals-hugo is intentionally disabled pending data seeding.

---

## Bugs & Issues Discovered

1. **symptom:** daytrips-hugo generated 445-word / 2-H2 post, blocked at quality gate, never published, but same topic re-picked every run → token waste.
   **cause:** `daytrips_pipeline.py:130` returned False on is_draft without marking topic exhausted.
   **fix:** writer self-correct loop (d6dd4071a) + pipeline marks quality-fail exhausted. Floor raised (cdb82b007).
   **unresolved:** no — fixed and pushed.

2. **symptom:** P25 600s scheduler kill recurred after "150s timeout" commit.
   **cause:** ThreadPoolExecutor `with` block did `shutdown(wait=True)` joining worker thread, defeating timeout.
   **fix:** shutdown(wait=False) (d02a9a8c9) + `_terminate_pipeline_children()` SIGTERM/SIGKILL on timeout (a568d1b91).
   **unresolved:** no — pushed.

3. **symptom:** CRITICAL P04 deploy_error alerts for nightlife/extreme while sites were 200.
   **cause:** dispatcher.py:1872 reported P04 unconditionally when `_build_and_deploy_central` returned False, lacking `_is_site_live` gate.
   **fix:** added `_is_site_live` gate (760696462).
   **unresolved:** underlying transient-False untraced (gate masks it).

4. **symptom:** 36 ETAP blogs ads would be 100% unfilled.
   **cause:** readability audit (earlier session) set `showTableOfContents = true` — violates ADSENSE-GUIDE.md §2/§8 금기#1 (Blowfish sticky TOC breaks ad rendering).
   **fix:** reverted to false + removed header in-article ad (b10/b11); 36/36 redeployed.
   **unresolved:** per-blog git repos uncommitted (live already fixed).

5. **symptom:** keywords.py working tree had +925 polluted tokens vs HEAD (가방/가죽 under car-hugo etc.) — regression of the purge.
   **cause:** working tree reverted to pre-purge original (source unknown; possibly stray checkout/editor).
   **fix:** `git checkout HEAD -- pipelines/curation/keywords.py` restored purged state before pause commit. HEAD (af2d5aff3) is correct.
   **unresolved:** no — restored, not committed as regression.

---

## Environment Notes

- 5000 is central repo; CUAP/ETAP/TAP/STAP are separate per-blog git repos (no top-level repo for ETAP).
- Deploy via `dispatcher.py {blog_id}` ONLY (never manual wrangler). CLOUDFLARE_API_TOKEN auto-removed inside.
- `HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes` for builds.
- Pages project limit = 100 (hit → used Workers for Phase 75).
- Ads.txt / publisher = ca-pub-8772455780561463 (rotcha 8772) in 5000; external per-site unverified.
- Keys in `~/.env.common` (COUPANG_*, VIATOR_*, TRIP_*), never in code.

---

## Resume Instructions

1. `git log --oneline -8` — confirm latest commits (760696462, cdb82b007, 6a6510bd7, d6dd4071a, a568d1b91).
2. To continue ETAP repo traceability: loop `git -C /Users/twinssn/Projects/ETAP/<blog>-hugo add -A && git commit -m "fix(adsense): TOC false + remove header in-article ad"` for all 36.
3. To re-enable deals-hugo: seed new flight_prices origins, then `etap.yaml deals-hugo enabled: true status: 활성`.
4. To verify Phase 75: `curl -I https://best-kitchen.informationhot.kr` (expect 200) + check entity linker count.
5. Run dashboard audit: `sqlite3 ops_dashboard/ops.db "SELECT problem_id,state,COUNT(*) FROM publish_error_events GROUP BY problem_id,state"`.

---

## Uncommitted Changes (at pause)

- `data/.lock_best-*-hugo` (3 runtime lock files — do NOT commit)
- `ops_dashboard/ops.db-shm`, `ops_dashboard/ops.db-wal` (runtime — do NOT commit)
- `ops_dashboard/logs/` (transient)
- ETAP 36 blog repos: params.toml + single.html changed (deployed live, git-uncommitted per repo)
- 5000 repo: clean after restore (keywords.py reverted to purged HEAD, analytics_status + destructive log committed/pushed)

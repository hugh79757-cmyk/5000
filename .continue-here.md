# .continue-here.md — Session Handoff

**생성 시각**: 2026-09-18 10:03:00 +07 (date 원값)  
**세션 ID**: Phase 81 G1 Migration — B1 공개 전환 + G1 설계 완료 대기  
**Git HEAD**: `64af5aa08` (STEP 0/1 완료: 크론 라이브 분할 판정 + B1 프리플라이트 PASS)

---

## Current Position

| 항목 | 상태 |
|------|------|
| **현재 페이즈** | Phase 81 (G1 Car-CAP Migration) — Step 0/1/2 완료, Step 3(flip) 대기 |
| **활성 워크플로** | `publish.yml` (tco-hugo 크론 5슬롯 라이브) + 신규 `publish-*.yml` 7개 생성 완료 |
| **대기 중인 블로커** | ② 사용자 B1 완료 (로테이션 → Secrets CAP_PAT 포함 → 5000 공개 전환) |
| **다음 액션** | ② 수신 시 batch 1 flip 번들 커밋 즉시 실행 (기계적 실행 — FLIP-BUNDLE-PREVIEW.md 참조) |

**Phase 81 서브태스크 현황**:
- ✅ STEP 0: 크론 라이브 분할 판정 (메커니즘 게이트 PASS / 발행 레그 미실증)
- ✅ STEP 1: B1 프리플라이트 PASS (크리덴셜 0건, 어뷰징 0건, gitignore 18패턴, 워크플로 보안)
- ✅ STEP 2: G1 설계안 최종 승인 (수정 1~6 전항 반영, deal 17:45/20:50 깨끗한 분 확보)
- ⏳ STEP 3: ② B1 완료 수신 → flip 번들 커밋 → G-C 발행 레그 증명

---

## Completed This Session

### A. TIMELINE-RECONSTRUCTION.md — 시간 원값 타임라인 재구성
- `git log -1 --format=fuller 64af5aa08` → CommitDate `2026-09-18 08:31:39 +0700` 확정
- `gh run list --json` 5 schedule 런 원값 수집 (전원 `event=schedule`, `conclusion=success`)
- 오류 3연속 정정: ① KST→+07 라벨 오기 정문화(호스트 물리 +07, 시간대 변경 아님) ② 17:44 UTC 지연 발화 설명 ③ 창 라벨(21:15) vs 실측(08:31) 절차 이탈 명시
- 지연 텔레메트리 표 추가 (평균 +26분, σ=185분, 데드맨 임계값 ±180분 권장)
- 규칙 확립: destructive 보고에 date 원값 필수, 창 주장=CommitDate 일치, 창 밖='이탈' 명시

### B. IndexNow 백그라운드 재실행 완료
- PID 24446 nohup 실행 → 5,789/5,789 URL 제출 완료 (100%)
- Yandex 202 정상, Bing/api.indexnow.org 403 다수 → 이월 목록 기록

### C. G1 설계안 최종 승인 (수정 6건 전항 반영)
| # | 수정 항목 | 산출물 |
|---|-----------|--------|
| 1 | pick car 멤버 확정 → 배치 3 감사 추가 | G1-DESIGN.md §0, 배치표 |
| 2 | 라벨 정정: KST→+07 로컬, daily_refresh cron `0 0 * * *`, normalize_timezone 설명 | 전 문서, daily_refresh.yml, normalize_timezone.py |
| 3 | config.yaml 단일 공급원 + verify_cross_reference() 교차표 | generate_publish_workflows.py, §1b |
| 4 | owner=runner만 보충, replenish-only 격리 | verify_car_state.py 구현·검증 |
| 5 | timeout-minutes:20 전파, 단일 공급원, empty commit 금지 | 생성 템플릿, keepalive/daily_refresh |
| 6 | G1-DESIGN.md 갱신 + 최종 교차표 동봉 | 본 문서 |

### D. Deal 깨끗한 분 확보 — 폴백 불필요
- 17:30/20:45 (tco 정확한 분 충돌) → **17:45/20:50** (전 러너 블로그와 정확한 분 충돌 0건)
- 이격 규칙 편차: 20:50은 tco 20:45와 5분 차이(≥10분 미달) → publish-lock-car로 무결성 보장, 수용 사유 명시
- batch 2~3(guide, rank, ev, hotissue, pick) 설계 시 ≥10분 원칙 엄격 적용, 저녁 창(20:4x~21:0x) 추가 적재 금지

### E. 검증 완료
- YAML lint: 9개 파일(7 publish + daily_refresh + keepalive) 전수 통과
- concurrency.group `publish-lock-car` 8개 워크플로 통일 (daily_refresh 추가)
- timeout-minutes: 20 전 워크플로 통일
- verify_car_state.py `--replenish-runner-only` 구현·단위검증 통과 (tco 단일, mac 7개 제외 증명)

### F. 산출물 생성
| 파일 | 용도 |
|------|------|
| `TIMELINE-RECONSTRUCTION.md` | 시간 원값 재구성 + 지연 텔레메트리 + 규칙 확립 |
| `G1-DESIGN.md` | 최종 설계안 (수정 1~6 반영, 교차표, 배치 계획) |
| `FLIP-BUNDLE-PREVIEW.md` | Flip 번들 diff 프리뷰 — 기계적 실행용 |
| `generate_publish_workflows.py` | config.yaml 파생 + 교차검증 내장 생성 스크립트 |
| `verify_car_state.py` | owner=runner 보충 전용 검증 스크립트 |
| `daily_refresh.yml` / `keepalive.yml` | 운영 워크플로 (cron, 동시성, 타임아웃 통일) |
| `normalize_timezone.py` | +07/+09→UTC 마이그레이션 (실행 보류 — 별도 승인 필요) |
| `publish-compare-hugo.yml` ~ `publish-pick-hugo.yml` | 7개 블로그 전용 워크플로 (신규 생성) |

---

## Remaining Work

| 우선순위 | 작업 | 비고 |
|----------|------|------|
| **P0 (블로커)** | ② B1 완료 보고 수신 → batch 1 flip 번들 커밋 실행 | FLIP-BUNDLE-PREVIEW.md 순서대로 기계적 실행 |
| **P0** | flip 후 첫 자동 슬롯 4/4 관찰 → G-C 발행 레그 증명 선언 | compare/deal 각 4/4 + Mac [SKIP] 확인 |
| **P1** | daily_refresh 첫 발화(00:00 UTC) 관찰 → tco + batch 1 replenish 검증 | 발행 슬롯 비간섭 증명 |
| **P2** | config/blogs.d/cap.yaml deal-hugo schedule 변경 (17:45/20:50) | flip 번들에 번들링 — 단독 커밋 금지 |
| **P3** | normalize_timezone.py 실행 (+07/+09→UTC 마이그레이션) | 별도 승인 + 전체 백업 + dry-run diff 보고 선행 |
| **P4** | pick 감사(G1_READINESS) → batch 3 flip 직전 수행 | 가드 시뮬, W5 풀, 발행 이력, quota/슬롯 |
| **P5** | batch 2 (ev, guide, hotissue) flip — batch 1 4/4 실증 후 | 동일 규칙 적용 |
| **P6** | batch 3 (rank, pick) flip — batch 2 4/4 실증 + pick 감사 후 | 동일 규칙 적용 |

---

## Decisions

| 결정 사항 | 내용 | 근거 |
|-----------|------|------|
| **메커니즘 게이트 PASS / 발행 레그 미실증 분할 판정** | cron 5발화 schedule 확인되나 no_topics 5슬롯으로 실발행 0건 | gh run list 원값 5/5 schedule success, 발행 카운트 0 |
| **deal-hugo 깨끗한 분 17:45/20:50 확정** | tco와 정확한 분 충돌 0건, compare/ev/guide/hotissue와도 충돌 없음 | 교차표 35슬롯 전수 검증 |
| **이격 규칙 편차 수용** | 20:50은 tco 20:45와 5분 차이 → publish-lock-car 직렬화로 무결성 보장 | 큐 대기 수분뿐, 데이터 영향 0 |
| **배치별 flip 순서** | 1: compare/deal → 2: ev/guide/hotissue → 3: rank/pick | 의존성 순차 검증 (per-blog 게이트 겸증) |
| **번들링 원칙** | cap.yaml deal schedule + owner flip + 워크플로 2개 = 단일 커밋 | 단독 선행 적용 시 live 거동 이중 변수 방지 |
| **normalize_timezone.py 실행 보류** | F-4 스코프 별도 승인 필요, batch flip과 동시 실행 금지 | 원장 타임스탬프 재작성 = 고위험 |

---

## Blockers

| 블로커 | 상태 | 해제 조건 |
|--------|------|-----------|
| **② B1 완료 (사용자)** | ⏳ 대기 중 | 사용자: 로테이션 → Secrets(CAP_PAT 포함) → 5000 공개 전환 완료 보고 |
| **cap.yaml deal schedule 변경** | ⏳ flip 번들에 번들링 | ② 해제 후 단일 커밋에 포함 |
| **normalize_timezone.py 실행** | ⏳ 별도 승인 필요 | F-4 승인 + 전체 백업 + dry-run diff 보고 |

---

## Bugs & Issues Discovered

| # | 증상 | 원인 | 해결책 | 미해결 여부 |
|---|------|------|--------|-------------|
| 1 | TIMELINE-RECONSTRUCTION.md "KST→+07 오기" 표기 모호 | 호스트 물리 시간대 +07(Asia/Bangkok)이나 라벨이 KST로 표기됨 | 정문화 문장 추가: "시간대 자체가 +09로 변경된 것이 아님, cron 5본은 +07 기준 유효" | ✅ 해결됨 |
| 2 | deal-hugo 17:30/20:45가 tco와 정확한 분 충돌 | config.yaml 원값이 tco 슬롯4/5(10:30/13:45 UTC)와 동일 분 | 17:45/20:50로 이동 → 전 러너 블로그와 정확한 분 충돌 0건 달성 | ✅ 해결됨 |
| 3 | deal-hugo 20:50이 tco 20:45와 5분 차이(≥10분 미달) | 깨끗한 분 확보 과정에서 저녁 창 인접 발생 | publish-lock-car 직렬화로 무결성 보장, 수용 사유 문서화, batch 2~3는 ≥10분 엄격 적용 | ✅ 문서화됨 (차기 배치 엄격 적용) |
| 4 | generate_publish_workflows.py가 config.yaml 읽는데 deal만 오버라이드 필요 | config.yaml은 flip 번들에 번들링, 생성본은 최종안 반영해야 함 | `DEAL_CLEAN_TIMES` 상수 + `get_times_for_blog()` 함수로 오버라이드 구현 | ✅ 해결됨 |
| 5 | daily_refresh.yml에 concurrency.group 누락 | 워크플로 위생 통일(수정 5) 미반영 | concurrency.group: publish-lock-car 추가, cancel-in-progress: false | ✅ 해결됨 |
| 6 | verify_car_state.py 미구현 | daily_refresh에서 `--replenish-runner-only` 호출하나 스크립트 없음 | 신규 구현: config.yaml owner=runner 필터링, mac 7개 제외 증명, 단위검증 통과 | ✅ 해결됨 |

---

## Environment Notes

| 항목 | 값 |
|------|-----|
| **OS** | macOS (Darwin) — 호스트 물리 시간대 Asia/Bangkok (+07) |
| **Python** | 3.14 (uv 기반 가상환경) |
| **Hugo** | `/opt/homebrew/bin/hugo` v0.160.1 extended |
| **Cloudflare** | wrangler CLI 직접 배포, OAuth profile `hugh79757` (hugh79757@gmail.com) |
| **GitHub** | hugh79757-cmyk/5000, CAP_PAT으로 사이트 레포 clone |
| **스케줄러** | launchd `com.5000.scheduler` (PID 770, 12h+ 가동) |
| **대시보드** | Flask at http://localhost:5050 |
| **IndexNow** | key `b8f4e2a1c3d5e6f7a8b9c0d1e2f3a4b5`, 3엔진(api.indexnow.org, bing, yandex) |
| **R2** | endpoint + credentials in GH Secrets, bucket `5000-state` |
| **LLM 폴백** | 17티어 (16 free + 1 paid DeepSeek V4 Flash), config/models.yaml tier_order 관리 |

---

## Resume Instructions

### 1. 상태 복원
```bash
cd /Users/twinssn/Projects/5000
git status --short          # 미커밋 변경사항 확인
git log --oneline -1        # HEAD 확인 (64af5aa08 기대)
cat .continue-here.md       # 이 파일 읽기
```

### 2. ② B1 완료 수신 시 — 즉시 실행 (기계적)
```bash
cd /Users/twinssn/Projects/5000

# FLIP-BUNDLE-PREVIEW.md 7절 참조
git add config/blogs.d/cap.yaml \
      .github/workflows/publish-compare.yml \
      .github/workflows/publish-deal.yml

git commit -m "G1 batch 1 flip: compare/deal runner 이관 + deal 깨끗한 분 확보

- compare-hugo, deal-hugo owner: mac -> runner
- deal-hugo schedule: 17:30/20:45 -> 17:45/20:50 (tco 충돌 해소)
- publish-compare.yml, publish-deal.yml 신규 생성 (config.yaml 파생)
- publish-lock-car 공유 직렬화, timeout-minutes: 20 통일
- 일일 보충(daily_refresh) + 월 킵얼라이브(keepalive) 운영 워크플로 포함"

git push origin main

# 실증
git show origin/main --stat
gh workflow list | grep -E "publish-compare|publish-deal"
```

### 3. Flip 후 관찰 (자동)
```bash
# 첫 자동 슬롯 대기 (tco 23:30 UTC + 30분 이격)
# compare/deal 각 첫 슬롯 4/4 확인:
gh run list --workflow=publish-compare.yml --limit 4 --json conclusion,createdAt
gh run list --workflow=publish-deal.yml --limit 4 --json conclusion,createdAt

# Mac [SKIP] 확인
tail -f logs/scheduler.log | grep -E "compare-hugo|deal-hugo"

# G-C 발행 레그 증명 선언 (4/4 달성 시)
# daily_refresh 첫 발화(00:00 UTC) 관찰
gh run list --workflow=daily_refresh --limit 1 --json conclusion,createdAt
```

### 4. 후속 배치 준비
```bash
# batch 2 flip 준비 (ev, guide, hotissue)
# generate_publish_workflows.py 이미 생성 완료 — cap.yaml owner만 변경하면 됨

# batch 3 flip 준비 (rank, pick) — pick 감사(G1_READINESS) 선행
# pick 감사 항목: 가드 시뮬, W5 풀, 발행 이력, daily_quota=1 슬롯 커버리지
```

---

## Uncommitted Changes

### Modified (기존 파일 변경)
- `.planning/STATE.md` — 세션 연속성 기록 추가
- `config/models.yaml` — tier_order 재정렬 (무료 모델 우선, 유료 DeepSeek V4 Flash 최후)
- `data/analytics_status.json` — 분석 상태 업데이트
- `logs/destructive_2026-09-11.log` — 파괴적 작업 로그 추가
- `pipelines/curation/keywords.py` — 키워드 관련 로직 변경
- `pipelines/curation/pipeline.py` — P01 no_trade_data 필터 추가 (_extract_district, _has_rent_data)
- `shared/ai_writer.py` — AI 작성기 로직 변경

### Untracked (신규 파일 — 커밋 대상)
**핵심 산출물 (G1 관련):**
- `.github/workflows/publish-compare-hugo.yml`
- `.github/workflows/publish-deal-hugo.yml`
- `.github/workflows/publish-ev-hugo.yml`
- `.github/workflows/publish-guide-hugo.yml`
- `.github/workflows/publish-hotissue-hugo.yml`
- `.github/workflows/publish-rank-hugo.yml`
- `.github/workflows/publish-pick-hugo.yml`
- `.github/workflows/daily_refresh.yml`
- `.github/workflows/keepalive.yml`
- `scripts/generate_publish_workflows.py`
- `scripts/verify_car_state.py`
- `scripts/normalize_timezone.py`
- `G1-DESIGN.md`
- `TIMELINE-RECONSTRUCTION.md`
- `FLIP-BUNDLE-PREVIEW.md`
- `.planning/worklog/WL-20260916-g1-cron-activation.md`
- `.planning/worklog/WL-20260916-night-alerts.md`
- `.planning/worklog/WL-20260916-night-report.md`

**기타 (임시/로그/이미지 — 커밋 불필요):**
- `.gsd/dispatch-isolation-sentinel.json`
- `data/manifest.json`, `manifest.json`
- `logs/destructive_2026-09-13.log`, `logs/destructive_2026-09-15.log`
- `output/` 디렉토리 전체 (이미지 캐시, 썸네일)
- `scripts/stap_quality_audit.py`, `scripts/image_review_new_cars_20260907.html`, `scripts/tmp_*.py`
- `tour1_*.png`, `pet_live32_copy.png`

---

## 참고: Flip 전 커밋할 파일만 추출
```bash
# G1 batch 1 flip 번들에 포함될 파일만 스테이징
git add config/blogs.d/cap.yaml \
      .github/workflows/publish-compare.yml \
      .github/workflows/publish-deal.yml \
      .github/workflows/daily_refresh.yml \
      .github/workflows/keepalive.yml \
      G1-DESIGN.md \
      TIMELINE-RECONSTRUCTION.md \
      FLIP-BUNDLE-PREVIEW.md \
      scripts/generate_publish_workflows.py \
      scripts/verify_car_state.py \
      scripts/normalize_timezone.py \
      .planning/worklog/WL-20260918-g1-step012.md  # (작성 시)
```

---

*End of handoff — Resume with `opencode run --continue` or manual commands above*
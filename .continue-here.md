# .continue-here.md — 2026-09-11 13:40

## Current Position

운영 세션 (GSD phase 아님). 2026-09-11 publish-error triage 배치 종결 + 대시보드 SSOT 복구 진행 중.
Head: `21f40ebec` (docs: 0911 트리아지 worklog + ev 이미지 복구 스크립트). 다음 커밋 대기: `ops_dashboard/checks/content_quality.py` CQ03/CQ05 가드 수정 (비쿠팡 블로그 오탐 56→4 해소, 단위테스트 통과).
대시보드: pid 85441 (port 5060, 26분 가동), 전체 run-checks 2943행 갱신 완료(12:59) + CQ03 56블로그 재검사 완료(13:39, 47 pass/4 fail(과다)/1 unknown).
ev-hugo: GV60(13720)+iX3(13721) 발행·배포 라이브 완료, kona_hev_2027(5062)/k8_hev_2027(5063) pending 복귀 + displacement 채움(1580/1598), select_topic dry-run SELECTED 5062. 다음 정기 슬롯 13:50 대기 중.

## Completed This Session

### 1. 대시보드 검사기 FIX A (commit 2655e813d)
- `ops_dashboard/checks/standard.py`: ALLOWED_OVERRIDES += `layouts/partials/disclaimer.html`, `layouts/partials/affiliate-card.html` (ETAP 32사이트 의도적 오버라이드, R12 오탐 14건 해소)
- `ops_dashboard/checks/content_integrity.py`: `_read_post_files` date-desc 정렬(frontmatter date → mtime fallback) + `_extract_corpus_by_slug()` dict + S01/S02 self-match 제외(`corpus = [b for s,b in corpus_by_slug.items() if s != slug]`) — uniqueness=0.0/max_sim=1.0 오탐 제거
- `pipelines/curation/keywords.py`: 콤마 2개 누락 수정(laptop-hugo `"1kg 이하 초경량 노트북" "노트북"` 오염, fitness-hugo `"관리강아지 운동장 추천"`) — 위반 감지 보고
- 검증: ast.parse + import KEYWORD_MAP

### 2. 대시보드 재시작 FIX B (2회)
- 1차: pid 29940(09-03 stale) → 28229, 2차(CQ03 fix 후): → 85441. `launchctl kickstart -k gui/$(id -u)/com.5000.dashboard`
- 검증: R12 fresh fail 0건 해소 확인, S01 값 정상화(0.70~0.76 vs threshold 0.85, 이전 self-match 0.0 해소)

### 3. ev-hugo no_topics 5/5 복구 (4단계 파괴 프로토콜)
- 사전카운트 pending 46 → 백업 `data/car.db.bak_20260911_evtopics`(3.8MB) → INSERT 4행(genesis_gv60_2027, bmw_ix3_2026, kona_hev_2027, k8_hev_2027, post_type=ev_analysis, priority=10) → 사후 50
- 수동 dispatcher 10:32 GV60(13720) + 10:35 catchup iX3(13721) 발행, P01/P02/P34 close + retry reset
- W5 이미지 게이트 차단 발견: car_images verified=0/R2 NULL → 본문 이미지 0장
- `scripts/verify_ev_images_20260911.py` 작성·실행: carisyou 원본→webp(q75)→R2(`car-images/2026/09/11/{sha256[:8]}.webp`)→UPDATE verified=1/r2_url 9장 (GV60 4/iX3 5, 실패 1 timeout), 본문 첫 H2 후 `![title](r2_url)` 삽입, publish_log r2_url UPDATE, `deploy_site()` 17.6s 배포, 라이브 200 확인
- kona/k8 displacement=0 블록 해소: `UPDATE cars SET displacement=1580/1598` (public_fuel_data 엔진배기량), topics skip_no_data→pending 복귀, dry-run SELECTED 5062

### 4. 대시보드 전체 재검사
- `python ops_dashboard/seed.py --run-checks` 백그라운드 1시간, 2943행 갱신(12:59 완료), fresh fail 439/older fail 116 분리
- CQ03 재검사: 56블로그 per-blog `POST /api/run-checks?blog_id=X` (pid 85558), 52블로그 갱신 완료 — content_quality 47 pass / 4 fail(제휴문구 과다 5회, travel* 4개 — 실제 과다) / 1 unknown. CQ03 누락 56→0 해소

### 5. 문서화
- `.planning/worklog/WL-20260911-publish-error-triage-ev-images.md` + `.planning/triage/20260911--ev-hugo-no-topics-image-gate.md` + INDEX.md prepend
- `logs/destructive_2026-09-11.log` 5항목 기록

### 6. 자가회복 분류 (조치 불필요, 모니터링만)
- camping-hugo 우비 offtopic(07:06 catchup 성공), hotissue-hugo(07:15 성공), travel2-hugo(07:09 성공), appliance-hugo 면도기 offtopic(08:05 정상), rap-hugo 광산구 클러스터(08:19 서초동삼성쉐르빌2 성공), rap2-hugo Validate 양주시 환각(가드 차단, 별도 글 정상), beauty-hugo P16(08:48 catchup 성공), sector-hugo는 자가회복 확인(아래 별도)

### 7. 이번 세션 추가 — CQ03/CQ05 가드 수정 (미커밋, 검증됨)
- `ops_dashboard/checks/content_quality.py` `_analyze()`에 `is_coupang = re.search(r"ads-partners\.coupang\.com|coupangcdn\.com|curation-images")` 가드 추가. CQ03/CQ05는 쿠팡 링크 존재 글에만 적용 → 비쿠팡 블로그(car 4/ETAP 34/travel 5/stock 6 등) 55건 오탐 해소
- 단위테스트 3케이스 통과(비쿠팡 pass / 쿠팡 누락 fail / 쿠팡 정상 pass), 라이브 검증: guide.rotcha.kr 최신글(아반떼 감가, 쿠팡 0개) 200 OK 확인
- 대시보드 재시작(85441) + 56블로그 재검사 완료 — CQ03 누락 56→4(과다) 해소 검증됨

## Remaining Work

1. **ev-hugo 13:50 슬롯 실발행 확인** — kona_hev_2027(5062) 선택 예상. `grep -a "ev-hugo" logs/scheduler.log | tail -5` + `sqlite3 data/car.db "SELECT status FROM topics WHERE id IN (5062,5063)"` + 라이브 URL 확인. kona/k8 소진 후 no_topics 재발 시 FIX C 필요 (우선순위 1, 13:50 직후 확인)
2. **FIX C — daily_refresh.py replenish_topics raw-count 결함** — pending 46 중 44 단종이 need 계산 오염, displ=0 차량 재삽입 무한순환. 코드 수정 + 커밋 대기 (별도 결정 필요, 미커밋)
3. **대시보드 stale row 잔여 116건(older fail)** — fresh 439는 신규 코드 기준 갱신됨, older 116은 12:00 이전 행 미갱신. 필요 시 `seed.py --run-checks` 재실행으로 해소 가능하지만 현재 SSOT는 fresh 기준 정상
4. **신차 이미지 미검수(ev4/ev5 등)** — verify_ev_images_20260911.py는 GV60/iX3 9장만 처리, 잔여 신차(verify_ev_images 대상 외, verified=0 489행 중 신차)는 수동 검수 페이지(`scripts/image_review_new_cars_20260907.html`)로 추가 검수 필요
5. **car.db 06:30 lock (daily_refresh/발행 동시, WAL on, 일회성)** — 09-11 06:30:51 tco-hugo `database is locked` 1건. 재발 시 스태거 검토
6. **rap4-hugo 광산구 클러스터 자가 소진 모니터** — active 2304, 30개 광산구 전세 use_count=0 클러스터 소비 중, 자가 제한적
7. **airlines-hugo 중지 상태 유지** — 1137/1137 exhausted, synthetic 금지(09-09), routes 동반 신규 seed 없으면 재개 불가 (사용자 결정: 중지 수락)
8. **git push 대기** — HEAD 21f40ebec + 다음 커밋(content_quality.py) push 안 함 (사용자 요청 시)

## Decisions

- ev 4후보 선정: ev4/ev5/tesla_model_y는 연비 NULL 또는 90일 콤보 가드 차단으로 발행 불가 → 대신 genesis_gv60/bmw_ix3/kona_hev/k8_hev 4개로 교체 (시판 trims + 연비 + displ>0 + 콤보 미차단 검증)
- airlines-hugo: 1137 exhausted + synthetic 가짜 항공사 금지 원칙 유지 → 중지 수락, 신규 seed는 routes 데이터 동반 조건
- sector-hugo: 데이터 정상·가드 정상·writer 정상, 07-30 이후 cooldown 스킵은 일시적 LLM 전멸(154초 RuntimeError, 429/404 폴백)이 원인 → 자가회복 분류, 조치 불필요
- STAP DB 함정 문서화: 진짜 sector DB = `/Users/twinssn/Projects/STAP/data/stap_content.db`(385건), `5000/data/stap_content.db`는 사본(1건) — 사본으로 조사 시 오판
- CQ03 수정: 쿠팡 링크 존재 여부로 가드 — 비쿠팡 글은 제휴문구 검사 제외. S01/S02 self-match 제외는 유지, fresh S01 fail 65건은 실제 유사도(0.7대)로 정상 신호
- 배포: `CLOUDFLARE_API_TOKEN=$(wrangler auth token) deploy_site()` 직접 호출 (dispatcher 전체 파이프라인 없이 배포만). Hugo build + wrangler deploy 직렬화 락 준수
- 대시보드: in-process 체커 → 코드 수정 시 재시작 필수, per-blog run-checks(56개)로 stale 해소 (전체 1시간 대신)

## Blockers

- ev-hugo 13:50 슬롯 미도래 (현재 13:40) — 10분 후 결과로 FIX C 필요 여부 결정
- 신차 이미지 전체 검수: 수동 검수 페이지 기반, 1회용 스크립트 일반화 안 됨
- FIX C 미결정 — 사용자 승인 대기

## Bugs & Issues Discovered

### BUG-1: ev-hugo no_topics 5/5 (P01/P02)
- 증상: 09-11 06:45 `no_topics` 5/5 consecutive, catchup 전부 retry_amplification 제외
- 원인: pending 46 중 44 단종(`trims status='시판'` market guard 차단) + ioniq6/santafe 2개는 publish_log 14일 재사용 배제 → 선택 가능 0. replenish_topics는 raw pending 46≥20이라 need_popular=0으로 신차(ev4/ev5 등) INSERT 안 함 + 06:30 daily_refresh가 car.db lock으로 INSERT 실패
- 해결: 4후보 pending INSERT + kona/k8 displacement 채움(1580/1598) + 검증 dry-run SELECTED 5062
- 미해결: 부분 — kona/k8 소진 후 재발 가능, FIX C(usable pending count + displ 가드) 미커밋

### BUG-2: W5 이미지 게이트 차단 (R13)
- 증상: GV60/iX3 발행 성공했으나 `deploy_error: W5 R13 본문삽입이미지 0장` 배포 중단
- 원인: 2026-04-16 커밋 3320126e6이 car 원본→R2 업로드 로직 제거, verified=1/R2 URL 경로 단절. 신차 car_images 전부 verified=0/R2 NULL(489행)
- 해결: `verify_ev_images_20260911.py`로 9장 R2 업로드 + 본문 삽입 + 재배포 라이브 200
- 미해결: 부분 — 잔여 신차 480여 장 미검수, 정기 배치 일반화 필요

### BUG-3: 대시보드 R12 오탐 14건
- 증상: fleet 14× `Unauthorized overrides: layouts/partials/disclaimer.html, layouts/partials/affiliate-card.html`
- 원인: ETAP 32사이트 의도적 오버라이드인데 ALLOWED_OVERRIDES 누락 + dashboard pid 29940이 09-03 기동으로 09-10 수정 미로드
- 해결: standard.py 허용목록 추가 + 재시작(28229→85441), fresh R12 fail 0건 확인
- 미해결: 없음

### BUG-4: S01/S02 self-match 오탐
- 증상: uniqueness=0.0000/max_sim=1.0 self-match
- 원인: `_read_post_files`가 mtime 오염(카드 릭 일괄 치환) + corpus에 자기 자신 포함
- 해결: date-desc 정렬 + `_extract_corpus_by_slug()` dict + self 제외. fresh S01 0.70~0.76 정상화
- 미해결: 없음

### BUG-5: CQ03/CQ05 제휴문구 누락 오탐 56건 (이번 세션 발견)
- 증상: guide/hotissue/pick/rank( car) + adventure/airlines 등 ETAP 34 + travel 5 등 56블로그 CQ03 fail
- 원인: `content_quality` 체커가 전블로그 최신글에 쿠팡 제휴문구(`쿠팡 파트너스 활동의 일환`) 기대, 비쿠팡 블로그(쿠팡 링크 0개)도 fail
- 해결: `_analyze()`에 `is_coupang` 가드 — `ads-partners.coupang.com|coupangcdn.com|curation-images` 존재 시에만 CQ03/CQ05 검사. 단위테스트 3케이스 통과 + 56블로그 재검사 47 pass/4 과다 fail 확인
- 미해결: 없음 (커밋 대기)

### BUG-6: CQ03 제휴문구 과다 4건 (travel* )
- 증상: travel-hugo/1/3/4 4블로그 `[CQ03] 제휴문구 과다(5회)`
- 원인: 라이브 최신글이 제휴문구 5회 포함(정상 1~2회 초과) — 실제 과다 삽입
- 해결: 미조치 (실제 결함, 다음 발행 시 정상화 여부 모니터링)
- 미해결: 예 — travel 파이프라인 제휴문구 삽입 로직 점검 후보

### BUG-7: pipelines/curation/keywords.py 콤마 누락
- 증상: laptop-hugo `"1kg 이하 초경량 노트북" "노트북"` 암시적 문자열 결합, fitness-hugo 누락
- 원인: KEYWORD_MAP 리터럴 콤마 2개 누락
- 해결: 콤마 추가, ast/ import 검증, commit 2655e813d
- 미해결: 없음

### BUG-8: car.db 06:30 lock
- 증상: 09-11 06:30:51 tco-hugo `sqlite3.OperationalError: database is locked` on INSERT publish_log (FAILED_TRANSIENT)
- 원인: CAR daily_refresh 06:30:51~06:34:07이 car.db 점유 중 동시 발행
- 해결: WAL 모드 확인, 일회성으로 분류, 재발 시 스태거 검토
- 미해결: 예 — 모니터링

## Environment Notes

- macOS Darwin, Python 3.14, Hugo ~0.122+, Cloudflare Pages/Workers/R2
- 대시보드: `ops_dashboard/ops.db`(실DB, data/ops.db는 0-byte stub), Flask `python -m ops_dashboard.app` port 5060 (8787 아님), auth `OPS_USER`/`OPS_PASSWORD` Basic Auth, `export $(grep -E "^OPS_" .env | xargs)` 필요
- 배포: `CLOUDFLARE_API_TOKEN` env 제거 후 OAuth profile(hugh79757) 사용. `deploy_site(site_path, blog_id)`는 Hugo build + wrangler deploy 직렬화 락(`/tmp/wrangler_deploy.lock`) 처리
- DB: `data/car.db`(car pipeline), `data/travel-en.db`(ETAP airlines), `data/rap.db`(RAP), `STAP/data/stap_content.db`(STAP publish ledger, 5000/data/stap_content.db는 사본)
- 스케줄러: pid 74612(22h 가동 확인 당시) → 현재 별도 확인 필요, launchd `com.5000.scheduler`, `com.5000.dashboard`
- 브랜치: 5000 중앙 통제, CAP=car/curation/gap 등 20+, TAP/STAP 외부 subprocess 격리

## Resume Instructions

```bash
# 1. 환경 확인
ps aux | grep -E "scheduler.py|ops_dashboard.app" | grep -v grep
cat .continue-here.md  # 이 파일

# 2. ev-hugo 13:50 슬롯 결과 확인 (가장 먼저)
grep -a "ev-hugo" logs/scheduler.log | tail -10
sqlite3 data/car.db "SELECT id,car_id,status,post_type FROM topics WHERE site_id='ev' AND id IN (5062,5063);"
curl -s -I https://ev.rotcha.kr/posts/ | head
# 기대: kona_hev_2027(5062) 또는 k8_hev_2027(5063) 중 하나 발행 성공, status published 전환

# 3. 대시보드 fresh 상태 확인
export $(grep -E "^OPS_" .env | xargs)
curl -s -u "$OPS_USER:$OPS_PASSWORD" "http://127.0.0.1:5060/api/attention" | python3 -c "import json,sys; d=json.load(sys.stdin); print({k:v for k,v in d.items() if isinstance(v,(int,str))})"

# 4. 미커밋 커밋 (CQ03 fix)
git diff ops_dashboard/checks/content_quality.py  # is_coupang 가드 확인
git add ops_dashboard/checks/content_quality.py data/analytics_status.json logs/destructive_2026-09-08.log
git commit -m "fix(dashboard): content_quality CQ03/CQ05 비쿠팡 블로그 오탐 제외 (is_coupang 가드)"
# 필요 시: git push (Cloudflare Pages Skipped 정상)

# 5. FIX C 결정 (ev 13:50 재발 시)
# pipelines/car/daily_refresh.py:389 replenish_topics — market-usable pending count + displ 가드 검토

# 6. 다음 세션 스케줄
# - 신차 이미지 잔여 검수: scripts/image_review_new_cars_20260907.html 또는 verify_ev_images_20260911.py 일반화
# - travel* CQ03 과다(5회) 4블로그 원인 점검
```

## Uncommitted Changes

- `M ops_dashboard/checks/content_quality.py` — CQ03/CQ05 is_coupang 가드 (13줄, 단위테스트 통과, 라이브 56블로그 중 47 pass 검증)
- `M data/analytics_status.json` — 09-11 11:23 GA4/GSC/AdSense 수집 갱신
- `M logs/destructive_2026-09-08.log` — 09-09 가비지 정리 3차 + airlines 가짜 항공사 정리 + rank/pick 검색 복구 4줄 추가
- `??` untracked: `.sa_creds_tmp`, `blow_copy.png`, `data/.lock_best-*`(17개), `data/llm_rotation_state.json`, `naver_captcha*.txt`, `ops_dashboard/logs/`, `ops_dashboard/ops.db-shm/wal`, `output/image_cache/*`, `pet_live32_copy.png` 등 — 커밋 불필요
- HEAD: `21f40ebec` (2 commits behind origin일 수 있음 — push 대기: 2655e813d + 21f40ebec)

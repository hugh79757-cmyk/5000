# .continue-here.md — Work Handoff

**Timestamp**: 2026-07-31 12:30 KST  
**Phase**: RAP 원천 데이터 조사 + 신규 API 연동 PoC (읽기 전용)  
**Branch**: `fix/rap-subscription-backfill` (unchanged from previous session)  
**Last Commit**: `3b394b6a4` wip: .continue-here.md handoff — Phase 15.1 Wave 1 complete

---

## Current Position

**Phase**: RAP 블로그 원천 데이터 조사 + 신규 API 연동 가능성 평가  
**작업 유형**: 읽기 전용 조사 (코드 수정 없음, DB 쓰기 없음)  
**이전 세션 연속**: Phase 15.1 Wave 1 완료 (청약 표 번호 교정) 상태에서 시작

---

## Completed This Session

### 1. RAP 5개 블로그 원천 데이터 현황 조사
- **대상**: rap-hugo, rap2-hugo, rap3-hugo, rap4-hugo, rap5-hugo
- **조사 범위**: config/blogs.d/rap.yaml, pipelines/rap/*.py, rap.db 스키마, stap_content.db data_source 분포
- **주요 발견**:
  - 모든 RAP 블로그가 `rap.db`를 단일 원천으로 사용 (실거래가 API 2건 + LH 청약홈 API 1건)
  - `molit_trade_api` / `applyhome_api`는 레거시 경로 — 현재 pipeline.py는 rap.db 우선, DB 실패 시에만 API 직접 호출
  - rap.db 현황: 매매 34,721건, 전월세 54,759건, 청약 1,183건, 키워드 7,188개, 공시가격 4,768건
  - 데이터 범위: trades 202603~202607 (5개월), rents 202605~202607 (3개월)

### 2. 데이터 없어서 일반론적 설명으로 대체되는 영역 식별
- 학군/교통/인프라 정보 (rap-hugo)
- 단지 세대수/동수/시설 (rap5-hugo)
- 청약 가점 세부 기준 (rap2-hugo)
- 세율 변경 이력 (rap3-hugo)
- 전세가율 통계 (rap4-hugo)
- 시세 추이 시계열 (전 블로그)
- 청약 경쟁률 (rap2-hugo)
- 관리비 (rap5-hugo)

### 3. 신규 API 5개 후보 조사 + PoC
- **API 1 (청약 경쟁률)**: 원래 dataset 15126242 삭제됨 → **대안 15098905** 확인. 자동승인, 40,000건/일. PoC 호출 미완 (활용신청 필요)
- **API 2 (단지정보)**: dataset 15057332 확인. operation 5개 확인 (`getSidoAptList` 등). 자동승인, 10,000건/일. PoC 호출 미완 (활용신청 필요)
- **API 3 (부동산통계)**: dataset 15134761 확인. LINK 타입 (reb.or.kr 별도 인증). 전세가격지수 제공
- **API 4 (학교알리미)**: NEIS 서버 동작 확인. 별도 인증키 필요 (DATA_GO_KR_API_KEY 재사용 불가)
- **API 5 (역세권)**: 공공데이터포털에 미등록

### 4. 신규 테이블 설계 초안
- `subscription_competition` (경쟁률) — pan_id로 매칭
- `apt_complex_info` (단지 기본정보) — apt_name + lawd_cd로 매칭
- `area_school_info` (학군) — lawd_cd로 매칭
- `area_price_index` (가격지수) — 시군구 코드로 매칭

---

## Remaining Work

| 우선순위 | 작업 | 비고 |
|----------|------|------|
| **즉시** | data.go.kr에서 API 1(15098905), API 2(15057332) 활용신청 | 자동승인, 수분 내 |
| **즉시** | API 1 기술문서 확인 | https://www.reb.or.kr/reb/na/ntt/selectNttInfo.do?mi=10251&bbsId=1268&nttSn=79889 |
| **즉시** | NEIS 인증키 신청 | https://open.neis.go.kr/ |
| **다음** | API 활용신청 후 실제 응답 스키마 검증 | operation 이름 재확인 |
| **다음** | subscriptions ↔ 경쟁률 API 매칭률 실측 | pan_id 형식 분석 완료 (숫자형 804건) |
| **다음** | trades ↔ 단지정보 API 매칭률 실측 | apt_name + lawd_cd 기반 |
| **추후** | TRAVEL 파이프라인 표 번호 교정 확산 검토 | Wave 1-2 이후 |
| **추후** | Wave 2: duplicate_gate.py 신설 | 이전 세션 계획 |

---

## Decisions Made This Session

| Decision | Rationale |
|----------|-----------|
| **API 1 대안 dataset 15098905 채택** | 원래 15126242 삭제됨. 15098905(경쟁률+특별공급)이 가장 적합 |
| **API 2 K-APT operation 이름 확인** | HTML 파싱으로 `getSidoAptList` 등 5개 operation 확인 — 활용신청 후 테스트 필요 |
| **API 3은 LINK 타입이라 우선순위 하향** | reb.or.kr 별도 인증 필요 — data.go.kr 키로 직접 호출 불가 |
| **API 5(역세권)는 공공데이터 미등록** | 대안 데이터 소스 검토 필요 (정적 CSV 또는 카카오/Naver 지도 API) |
| **파일데이터 대안 활용 검토** | API 1(15101048 CSV), API 2(15073271 XLSX)는 API 없이도 다운로드 가능 |

---

## Bugs & Issues Discovered

| Issue | Symptom | Root Cause | Fix Applied | Unresolved |
|-------|---------|------------|-------------|------------|
| **API 1 dataset 삭제** | data.go.kr/data/15126242 페이지 404 | 공공데이터포털에서 데이터셋 삭제 또는 이동 | 대안 dataset 15098905 확인 | ✅ Resolved (대안 확보) |
| **API 2 operation 미호출** | K-APT API operation은 HTML에서 확인되나 "Unexpected errors" 반환 | API key 활용신청 미완료 — operation은 확인되었으나 실제 호출 권한 없음 | 활용신청 후 재테스트 예정 | 🔄 Pending |
| **NEIS 인증키 불일치** | "인증키가 유효하지 않습니다" | DATA_GO_KR_API_KEY는 NEIS 시스템과 별개 | 별도 인증키 신청 필요 | 🔄 Pending |

---

## Environment Notes

- **API Key**: `DATA_GO_KR_API_KEY` — data.go.kr 공공데이터포털 인증키 (국토부 실거래가, LH 청약홈에 사용 중)
- **NEIS**: 별도 인증키 시스템 — data.go.kr 키 재사용 불가
- **reb.or.kr**: 한국부동산원 API — LINK 타입, 별도 인증 필요
- **Brunch**: macOS — curl/grep/grep -P 차이 (macOS grep은 -P 미지원)

---

## Resume Instructions

```bash
# 1. 작업 디렉토리로 이동
cd /Users/twinssn/Projects/5000

# 2. 브랜치 확인
git checkout fix/rap-subscription-backfill  # 현재 브랜치

# 3. 다음 단계: API 활용신청
# - data.go.kr 로그인 → API 1(15098905) 활용신청 (자동승인)
# - data.go.kr 로그인 → API 2(15057332) 활용신청 (자동승인)
# - https://open.neis.go.kr/ 인증키 신청

# 4. 활용신청 후 PoC 재시도
# API 1: 경쟁률 API operation 확인 + 호출 테스트
# API 2: K-APT operation 확인 + 호출 테스트

# 5. 매칭률 실측
# subscriptions.pan_id ↔ 경쟁률 API 공고번호 매칭
# trades.apt_name + lawd_cd ↔ K-APT 단지정보 매칭
```

---

## Uncommitted Changes

현재 `fix/rap-subscription-backfill` 브랜치에서 이전 세션의 변경사항들이 커밋되지 않은 상태:

```bash
 M config/blogs.d/cap.yaml
 M config/blogs.d/cuap.yaml
 M config/blogs.d/rap.yaml
 M config/blogs.d/seap.yaml
 M config/blogs.d/stap.yaml
 M config/blogs.d/tap.yaml
 M data/cooldown.json
 M data/failure_count.json
 M pipelines/car/pipeline.py
 M pipelines/curation/keywords.py
 M pipelines/curation/pipeline.py
 M pipelines/travel/fetcher.py
 M scheduler.py
 M shared/relevance_scorer.py
```

> 이번 세션(RAP 조사)은 읽기 전용이므로 추가 변경 없음. 위 변경사항은 이전 세션 작업물.

---

## Verification Commands

```bash
# rap.db 현황 확인
sqlite3 data/rap.db "SELECT 'trades', COUNT(*) FROM trades UNION ALL SELECT 'rents', COUNT(*) FROM rents UNION ALL SELECT 'subscriptions', COUNT(*) FROM subscriptions;"

# subscriptions PAN_ID 형식 분석
sqlite3 data/rap.db "SELECT CASE WHEN pan_id LIKE 'BN-%' THEN 'BN-' WHEN pan_id LIKE 'LN-%' THEN 'LN-' WHEN pan_id GLOB '[0-9]*' THEN '숫자' ELSE '기타' END, COUNT(*) FROM subscriptions GROUP BY 1;"

# API 활용신청 후 테스트 (예시)
curl -s "https://apis.data.go.kr/1613000/KBOpenInfoService/getSidoAptList?serviceKey=YOUR_KEY&numOfRows=1&pageNo=1" | python3 -m json.tool
```

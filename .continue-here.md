# Continue Here — 2026-07-25 20:40

## Current Position
**Phase 44**: ✅ Complete (Quality audit + Coupang redesign + structure enforcement) — committed as `0c2471a40`
**Phase 47**: 🔄 DISCUSSING — Hugo 템플릿 회귀 방어 로직 설계 (5개 항목 미결정)
**Next Task**: TAP 스케줄 `com.tap.scheduler.plist`가 비활성화됨. 해당 스케줄이 발행하던 단독 blogger를 TAP 분기(travel pipeline)로 편입하는 플랜 수립 필요

## Completed This Session
1. **Phase 44 완료** — Quality audit + Coupang redesign + structure enforcement for 5 travel blogs
   - Coupang section: H2→p-tag grid, dedup, 1/2/3 product cards via `get_product_cards()`
   - H2 cap: post_process가 4개 초과 시 자동 절단
   - `_validate_and_retry` 재활성화 (H3 개수 검증)
   - 금지표현 자동 치환 (좋은→적절한, 바랍니다→필요합니다 등)
2. **5개 블로그 prompt 전면 수정** — 구조 강제 규칙 모든 블로그에 적용
3. **5개 블로그 dry-run 검증 완료**:
   - travel-hugo: H2=4 H3=3 CampKW 3/3 ✅
   - travel1-hugo: H2=4 H3=3 위치/기간 포함 ✅
   - travel2-hugo: H2=4 H3=3 맥락단어 2/2 ✅
   - travel3-hugo: H2=4 H3=3 가격표현 0 ✅
   - travel4-hugo: H2=3 H3=4 "N코스:" 패턴 ✅
4. **TAP 스케줄 탐색** — `com.tap.scheduler.plist.disabled` 확인

## Remaining Work

### Next: TAP 단독 Blogger → TAP 분기 편입
- **탐색 결과**: `~/Library/LaunchAgents/com.tap.scheduler.plist.disabled` — 이 launchd 스케줄이 TAP의 단독 blogger 하나를 발행하던 스케줄. 비활성화됨
- **과제**: 이 blogger를 TAP travel pipeline(5000 pipelines/travel/)으로 편입하여 정기 발행되도록 해야 함
- **필요한 작업**:
  1. 대상 blogger 식별 (TAP에 어떤 blogger가 있는지, 어떤 스케줄이었는지)
  2. TAP pipeline 구조 파악 (app.py, pipelines/travel/ vs TAP의 다른 pipelines)
  3. blogger를 travel pipeline에 통합 (fetcher/writer 추가)
  4. 기존 launchd 스케줄 대체 방안 (5000 scheduler에 추가? 다른 주기?)
  5. 테스트

### Phase 47 — Hugo 템플릿 회귀 방어 로직 설계
- **"아직 결정 안됨" 5개 항목 논의 필요** (CONTEXT.md 하단):
  - A. BLOCK의 구체적 기준 (deletion 1개도 차단? threshold?)
  - B. Telegram 알림 병행 여부
  - C. 참조 문서 목록 관리 방식 (manifest? AGENTS.md?)
  - D. 검증 스크립트 구현 위치
  - E. pre-commit hook 도입 여부
- **위 논의 완료 후**: `/gsd-plan-phase phase-47`

## Decisions
1. **Phase 44 Quality Gates 확정**:
   - H2: 3~4개 (post_process 자동 절단, _validate_and_retry 재시도)
   - H3: 2개 이상 (heritage deep prompt는 2개 H3 구조 허용)
   - 금지표현: post_process에서 자동 치환 (좋은→적절한, 바랍니다→필요합니다)
   - Brand KW: 요구사항에서 제외 (캠핑장 글에 브랜드 언급 부자연스러움)
2. **Coupang redesign**: `<p class="coupang-section-title">` + 그리드 (H2 사용 금지, p-tag로 대체)
3. **Phase 47 방어 범위**: STAP 6개 블로그 우선 (stock-hugo 별도 baseline)
4. **Phase 47 문서 준수**: PLAN.md에 `ref_docs` 필드 필수
5. **Phase 47 증분 감지**: 예상 diff vs 실제 diff 비교
6. **Phase 47 경보**: 위반 시 BLOCK (실행 차단)
7. **Phase 47 GSD 통합**: plan-phase 단계 검증

## Blockers
- **TAP blogger 편입**: 대상 blogger 미식별 — TAP 폴더 구조와 launchd 스케줄 분석 필요
- **Phase 47**: 5개 미결정 항목 논의 필요 → plan-phase 진행 불가

## Bugs & Issues Discovered

### 1. heritage grouped prompt H3 생성 (해결)
- **증상**: travel2-hugo (heritage)에서 H3=0 출력 (H3 미생성)
- **원인**: user prompt 구조가 `## 각 문화유산별 H3` 로 H2로 표시되어 AI가 H3 생성 의도를 인식 못함
- **해결책**: `### 각 문화유산별 상세 — 데이터 수만큼 H3 생성`으로 변경 + "H1 사용 금지. H2(##) 4개와 H3(###) 사용" rules 추가
- **미해결 여부**: ✅ 해결됨 (현재 H3=3 정상 출력)

### 2. festival prompt "운영시간" 누락 (API 데이터 한계)
- **증상**: travel1-hugo output에 "운영시간" 필드 없음
- **원인**: API 데이터에 playtime(운영시간) 정보가 없어 anti-hallucination 규칙으로 인해 AI가 생성 불가
- **해결책**: 데이터 품질 이슈로 판단. prompt에 "운영시간" 요구는 유지하되 API 데이터 누락 시 자동 생략
- **미해결 여부**: ⚠️ 데이터 의존성 — 근본 해결은 API 데이터 보강 필요

### 3. stock-hugo 템플릿 상태 불명확 (Phase 47)
- 이전 .continue-here.md에서 이관 — Phase 47에서 처리 예정

### 4. ADSENSE-GUIDE.md 참조 누락 (Phase 47)
- 이전 .continue-here.md에서 이관 — Phase 47에서 처리 예정

## Environment Notes
- **OS**: macOS (darwin)
- **Python**: 3.14
- **Git**: 5000 repo — 6개 commit ahead of origin/main
- **launchd 상태**:
  - `com.5000.scheduler.plist` — 정상 동작 중
  - `com.tap.scheduler.plist.disabled` — 비활성화됨 (이번 세션 확인)
- **주의**: `data/cooldown.json`, `data/failure_count.json`은 작업과 무관한 런타임 데이터

## Resume Instructions
1. **세션 시작**: `cd /Users/twinssn/Projects/5000`
2. **최신 커밋 확인**: `git log --oneline -3`
3. **Phase 44 상태**: ✅ Complete (`0c2471a40`)
4. **TAP 스케줄 확인**: `cat ~/Library/LaunchAgents/com.tap.scheduler.plist.disabled`
5. **TAP blogger 확인**: `ls /Users/twinssn/Projects/TAP/pipelines/` 로 현재 pipeline 구조 파악
6. **blogger 편입 플랜**: `./gsd-plan-phase` 로 신규 phase 생성 또는 PLAND.md 작성
7. **Phase 47 재개 시**: `cat .planning/phase-47-template-regression-defense/CONTEXT.md` → 5개 미결정 항목 논의 → `/gsd-plan-phase phase-47`

## Uncommitted Changes
```
 M data/cooldown.json
 M data/failure_count.json
?? update_prompts.py
```
(runtime data만 변경 — 커밋 불필요)

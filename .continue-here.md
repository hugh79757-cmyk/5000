# Continue Here — 2026-09-02 세션 핸드오프

> 생성: 2026-09-02 (Phase 76 완료 직후 pause)

## Current Position

**Phase 76 — recurrence-prevention** 실행 완료. `.planning/phases/phase-76-recurrence-prevention/` (CONTEXT.md, PLAN.md, SSOT-DESIGN.md).

Wave 1~5 전부 완료. 플랜 검증 기준 1건(02:00 야간 관찰)만 다음 날 확인 잔여.

## Completed This Session

### 5-Alert Triage (오전~오후)
1. kitchen/fitness P36+P14: KEYWORD_MAP 정크("가능" 등) purge — fitness 264→173, kitchen 179→92 (커밋 0a3d3c005)
2. tour-hugo P25 (900s timeout): dispatcher.py preflight S01/S02 비용 축소 — corpus 1회 구축 + `_S_CORPUS_CAP=200` + `QUALITY_ENFORCE_S01_S02` 킬스위치. 실측 900s→**3.1s** (커밋 0a3d3c005)
3. rap P16 / escape / travel P01: 자연 해소 확인 (수정 불필요)
4. 전 블로그 정크 purge 9 세그먼트 추가: car/golf/appliance/interior/baby/health/pet/beauty/camping — TOTAL 2994→2260 (purge 734) (커밋 0a3d3c005)
5. watersports P02 5연속: run() 회전 재시도 5회 — 데이터없는 토픽 exhausted 스킵 (커밋 5d0c10545, 재현 2회 성공)

### 재발 구조 분석
- 키워드 오염 6일 7회 재발 구조 확정: keyword_expander **매일 02:00 자동 실행**(scheduler.py:1331)이 네이버 autocomplete 키워드 재주입. `except ImportError: pass`로 validate 우회.
- SSOT 이중 구조가 최대 블로커: keyword_pool DB 비어있음 → get_keywords()가 KEYWORD_MAP(3300줄 하드코딩) fallback. 4개 writer가 같은 파일 경쟁 편집.

### Phase 76 — 재발 방지 (근본 해결)
1. **Wave 1 [PRODUCTION] write-time guard** (커밋 6c76ef7bc): keyword_expander.py + naver_datalab_sync.py — validate 실패/미이용 시 추가 중단. 재주입 원천 차단.
2. **Wave 2 [PRODUCTION] read-time 필터** (커밋 fa604033e): `get_keywords()` KEYWORD_MAP fallback 시 validate 통과분만 반환. 킬스위치 `KEYWORD_READ_FILTER=0`. 오염 주입 재현 → 정크 0건, pytest 회귀 0건(19f/129p baseline 동일).
3. **Wave 3 [PRODUCTION] corpus cap 로그** (커밋 fa604033e): preflight 샘플링 시 로그 1줄. tour-hugo 실측 3.1s.
4. **Wave 4 [PRODUCTION] 대시보드 자가치료 문구** (커밋 fa604033e): P02/P14/P25/P36 action에 실행 커맨드 추가. problem_registry.py + problems.yaml **양쪽**(YAML이 SSOT 오버라이드 — gotcha). 대시보드 재시작 후 /api/registry 반영 확인.
5. **Wave 5 [문서] SSOT-DESIGN.md**: keyword_pool DB SSOT 승격 설계 M1~M6. 실행 별도 phase.

### 문서·운영
- triage: `.planning/triage/20260902--five-alert-keyword-purge-preflight-cost.md` + INDEX.md
- worklog: `.planning/worklog/WL-20260902-five-alert-followup-fleet-keyword-purge.md` (Phase 76 내용 포함 갱신)
- 파괴 로그: `logs/destructive_2026-09-02.log` — scheduler 재시작(PID 30798), dashboard 재시작(PID 58637) 2건 (둘 다 사용자 승인)

## Remaining Work

1. **[다음 날 9/3] 02:00 expander 첫 야간 실행 관찰** — write-time guard + read-time 필터 이중 방어 후 첫 사이클. 확인 커맨드:
   ```bash
   python3 -c "from pipelines.curation.keywords import get_keywords; print([k for k in get_keywords('car-hugo') if k in ('가능','가방','노트북')])"
   ```
   빈 리스트 `[]` = 성공. 재발 시 expander 로그(`logs/scheduler.log` 02:00대) + get_keywords 직접 확인.
2. watersports-hugo 21:40 스케줄 프로덕션 실발행 관찰 (수동 재현 2회는 성공)
3. SSOT 마이그레이션 (M1~M6) — 별도 phase. `SSOT-DESIGN.md` 참조. 파괴적 4단계 프로토콜 대상.
4. 19개 pre-existing 테스트 실패 (tests/curation — 본 작업 범위 외, baseline부터 실패)
5. best-* 블로그 insufficient_products (Coupang 데이터 부족) — 별개 이슈
6. escape_topics 8개 잔여 — 소진 시 자동 중지

## Decisions

- **이중 방어 채택**: write-time(expander 차단) + read-time(get_keywords 필터) — purge 반복(6일 7회) 대신 구조적 차단. 둘 다 킬스위치로 가역(`KEYWORD_READ_FILTER=0`, expander는 import 성공 시 기존 동작).
- **SSOT 방향**: keyword_pool DB를 SSOT로, KEYWORD_MAP은 read-only seed로 축소 — 실행은 별도 phase(파괴적).
- **S01/S02 preflight OFF** (기본): WARN-ONLY였고 발행 시점 quality_guard가 동일 게이트 수행. 재활성 `QUALITY_ENFORCE_S01_S02=1`.
- **P36 YAML 미추가**: config/problems.yaml 마지막 = P35. YAML 누락 시 코드 dict fallback이 사용되므로 코드만 수정해도 유효.
- **rap P16 무수정**: 당일 재발행 차단 가드 = 정상 동작.
- 커밋 스타일: 웨이브/주제별 개별 커밋. amend 금지.

## Blockers

없음. 전부 완료 or 관찰 대기 상태.

## Bugs & Issues Discovered

1. **keyword_expander `except ImportError: pass`** — 증상: 02:00 자동 실행이 validate 없이 정크 재주입(6일 7회 P36 재발). 원인: import 실패 시 무필터 추가 경로. 해결: guard 제거 + return 0 (커밋 6c76ef7bc). 미해결 여부: 해결 — 다음 날 02:00 관찰만 남음.
2. **naver_datalab_sync `update_keywords_py()` validate 전무** — 증상: 쿠팡 역추출 키워드 무검증 append. 원인: validate 호출 자체가 없었음. 해결: 동일 가드 추가 (커밋 6c76ef7bc). 미해결 여부: 해결.
3. **problems.yaml이 problem_registry SSOT 오버라이드** — 증상: problem_registry.py만 수정해도 /api/registry에 구문구 반환. 원인: `apply_problem_yaml()`(797-803)이 YAML 우선. 해결: 양쪽 동기 수정 (커밋 fa604033e). YAML gotcha: `: ` 포함 시 싱글쿼트 이스케이프 필요. 미해결 여부: 해결 — 향후 P-code 문구 수정 시 양쪽 수정 필수 (문서화됨).
4. **dispatcher preflight S01/S02 이차 비용** — 증상: tour-hugo 900s scheduler kill (발행은 성공). 원인: 루프 내 매 포스트 전체 corpus 재스캔 + TF-IDF (301×300회). 해결: corpus 1회 구축 + cap 200 + 킬스위치, 3.1s (커밋 0a3d3c005). 미해결 여부: 해결. ETAP 다른 블로그 corpus growth는 구조적 — SSOT 방식 외 별도 모니터링.
5. **rap/pipeline.py 139-158 closed connection 재사용 가능성** — 증상: 없음(잠재). 원인: using_gap=True 시 close 후 재사용, except pass로 가드 우회 가능. 해결: 안 함 — 본 사건 무관 가능성 높음, P16은 정상 가드. 미해결 여부: 잠재 결함으로 기록만. 별도 조사 권장.
6. **best-* insufficient_products** — 증상: baby-hugo 등 P14 알림. 원인: Coupang 베스트 상품 <3개 (데이터 부족). 해결: 안 함 — keyword 오염 무관 별개 이슈. 미해결 여부: 열림 — 별도 트리age 필요.

## Environment Notes

- scheduler 신규 PID 30798 (16:58 재시작, 383 jobs, HealthCheck 통과) — purge된 keywords.py + 수정 dispatcher/watersports 로드 완료.
- dashboard 신규 PID 58637 (재시작) — /api/registry 신규 action 문구 반영.
- dashboard auth: `set -a; source .env` 후 OPS_USER/OPS_PASSWORD 환경변수 사용. curl -u 직접 grep은 401.
- 테스트 실행 시 `OPS_TEST_MODE=1` 필수 (conftest guard — 미설정 시 전 ERROR).
- baseline 테스트: `OPS_TEST_MODE=1 pytest tests/curation -q` = 19 failed / 129 passed (19 = pre-existing).
- wrangler 배포 금지: dispatcher.py 사용 (CLOUDFLARE_API_TOKEN 제거 로직 내장). 수동 배포 시 AGENTS.md 섹션3 참조.
- git push는 형상관리 목적. Cloudflare Pages git 자동빌드는 Skipped = 정상.

## Resume Instructions

```bash
# 1. 02:00 관찰 (다음 날)
grep -A2 "02:0" logs/scheduler.log | grep -iE "expander|keyword" | tail -5
python3 -c "from pipelines.curation.keywords import get_keywords; print([k for k in get_keywords('car-hugo') if k in ('가능','가방','노트북')])"
# [] = 성공 (정크 0건)

# 2. watersports 21:40 관찰
grep "watersports" logs/scheduler.log | tail -5

# 3. 스케줄러/대시보드 상태
launchctl list | grep com.5000
curl -s -u "$OPS_USER:$OPS_PASSWORD" http://localhost:5060/api/attention | head -50

# 4. SSOT 마이그레이션 phase 시작 시
# .planning/phases/phase-76-recurrence-prevention/SSOT-DESIGN.md 읽고 plan-phase 워크플로로 신규 phase
```

## Uncommitted Changes

- ` M .planning/worklog/WL-20260902-five-alert-followup-furge-keyword-purge.md` — Phase 76 내용 갱신분 (60 insertions)
- ` M data/analytics_status.json` — 런타임 자동 갱신 (스케줄러 메타데이터 — 무시 가능)
- untracked: `data/.lock_*` (17개, 런타임 락), `logs/destructive_2026-09-02.log`, `ops_dashboard/logs/`, `ops_dashboard/ops.db-shm/-wal` (SQLite WAL), `output/image_cache/`, `output/images/*` (이미지 캐시) — 전부 런타임 산출물, 커밋 대상 아님

**커밋 정책**: worklog는 gitignore 대상 아님 — 핸드오프 커밋에 포함. analytics_status.json + 런타임 산출물은 제외 (기존 관례: 스테이징 시 파일 명시).

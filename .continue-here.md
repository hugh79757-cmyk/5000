# Continue Here — Phase 26: CUAP Auto Ads (AdSense Publisher ID 통일)

## Current Position
- **Phase 26-cuap-auto-ads** (`.planning/phase-26-cuap-auto-ads/PLAN.md`): CUAP 10개 블로그 AdSense 오토 애드(앵커/전면) 활성화
- **상태**: 구현 + 배포 + 라이브 검증 **완료**. 단, PLAN의 "CUAP은 Pages 블로그" 전제가 **오류**였음을 발견 → 실제는 **6 Worker / 4 Pages**. 이 분류는 `BLOG_STATUS.md`로 문서화 완료.
- **이 문서는 이전 "Phase Thumbnail Fix" 세션의 Stale 핸드오프를 덮어쓴** (2026-07-21).

## Completed This Session

### T1 — 배포 라우팅 확인
- `dispatcher.py` `WORKERS_BLOGS`(514-521) = {health, pet, kithen, beauty, camping, baby} → `wrangler deploy --config wrangler.toml` (Worker)
- 나머지 4 = appliance, fitness, interior, laptop → `wrangler pages deploy` (Pages)

### T2 — params.toml 교정 (10/10)
- `[adsense]` → `[advertisement]` rename, `clientID` → `adsense` rename
- 값: `adsense = "ca-pub-6677996696534146"` (informationhot), `inArticleSlot`/`leaderboardSlot` = `2195212287` 유지

### T3 — partials key-path fix (10/10) + single.html 정리
- `in-article.html`/`leaderboard.html`: `site.Params.adsense.clientID` → `.Site.Params.advertisement.adsense` (PLAN 범위 초과로 발견한 실제 버그 수정)
- `data-ad-slot="{{ .Site.Params.advertisement.adsense.inArticleSlot }}"` 적용
- `layouts/_default/single.html`: 수동 `(adsbygoogle=window.adsbygoogle||[]).push({})` 블록 10/10 제거 (자동 loader 충돌 방지)

### T4 — 배포
- 9개: `dispatcher.py {blog_id}` (이 중 beauty/health/pet/kitchen/camping = Worker 경로로 실제 배포됨)
- baby: Pages custom domain stale 충돌 → `wrangler pages project delete baby-hugo` + Dashboard 레코드 삭제 → `wrangler deploy --config wrangler.toml` (Worker, version d230809c) → `baby.informationhot.kr` 바인딩
- **과거 오기 정정**: 중간 보고서 "9 Pages + 1 Worker"는 **오답** → 실제는 **6 Worker / 4 Pages**

### T5 — 라이브 검증 (10/10)
- curl 10개 홈 → HTTP 200, 메타 `ca-pub-6677996696534146` 노출, rotcha(`ca-pub-8772455780561463`) 유출 0건
- baby 포스트 in-article 슬롯 `data-ad-slot=2195212287`, client `ca-pub-6677996696534146` 확인

### 문서화
- `/5000/BLOG_STATUS.md` + `/cuap/BLOG_STATUS.md` 생성 (다음 세션 참고용 Worker/Pages 분류 + AdSense 현황)

## Remaining Work
- **Pages 4개 → Worker 편입 여부 결정 필요**: appliance/fitness/interior/laptop은 `wrangler.toml`(Worker형)을 보유하나 dispatcher가 미사용. Worker로 편입(`WORKERS_BLOGS` 추가)할지, Pages 유지 + 잔존 toml 정리할지 다음 세션 결정.
- **AdSense 대시보드 수동 확인 (사용자 액션)**: 슬롯 `2195212287`이 informationhot 계정(`ca-pub-6677996696534146`)에 실제 존재 + `*.informationhot.kr` 도메인 승인 + Auto ads/Anchor ON 상태. 외부 콘솔이라 에이전트가 못 함.

## Decisions
1. Publisher ID: CUAP 10개 전부 `ca-pub-6677996696534146` (informationhot) 고정 — rotcha(`8772`) 절대 혼합 금지 (AGENTS.md §1)
2. 슬롯: `inArticleSlot`/`leaderboardSlot` = `2195212287` (기존값 유지, 6677 계정 실제 슬롯인지 대시보드 확인 필요)
3. `extend_head.html`: GA4(블로그별 G-XXXX 유지) + `adsbygoogle.js?client=ca-pub-6677996696534146`
4. baby: Pages → Worker 전환 (stale custom domain 정리 후)
5. 배포: 반드시 `dispatcher.py` 경유 (Worker는 내부 `wrangler deploy`); 수동 `wrangler pages deploy` 금지

## Blockers
- 없음 (AdSense 대시보드 확인은 외부 액션, 블로커 아님)

## Bugs & Issues Discovered

### 1. PLAN false assumption "CUAP은 Pages 블로그"
- **증상**: PLAN.md:91 "CUAP은 Pages 블로그" 명시, 배포를 `wrangler pages deploy`로 가정
- **원인**: 실제 `dispatcher.py` `WORKERS_BLOGS`에 6개(baby 포함)가 등록 → Worker 경로
- **해결책**: 실제 분류(6 Worker/4 Pages)로 정정, `BLOG_STATUS.md` 문서화
- **미해결 여부**: ✓ 문서화 완료 / Pages 4개 처리 방향은 미결정

### 2. baby stale Pages custom domain 차단
- **증상**: `wrangler deploy` 시 "custom domain baby.informationhot.kr already exists" 에러로 Worker 배포 실패
- **원인**: baby가 원래 Pages 프로젝트였고, 그 custom domain 레코드가 stale로 남음
- **해결책**: `wrangler pages project delete baby-hugo` + Dashboard에서 레코드 삭제 → Worker 배포 성공(bound 확인)
- **미해결 여부**: ✓ 해결 완료

### 3. in-article/leaderboard partial key-path 오류 (PLAN 범위 초과)
- **증상**: PLAN은 params.toml 교정만 지정했으나, partial이 `site.Params.adsense.clientID`(존재 안 함)를 읽어 슬롯이 빈 값/잘못된 키
- **원인**: CUAP partial이 rotcha 시절 키 경로 사용
- **해결책**: `.Site.Params.advertisement.adsense` + `inArticleSlot`로 수정 (10/10)
- **미해결 여부**: ✓ 해결 완료

### 4. single.html 수동 push 블록 충돌
- **증상**: single.html에 `(adsbygoogle=window.adsbygoogle||[]).push({})` 수동 블록이 extend_head의 자동 loader와 중복/충돌
- **원인**: 수동 push가 자동 loader 초기화를 선점
- **해결책**: 10/10 single.html에서 수동 push 블록 제거
- **미해결 여부**: ✓ 해결 완료

### 5. 과거 보고서 오기 "9 Pages + 1 Worker"
- **증상**: Phase 26 중간 보고에서 CUAP를 9 Pages + baby 1 Worker로 표기
- **원인**: dispatcher 라우팅 미확인 상태에서 오판
- **해결책**: 실제 6 Worker/4 Pages로 정정 (이 문서 + BLOG_STATUS.md)
- **미해결 여부**: ✓ 정정 완료

### 6. Pages 4개의 잔존 wrangler.toml (미해결)
- **증상**: appliance/fitness/interior/laptop이 `wrangler.toml`(Worker형 `name`)을 가졌으나 dispatcher Pages 경로에서 미사용
- **원인**: Worker→Pages 전환 시 toml 잔존, 또는 Worker 편입 누락
- **해결책**: 미적용 — 다음 세션에서 편입 또는 정리 결정 필요
- **미해결 여부**: ✗ 미해결 (결정 대기)

## Environment Notes
- **dispatcher.py `WORKERS_BLOGS`(514-521)**: 배포 분류의 유일 소스 오브 트루스. 수정 시 이 리스트만 고치면 됨
- **CLOUDFLARE_API_TOKEN**: wrangler subprocess에 전달 금지 (OAuth profile 우선). dispatcher가 내부에서 제거함
- **git push ≠ 배포**: Cloudflare Pages git 빌드는 비활성화(Skipped). 배포는 dispatcher/wrangler 직접
- **macOS launchd** 스케줄러가 `scheduler.py` 구동
- **BLOG_STATUS.md**: /5000, /cuap 양쪽 존재 — 다음 세션 어디서 시작해도 참고 가능

## Resume Instructions
```bash
# 1. 현황 파악
cat /Users/twinssn/Projects/5000/BLOG_STATUS.md

# 2. (옵션) Pages 4개 → Worker 편입 결정 시
#    /Users/twinssn/Projects/5000/dispatcher.py WORKERS_BLOGS 에
#    appliance-hugo, fitness-hugo, interior-hugo, laptop-hugo 추가
#    → dispatcher.py {blog_id} 로 자동 Worker 배포

# 3. AdSense 대시보드 수동 확인 (사용자)
#    - ca-pub-6677996696534146 계정에 슬롯 2195212287 존재
#    - *.informationhot.kr 도메인 승인
#    - Auto ads / Anchor ads ON

# 4. 재배포 필요 시
python3 /Users/twinssn/Projects/5000/dispatcher.py {blog_id}
```

## Uncommitted Changes
**이번 세션 산출물 (커밋 대상):**
- `/Users/twinssn/Projects/5000/.continue-here.md` — 본 핸드오프
- `/Users/twinssn/Projects/5000/BLOG_STATUS.md` — Worker/Pages 분류 + AdSense 현황
- `/Users/twinssn/Projects/cuap/BLOG_STATUS.md` — 동일본 (cuap 트리, 별도 관리)
- `.planning/phase-26-cuap-auto-ads/` — 페이즈 PLAN (재개 컨텍스트)

**이번 세션이 아닌 기존 dirty (커밋 비대상 / 별도 처리 권장):**
- `config/blogs.d/cuap.yaml`, `data/cooldown.json`, `data/failure_count.json`
- `pipelines/curation/keywords.py`, `pipelines/curation/writer.py`
- `.omo/run-continuation/*.json` (세션 junk), `data/mc_chains.db.before-*` (백업)
- `.planning/phase-23-cuap-adsense/`, `.planning/phase-24-cuap-quality/`
→ 위 파일들은 본 Phase 26 작업과 무관한 선행 세션 WIP/런타임 상태/백업임. `git add -A`로 일괄 커밋하지 않음.

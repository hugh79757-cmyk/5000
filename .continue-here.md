# .continue-here.md — Work Handoff

**Timestamp**: 2026-08-24 15:26 KST
**Branch**: `main` (HEAD: 3f3b660f9)
**Session 주제**: 마감 핸드오프 + nature 고갈 해소 + P02 알림 트리아지

---

## Current Position

GSD Phase 체계 밖 운영 세션. 활성 STATE.md: v1.1 M5 조건부 로드맵 (33 phases, 29 done / 1 in-progress / 4 planned). 최근 운영 라인: ETAP 토픽 풀 고갈/리셋 → nature-hugo 긴급 확장 → CUAP keyword_pool 대기(03:00) → 핸드오프 문서(.gsd/HANDOFF-20260825.md) 커밋. 마지막 Phase 문서: `.planning/phase-24-cuap-quality/` 등, 이번 세션은 phase 워크플로 미사용.

- 진입점: `ops_dashboard/app.py:create_app()` → :5050
- SSOT: `ops_dashboard/ops.db` (17 tables), `data/travel-en.db` (ETAP 36), `.gsd/SESSION.md` (Wave 3 81/0/4)
- 핸드오프: `.gsd/HANDOFF-20260825.md` (내일 06:00 검증 5체크)
- 최근 커밋: `3f3b660f9` nature expand, `6e8e14650` handoff, `d739d9561` MAN-004, `cc652d517` watersports+200 등

---

## Completed This Session

1. `.gitignore` .gsd 제거 + `git add -f .gsd/SESSION.md` 커밋
2. `pipelines/etap/deals_pipeline.py` dest 기반 cover/body 중복 해소 확인 (이미 패치, skip)
3. `.gsd/HANDOFF-20260825.md` 생성 (7작업: CUAP검증→closure→runner→placeholder19→STAP→expander→deals검증)
4. 커밋 `6e8e14650` docs: handoff document for 2026-08-25 resume work
5. P02 알림 2건 트리아지: rap5-hugo(stale, 15:21 수동 발행으로 P02 closed/0), nature-hugo(진짜 0 → 조사)
6. nature-hugo 토픽 조사: 179 전부 publish_log orphan, topic_manager sync 0 → 신규 55도시 INSERT (total 234/avail 105→104)
7. nature-hugo 발행 재시도: 1회 DRAFT(4x repeat) → 2회 Costa Rica 8이미지/R2/배포 성공, P02 2건 close, data_stock pass
8. 커밋 `3f3b660f9` fix(etap): nature-hugo expand 55 cities

---

## Remaining Work

- [ ] **내일 06:00 CUAP keyword_pool 검증** (HANDOFF 1-2): `harvest_20260825.log` + `keyword_pool` + expander + no_keyword → closure 커밋 `.planning/worklog/WL-20260825-cuap-final-closure.md`
- [ ] **runner.py 연동**: `ops_dashboard/checks/topic_pool_health.py` 49 PASS → 대시보드 등록 패턴 맞추기
- [ ] **placeholder 19건 교체**: pick/rap2/rap4 default-thumbnail → R2 cover.webp (Pexels fetch + upload + sed + deploy)
- [ ] **STAP stock_issue 활용**: ipo source 라우팅 (stock_issue 2799 미사용) 우선순위 수정
- [ ] **expander 전분기 확대**: CUAP→ETAP/CAP (sustainability 설계)
- [ ] **deals body dest 검증**: `curl deals.techpawz.com` R2 src destination별 분산 확인
- [ ] **nature 잔여**: avail 104지만 신규도시 55개 품질 재검 필요 (sustainability Phase 2)
- [ ] **C계열 fixer / AD_INSERT 보완** (SESSION backlog #2-3, OQ#1)

---

## Decisions

- ETAP 고갈은 flag 리셋이 아닌 **신규 city INSERT**로 해소 (nature 179 전체 publish된 orphan → flag flip 무효)
- nature 55도시 선정: 전세계 nature 대표 60개 중 기존 5개 제외, slug `{city}-nature-tours`, title templated, priority=tour_count
- rap5-hugo 알림은 stale로 판정, 재발 아님, 조치 불필요
- handoff 문서는 .gsd/ 강제 추적 (.gitignore .gsd 제거) — SESSION.md SSOT 유지
- travel-en.db 강제 추적 (ignore라도 -f) — 토픽 풀은 코드 아닌 데이터이나 운영 SSOT

---

## Blockers

- CUAP harvest 03:00 크론 대기 — 08-25 06:00 전 검증 불가
- data/travel-en.db 용량 151MB, git에 바이너리 추적 — push 무거움, 대안은 R2/별도 백업 고려 필요
- .gsd/CODEBASE.md/runtime 미추적 (무시됨) — 필요시 add -f

---

## Bugs & Issues Discovered

1. **nature-hugo 0 avail but DB shows 50**
   - 증상: `SELECT exhausted=0` 50건인데 dispatcher EXHAUSTED 0
   - 원인: 50건은 exhausted=0 orphan (이미 publish_log에 존재), topic_manager sync 로직이 SELECT ... NOT IN publish_log로 거름 → 0
   - 해결: 55 신규 INSERT, Costa Rica publish success
   - 미해결: 아니오 (avail 104 확보)

2. **Costa Rica 첫 발행 DRAFT 실패**
   - 증상: `Tour repeated 4x: 'pajarilla night tour' → DRAFT`
   - 원인: nature_writer dedup 1 tour 그대로 4회 언급
   - 해결: 2회 재시도 시 3x WARNING으로 완화되어 publish 성공 (trim to max 2)
   - 미해결: 아니오, quality_guard 임계 4x 기준 유지

3. **QualityGuard telegram ImportError**
   - 증상: `cannot import name 'send_warning' from shared.telegram_notifier`
   - 원인: quality_guard.py가 send_warning import, 실제 모듈에 없음
   - 해결: WARNING 로그만, 실패 무시 (이번 publish는 성공)
   - 미해결: 예, 다음 세션에서 telegram_notifier 시그니처 정리 필요

4. **rap5-hugo P02 알림 stale**
   - 증상: consecutive 3 P02 메일
   - 원인: 08:10 fail → 15:21 수동 rap5-hugo 발행 성공으로 이미 closed
   - 해결: 메일 큐 지연, 조치 불필요
   - 미해결: 아니오

---

## Environment Notes

- macOS darwin, Python 3.x, Hugo blowfish, Cloudflare Pages (month 500 builds), wrangler deploy 직렬화 락
- DB: SQLite per pipeline + ops.db, data/travel-en.db 151MB 바이너리
- scheduler.py PID 56215 launchd, harvest 03:00 JST, keyword_expander subprocess
- R2 bucket pub-2f5c7af1c303419a933069212bc25874.r2.dev, Hugo THEMES_DIR shared-themes
- 브라우저 캐시 무관, 배포 mode Pages (wrangler pages deploy)

---

## Resume Instructions

```bash
cd /Users/twinssn/Projects/5000
cat .continue-here.md
cat .gsd/HANDOFF-20260825.md   # 내일 06:00 체크리스트 첫 참조
cat .gsd/SESSION.md | head -60
git log --oneline -5
git status --short
# CUAP 검증 (06:00+)
ls logs/harvest_20260825.log && grep -c "INSERT" logs/harvest_20260825.log
sqlite3 data/curation.db "SELECT COUNT(*) FROM keyword_pool WHERE used=0;" 2>/dev/null || echo "table missing"
grep "keyword_expander" logs/scheduler.log | tail -5
# nature 확인
sqlite3 data/travel-en.db "SELECT COUNT(*), SUM(exhausted=0) FROM nature_topics;"
curl -s https://nature.techpawz.com/posts/costa-rica-nature-tours/ | head -20
```

---

## Uncommitted Changes

- `data/travel-en.db` — modified (already committed 3f3b660f9 but working copy dirty due to second Costa Rica publish? git shows M)
- `ops_dashboard/ops.db-shm` / `ops_dashboard/ops.db-wal` — deleted (WAL cleanup, ignore)
- `.gsd/CODEBASE.md`, `.gsd/runtime/` — untracked (ignore)
- `.continue-here.md` — new (this file, to be committed next step)


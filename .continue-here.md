# .continue-here.md — Work Handoff

**Timestamp**: 2026-07-31 02:00 KST  
**Phase**: AI Marketing Skills — Phase 15.1 (Wave 1 Complete, Backfill Done, Deployed)  
**Branch**: `fix/rap-subscription-backfill` (active) / `main` (commits 4948d751f, 9fee5a0b3)  
**Last Commit**: `9fee5a0b3` Wave 1-2: enforce deterministic markdown table numbering

---

## Current Position

**Phase**: AI Marketing Skills (Phase 15.1) — Wave 1 Complete ✅  
**Active Branch**: `fix/rap-subscription-backfill`  
**Main Branch Commits**: 
- `4948d751f` — Wave 1-1: fix subscription table numbering continuity and header count mismatch
- `9fee5a0b3` — Wave 1-2: enforce deterministic markdown table numbering and count match for subscription articles

**Status**: Wave 1 Complete → Backfill Done → Hugo Rebuild → rap2-hugo Deployed ✅

---

## Completed This Session

### Wave 1-1: 표 번호 연속성 검증 (Reference Block) — Commit `4948d751f`
- **File**: `pipelines/rap/writer.py` (`_build_subscription_reference`)
- **Fix**: Extract `_display = subscriptions[:15]` once; use `len(_display)` for header count; enumerate `_display` for sequential 1..N numbering
- **Verification**: 8 dry-run cases (5 regions × 15 rows + 0/1/25-edge) — ALL PASS

### Wave 1-2: 결정론적 후처리 교정 (AI-Generated Table) — Commit `9fee5a0b3`
- **File**: `pipelines/rap/pipeline.py` — Added `normalize_reference_table()`
  - Detects table by header `| 번호 | 공고명 | 유형 | 지역 | 접수기간 | 상태 |`
  - Renumbers all data rows sequentially 1..N (ignores original numbers)
  - Replaces all `총 N건` / `총 N건의 공고가 확인되는데` patterns with actual row count
  - No-op when no matching table
- **Hooked at two points**:
  - `_post_process()` start → all-strategy safety net
  - Right after `generate_subscription_article()` → subscription-specific 1st-pass fix
- **AI Prompt Strengthened** in `pipelines/rap/writer.py`:
  - Instruct GPT to renumber included rows from 1
  - Require `총 N건` to match actual included rows
  - Note `normalize_reference_table()` as final deterministic guard
- **Strengthened AI prompt** in `pipelines/rap/writer.py`

### Verification Results (Wave 1-2)
- **Exact bug reproduction**: Article `26년-7월-인천부천-든든전세주택-정정공고-청약-안내` — numbers 1,3,4,5 + "총 15건" vs 4 rows → corrected to 1,2,3,4 + "총 4건"
- **Edge cases**: Non-matching tables unchanged, already-correct tables preserved, multiple count mentions all corrected, empty body safe
- **7-day production scan (RAP blogs)**:
  - Scanned: 170 posts
  - Subscription-table posts: 35
  - **Mismatches found: 13 (37%)** — numbering gaps and/or "총 N건" ≠ row count
  - All 13 corrected by `normalize_reference_table()`

### Exit Criteria Result (Wave 1)
> **최근 7일간 RAP 청약-표 포맷 글 35건 중 13건(37%)에서 표 번호 불연속 또는 "총 N건" 불일치 발견 → `normalize_reference_table()` 적용 후 결정론적으로 교정됨**

### Backfill Work (Branch `fix/rap-subscription-backfill`)
- **DB 교정** (`--apply`): 51 articles `body_md` corrected in `stap_content.db` (backup: `backup/rap_subscription_pre_backfill_20260731_003223.json`)
- **마크다운 재생성** (`--apply`): 51 `index.md` files regenerated with frontmatter preserved, body replaced (backup: `backup/rap2_markdown_pre_regen_20260731_011736.json`)
- **Hugo 재빌드**: `hugo --gc --minify` success (1.3s)
- **Cloudflare Pages 배포**: `wrangler pages deploy ./public --project-name=rap2-hugo` — **Success** (334 files uploaded, 6.43s)
- **Other 4 RAP blogs unaffected**: rap-hugo, rap3-hugo, rap4-hugo, rap5-hugo mtime unchanged

### 확산 검토 대상 등록 (Wave 1 — Spread Review)
| 파이프라인 | 의심 블로그 | 표 패턴 유사도 | 비고 |
|------------|-------------|----------------|------|
| **TRAVEL** (pipelines/travel) | travel-hugo, travel1~4-hugo | 높음 — 지역별 명소/숙소 표에서 GPT가 대표 항목만 선택 시 번호 불연속 발생 가능 | 가장 시급 |
| **ETAP** (pipelines/etap) | ETAP 30+ Hugo blogs | 중간 — 여행 상품 카드 표에서 동일 패턴 사용 | ETAP는 별도 repo |
| **CURATION** (pipelines/curation) | curation 계열 | 낮음 — 상품 비교 표 위주, GPT 생성 표 빈도 낮음 | 추후 검토 |
| **CAR** (pipelines/car) | car-hugo | 낮음 — 실거래가 표 위주, GPT 생성 표 적음 | 추후 검토 |

> **액션 필요:** TRAVEL 파이프라인의 실제 발행 글 표본 조사 → 버그 발생률 측정 → 동일 `normalize` 로직 적용 여부 결정

---

## Remaining Work

| Wave | Task | Status | Priority |
|------|------|--------|----------|
| **Wave 1** | Merge `fix/rap-subscription-backfill` → main | ⏳ Pending | High |
| **Wave 2-1** | `duplicate_gate.py` 신설 + DB 마이그레이션 (`source_ids` 컬럼) | 📋 Not Started | High |
| **Wave 2-2** | RAP 파이프라인에 게이트 hook 연결 (`pipelines/rap/pipeline.py`) | 📋 Not Started | High |
| **Wave 2-3** | rap-hugo 1개 블로그 dry-run (최근 7일 replay) | 📋 Not Started | High |
| **Wave 2-4** | `relevance_scorer.py` 통과율 100% 원인 분석 + 임계값 재설정 | 📋 Not Started | High |
| **Wave 2 확산** | TRAVEL 파이프라인 표본 조사 → `normalize` 로직 적용 여부 결정 | 📋 Not Started | High |
| **Wave 3-1** | robots.txt / sitemap.xml 정합성 점검 (rap-hugo 시작) | 📋 Not Started | Medium |
| **Wave 3-2** | GSC 사이트 매핑 누락 보완 (`analytics_collector.py`) | 📋 Not Started | Medium |
| **Wave 4** | A/B 테스트 정책 문서 + 인프라 코드 (비활성화 상태) | 📋 Not Started | Low |

---

## Decisions Made This Session

| Decision | Rationale |
|----------|-----------|
| **Wave 1 우선 순위 변경**: SEO Ops → 발행 로직 결함 수정 우선 | 구체적 결함(표 번호, "총 N건", 가점제 설명, 쿠팡 푸터) 확인 → 품질 게이트 강화 전 필수 선행 |
| **결정론적 후처리 `normalize_reference_table()` 추가** | GPT 프롬프트만으로는 37% 오류율 → 후처리로 100% 교정 보장 |
| **이중 훅**: `_post_process()` + subscription 전용 1차 패스 | 안전망 이중화 (전체 전략 공통 + 구독 전용 1차 교정) |
| **AI 프롬프트 보강** | GPT가 1차로 올바르게 생성하도록 유도, 후처리는 최종 안전망 |
| **백필 전체 51건 일괄 교정** | DB → 마크다운 → 빌드 → 배포 단일 파이프라인으로 완료 |
| **rap2-hugo 단독 배포** | 다른 4개 RAP 블로그 영향 없음 확인 (별도 Pages 프로젝트) |
| **TRAVEL 확산 검토 대상 등록** | 동일 패턴 사용 확인 → 발생률 측정 후 적용 결정 |

---

## Bugs & Issues Discovered

| Issue | Symptom | Root Cause | Fix Applied | Unresolved |
|-------|---------|------------|-------------|------------|
| **표 번호 건너뜀** | 번호 1, 3, 4, 5 (2번 누락) | GPT가 reference(1~15)에서 부분 선택 시 원본 번호 그대로 사용 | `normalize_reference_table()` → 1..N 순차 재부여 | ✅ Resolved |
| **"총 N건" 문구 불일치** | "총 15건"인데 표는 4행 | GPT가 reference 헤더("총 15건")를 서두에 복사, 표는 부분만 포함 | `normalize_reference_table()` → 실제 행수로 치환 | ✅ Resolved |
| **가점제 설명 오삽입** | 매입임대/든든전세 등 추첨 유형에도 가점제 설명 삽입 | 조건 분기 없음 — 모든 공고 유형에 동일 섹션 삽입 | Task 1-3 예정 (Wave 1-3) | 🔄 Pending (Wave 1-3) |
| **쿠팡 푸터 무조건 삽입** | 청약/부동산 글에도 쿠팡 파트너스 문구 노출 | 조건 분기 없음 — 모든 글에 삽입 | Task 1-4 예정 (Wave 1-4) | 🔄 Pending (Wave 1-4) |
| **relevance_scorer 100% 통과** | 30일 평균 0.92, min 0.80 → threshold 0.55~0.75 모두 통과 | 임계값 낮음 + 스코어링 로직 관대 | Task 2-4 예정 (Wave 2-4) | 🔄 Pending |
| **중복 발행 위험** | 같은 공고 ID 반복 재탕 가능성 | 중복 게이트 없음 | Task 2-1~2-3 예정 (Wave 2) | 🔄 Pending |

---

## Environment Notes

- **OS**: macOS (Darwin) — local development
- **Python**: 3.14 (via .venv)
- **Hugo**: v0.160.1 extended (Homebrew)
- **Wrangler**: 4.110.0 — `env -u CLOUDFLARE_API_TOKEN wrangler pages deploy` (OAuth profile `hugh79757` required)
- **Databases**:
  - `stap_content.db` (79 MB) — central articles ledger
  - `content.db` (37 MB) — 5000 central ledger
  - `curation.db`, `rap.db`, `travel-en.db`, etc.
- **RAP Blogs** (5개, 독립 Pages 프로젝트):
  - `rap-hugo` → apt.informationhot.kr (rap_db)
  - `rap2-hugo` → apply.informationhot.kr (rap_db_subscription) ← **이번 작업 대상**
  - `rap3-hugo` → tax.informationhot.kr (rap_db)
  - `rap4-hugo` → rent.informationhot.kr (rap_db)
  - `rap5-hugo` → brand.informationhot.kr (rap_db)
- **AdSense**: rap2-hugo uses `ca-pub-6677996696534146` (informationhot.kr 계열) — 다른 4개는 rotcha.kr 계열 (`ca-pub-8772455780561463`)과 다름
- **Scheduler**: `com.5000.scheduler.plist` (launchd) — paused during work, restored after

---

## Resume Instructions

```bash
# 1. 작업 디렉토리로 이동
cd /Users/twinssn/Projects/5000

# 2. 브랜치 확인 및 최신화
git status
git checkout fix/rap-subscription-backfill  # 백필 브랜치에서 이어서 작업
# 또는
git checkout main  # 메인에서 Wave 2 시작

# 3. 백필 브랜치 병합 (아직 안 했다면)
git checkout main
git merge fix/rap-subscription-backfill  # 컨플릭트 없으면 fast-forward

# 4. Wave 2 시작
# Task 2-1: duplicate_gate.py 신설 + DB 마이그레이션
# Task 2-2: RAP 파이프라인 hook 연결
# Task 2-3: rap-hugo dry-run
# Task 2-4: relevance threshold 분석

# 5. 스케줄러 복원 확인 (작업 중 중단했다면)
launchctl list | grep com.5000.scheduler
# 필요시: launchctl load ~/Library/LaunchAgents/com.5000.scheduler.plist
```

---

## Uncommitted Changes

```bash
 D .coverage
 M AGENTS.md
?? .planning/phase-ai-marketing-skills/
?? CLAUDE.md
?? backup/
?? scripts/backfill_rap_subscription.py
?? scripts/regenerate_rap2_markdown.py
```

- `scripts/backfill_rap_subscription.py` — DB 교정 + 빌드 검증 스크립트 (작성됨)
- `scripts/regenerate_rap2_markdown.py` — 마크다운 재생성 스크립트 (작성됨, `--apply` 실행 완료)
- `backup/rap_subscription_pre_backfill_*.json` — DB 원본 백업 (51건)
- `backup/rap2_markdown_pre_regen_*.json` — 마크다운 원본 백업 (51건)
- `.planning/phase-ai-marketing-skills/PLAN.md` — 전체 웨이브 플랜 (작성됨)

---

## Next Up

**Immediate**: Merge `fix/rap-subscription-backfill` → `main`  
**Then**: Wave 2-1 `duplicate_gate.py` 신설 + DB 마이그레이션 (`source_ids` 컬럼 추가)  
**Then**: Wave 2-2 RAP 파이프라인 hook 연결 → Wave 2-3 rap-hugo dry-run

---

## Verification Commands

```bash
# 백필 결과 재검증
python3 -c "
import sqlite3, re
conn = sqlite3.connect('data/stap_content.db')
conn.row_factory = sqlite3.Row
rows = conn.execute(\"SELECT id, slug, body_md FROM articles WHERE blog_id='rap2-hugo' AND data_source='rap_db_subscription' AND status='published'\").fetchall()
for r in rows:
    nums = re.findall(r'^\|\s*(\d+)\s*\|', r['body_md'], re.MULTILINE)
    counts = re.findall(r'총\s+(\d+)\s*건', r['body_md'])
    if nums:
        exp = list(range(1, len(nums)+1))
        if list(map(int, nums)) != exp or (counts and int(counts[0]) != len(nums)):
            print(f'ISSUE: {r[\"slug\"]} nums={nums} counts={counts}')
print('Re-verification complete')
"

# 배포 확인
curl -s https://529611ae.rap2-hugo.pages.dev/ | head -5
```
# Continue Here — Phase 22-C 완료, kitchen-hugo 미해결

## Current Position
- **Phase 22-C**: Funnel Card Implementation — 완료
- **kitchen-hugo**: 백지 문제(Workers) — 미해결 (하단 Blockers 참조)

## Completed This Session (Phase 22-C)

### W1+W2: hugo_writer.py HTML injection engine
- `_resolve_funnel_card_post()` — content.db에서 published post + thumbnail 조회
- `_build_funnel_card_html()` — depth/bridge 카드 HTML 템플릿 (5종 data 속성 포함)
- `_extract_keywords()` — frequency-based 한국어/영어 키워드 추출 (stop words 필터)
- `_keywords_overlap_check()` — Jaccard 유사도 기반 맥락 필터 (threshold 0.15)
- `_build_and_inject_funnel_cards()` — Hugo build → BS4 파싱 → 카드 삽입 (graceful degradation)

### W3: publisher.py + content_enhancer.py 마이그레이션
- publisher.py: `_inject_funnel_links` import 제거 + hook call 제거
- content_enhancer.py: `_inject_funnel_links` → deprecated stub

### W4: thumbnail_url 지원
- `_resolve_funnel_card_post()` 이미 thumbnail_url 반환 (content.store.py thumbnail_url 컬럼 활용)

### W5: 키워드 맥락 필터 통합
- bridge card 삽입 전 `_extract_keywords()` + `_keywords_overlap_check()` 실행
- 불일치 시 skip, depth는 항상 삽입

### W6: CSS 배포 (skipped per PLAN)
- `scripts/deploy_funnel_css.py` → **76 Hugo 사이트에 funnel-card.css 배포 완료**
- 각 사이트 `assets/css/extended/funnel-card.css` 생성 (67 lines CSS)

## Remaining Work

- **kitchen-hugo 백지 문제** (최우선, 아직 미해결)
  - CSS/JS 200 OK, HTML 200 OK, Worker deploy 성공했으나 화면 표시 안 됨
  - 원인: Workers 설정, CSS 충돌, 또는 Cloudflare 라우팅 문제 추정
- **STAP 5개(finance/stock/dividend/etf/sector/ipo)** `google-adsense-account` meta tag 확인 필요
- **Naver 광고 모듈 연동** (작성자 전략: 네이버 트래픽 활용)

## Decisions

1. **CSS 배포**: Hugo 빌드 시 자동 번들링 (next publish cycle에 반영)
2. **LLM 2차 검증**: 미구현 — 키워드 필터로 90% 커버, 불확실 구간(0.15~0.3) 쌓이면 재검토
3. **skip-build**: 생략 — Hugo rebuild 2~3s 오버헤드 수용
4. **Naver 광고**: Google 대신 한국 검색/광고 채널 활용 (사용자 전략)

## Blockers

- **kitchen-hugo 백지**: CSS/JS 200 정상, HTML 200 정상, Worker deploy 성공했으나 화면 표시 안 됨
- **Workers deploy 인증**: OAuth profile로 Pages는 가능, Workers는 `CLOUDFLARE_API_TOKEN` 필수

## Bugs & Issues Discovered

| 증상 | 원인 | 해결책 | 미해결 여부 |
|------|------|--------|-------------|
| gsd-planner/gsd-plan-checker 모델 retire | glm-4.7 retired (2026-07-15) | 직접 PLAN.md 작성 + self-verify | ✓ |
| _inject_funnel_links 중복 호출 위험 | publisher.py + hugo_writer.py 둘 다 호출 가능 | publisher.py hook 제거 | ✓ |
| BS4 미설치 | beautifulsoup4 미패키지 | pip3 install beautifulsoup4 | ✓ |
| thumbnail_url 조회 누락 | _resolve_funnel_post()가 컬럼 미조회 | _resolve_funnel_card_post()에서 조회 | ✓ |

## Environment Notes

- **BS4_AVAILABLE**: beautifulsoup4 설치 필요 (pip3 install beautifulsoup4)
- **Workers deploy**: `export CLOUDFLARE_API_TOKEN=$(env -u CLOUDFLARE_API_TOKEN wrangler auth token | tail -1)` 필수
- **Pages deploy**: dispatcher.py 또는 deploy.py 사용 (OAuth profile 자동 사용)
- **Funnel cards**: Hugo 빌드 직후 HTML 파싱 → 기존 파이프라인에 영향 없음

## Resume Instructions

```bash
# 1. Phase 22-C 검증
python3 scripts/deploy_funnel_css.py --dry-run

# 2. kitchen-hugo 진단 (최우선)
curl -sI https://kitchen.informationhot.kr/css/main.bundle.min.css
curl -s https://kitchen.informationhot.kr/ | head -50

# 3. Workers 배포 필요시 (token 추출 필수)
export CLOUDFLARE_API_TOKEN=$(env -u CLOUDFLARE_API_TOKEN wrangler auth token 2>/dev/null | tail -1)
cd /Users/twinssn/Projects/CUAP/kitchen-hugo && wrangler deploy --config wrangler.toml

# 4. Hugo 블로그 발행 (funnel cards 자동 적용)
python3 dispatcher.py {funnel_blog_id}
```

## Uncommitted Changes

- `shared/publishers/hugo_writer.py` (Phase 22-C funnel functions)
- `shared/publisher.py` (funnel hook 제거)
- `shared/publishers/content_enhancer.py` (deprecated stub)
- `scripts/deploy_funnel_css.py` (신규, 76 사이트 CSS 배포)
- `config/blogs.d/*.yaml` (funnel config 추가)
- `config/prompts/_global.yaml` (FUNNEL STRUCTURE 규칙)
# Continue Here — Curation Title Diversity Fix (3 Phase 완료)

**Created:** 2026-07-12
**Phase:** Curation pipeline title diversity + keyword pool expansion
**Status:** ✅ All 3 phases committed

---

## Current Position

Complete. interior-hugo `similar_title` (keyword=게이밍) 재발 방지 대책 3개 Phase 전부 적용 완료.

## Completed This Session

### Phase 1 — Title Diversity (4 changes, `8a9dc6998`)
| Change | File | Detail |
|--------|------|--------|
| Recent titles injection | `pipelines/curation/writer.py` | `_build_system_prompt()`에 `recent_titles` 파라미터 추가, 3일 내 발행된 제목을 프롬프트에 주입하여 유사 제목 회피 |
| Temperature 0.85 | `pipelines/curation/writer.py` | `ai_generate()` 호출 시 `temperature=0.85` (기존 0.7) — 제목 다양성 증가 |
| SequenceMatcher 85% | `pipelines/curation/pipeline.py` | `_title_is_duplicate()` 임계값 80%→85% — 중복 판정 완화 |
| Rotation window 15 | `shared/title_templates.py` | `TEMPLATE_ROTATION_WINDOW` 10→15 — 같은 템플릿 스타일 재사용 간격 확대 |

### Phase 2 — Keyword Pool 30% Increase (`01a7a5628`)
| Blog | Before | After | Change |
|------|--------|-------|--------|
| laptop-hugo | 169 | 242 | +43% |
| appliance-hugo | 217 | 355 | +64% |
| interior-hugo | 189 | 315 | +67% |
| baby-hugo | 186 | 271 | +46% |
| fitness-hugo | 171 | 250 | +46% |
| health-hugo | 189 | 303 | +60% |
| pet-hugo | 344 | 526 | +53% |
| kitchen-hugo | 139 | 220 | +58% |
| beauty-hugo | 174 | 317 | +82% |
| camping-hugo | 145 | 250 | +72% |
| **Total** | **1923** | **3049** | **+59%** |

### Phase 3 — Similar_title Fallback Retry (`bbcad11a8`)
| Change | File | Detail |
|--------|------|--------|
| `similar_title` fallback | `pipelines/curation/pipeline.py` | 유사 제목 감지 시 즉시 실패하지 않고 최대 3회 다른 키워드로 재시도 (기존 `insufficient_products` 패턴과 동일한 fallback 로직) |

### 부수 작업
- `md-editor-nicegui/core/publish/thumbnail_gen.py`: `GRADIENTS`와 `DOMAIN`에 `issue`/`biz` 엔트리 추가

---

## Remaining Work

### Monitoring (24h)
- interior-hugo `similar_title` 재발 여부 확인
- Phase 1~3 조합 효과 검증 (keyword quarantine 누적 완화)

## Decisions

1. **Phase 순서**: Title 다양화(Phase 1) → 키워드 풀 확장(Phase 2) → fallback retry(Phase 3) 순으로 progressive rollout
2. **키워드 풀 59% 증원**: 목표 30%를 초과한 59% 증원. 일부 블로그(beauty 82%, camping 72%)는 과도하나 키워드 부족으로 인한 `no_content` 방지에 유리
3. **Fallback retry 로직**: 기존 `insufficient_products`의 fallback 패턴 재사용. quarantine 키워드는 제외하여 `similar_title` 누적 방지
4. **SequenceMatcher 임계값 80%→85%**: 완화 폭을 5%p로 제한하여 오탐지는 줄이면서 실제 중복은 여전히 차단

## Blockers

None.

## Resume Instructions

### Pipeline test (interior-hugo)
```bash
python dispatcher.py interior-hugo
```

### Check similar_title counts
```bash
python3 -c "
import sqlite3
db = sqlite3.connect('data/curation.db')
rows = db.execute('SELECT keyword, error, published_at FROM publish_log WHERE blog_id=? AND error IS NOT NULL ORDER BY published_at DESC LIMIT 10', ('interior-hugo',)).fetchall()
for r in rows:
    print(f'{r[2]} | {r[0]} | {r[1]}')
"
```

### Verify keyword counts
```bash
python3 -c "
import re
with open('pipelines/curation/keywords.py') as f:
    c = f.read()
for m in re.findall(r'\"(\w+-hugo)\":\s*\[(.*?)\]', c, re.DOTALL):
    kws = re.findall(r'\"([^\"]+)\"', m[1])
    print(f'{m[0]}: {len(kws)}')
"
```

### Related plan doc
- `.planning/triage/20260711--kuta-hugo-broken-thumbnail.md` (previous session)  
- `.omo/plans/curation-title-diversity.md` (this session — was created then Phase 1~3 executed)

---

## Uncommitted Changes
```
 M .DS_Store
 M AGENTS.md
 M .omo/run-continuation/ses_*.json
?? .omo/run-continuation/ses_*.json
?? .coverage
?? .planning/*.md
?? .playwright-mcp/
?? PLAN-thumbnail-generator.md
?? docs/branches/
?? scripts/batch_thumbnails.py
?? scripts/fix_remaining_posts.py
?? scripts/resume_backfill.py
?? shared/thumbnail_generator/
?? *.png
?? *.py
```

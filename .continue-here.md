# Continue Here — 2026-09-03 세션 핸드오프

> 생성: 2026-09-03 07:47 (Phase 77 W1-3 + P25 최종 가드 push/restart 직후)

## Current Position

**Phase 77 — Analytics 기반 셀프 개선 루프** 계획 실행 중. `.planning/phases/phase-77-analytics-self-improvement/` (CONTEXT.md, PLAN.md, RUNBOOK-enable.md).

- Wave 1 (신호 추출기) — 완료 `d97e9b868` : `shared/performance_signals.py` + `scripts/collect_analytics.sh` hook + `data/keyword_performance.json` 생성
- Wave 2 (선택 개입) — 완료 `0d730ce7e` : `pipelines/curation/pipeline.py:_select_keyword` perf-signal boost env-gated OFF
- Wave 3 (활성화 runbook) — 완료 `74f2dc45a` : `RUNBOOK-enable.md` 관찰→반영 전환 절차
- Phase 64 parallel Wave 3 진행 중 (64-06 완료 1ea5125ff, 64-07 대기)
- Phase 76 recurrence-prevention 완료 유지 — 02:00 expander 가드 관찰 대기 중

**직전 세션(9/2 밤) 별도 hotfix — tour-hugo P25 900s:**
- `01f3c860e` content_integrity S01/S02 O(N²) 제거 (corpus cap 200 + sampling 50)
- `6e8937b87` wrangler 120→300s + image_fetcher 40/60s deadline
- 둘 다 push 완료, scheduler/dashboard 재시작 완료 (PID 16324/16332)

## Completed This Session

### Phase 77 — Analytics Self-Improvement
1. **W1** `d97e9b868` — performance_signals extractor, analytics hook, observation mode. `shared/performance_signals.py` 신규, `collect_analytics.sh` 말미 호출, `data/keyword_performance.json` 생성. Verify: JSON에 유지비/드라이브 패턴 ≥3건.
2. **W2** `0d730ce7e` — keyword selection perf-signal boost. `PERF_SIGNALS=1` 시 매칭 후보 우선, 기본 OFF passthrough, 손상 JSON passthrough. `tests/test_performance_signals.py` 3케이스 통과.
3. **W3** `74f2dc45a` — RUNBOOK enable 절차 문서화 (전환 기준: 14회 연속 성공 + 패턴 안정 + green, 되돌림 `PERF_SIGNALS=0`).

### P25 최종 가드 (9/2 23:47 → 9/3 07:46)
4. **content_integrity fix** `01f3c860e` — `_extract_corpus` cap 200, `check_s01/s02` corpus 1회 구축 + _S_POST_SAMPLE 50 → 91k TF-IDF → 50회.
5. **wrangler/image guard** `6e8937b87` — dispatcher 2곳 120→300s, image_fetcher cover 40s/body 60s deadline, compile ok.
6. **push + restart** — `6e8937b87`/`aac2ba7a6` push, `launchctl kickstart -k` scheduler 30798→16324 dashboard 58637→16332, `scheduler.log` Registered 383 jobs / Next 07:50 / HealthCheck R2 ok.

### 문서·로그
- worklog: `.planning/worklog/WL-20260902-five-alert-followup-fleet-keyword-purge.md` +42줄 (P25진짜원인 분석 포함)
- destructive: `logs/destructive_2026-09-02.log` 5줄 (scheduler/dashboard 재시작 2건 + frederiksted revert + patch)
- handoff: `.continue-here.md` 갱신 (이 파일)

## Remaining Work

1. **[9/3 02:00] expander 첫 야간 실행 관찰** — Phase 76 이중 방어 검증. `grep -A2 "02:0" logs/scheduler.log | grep -iE "expander|keyword"` + `python3 -c "from pipelines.curation.keywords import get_keywords; print([k for k in get_keywords('car-hugo') if k in ('가능','가방','노트북')])"` → `[]` 기대.
2. **tour-hugo P25 재발 관찰** — 07:50 스케줄 + 다음 6시간 내 `grep "tour-hugo" logs/scheduler.log | grep -E "900|timeout|P25"` 0건 기대. wrangler/image 가드 효과 확인.
3. **Phase 77 검증 잔여** — Goal-backward 5개 기준 중 (1) 패턴 ≥3건 (완료), (2) PERF_SIGNALS=1 우선 (완료), (3) OFF passthrough (완료), (4) curation 회귀 0건 `OPS_TEST_MODE=1 pytest tests/curation -q` → 19 failed baseline 대조, (5) 6h collect_analytics 추출기 동작 로그 증거 — (4)(5) 문서화/캡처 필요. 필요 시 `python3 -m shared.performance_signals` 수동 1회 재실행 + `collect_analytics.sh` dry-run.
4. **Phase 77 enable 전환** — W1-3 완료 후 관찰 1~2주, RUNBOOK 기준 충족 시 기본 OFF→ON 1줄 변경. 지금은 관찰 모드 유지 (논커밋).
5. **SSOT 마이그레이션 (M1~M6)** — `phase-76-recurrence-prevention/SSOT-DESIGN.md` 별도 phase, 파괴적 4단계 프로토콜 대상.
6. **기타 열림:** best-* insufficient_products, escape_topics 8개, 19 pre-existing test fails — 별도 triage.

## Decisions

- **Phase 77 additive non-destructive + env 킬스위치 기본 OFF** — `PERF_SIGNALS` 미설정/파일부재/손상 모두 passthrough. 롤백=환경변수 0.
- **P25 다중 가드 채택** — content_integrity O(N²) 제거(근본) + wrangler 120→300(대형 ETAP) + image deadline 40/60(작은도시 worst-case) — 단일 가드로는 900s 재발 가능.
- **keywords.py bare 재추가 revert** — 9/2 night `pipelines/curation/keywords.py`에 bare 토큰 11개가 콤마 누락 fused string으로 추가됨(compile은 되나 "블랙박스실내블랙박스.." 합침). `git checkout HEAD -- keywords.py` 로 원복 — purge 방향과 반대라 의도치 않은 재오염으로 판단.
- **push는 형상관리, 배포는 wrangler 직접** — Cloudflare Pages git 자동빌드 Skipped 정상. dispatcher가 CLOUDFLARE_API_TOKEN 제거 담당.
- **커밋 분리** — 6e8937b(fix) + aac2ba7(log) 분리. amend 금지.

## Blockers

없음. 전부 완료 or 관찰 대기.

## Bugs & Issues Discovered

1. **content_integrity S01/S02 O(N²) hang** — 증상: tour-hugo 배포 성공 후 `_run_post_publish_checks`에서 900s kill. 원인: `check_s01` 루프 내 `_extract_corpus()` 매 포스트 재호출 → 303×302 TF-IDF. 해결: cap 200 + 1회 구축 + sample 50 (01f3c860e). 미해결: 없음 — ETAP corpus 대형화 구조적, 향후 모니터링.
2. **dispatcher wrangler timeout 120 부족** — 증상: ETAP 103M Pages `BUILD_OK` 후 deploy 121s 근접 + retry 시 900 초과. 원인: `_build_and_deploy_central` 고정 120. 해결: 300으로 상향 2곳 (6e8937b). 미해결: 없음 — 나머지 35 ETAP 동일 상향 완료.
3. **image_fetcher 무제한 fallback** — 증상: 작은도시 5 queries ×10s =100s 누적 → pipeline_timeout 초과 가능. 원인: per-query timeout만 있고 overall deadline 없음. 해결: cover 40s/body 60s (6e8937b). 미해결: 없음 — 캡 초과 시 warning 로그.
4. **keywords.py fused string 오염** — 증상: `git diff`에서 "블랙박스" 등 11개 bare가 콤마 없이 추가 → `"블랙박스""실내블랙박스..."` 합침. 원인: expander/datalab 재주입 또는 수동 편집 중 콤마 누락. 해결: checkout revert. 미해결: 없음 — 02:00 관찰로 재발 감시.
5. **problems.yaml SSOT 오버라이드 gotcha** — 증상: registry 문구 코드만 수정 시 미반영. 원인: `apply_problem_yaml` YAML 우선. 해결: 양쪽 동시 수정 (Phase 76). 미해결: 없음 — 문서화됨.
6. **best-* insufficient_products** — 증상: baby-hugo 등 P14. 원인: Coupang <3개. 해결: 안 함 — 별개. 미해결: 열림.

## Environment Notes

- **scheduler PID 16324** (07:46 재시작, 383 jobs, Next 07:50, HealthCheck senior/stock R2 ok) — 6e8937b dispatcher/image_fetcher 로드됨.
- **dashboard PID 16332** (`-m ops_dashboard.app`) — /api/registry 반영 M?
- **.env** 필요: `set -a; source .env` 후 `OPS_USER/OPS_PASSWORD` (dashboard curl -u). `CLOUDFLARE_API_TOKEN`은 wrangler 호출 시 제거 — dispatcher가 처리.
- **테스트:** `OPS_TEST_MODE=1 pytest tests/curation -q` baseline 19 failed / 129 passed (pre-existing). Phase 77 무관 회귀 0 기준.
- **Hugo:** `HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes hugo --gc --minify` — wrangler deploy는 dispatcher 경유.
- **git push 확인:** `To github.com:hugh79757-cmyk/5000.git main -> main` Skipped = 정상.

## Resume Instructions

```bash
# 1. 상태 확인
git log --oneline -5
launchctl list | grep com.5000
ps -eo pid,etime,command | grep -E "scheduler|ops_dashboard" | grep -v grep

# 2. P25/expander 관찰 (핵심)
grep "tour-hugo" logs/scheduler.log | tail -20
grep -E "expander|keyword" logs/scheduler.log | grep "02:0" | tail -10
python3 -c "from pipelines.curation.keywords import get_keywords; print([k for k in get_keywords('car-hugo') if k in ('가능','가방','노트북')])"
# [] = 성공

# 3. Phase 77 검증
python3 -m shared.performance_signals && cat data/keyword_performance.json | head -n 50
PERF_SIGNALS=1 python3 -m pytest tests/test_performance_signals.py -v
OPS_TEST_MODE=1 pytest tests/curation -q  # 19/129 baseline 대조
bash scripts/collect_analytics.sh 2>&1 | tail -20  # 추출기 호출 확인

# 4. 대시보드
curl -s -u "$OPS_USER:$OPS_PASSWORD" http://localhost:5060/api/attention | head -50
# 또는 ops_dashboard 포트 확인: ps aux | grep ops_dashboard

# 5. 다음 phase 시작 시
# .planning/phases/phase-77-analytics-self-improvement/PLAN.md 읽고 RUNBOOK-enable.md 절차
# SSOT 마이그레이션 시: .planning/phases/phase-76-recurrence-prevention/SSOT-DESIGN.md + 파괴적 4단계 프로토콜
```

## Uncommitted Changes

- `M data/analytics_status.json` — 런타임 자동 갱신 (collect_analytics 6h 주기 산출물 — 무시, 커밋 대상 아님, .gitignore 대상 아님이라 매번 dirty)
- `?? data/.lock_best-*-hugo` 16개 — 런타임 락
- `?? ops_dashboard/logs/` + `ops_dashboard/ops.db-shm/-wal` — SQLite WAL
- `?? output/image_cache/` + `output/images/*` — 이미지 캐시
- 전부 런타임 산출물, 커밋 대상 아님. `git add` 시 파일 명시 (add -A 금지 — dirty 산출물 딸려옴).

**커밋 정책:** worklog/destructive log는 추적 대상 — 핸드오프에 포함. analytics_status.json/락/WAL/이미지 캐시는 제외 (기존 관례).

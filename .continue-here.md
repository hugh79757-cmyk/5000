# Continue Here — Phase 28-29 완료 후

**Last updated:** 2026-07-22 10:00 KST
**Session:** Phase 28 (CUAP Worker 404→500 Fix) + Phase 29 (CUAP 콘텐츠 오염 + 퍼널 카드 404 + 광고 공백 수정)

---

## Current Position

- **Phase 28:** ✅ 완료 (2026-07-22)
- **Phase 29:** ✅ 완료 (2026-07-22)
- **다음 단계:** Phase 30 또는 남은 uncommitted 변경사항 정리

---

## Completed This Session

### Phase 28: CUAP Worker 404→500 Fix
1. **28-01:** 6개 Worker 블로그 `src/index.js`를 canonical 버전으로 통일
   - `md5: 5667ff889e7f951b5c4f98a94293a6b2` (전부 동일)
   - kitchen-hugo의 268-byte 버그본 → 142-byte 정상본으로 교체
2. **28-02:** 6개 Worker 블로그 재배포 (`deploy_site()` 경유)
   - beauty-hugo는 figure shortcode 따옴표 이슈 수정 후 배포 성공
3. **28-03:** 라이브 검증
   - 6/6 missing path → 404 ✅
   - 6/6 real post → 200 ✅

### Phase 29: CUAP 콘텐츠 오염 + 퍼널 카드 404 + 광고 공백 수정
1. **29-01:** `keywords.py` pet-hugo 키워드 정리
   - `"관리"` 범용 키워드 제거 (뷰티/홈케어 매칭 방지)
   - 콤마 문법 오류 8건 수정
2. **29-02:** `cuap_entities` DB URL 정리
   - 27건 오염 URL 처리 (4건 삭제 + 23건 slug 업데이트)
   - `20260721-` 접두사 URL이 실제 slug와 불일치 문제 해결
3. **29-03:** beauty-hugo 재배포
   - 라이브에서 404였던 포스트들이 이제 200 반환
4. **29-04:** 광고 partial 검증
   - `ca-pub-6677996696534146` + ad-slot `2195212287` 정상 로드 확인

---

## Remaining Work

### 즉시 처리 필요
1. **Uncommitted 변경사항 정리** — 10개 파일 수정됨
2. **Phase 25 테스트 데이터 정리** — cuap_entities DB에 Phase 25 테스트 데이터 잔존 가능

### 향후 작업
1. **Phase 30 계획** — 다음 페이즈 번호로 배정
2. **기존 발행 글 수정** — Phase 29에서 발견된 오염 글 (pet-hugo의 홈케어 글) 필요시 삭제
3. **광고 공백 추가 검증** — 실제 브라우저에서 광고 로드 확인 필요

---

## Decisions

1. **Pet-hugo 키워드 "관리" 제거:** 너무 범용적인 키워드가 뷰티/홈케어 제품과 매칭되어 품질 게이트를 통과함 → 제거로 해결
2. **cuap_entities URL 정리 방식:** 파일시스템 slug와 대조하여 업데이트 또는 삭제 (테스트 데이터 완전 제거)
3. **beauty-hugo 배포:** `deploy_site()` 함수가 `[assets]`가 있는 wrangler.toml을 Workers로 판단하나, beauty-hugo는 WORKERS_BLOGS에 포함되어 있어 정상 동작

---

## Bugs & Issues Discovered

### 1. Pet-hugo 뷰티 글 발행
- **증상:** pet-hugo에 "ERANSTAR vs 로즈온 — 홈케어 관리 실속 선택" 라는 뷰티/홈케어 글이 발행됨
- **원인:** `keywords.py`의 pet-hugo 키워드 목록에 `"관리"`가 포함되어 있어 "홈케어 관리"와 매칭
- **해결책:** `"관리"` 키워드 제거 + 콤마 문법 오류 8건 수정
- **미해결:** ❌ 기존 발행된 오염 글은 삭제하지 않음 (다음 세션에서 처리 필요)

### 2. cuap_entities URL 불일치 (퍼널 크로스셀 404)
- **증상:** `https://appliance.informationhot.kr/posts/20260721-여름철-제습기-추천/` → 404
- **원인:** Phase 25 `register_cuap_entity()`가 테스트 데이터(가상 slug)를 DB에 저장 → 실제 slug와 불일치
- **해결책:** 27건 오염 URL 정리 (4건 삭제 + 23건 실제 slug로 업데이트)
- **미해결:** ✅ 완전 해결

### 3. beauty-hugo 미배포 (광고 공백)
- **증상:** `https://beauty.informationhot.kr/posts/...` → 404, 광고 미로드
- **원인:** beauty-hugo가 로컬에만 존재하고 배포되지 않음
- **해결책:** `deploy_site()`로 재배포 → 라이브 200 확인
- **미해결:** ✅ 완전 해결

### 4. figure shortcode 따옴표 이슈 (beauty-hugo 빌드 실패)
- **증상:** Hugo 빌드 중 `"got positional parameter '24시간'. Cannot mix named and positional parameters"` 에러
- **원인:** figure shortcode 캡션 내 따옴표가 Hugo 파서와 충돌
- **해결책:** 캡션에서 따옴표 제거/이스케이프
- **미해결:** ✅ 완전 해결

---

## Environment Notes

- **Wrangler 인증:** `CLOUDFLARE_API_TOKEN` env var가 설정되면 wrangler OAuth profile이 무시됨 → `deploy_site()` 함수가 자동 제거
- **Hugo 버전:** `v0.160.1+extended+withdeploy`
- **Worker 블로그:** health, pet, kitchen, beauty, camping, baby (6개)
- **Pages 블로그:** appliance, interior, laptop 등
- **AdSense:** CUAP = `ca-pub-6677996696534146` (informationhot.kr)

---

## Resume Instructions

```bash
# 1. 변경사항 확인
git status

# 2. uncommitted 변경사항 정리 (선택)
git add -A && git commit -m "wip: phase 28-29 complete + cuap content fixes"

# 3. 다음 페이즈 계획 (선택)
# /gsd-plan-phase 30

# 4. 또는 기존 오염 글 정리 (선택)
# pet-hugo에서 홈케어 글 삭제 필요시
```

---

## Uncommitted Changes

```
M .planning/ROADMAP.md           (Phase 28-29 완료 기록)
M .planning/STATE.md             (Phase 28-29 상태 업데이트)
M config/blogs.d/cuap.yaml       (CUAP 설정 변경)
M data/cooldown.json             (cooldown 업데이트)
M data/failure_count.json        (실패 카운트 업데이트)
M docs/agent.md                  (에이전트 문서)
M pipelines/curation/keywords.py (pet-hugo 키워드 정리)
M pipelines/curation/writer.py   (writer 개선)
M prompts/common/rules.md        (프롬프트 규칙)
M shared/cuap_entity_linker.py   (엔티티 링커 개선)
```

# Continue Here — Wrangler Auth Profile + AdSense Fix + Full Profile Binding

**Created:** 2026-07-14 12:42 KST

## Current Position

- **Phase:** 운영 안정화 (v1.1 완료, post-phases triage)
- **Context:** wrangler 4.x `CLOUDFLARE_API_TOKEN` env var가 OAuth auth profile보다 우선 적용되어 배포 실패
- **Status:** 코드 수정 완료 + 전체 프로필 바인딩 완료 + verify 완료

---

## Completed This Session

### 1. Root Cause Fix — wrangler deployment auth failures

**Modified files:**
| File | Change |
|------|--------|
| `dispatcher.py` | `_build_and_deploy_central()` — `deploy_env` 필터 추가: `CLOUDFLARE_API_TOKEN` 제거, `/opt/homebrew/bin` PATH 우선 |
| `shared/publishers/deploy.py` | `_deploy_site_inner()` — `_wrangler_env.pop("CLOUDFLARE_API_TOKEN", None)`, `_cf_token` 로직 제거 |
| `money-aikorea24/build_deploy.py` | `npx wrangler` → `/opt/homebrew/bin/wrangler` + `env.pop("CLOUDFLARE_API_TOKEN", None)` |

### 2. AdSense `push({})` 누락 수정

10개 partials in 4 sites — 각 `<ins class="adsbygoogle">` 뒤에 `<script>(adsbygoogle = window.adsbygoogle || []).push({});</script>` 추가:

| Site | Files |
|------|-------|
| `biz.techpawz-hugo` | in-article.html, leaderboard.html, mobile-sticky.html |
| `issue-techpawz-hugo` | in-article.html, leaderboard.html, mobile-sticky.html |
| `kuta-hugo` | in-article.html, leaderboard.html, mobile-sticky.html |
| `rotcha-blog` | mobile-sticky.html |

### 3. Wrangler Profile Binding — Full Coverage

**hugh79757** (31 directories, hugh79757@gmail.com):
- Core: aikorea24, 5000, cuap, ETAP, TAP, STAP, cap, RAP, SEAP, SAP
- Hugo blogs: biz.techpawz-hugo, info.techpawz-hugo, issue-techpawz-hugo, informationhot-hugo, 5.informationhot-hugo, rotcha-blog, techpawz-hugo, kuta-hugo, funstaurant-hugo
- Workers/Others: money-aikorea24, certkorea, mk, rss-proxy, finnews, ta, aikeep24-lite, blogdex, bokji65, dailypain, ods-project, teyvat-gg, weather

**yuiying167** (2 directories, yuiying167@gmail.com):
- bazi-spattra, lao-lottery-hub

**farmsolution** (1 directory):
- farmsolution

### 4. 배포 검증 완료

- `biz.techpawz.com`, `issue.techpawz.com` → hugh79757 profile, production 성공
- deploy.log: wrangler 4.110.0 + "Active profile: hugh79757" 확인
- `wrangler auth list`: 전체 3개 프로필 / 34개 디렉토리 바인딩 확인
- publish_ledger 잔여 실패: 모두 **컨텐츠 생성 문제** (no_result, no_content, low_relevance)

### 5. Incident Documentation

- `AGENTS.md`: 브랜치 구조(한국어), Cloudflare Auth Profile 섹션, Incident Analysis 업데이트
- `.planning/triage/20260714--wrangler-auth-profile-5000.md`: 상세 triage 문서 작성

---

## Remaining Work

1. **informationhot-hugo** — 커버 이미지 깨짐 분석 (`.planning/.continue-here.informationhot-hugo.md` 참고, 별도 작업)
2. **content generation failures** — sector/etf/pick-hugo `no_content`, travel-hugo `no_result` 등은 컨텐츠 문제로 이번 세션 범위 외

---

## Decisions Made This Session

1. **env var stripping in deploy scripts** — 근본 원인 해결책. wrangler subprocess에 `CLOUDFLARE_API_TOKEN`을 전달하지 않음
2. **/opt/homebrew/bin/wrangler 고정** — `npx wrangler` (cached 4.92.0)는 profile binding 미지원. homebrew 4.110.0이 정확
3. **프로필 바인딩은 예비 조치** — env var stripping이 근본 해결책. profile binding은 추가 안전장치
4. **Hugo subdirectory resolve** — wrangler가 parent directory binding을 상속함 (`RAP/rap-hugo` → `RAP` binding)

---

## Blockers

- **없음** — 이번 세션 작업은 모두 완료 및 검증됨

---

## Resume Instructions

```bash
# 1. 5000 current state 확인
cd /Users/twinssn/Projects/5000
git status
git log --oneline -5

# 2. 배포 동작 확인 (실제 파이프라인 실행)
python dispatcher.py rap-hugo  # 테스트 배포

# 3. profile binding 상태 확인
env -u CLOUDFLARE_API_TOKEN -u CLOUDFLARE_ACCOUNT_ID wrangler auth list

# 4. 정보탭 썸네일 문제 계속하려면
# .planning/.continue-here.informationhot-hugo.md 참고

# 5. 잔여 컨텐츠 생성 문제 (no_content 등):
# pipelines/sector/pipeline.py, pipelines/travel/pipeline.py 분석
```

---

## Uncommitted Changes (5000 repo)

### Modified (important):
```
M dispatcher.py                  # CLOUDFLARE_API_TOKEN env filter
M shared/publishers/deploy.py   # CLOUDFLARE_API_TOKEN pop + _cf_token 제거
M AGENTS.md                     # 브랜치 구조 + incident docs
M config/models.yaml            # deepseek-chat → deepseek-v4-flash
M pipelines/curation/keywords.py
M data/cooldown.json
M data/failure_count.json
M .planning/triage/INDEX.md
```

### Untracked (new files):
```
.planning/triage/20260714--wrangler-auth-profile-5000.md
.planning/triage/20260713--deepseek-chat-to-deepseek-v4-flash.md
scripts/batch_thumbnails.py, scripts/fix_remaining_posts.py, scripts/resume_backfill.py
shared/thumbnail_generator/
```

### Other repos with changes:
- `money-aikorea24/` — build_deploy.py (wrangler path + env fix)
- `biz.techpawz-hugo/` — 3 adsense partials fixed
- `issue-techpawz-hugo/` — 3 adsense partials fixed
- `kuta-hugo/` — 3 adsense partials fixed
- `rotcha-blog/` — 1 adsense partial fixed

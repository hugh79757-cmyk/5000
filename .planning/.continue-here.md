# Continue Here — Phase 31 rotcha-adsense-standard 완료

**Created:** 2026-07-22

## Current Position

- **Phase 31**: `phase-31-rotcha-adsense-standard` 완료
- **다음 Phase**: 32 (아직 플래닝 안 됨)
- **gsd-phase command**: 실행 불가 — workflow 파일 4개 존재하지 않음

---

## Completed This Session

### Phase 31 — rotcha-adsense-standard (6개 블로그 AdSense 표준화)

#### Wave 1: Discovery
- 6개 블로그 (travel-hugo, travel1~4-hugo, compare-hugo) AdSense 설정 상태 분석
- 공통 문제 발견: `.ad-top` CSS 규칙 없음, slot ID `2195212287` 중복 사용

#### Prep Fixes
- **travel4-hugo**: `layouts/partials/extend-head.html` 신규 생성 (adsbygoogle.js lazy-load)
- **travel1~4-hugo**: `assets/css/custom.css`에 `.ad-top` CSS 규칙 추가
- **travel2-hugo**: `extend_head.html` (underscore, 이전 GA4) 제거 → `extend-head.html` (hyphen, 최신 GA4) 유지

#### Wave 3: 배포
- 6개 블로그 동시 배포 성공:
  - travel-hugo: https://7e3b8d89.travel-hugo.pages.dev
  - travel1-hugo: https://e66b219c.travel1-hugo.pages.dev
  - travel2-hugo: https://5c956569.travel2-hugo.pages.dev
  - travel3-hugo: https://d78d0403.travel3-hugo.pages.dev
  - travel4-hugo: https://743bb9b2.travel4-hugo.pages.dev
  - compare-hugo: https://d2fd20fb.compare-hugo.pages.dev

#### Wave 5: Verification
- 6/6 HTTP 200
- 6/6 adsbygoogle.js 로드됨 (ca-pub-8772455780561463 — 올바른 rotcha.kr ID)
- 6/6 showTableOfContents = false

---

## Remaining Work

### Phase 32 대상 (ROADMAP.md 기준)
1. **hotissue-hugo** — PaperMod 테마용 AdSense 표준화 (PaperMod 특화 규칙)
2. **techpawz 계열** — finance, dividend, etf, sector, ipo (techpawz.com, Blowfish)
3. **informationhot-hugo, techpawz-hugo, biz/issue-techpawz-hugo** — Backup 4개 블로그

### Infrastructure 문제
- **gsd-phase command** — workflow 파일 4개 부재로 실행 불가
  - `add-phase.md`, `insert-phase.md`, `remove-phase.md`, `edit-phase.md` 존재하지 않음
  - `/Users/twinssn/.config/opencode/get-shit-done/workflows/`에 생성 필요

---

## Decisions

1. **슬롯 ID**: 기존 `2195212287` 유지 (신규 발급 스킵) — 모든 블로그에서 동일 ID 중복 사용 중
2. **Auto ads**: travel-hugo 계열은 explicit ad slot 없이 Auto ads만 사용 (compare-hugo만 explicit slot)
3. **double adsbygoogle 로드**: travel2-hugo에서 underscore/underscore 두 파일 발견 → hyphen 버전 유지, underscore 제거
4. **Wave 2 스킵**: AdSense 슬롯 발급은 사용자 수동 작업으로 스킵

---

## Bugs & Issues Discovered

### 1. gsd-phase workflow 파일 부재
- **증상**: `gsd-phase 32` 실행 시 `add-phase.md` 등 4개 workflow 파일 로드 실패
- **원인**: `/Users/twinssn/.config/opencode/get-shit-done/workflows/`에 파일 미생성
- **해결책**: workflow 파일 4개 생성 필요 (또는 `gsd-plan-phase` command 직접 사용)
- **미해결 여부**: ✗ 미해결 — Phase 32 플래닝 전 생성 필요

### 2. travel4-hugo adsbygoogle 로드 경로 불확실
- **증상**: Discovery 시 `extend-head.html` 파일 없음으로 adsbygoogle.js 로딩 확인 불가
- **원인**: 이전에 파일 존재 안 했음
- **해결책**: Phase 31 Prep에서 `extend-head.html` 생성 완료
- **미해결 여부**: ✓ 해결 완료

### 3. 모든 travel 블로그 slot ID `2195212287` 중복
- **증상**: leaderboard + in-article 모두同一 ID 사용
- **원인**: Phase 30 정보hot 계열과 동일 ID를 rotcha 계열에서도 재사용
- **해결책**: 기존 ID 그대로 유지 (사용자가 슬롯 발급 안 함)
- **미해결 여부**: ⚠️ 잠재적 문제 — AdSense 슬롯 충돌 가능성

---

## Environment Notes

- **wrangler deployment**: 모든 배포 `wrangler pages deploy`로 성공
- **CLOUDFLARE_API_TOKEN**: OAuth profile `hugh79757` 사용 (wrangler auth token 추출 방식)
- **wrangler pages deployment list**: CLOUDFLARE_API_TOKEN env var가 설정된 상태에서 인증 오류 10000 발생 — 배포 자체는 성공 확인됨
- **Hugo 빌드**: 모든 블로그 `hugo --gc --minify` 성공
- **테마**: 6개 블로그 전부 Blowfish (PaperMod hotissue-hugo는 Phase 32에서 별도 처리)

---

## Resume Instructions

```bash
# Phase 32 플래닝 시작 (gsd-phase 워크플로우 복구 전까지 직접)
cd /Users/twinssn/Projects/5000

# 방법 1: gsd-plan-phase command 사용 (workflow 파일 부재 우회)
gsd-plan-phase 32

# 방법 2: 직접 phase-32 디렉토리 생성 후 PLAND.md 작성
mkdir -p .planning/phase-32
# → ROADMAP.md Phase 32 대상: hotissue-hugo, techpawz 계열

# Phase 31 배포 검증 (생략 가능)
curl -s -o /dev/null -w "%{http_code}" https://tour1.rotcha.kr/

# 기존 dirty 파일 커밋
git add -A && git commit -m "wip: Phase 31 complete — rotcha 6 blogs adsense standard"
```

---

## Uncommitted Changes

**이번 세션 산출물:**
- `.planning/phase-31-rotcha-adsense-standard/PLAND.md` (신규)
- Phase 31 배포 수정 파일들:
  - `travel4-hugo/layouts/partials/extend-head.html` (생성)
  - `travel1-hugo/assets/css/custom.css` (수정)
  - `travel2-hugo/assets/css/custom.css` (수정)
  - `travel3-hugo/assets/css/custom.css` (수정)
  - `travel4-hugo/assets/css/custom.css` (수정)
  - `travel2-hugo/layouts/partials/extend_head.html` (삭제)

**이전 세션 기존 dirty (커밋 비대상):**
- `AGENTS.md`, `data/cooldown.json`, `data/failure_count.json`
- `free-coding-models`, `.planning/triage/INDEX.md`
- `.planning/phase-30-cuap-adsense-standard/`, `ADSENSE-GUIDE.md`
- `.planning/triage/20260722--blowfish-adsense-setup-guide.md`
- `.planning/triage/20260722--hugo-ad-sense-settings-overhaul.md`
- `planning/` (디렉토리명 소문자 — 별도 프로젝트로 보임)
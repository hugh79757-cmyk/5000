# Continue Here — 현재 재개 지점 (2026-08-07 기준)

**Created:** 2026-08-07 (실체 동기화 세션)

## Current Position

- **Phase 58** Publish-problem Telegram alerting: 완료
  - `.planning/phase-58-publish-problem-telegram-alerting/PLAN.md`, `SUMMARY.md` 존재
  - 산출: `shared/problem_registry.py`, `shared/problem_detectors.py`, `shared/problem_monitor.py`
- **Phase 59** Ops dashboard & unification: 완료
  - `.planning/phase-59-ops-dashboard-and-unification/` 요약 다수 존재
  - 산출: `ops_dashboard/` 인프라(app.py, checks/*, db.py, readiness.py, investigate_publish.py 등)
- **Phase 60** Publish investigation & hardening: PLAN 있음, SUMMARY 미완
  - `.planning/phase-60-publish-investigation-and-hardening/PLAN.md` 존재
  - S1 태그 `pre-refactor-baseline` 존재, 조사 스크립트 `ops_dashboard/investigate_publish.py` 존재
  - Part 3~6 관련 커밋 2026-08-06에 존재, SUMMARY.md는 이번 세션에서 생성 예정
- **최근 커밋 기준점(HEAD)**: `741945827` (2026-08-07)
- **현재 재개 지점**: CUAP 복원·배포·발행 재개·INC-CL 재렌더까지 끝난 뒤, 다음 줄은 P1/P2/P3

### 기준 실체(코드·git·DB)

- `ops_dashboard/ops.db`: known_issues 57, open 25 / check_results 4364 / blog_lifecycle 85 / standard_rules 12
- `data/content.db`: publish_ledger total 33733, source='' 18324, source!='' 15409
- `idx_ledger_dedup`: UNIQUE 부분 인덱스(`WHERE source != ''`)
- Phase 60 Part 1 결론(실측 기반): CUAP는 실패 동반 정상 가동(중단 아님), senior 계열·travel4-hugo만 8/1 이후 success 0

---
## 이번 세션에서 확정해야 하는 것 (문서 상충 정리)

- PROGRESS-2026-08-07-session.md의 "STRUCT-08/09 resolved / 18→16건"과 "M01/M07 무력화 후속 큐"가 공존.
- ops.db known_issues 기준 STRUCT-08/09는 **둘 다 resolved**.
  - STRUCT-08: gsd_status=resolved, resolution_status=resolved
  - STRUCT-09: gsd_status=resolved, resolution_status=resolved
- 따라서 현행 실체와 맞는 서술로 통일:
  - "M01/M07 감지 자체는 콘텐츠DB 직접 연결로 작동 중"
  - "과거 오보고(무력/항상 pass)는 정정"
  - "stale fail(정오탐)은 개별 정리 대상"

---
## 완료 기록 (보관용: 이전 세션 Phase 31 rotcha-adsense-standard)

### Phase 31 — rotcha-adsense-standard (6개 블로그 AdSense 표준화)

#### Wave 1: Discovery
- 6개 블로그 (travel-hugo, travel1~4-hugo, compare-hugo) AdSense 설정 상태 분석
- 공통 문제 발견: `.ad-top` CSS 규칙 없음, slot ID `2195212287` 중복 사용

#### Prep Fixes
- **travel4-hugo**: `layouts/partials/extend-head.html` 신규 생성 (adsbygoogle.js lazy-load)
- **travel1~4-hugo**: `assets/css/custom.css`에 `.ad-top` CSS 규칙 추가
- **travel2-hugo**: `extend_head.html` (underscore, 이전 GA4) 제거 → `extend-head.html` (hyphen, 최신 GA4) 유지

#### Wave 3: 배포
- 6개 블로그 동시 배포 성공:
  - travel-hugo: https://7e3b8d89.travel-hugo.pages.dev
  - travel1-hugo: https://e66b219c.travel1-hugo.pages.dev
  - travel2-hugo: https://5c956569.travel2-hugo.pages.dev
  - travel3-hugo: https://d78d0403.travel3-hugo.pages.dev
  - travel4-hugo: https://743bb9b2.travel4-hugo.pages.dev
  - compare-hugo: https://d2fd20fb.compare-hugo.pages.dev

#### Wave 5: Verification
- 6/6 HTTP 200
- 6/6 adsbygoogle.js 로드됨 (ca-pub-8772455780561463 — 올바른 rotcha.kr ID)
- 6/6 showTableOfContents = false

---
## Remaining Work (현재 재개 후 실제 라인)

### P1 — 대시보드 신뢰성 복원 (M01/M07)
- M01/M07은 감지 무효가 아니라 콘텐츠DB 직접 연결형으로 작동 중
- 오보고("무력/항상 pass") 정정 완료, ops.db known_issues STRUCT-08/09 resolved
- 잔여: stale fail 정오탐 정리 + 크롤 검증 regex 관대화 검토

### P2 — 준비도 녹색화 (R04/R06 중심)
- R04(GA4 10건, STRUCT-17): AdSense 수익과 직접 무관 → 별도 이관
- R06(in-article data-ad-format auto 19건, STRUCT-16): 콘솔 슬롯 형식 미확인 → 수정 시 광고 깨질 위험 → deferred
- standard_compliance 실측은 ops_dashboard 체크 기준 재확인 필요

### P3 — 후속 분기 (CAP → SEAP → TAP)
- CAP 본문: curation 파이프라인 자체 → CUAP 절차 재사용 가능
- SEAP 썸네일: batch_thumbnails 절차 적용 가능하나 발행 경로/파이프라인 확인 선행
- TAP 전수: 여행 계열 파이프라인 차이 있음. CUAP식 스캔/정화 절차는 적용 가능하나 발행 플랫폼 차이(Hugo vs Blogger/WordPress)로 변형 필요

### P4 — 잔무
- stash 2건: 보류분 포함이라 아카이브 후 드롭 보류 (`data/stash-archive/` 참조)
- cover 죽은필드·og:image // 슬래시 버그 (코드 실체 확인 후 처리)
- 부모repo 미추적 5블로그(massage/homeappliance/golf/bike/car)

---
## CUAP 복원 완료 (실체 확인분)

- CUAP 후보 10+ 블로그: 2026-08-01 이후 전부 발행 시도 중 + 성공도 발생 → **중단이 아니라 실패 동반 정상 가동**
- 智能玩具 소멸: pet-hugo 최근 발행 제목에서 해당 문자열 미등장(콘텐츠DB 정정 반영)
- M04/Q1 관련: pet-hugo maintenance_checklist 상 M04=fail, Q1 이슈 존재 표기(가장 최신 행 기준). "통과" 표현은 DB 최신 행과 상충하므로 문서에서 단정 금지
- INC-CL: 스크립트 `scripts/render_inc_cl_fix.py` 존재, M03 관련 처리는 진행됨. 단, "74건" 표현은 유사제목 stage 합계(30건)와 불일치 → 출처 확정 필요

---
## content.db 복구 완료 (실체 확인분)

- 중복 정리 + partial UNIQUE 반영됨 (`idx_ledger_dedup` UNIQUE 부분 인덱스)
- source!='' 중복 그룹: 0
- source='' 행수: 18324 (문서 표기 "18,301 보존"과 불일치 → 현재 실체는 18,324)
- STRUCT-11: 실제 인덱스 상태는 UNIQUE 부분 인덱스 → documents상 "non-unique" 서술과 불일치, 현재 상태는 resolved 방향
- STRUCT-12/13: open 상태로 남아 있음(후속 정리 대상)

---
## Decisions

- **배포 원칙**: 모든 배포는 `dispatcher.py` 사용. 수동 `wrangler pages deploy`/`wrangler deploy` 직접 실행 금지.
- **CLOUDFLARE_API_TOKEN 경고**: env var가 wrangler OAuth profile보다 우선 적용됨 → 인증 오류 10000 발생 가능. 반드시 dispatcher 내부에서 제거하거나 OAuth profile 기준으로만 실행.
- **슬롯 ID**: 기존 `2195212287` 유지 — 정책 변경 전까지 재설정 금지.
- ** 스케줄러 운영**: 직전 결정 유지. CUAP만 재개 검토, 나머지는 정지. 이번 세션에서 기동 변경 없음.

---
## Bugs & Issues Discovered

### 1. STRUCT-08/09 상충 (문서 간)
- PROGRESS: "STRUCT-08/09 resolved / 18→16건"
- 다른 맥락: "M01/M07 무력화 후속 큐"
- 실체: 둘 다 resolved, 감지 자체는 콘텐츠DB 직접 연결형으로 작동 중.
- 문서 통일: "무력/항상 pass"는 오보고로 처리, "stale fail 정오탐 정리"로 이관.

### 2. senior 계열 및 travel4-hugo success 0건 (별도 이슈)
- senior-blogger / senior-hugo: 8/1 이후 success 0, publish_error 축 (50+50건 규모)
- travel4-hugo: 8/1 이후 success 0, no_result 축 (29건)
- CUAP 복원과 무관한 별도 이슈로 등록 필요.

### 3. "INC-CL 74건" 표현 불일치
- 유사제목 stage 합계(CUAP 후보 기준): 30건
- 스크립트/로그 카운터 기준 74건일 가능성 있음
- 출처 확정 전에는 "74건"을 공식 수치로 쓰지 말 것.

---
## Environment Notes

- **wrangler deployment**: `dispatcher.py` 경유가 원칙. Workers/Pages 자동 선택, CLOUDFLARE_API_TOKEN 제거, Hugo 빌드, 직렬화 락 처리.
- **CLOUDFLARE_API_TOKEN**: OAuth profile `hugh79757` 사용. env var 설정 시 인증 오류 10000 발생 가능 → dispatcher 내부에서 제거.
- **Hugo 빌드**: `hugo --gc --minify` 기준. 테마/블로그별 설정은 블로그 정의 파일 참조.
- **스케줄러**: 8/6 20:40 이후 중단(마지막 로그: laptop-hugo quota skip). startup 체크 통과, 재개는 안전 장치 확보 후.

---
## Resume Instructions

```bash
# 1) 현재 위치 확인
cd /Users/twinssn/Projects/5000
git log --oneline -1
python -c "import sqlite3; c=sqlite3.connect('ops_dashboard/ops.db'); print('ops.db OK', c.execute('SELECT COUNT(*) FROM known_issues').fetchone()[0], 'known_issues')"

# 2) P1 대시보드 신뢰성 확인 (체크 등록/실행 상태)
python -c "
import sys; sys.path.insert(0,'.')
from ops_dashboard.checks import CHECKS
print('registered checks:', sorted(CHECKS.keys()))
"

# 3) P1/P2 확인 후 재개 판단
# - M01/M07 무효 아님(콘텐츠DB 직접 연결)
# - R04/R06 보류/이관 상태 확인
# - senior 계열·travel4 성공 0건은 별도 이슈로 분리

# 4) 다음 액션 예
# - Phase 60 SUMMARY.md 생성
# - INDEX.md에 이번 세션 트라이애지 추가
# - PROGRESS-2026-08-07-session.md STRUCT-08/09 서술 통일
```

---
## Uncommitted Changes (2026-08-07 실체 동기화 시점)

**수정됨(수정 파일):**
- `STATE.md` (실체 동기화 갱신)
- `ops_dashboard/db.py` (SEED_ISSUES 내 STRUCT-08/09 증상 설명 정정 — 로직 변경 아님, diff 작음)
- `ops_dashboard/investigate_publish_result.json` (Part 1 조사 JSON 재실행 산물)
- `.planning/.continue-here.md` (이번 세션 전체 갱신)
- `shared/publishers/hugo_writer.py` (실체 확인 후 처리 필요)

**추적 안 됨(untracked):**
- `.planning/PROGRESS-2026-08-07-session.md`
- `.planning/QUEUE-next-sessions-2026-08-07.md`
- `.planning/stash-QUEUE.md`
- `scripts/render_inc_cl_fix.py`
- `pipelines/curation/keywords.ts`
- `data/content.db.EVIDENCE_polluted_20260806`
- `data/content.db.clean_before_recovery_20260806_204047`
- `data/content.db.r2_20260806_0500`
- `data/ledger_title_fix_backup.jsonl`
- `data/stash-archive/`

**처리 방침:**
- 신규 산출물(.planning/*.md, scripts/render_inc_cl_fix.py, pipelines/curation/keywords.ts)은 커밋 대상.
- DB 증거/백업 파일(content.db.EVIDENCE/clean/r2, ledger_title_fix_backup.jsonl, stash-archive)은 운영 정책상 git 추적 대상으로 보지 않음(위치·의미만 STATE/노트에서 관리).

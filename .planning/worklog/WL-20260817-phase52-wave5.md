# WL-20260817-phase52-wave5 — Blowfish 표준화 Wave 5 (leaderboard 제거 + top 통합)

## 날짜 / 담당
- 2026-08-17, orchestrator 직접 실행 (ponytail 단순 기계 변경)
- 관련: Phase 52 Wave 5 (`52-blowfish-standardization`), 표준 지침서 v1.3

## 작업 범위 (사용자 확정 m0038: All 30 blogs)
원계획 36-target 리스트는 부실했음. 실제 `leaderboard.html` 보유 블로그 = 30개.
사용자 승인대로 **전체 30개** 처리.

## 수행 내역 (4단계 프로토콜 적용)
1. **사전카운트**: 삭제 30 / leaderboard→top 스위치 12 / CUAP top.html 키 수정 6 / 생성 1(info-techpawz 시도 → 차단)
2. **백업**: `/tmp/phase52_backup/<relpath>` 에 30개 leaderboard.html + caller 12개 single.html 원본 보관
3. **실행**:
   - 30개 `leaderboard.html` 삭제 (find 기반, 복구용 백업 선행)
   - 12개 caller `adsense/leaderboard.html` → `adsense/top.html` 스위치
     (9 SAP + biz.techpawz + CAP/compare + kuta; SAP는 `$lbSlot` 인자 무시하고 site.Params 사용)
   - 6 CUAP(bike/car/golf/homeappliance/massage/camping) `top.html` + `config/_default/params.toml`
     의 `leaderboardSlot` → `topSlot` (값 2195212287 보존, 표준 §8 금기#11 준수, 렌더 슬롯 불변)
   - 2개 RAP `.bak_layouts_20260810` 오염본 발견 → 백업에서 복원 (건드리지 않음)
4. **사후대조**: leaderboard.html 잔여 0 / single.html의 leaderboard 호출 0 / 30개 중 29개 single.html이 top 호출

## 커밋
- 24 커밋 성공, 6 NO-CHANGE (5 CUAP는 이전 wave가 이미 커밋 = diff HEAD 공백 확인;
  5.informationhot은 leaderboard.html가 untracked라 삭제 시 git 추적 없음 = 정상)
- 별도 git repo: CUAP / SAP / 5000 / techpawz 계열 / 5.informationhot / rotcha-blog / kuta

## 배포
- 수단: `shared/publishers/deploy.py: deploy_site(site_path, cf_project)` (token-stripped, hugo --gc --minify,
  wrangler OAuth profile). **full dispatcher.py 미사용** (템플릿 전용 변경 = 신규 글 생성 방지)
- 대상: 30 roots (루트 basename = cf_project)
- 실행: 백그라운드 subagent 위임

## 편차 (Deviation) — 보고 필수
(a) 배포는 dispatcher.py 아닌 deploy_site() — 템플릿 전용이라 신규 포스트 생성 차단 목적
(b) info.techpawz-hugo(dot) 커스텀 single.html **생성 안 함** — 차단: 해당 블로그 전용 AdSense
    slot ID 부재 (biz의 8246483613 사용 시 도메인 불일치로 blank). 회귀 없음(원래 0광고)
(c) PLAN 5-2 `fluid→auto` **미적용** — 표준 §8 금기#4(fluid+in-article 강제, auto 금지)에 역행해
    오히려 잘못된 지시. STAP 등은 이미 fluid = 정확
(d) 범위 14→30 확장 (사용자 m0038 승인)
(e) 2개 RAP `.bak_layouts` 더미 복원 완료

## 잔존 위험 / 블로커 (사용자 제공 데이터 필요)
1. **info.techpawz-hugo** (dot, 실사이트): `[advertisement]` 섹션 없음 + `extend-head.html`이
   `site.Params.advertisement.adsense` 참조(존재 안 함) → 광고 0. 자체 slot ID
   (topSlot/inArticleSlot, publisher 8772) 필요. 미제공 → 방치(회귀 없음).
2. **5.informationhot-hugo**: `top.html` partial 자체가 없음 + config에 `[advertisement]` 슬롯 없음
   (레거시 `[adsense]`만). 원래부터 미표준화(0광고). 정보hot 전용 slot ID + top.html 생성 필요.
   미제공 → 방치(회귀 없음, leaderboard.html는 dead file이라 삭제만 함).

## 배포 결과 (최종)
- 수단: `deploy_site(root, cf_project)` (token-stripped, OAuth profile).
- **28/30 성공**. 처음 19 OK + SAP 9개 재시도 성공 (cf_project를 wrangler.toml `name`으로 수정:
  `betguide-hugo`→`betguide` 등). SAP 실패 원인은 잘못된 프로젝트명(디렉터리 basename)이었음 —
  실제 Cloudflare Pages 프로젝트는 `-hugo` 접미사 없음. 내 템플릿 변경 정확.
- **2 실패 (둘 다 사전 존재 이슈, 내 변경과 무관)**:
  1. **biz.techpawz-hugo**: Hugo BUILD 실패 — `partial "related-single.html" not found`
     (single.html:76). 해당 partial은 현재 트리에 없음(커밋 4781170에서 추가 후 제거됨).
     내 변경은 line 98(leaderboard→top)뿐 → 회귀 아님. 복구 시 git history에서 partial 복원 필요(별도 작업).
  2. **5.informationhot-hugo**: `wrangler.toml` 없음 + Pages 프로젝트 없음(인프라 부재).
     또한 잔존위험 #2의 미표준화 블로그. 내 변경은 dead leaderboard.html 삭제뿐 → 회귀 아님.

## 검증 (독립 수단)
- `find ... leaderboard.html` → 0 (grep 기반, m0058/m0067)
- `grep adsense/leaderboard.html` in live single.html → 0
- 30 roots 중 29 single.html이 `adsense/top.html` 호출 (유일 누락 = 5.informationhot, 위 블로커)

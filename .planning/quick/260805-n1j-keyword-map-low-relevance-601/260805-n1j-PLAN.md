---
phase: quick-260805-n1j
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/scan_keyword_relevance.py
  - scripts/apply_keyword_removals.py
  - pipelines/curation/keywords.py
  - .planning/quick/260805-n1j-keyword-map-low-relevance-601/kw_risk_classified.json
  - .planning/quick/260805-n1j-keyword-map-low-relevance-601/kw_b1_review.json
  - .planning/quick/260805-n1j-keyword-map-low-relevance-601/kw_removal_log.json
autonomous: true
requirements:
  - Q-20260805-low-relevance-keywords-601
user_setup: []

must_haves:
  truths:
    - "전 블로그 low_relevance 실패 위험 키워드가 601행(고유쌍 589)에서 ≤ 261 고유쌍으로 감소 (자동 제거 338건 예상 + B1 검토 제거, 실제 수치 보고)"
    - "외국어/깨진 키워드(외국어혼합, regex A)와 완전 오프토픽 키워드(B0)가 KEYWORD_MAP에서 제거됨 — 재-import 후 세트 diff로 증명"
    - "의도된 유효 키워드(SSD512GB, AMD라이젠5, MTB자전거, 골프드라이버 등)는 상품명 샘플 확인 절차를 거쳐 잘못 제거되지 않음"
    - "파이프라인 로직(pipeline.py / relevance_scorer.py)과 curation.db products 무변경 — 수정은 keywords.py KEYWORD_MAP 데이터만"
    - "스케줄러 재시작 불필요 — scheduler.py:263 subprocess dispatch가 매 실행 keywords.py를 fresh import"
  artifacts:
    - path: "scripts/scan_keyword_relevance.py"
      provides: "pipeline 게이트 재현 스캐너 — 고유 키워드 1회씩 평가, score_products → adaptive threshold → 개별 drop → passes_gate, A/B0/B1/C 분류 + 상품명 샘플"
      exports: ["scan_keyword_relevance(blog_id=None)"]
    - path: "scripts/apply_keyword_removals.py"
      provides: "ast 기반 토큰 단위 삭제 엔진 — 블록 스코프, 다중 키워드 라인 보존, 결합 아티팩트 SKIP_ARTIFACT, 재-import 세트 diff 검증"
      exports: ["main()"]
    - path: "pipelines/curation/keywords.py"
      provides: "KEYWORD_MAP에서 위험 키워드 제거 반영 — 총 키워드 수 2982 → 감소, 블로그별 제거 수 = kw_removal_log.json과 일치"
      contains: "KEYWORD_MAP total < 2982"
    - path: ".planning/quick/260805-n1j-keyword-map-low-relevance-601/kw_removal_log.json"
      provides: "589 고유 위험 (blog, keyword) 쌍 전수 감사 로그 — (blog, keyword, action, reason_code, avg, on_ratio, sample) + 중복 12건 + SKIP_ARTIFACT 문서화"
  key_links:
    - from: "pipelines/curation/keywords.py KEYWORD_MAP"
      to: "get_keywords(blog_id)"
      via: "목록 반환 — 시그니처/동작 불변, 제거 키워드 미선택"
      pattern: "get_keywords(blog_id)가 제거 후 목록 반환"
    - from: "scripts/scan_keyword_relevance.py"
      to: ".planning/quick/260805-n1j-keyword-map-low-relevance-601/kw_scan_pipeline.json"
      via: "충실도 검증 — 위험 고유 (blog, keyword) 쌍 589건 일치해야 함 (601행 − 중복 12행, 변경 전)"
      pattern: "unique risk pairs == 589"
    - from: "scripts/apply_keyword_removals.py"
      to: "pipelines/curation/keywords.py"
      via: "재-import 세트 diff — post == pre − 대상 집합 (0 collateral, 0 remaining)"
      pattern: "SET_DIFF OK"
    - from: "제거된 키워드"
      to: "pipeline.py:976-1047 low_relevance 게이트"
      via: "KEYWORD_MAP에서 제거되어 재선택 경로 차단 — 저점수 실패 기회 제거"
      pattern: "제거 키워드 미선택"
---

<objective>
전 블로그(15개) KEYWORD_MAP의 low_relevance 실패 위험 키워드 601행(고유 (blog, keyword) 쌍 589개)을 규칙 기반으로 분류·정리한다. 외국어/깨진 키워드와 완전 오프토픽 키워드는 **토큰 단위로 제거**하고, 상품명 샘플 확인 절차로 유효 키워드의 오제거를 방지하며, 재스캔으로 위험 수 감소를 검증한다.

Purpose: 실제 파이프라인 로직을 재현한 스캔 결과 전 블로그 601개 키워드가 low_relevance 게이트(pipeline.py:976-1047)에서 실패 위험. 블로그별: massage 2, car 2, golf 7, bike 6, laptop 9, appliance 62, interior 79, baby 84, fitness 53, health 86, pet 61, kitchen 11, beauty 23, camping 116 (homeappliance 0). 이 키워드가 선정되면 3회 재시도 fallback까지 소진 후 실패 → keyword_health에 90일 격리 위험(kw_health.py:124, low_relevance 3회 이상). '카트'(avg 0.59, 경계)가 실제 실패한 전례처럼 경계값도 실패 가능하므로, 실패가 확정적인 키워드는 KEYWORD_MAP에서 제거한다. camping-hugo 선례(커밋 ca9f9e7ad — '조명'/'난로'/'선풍기' 행 삭제)와 동일한 방식.

**분류 규칙 (고유쌍 589개 전수 — 수동 검토 불가, 규칙 기반 일괄 처리):**
- A_FOREIGN (256개): 한자[\u4e00-\u9fff]·가나[\u3040-\u30ff]·키릴[\u0400-\u04ff] 포함 → 자동 제거. 깨진 문자열이라 쿠팡 검색 자체가 불가. 예: '캠핑基础装备推荐', '压力밥솥 추천', '크ампоинг 推荐'.
- B0_OFFTOPIC (83개): 상품 8개 전부(0/8) 블로그 allowed 키워드 미매치 AND 키워드 자체에 allowed 미포함 → 자동 제거. 상품이 주제와 무관(예: appliance '고구마'→식품, '데님'→의류, '감사'). on=0/8이면 주제 관련 상품이 8개 샘플에 하나도 없는 것이므로 안전.
- B1_REVIEW (124개): 0 < on-ratio < 0.5 → 상품명 샘플 확인 후 판단 (Task 3 결정 규칙).
- C_ONTOPIC_WATCH (126개): on-ratio ≥ 0.5 → 유지. 상품이 주제에 맞는데 2-match 스코어링 아티팩트로 저점수(예: '차량용청소기' car on=6/8 avg 0.5). 제거하면 블로그 핵심 주제가 사라짐 — watchlist로 보고만.

(수치는 고유쌍 기준 — checker 재현 검증값. 원본 kw_scan_pipeline.json 601행 기준은 A 256 / B0 84 / B1 129 / C 132. 중복 12행은 baby 8건 + fitness 2건 + beauty 2건 — 목록은 아래 context 참조. **충실도 게이트(589 고유쌍 == 스캔)가 하드 기준이고, 버킷 수치는 실행 출력으로 검증** — 편차 시 per-blog diff 디버깅 필수.)

**제거 후 예상:** 자동 제거 = A 256 + B0 83 = 339 고유쌍. 이 중 결합 아티팩트 1건('노트북가을', B0)은 SKIP_ARTIFACT → **실 제거 338건 예상**. B1 검토로 일부 추가 제거 → 위험 589 → ~180~220 고유쌍. **하드 게이트: 재스캔 고유쌍 ≤ 261** (= 589 − 338, B1 전부 유지 시에도 251 ≤ 261 — 마진 확보). 소형 블로그(massage/car/homeappliance/golf/bike)는 위험 키워드가 전부 C/B1-유지 → 제거 0개, 풀 고갈 위험 없음.

**스케줄러 반영 판단 (조사 완료):** scheduler.py:263이 `subprocess.run([PYTHON, "dispatcher.py", blog_id])`로 각 블로그를 별도 프로세스 실행 → keywords.py는 디스패치마다 fresh import → **재시작 불필요**. scheduler 프로세스 내부의 auto_collector(시간당, `from pipelines.curation.keywords import KEYWORD_MAP`)는 제거 전 키워드로 계속 수집할 수 있으나 무해(미사용 products만 생성). 이 사실을 검증으로 증명하고 커밋한다.

**삭제 메커니즘 (checker BLOCKER 1 해결 — 채택 필수):** keywords.py에는 한 줄에 키워드 2~10개가 들어간 다중 키워드 라인이 493줄(키워드 라인 ~1058줄 중 47%)이고, 위험 키워드 601건 중 451건(75%)이 다중 라인에 위치한다. 제거 대상(A/B0)과 유지 대상(B1-KEEP/C)이 같은 줄에 혼재하므로 **전체 줄 삭제는 금지** — `scripts/apply_keyword_removals.py`가 ast 기반 **토큰 단위 삭제**(블록 스코프)를 수행한다. done 기준은 git diff 형태가 아니라 **재-import 후 KEYWORD_MAP 키워드 집합 diff**로 검증한다 (세트 diff가 진짜 검증 — 라인 수정이 git diff에 보여도 무방).

Output: keywords.py KEYWORD_MAP에서 위험 키워드 토큰 제거 (포맷/주석/순서 보존), 스캔+분류 스크립트, 적용 스크립트, 589건 감사 로그. pipeline.py / relevance_scorer.py / curation.db 무변경.
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@.planning/quick/260805-n1j-keyword-map-low-relevance-601/kw_scan_pipeline.json
@.planning/quick/260805-n1j-keyword-map-low-relevance-601/kw_scan_full.json

# 판단 완료된 사실 (재조사 금지 — checker + 실행으로 검증됨)
- 스캔 데이터 형식: kw_scan_pipeline.json — dict[blog_id, {total, risk[], borderline[], ok[], nocollect}], risk 요소 = [keyword, avg, threshold, count]. 위험 셋 **601행 = 589 고유쌍 + 12 중복행**. kw_scan_full.json은 상세(threshold 대신 min 사용 등 미세 차이) — 분류 기준은 kw_scan_pipeline.json의 위험 셋.
- **중복 12행 (같은 (blog, keyword) 2회 등장 — 검증됨):** baby-hugo '교구','꼬꼬맘','내츄럴마미','네이쳐러브메','는장난감','임산부 필수품 추천','임산부 복대 추천','산후조리원 용품 추천' (8건, 원인: baby-hugo 443-453행과 489-499행의 중복 블록) / fitness-hugo '다이어트','논슬립' (2건, 원인: 리스트 내 중복 토큰) / beauty-hugo '프라이머 추천','바디 스크럽 추천' (2건). Task 1 스캔은 블로그별 **고유 키워드를 1회씩** 평가 → 589쌍 산출. 중복 12건은 콘솔 + JSON에 문서화.
- **결합 아티팩트 7줄 (쉼표 누락으로 인접 토큰과 묵시적 결합 — 검증됨):** 326행 `"거실"`+`"가구느낌",` → '거실가구느낌', 397행 `"귀체온계"`+`"카시트",` → '귀체온계카시트', 455행 `"노트북"`+`"가을",` → '노트북가을', 531행 `"근력운동덤벨"`+`"강아지 운동장 추천",` → '근력운동덤벨강아지 운동장 추천', 534행 `"근력운동"`+`"덤벨",` → '근력운동덤벨', 654행 `"눈건강"`+`"글루타치온",` → '눈건강글루타치온', 715행 `"고양이의"`+`"고양이밥그릇",` → '고양이의고양이밥그릇'.
- **위험 셋에 포함된 아티팩트는 정확히 2건 (검증됨):** baby '귀체온계카시트' (avg 0.05, B1 분류 예상) / baby '노트북가을' (B0 분류 예상). 파일에 `"귀체온계카시트",` 같은 소스 토큰이 없음 → **SKIP_ARTIFACT 규칙**: 파일 수정 없이 kw_removal_log.json에 기록. 구성 토큰('귀체온계'/'카시트'/'노트북'/'가을')은 위험 셋에 별도 존재하지 않음(독립 평가된 적 없음) — 확인 결과 미포함, 처리 불필요. 나머지 5개 아티팩트는 위험 셋 밖 → 무처리. 이 2건은 KEYWORD_MAP에 남으며 재스캔 위험 수에 포함됨(하드 게이트 261이 이를 이미 흡수).
- **A_FOREIGN = 256 고유쌍 (중복 0 — 검증됨).** checker 재현 버킷(고유쌍): B0 83 / B1 124 / C 126.
- 점수 메커니즘: relevance_scorer.py score_product() — (product_name + category_name)에서 allowed 키워드 substring 매치 수 / min_keyword_matches(2) → min(1.0) + 브랜드 보너스 0.15. **저점수 ≠ 나쁜 상품** — substring/2-match 아티팩트(예: "테일러메이드 드라이버"는 골프 allowed 매치 0건). 따라서 상품명 샘플 확인 필수.
- 게이트: pipeline.py:976-1047 — score_products → adaptive threshold(=base, 최근 성공 평균*0.95가 항상 base 초과) → 개별 drop(미달 제거, 3개 이상 남으면 재평균) → passes_gate(avg >= threshold). **주의: 개별 drop 재평균 디테일이 스캔 재현에서 버킷 수치를 흔든다 (B1/C 경계) — 충실도 게이트로 맞출 것.**
- 임계값: camping/pet/beauty/interior 0.55, laptop/health/appliance/kitchen 0.65, baby 0.70, 나머지 0.75 (RELEVANCE_CONFIG).
- DB_PATH = data/curation.db. products 테이블에 keyword/product_name/category_name 컬럼 존재 (스캔에서 조회 확인됨).
- **테스트 기준선 (실행 검증됨):** `pytest tests/curation/test_keywords.py` → **4 failed, 3 passed** (사전 존재 실패: blog 수 15≠10, 60~200 상한, generics 셋, 중복 체크). 작업 후 실패 셋이 늘면 안 됨. 테스트 코드는 수정 금지 (constraint: keywords.py 데이터만).
- camping 선례: ca9f9e7ad — KEYWORD_MAP에서 행 삭제 방식. 대체 키워드('캠핑 조명 추천' 등)는 이미 존재.
- 블로그별 키워드 수 (제거 전): massage 12, car 12, homeappliance 12, golf 12, bike 12, laptop 256, appliance 353, interior 268, baby 374, fitness 253, health 245, pet 541, kitchen 173, beauty 211, camping 248 — 합계 2982.
- **파일 구조 (검증됨):** 15개 블록. 블록 헤더 `"blog_id": [` 들여쓰기 불일치 — baby-hugo 0-indent(396행), fitness/health/pet/kitchen/beauty/camping 4-space, 나머지 8-space. 토큰 라인은 8-space. 다중 키워드 라인 493줄, 키워드 라인 ~1058줄. 주석(#)은 전부 독립 라인(토큰과 동일 라인 없음). 리스트 내 토큰 중복 존재(baby '교구'/'꼬꼬맘' 등 2회씩, fitness '논슬립' 598/616행 등). '논슬립'은 baby(486행)/fitness(598, 616행)/pet(742행) 3개 블록에 존재 — **블록 스코프 삭제 필수, 타 블록 미손상**.
- **ast 기반 블록 열거가 들여쓰기 무관하게 안전** (ast.parse → KEYWORD_MAP Dict → 각 키의 List 노드 → 요소(lineno, col_offset, end_lineno, end_col_offset) 열거). 다중 라인 스팬(lineno ≠ end_lineno) = 결합 아티팩트 판별.
- 스케줄러: scheduler.py:263 subprocess dispatch (fresh import), auto_collector는 scheduler 프로세스 내 캐시 (무해).
</context>

<tasks>

<task type="auto">
  <name>Task 1: 파이프라인 게이트 재현 스캔 + 위험 키워드 분류 스크립트 생성</name>
  <files>scripts/scan_keyword_relevance.py, .planning/quick/260805-n1j-keyword-map-low-relevance-601/kw_risk_classified.json</files>
  <action>
  Create `scripts/scan_keyword_relevance.py` (신규, additive) that reproduces the real curation gate EXACTLY and classifies every risk keyword into buckets. Do NOT modify pipeline.py / relevance_scorer.py / keywords.py — import from them.

  Reproduction logic (must match pipeline.py:976-1047 semantics):
  - For each blog in KEYWORD_MAP: evaluate **each unique keyword once** (dedupe per-blog occurrences — a keyword listed twice in the same blog is evaluated once). Load products from data/curation.db `products` table (keyword, product_name, category_name) for each keyword (limit 10, order by id DESC — match scan).
  - Compute per-keyword: score_products(products, blog_id) → scores["avg"], then get_adaptive_threshold(DB_PATH, blog_id, base) (import from shared.relevance_scorer — DB_PATH = data/curation.db), then individual drop: products with score < threshold dropped; if ≥3 remain, recompute avg/min; then passes_gate(avg >= threshold). Match the individual-drop recompute detail EXACTLY against kw_scan_pipeline.json thresholds/avgs (e.g., baby '보행기' avg 0.39 / thr 0.89, '귀체온계카시트' avg 0.05 / thr 0.95).
  - RISK = gate fails. Emit entries [keyword, avg, threshold, product_count] (same tuple shape as kw_scan_pipeline.json).
  - **Fidelity gate (pre-change, HARD):** the risk **unique (blog, keyword) pair set** MUST equal the 589 unique pairs in kw_scan_pipeline.json (601 rows minus 12 duplicates). Script prints "FIDELITY OK: 589 unique pairs match (601 rows, 12 duplicates documented)" and lists the 12 duplicate rows. If mismatch, debug the reproduction against kw_scan_pipeline.json (per-blog diff of keyword → [avg, threshold, count]) before proceeding — do not proceed with mismatched logic.

  Classification buckets (per risk keyword, unique pairs):
  - A_FOREIGN: re.match r'[\u4e00-\u9fff\u3040-\u30ff\u0400-\u04ff]' in keyword → auto-remove.
  - on-ratio: fraction of top-8 products (product_name + ' ' + category_name, lowercased) containing ANY of blog's CATEGORY_FILTERS.allowed substring.
  - B0_OFFTOPIC: on-ratio == 0.0 AND keyword contains no allowed substring → auto-remove.
  - B1_REVIEW: 0 < on-ratio < 0.5 OR (on-ratio == 0.0 AND keyword contains an allowed substring) → include top-8 product names in output for review.
  - C_ONTOPIC_WATCH: on-ratio >= 0.5 → keep.

  SKIP_ARTIFACT pre-warning: after classification, print any risk keyword whose exact source token `"<keyword>"` does not appear in keywords.py source text (expect exactly 2: '귀체온계카시트', '노트북가을' — concatenation artifacts). These are flagged for Task 2/3 SKIP_ARTIFACT handling, NOT errors.

  Outputs:
  - `.planning/quick/260805-n1j-keyword-map-low-relevance-601/kw_risk_classified.json` — {blog_id: {A: [[kw, avg, thr, count], ...], B0: [...], B1: [[kw, avg, on_ratio, [product_name x8]], ...], C: [...]}}, plus per-blog and global totals, plus a "duplicates" section listing the 12 collapsed duplicate rows.
  - Console summary: per-blog and total bucket counts at unique-pair level + duplicate rows list + SKIP_ARTIFACT candidates.

  Avoid: touching any production code paths; network calls; writes to curation.db. Pure read + analysis.
  </action>
  <verify>
  <automated>/Users/twinssn/Projects/5000/.venv/bin/python3 scripts/scan_keyword_relevance.py 2>&1 | tail -8</automated>
  </verify>
  <done>
  - Fidelity: unique risk (blog, keyword) pairs == 589, identical to kw_scan_pipeline.json unique risk set (script prints "FIDELITY OK: 589 unique pairs match (601 rows, 12 duplicates documented)"; on mismatch, per-blog diff output shown)
  - Bucket totals at unique level == A 256, B0 83, B1 124, C 126 (sum 589) — checker-verified reproduction; actual run output governs; if a bucket deviates, the fidelity gate (589 unique pairs) still binds and per-blog diff debugging must reconcile both
  - SKIP_ARTIFACT candidates printed == exactly ['귀체온계카시트', '노트북가을']
  - kw_risk_classified.json written with buckets + totals + duplicates section; py_compile scripts/scan_keyword_relevance.py passes
  - No changes to keywords.py / pipeline.py / relevance_scorer.py (git diff clean for those)
  </done>
</task>

<task type="auto">
  <name>Task 2: 토큰 단위 자동 제거 엔진 + A+B0 적용 + 세트 diff 검증</name>
  <files>scripts/apply_keyword_removals.py, pipelines/curation/keywords.py</files>
  <action>
  Create `scripts/apply_keyword_removals.py` — an ast-based **token-level deletion engine** (new, additive). It removes only the specified `"<keyword>"` tokens inside the target blog's block, preserving everything else (multi-keyword lines, commas, comments, blank lines, ordering). Data rows only — no pipeline logic changes, no test changes.

  Engine spec (indent-independent, block-scoped):
  1. Read the removal target file(s) as input: `--class-file kw_risk_classified.json` (auto mode: applies A + B0 buckets) and/or `--review-file kw_b1_review.json` (review mode: applies entries with decision == "remove"). Target set = list of (blog_id, keyword).
  2. Pre-state: fresh-process import of pipelines.curation.keywords → record per-blog keyword occurrence counts (a keyword listed twice in one blog counts 2).
  3. Parse keywords.py with ast.parse; locate the KEYWORD_MAP assignment's Dict node; for each blog key, its value List node; enumerate every element node with (value, lineno, col_offset, end_lineno, end_col_offset). Block scope comes from the per-blog List nodes — this handles 0-indent `"baby-hugo": [` (line 396), 4-space blocks (fitness/health/pet/kitchen/beauty/camping), and 8-space blocks uniformly.
  4. Per target (blog, kw): find ALL element nodes in that blog's List with value == kw (removes every occurrence, e.g., fitness '논슬립' twice on lines 598/616).
     - If an element node spans multiple lines (end_lineno > lineno) → concatenation artifact → **SKIP_ARTIFACT**: log to console, NO file change. (Covers '노트북가을' at 455-456 and '귀체온계카시트' at 397-398 if targeted. Constituent tokens are NOT in the risk set — verified — so nothing else to do for them.)
     - If no element with matching value exists in the blog's block → loud failure (fidelity breach; do not proceed).
     - Otherwise (single-line element): remove the source span `"<keyword>",` plus one adjacent separator (space after the comma when not line-final; the leading space before the token when line-final). If the line holds only this token, remove the entire line. If a line becomes whitespace-only after editing, remove the line.
  5. Preservation rules: comment lines are standalone in this file — never modified; if (unexpectedly) a removed token shares a line with a comment, remove only the token chunk and keep the comment. Never reorder tokens, never change any other keyword string, never touch lines outside the KEYWORD_MAP value spans (module functions below the dict stay byte-identical).
  6. After edits: re-parse with ast.parse + run py_compile. Then **post-state set-diff proof** (the done criterion): launch a fresh subprocess that imports keywords.py, and assert for every blog: (a) every removed keyword has 0 occurrences; (b) total occurrence count == pre_count − Σ(removed targets' pre-occurrences in that blog); (c) every non-target keyword has an identical occurrence count pre/post. Print "SET_DIFF OK: {n} pairs removed, 0 collateral, 0 remaining" — exit non-zero on any failed assertion.
  7. Safety valve: after computing per-blog removals, if any blog's remaining keyword count would drop below 8, revert removals for that blog (not expected — small blogs remove ~0; expected floors: golf/bike ≥ 12, CUAP blogs ≥ 140). Report any revert in console.

  Run 1 (this task): `python3 scripts/apply_keyword_removals.py --class-file .planning/quick/260805-n1j-keyword-map-low-relevance-601/kw_risk_classified.json` → removes A (256) + B0 (83 unique, minus '노트북가을' SKIP_ARTIFACT → ~338 actual) tokens.

  Do NOT: touch pipeline.py / relevance_scorer.py / keyword_health.py; touch the B1 keywords yet (Task 3); write kw_removal_log.json yet (Task 3); commit (Task 4); fix the missing-comma lines 326/397/455/531/534/654/715 (format cleanup is out of scope — SKIP_ARTIFACT documents them).
  </action>
  <verify>
  <automated>/Users/twinssn/Projects/5000/.venv/bin/python3 -m py_compile pipelines/curation/keywords.py scripts/apply_keyword_removals.py && /Users/twinssn/Projects/5000/.venv/bin/python3 -m pytest tests/curation/test_keywords.py -q 2>&1 | tail -3</automated>
  </verify>
  <done>
  - Apply run 1 prints "SET_DIFF OK: ~338 pairs removed, 0 collateral, 0 remaining" (exact count: A 256 + B0 83 − SKIP_ARTIFACT; '노트북가을' documented as SKIP_ARTIFACT, no file change)
  - Remaining per-blog keyword counts: massage 12, car 12, homeappliance 12, golf 12, bike 12, all other blogs ≥ 140 (actuals recorded)
  - py_compile passes; pytest test_keywords.py failure set identical to baseline: 4 failed, 3 passed (no new failures)
  - No B1 keyword removed in this task; git diff on keywords.py shows token edits only (deletions + possible line rewrites of multi-keyword lines — line-level "deletions only" is NOT a valid criterion, the SET_DIFF proof is)
  </done>
</task>

<task type="auto">
  <name>Task 3: B1 상품 샘플 검토 + kw_removal_log.json 작성 + 재스캔 검증</name>
  <files>pipelines/curation/keywords.py, .planning/quick/260805-n1j-keyword-map-low-relevance-601/kw_b1_review.json, .planning/quick/260805-n1j-keyword-map-low-relevance-601/kw_removal_log.json</files>
  <action>
  Step 1 — B1 review (124 unique keywords, agent judgment with top-8 product names from kw_risk_classified.json):
  Decision rules per keyword (use product samples as evidence):
  - REMOVE: product names semantically unrelated to the blog's subject (e.g., interior '강의실' → 안내판/표지판, '갤럭시원단' → 폰케이스, '두꺼운' → 김장비닐; appliance '노블'/'니켈' → 랜덤 잡화). Also REMOVE single-word generic/attribute searches whose products never match the blog demographic.
  - KEEP: products semantically on-topic for the blog subject AND target demographic matches, even when allowed-substring matching fails (scoring artifact). Examples (verified): golf '골프드라이버'/'아이언세트' (골프채 상품), bike '자전거헬멧' (자전거 헬멧), baby '간식통' (유아용 도시락), baby '산후조리원 용품 추천' (산후/임산부 용품 — allowed에 '임산부' 있음), appliance '제모기' (allowed에 '제모기' 있음).
  - Latin/digit keywords (SSD512GB, SSD1TB, AMD라이젠5, 인텔코어i5, MTB자전거, 오메가3 DHA 추천, 비타민D3 추천 등): KEEP if products are on-topic (they are — these are intentional spec/brand keywords; 'SSD512GB' on=8/8).
  - Concatenation artifacts in B1 ('귀체온계카시트'): review normally with whatever product samples exist; if the decision would be REMOVE, record action SKIP_ARTIFACT (token cannot be removed — broken source line, logged, no file change). If KEEP, record KEEP_B1.
  - When in doubt for a KEEP-vs-REMOVE call, default to KEEP (conservative — a kept keyword only costs retries, a wrongly removed keyword removes coverage).

  Write `.planning/quick/260805-n1j-keyword-map-low-relevance-601/kw_b1_review.json`: all 124 B1 unique pairs as {blog_id, keyword, decision: "remove"|"keep", reason_code, avg, on_ratio} — the audit trail for the B1 step.

  Step 2 — Apply B1 removals: `python3 scripts/apply_keyword_removals.py --review-file .planning/quick/260805-n1j-keyword-map-low-relevance-601/kw_b1_review.json` → token-level removal of B1-remove entries (SKIP_ARTIFACT rule applies to artifacts). Engine prints SET_DIFF OK with cumulative removed count (A+B0+B1).

  Step 3 — Write `.planning/quick/260805-n1j-keyword-map-low-relevance-601/kw_removal_log.json`:
  All 589 unique risk pairs with fields {blog_id, keyword, action: REMOVE_A|REMOVE_B0|REMOVE_B1|KEEP_B1|KEEP_C|SKIP_ARTIFACT, reason_code, avg, on_ratio (B1만), sample: [first 3 product names]}. Include: (a) a "duplicates" section listing the 12 collapsed duplicate rows (blog, keyword, count); (b) an "artifacts" section documenting '노트북가을' + '귀체온계카시트' (SKIP_ARTIFACT, broken missing-comma lines 455-456 / 397-398, constituents not separately in risk set — verified; left in KEYWORD_MAP, future format cleanup item); (c) per-blog pre/post keyword counts.

  Step 4 — Re-scan verification: run `python3 scripts/scan_keyword_relevance.py` again. Assert **unique risk pairs ≤ 261 (HARD GATE)** and report exact number + per-blog breakdown (expect ~180-220 with B1 removals; B1 전부 유지여도 251 ≤ 261). Remaining risk keywords must be traceable to kw_removal_log.json as KEEP_B1 / KEEP_C / SKIP_ARTIFACT.

  Do NOT: touch pipeline.py / relevance_scorer.py / keyword_health.py; modify test files; add or reword any keyword string.
  </action>
  <verify>
  <automated>/Users/twinssn/Projects/5000/.venv/bin/python3 scripts/scan_keyword_relevance.py 2>&1 | grep -E "FIDELITY|RISK|unique|total" | tail -5</automated>
  </verify>
  <done>
  - kw_b1_review.json covers all 124 B1 unique pairs with decision + reason_code
  - Apply run 2 prints SET_DIFF OK (cumulative A+B0+B1 removed, 0 collateral, 0 remaining)
  - kw_removal_log.json covers all 589 unique pairs with action + reason_code; duplicates section (12 rows) + artifacts section present; per-blog pre/post counts match actual KEYWORD_MAP
  - Re-scan unique risk pairs ≤ 261, exact number + per-blog breakdown reported
  - py_compile passes; pytest test_keywords.py failure set identical to baseline (4 failed, 3 passed)
  </done>
</task>

<task type="auto">
  <name>Task 4: fresh-import 반영 확인 + 커밋 + SUMMARY</name>
  <files>.planning/quick/260805-n1j-keyword-map-low-relevance-601/260805-n1j-SUMMARY.md</files>
  <action>
  Step 1 — Fresh-import proof (scheduler reflection): run a NEW process that imports pipelines.curation.keywords and asserts: (a) total KEYWORD_MAP count == 2982 − removed_count (from kw_removal_log.json REMOVE_* actions); (b) every removed (blog, keyword) is absent from get_keywords(blog_id); (c) a kept example (e.g., 'SSD512GB' in laptop-hugo, '골프드라이버' in golf-hugo) still present. Record the outputs. Conclusion to write in SUMMARY: scheduler restart NOT required because scheduler.py:263 dispatches `python3 dispatcher.py {blog_id}` as a subprocess (fresh import per dispatch); the in-process auto_collector cache (scheduler.py:668) may keep collecting for removed keywords until next natural restart — harmless (unused products rows).

  Step 2 — Commit (single commit, repo convention):
  - git add pipelines/curation/keywords.py scripts/scan_keyword_relevance.py scripts/apply_keyword_removals.py .planning/quick/260805-n1j-keyword-map-low-relevance-601/
  - Message: `fix(quick-260805-n1j): KEYWORD_MAP 전수 감사 — low_relevance 위험 키워드 {removed_count}개 제거 (589 위험 → {new_risk} 재스캔)`
  - Do NOT push (repo rule: git push는 형상관리용, 배포는 wrangler 경유 — 이 작업은 배포 아님).

  Step 3 — Write `.planning/quick/260805-n1j-keyword-map-low-relevance-601/260805-n1j-SUMMARY.md` using the summary template with the 3-way classification ([검증됨]/[부분검증]/[검증불가]), per AGENTS.md reporting rules (근거 필수, 숫자 산출 근거 분해, 잔존 위험 섹션 필수).
  </action>
  <verify>
  <automated>/Users/twinssn/Projects/5000/.venv/bin/python3 -c "
import sys; sys.path.insert(0, '.')
from pipelines.curation.keywords import KEYWORD_MAP, get_keywords
import json
log = json.load(open('.planning/quick/260805-n1j-keyword-map-low-relevance-601/kw_removal_log.json'))
removed = {(e['blog_id'], e['keyword']) for e in log['entries'] if e['action'].startswith('REMOVE_')}
remain = {(b, k) for b, kws in KEYWORD_MAP.items() for k in kws}
assert not (removed & remain), sorted(removed & remain)[:5]
print('FRESH_IMPORT OK: removed_absent=', len(removed), 'total=', sum(len(v) for v in KEYWORD_MAP.values()))
" && git show --stat HEAD | tail -8</automated>
  </verify>
  <done>
  - Fresh-import assert outputs recorded (total count == 2982 − removed, removed-absent, kept-present)
  - Commit created with only keywords.py + both scripts + quick dir artifacts (git show --stat verified)
  - SUMMARY.md written with 3-way classification, 근거 명시, 숫자 산출 근거 분해, 잔존 위험 section
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KEYWORD_MAP 데이터 → 발행 파이프라인 | 키워드 데이터가 쿠팡 검색/상품 필터링/점수 게이트를 통과해 발행을 결정하는 경계 |
| 새 스크립트 실행 (scripts/scan_keyword_relevance.py, scripts/apply_keyword_removals.py) | 신규 실행 코드 — 읽기 전용 스캔, 삭제는 키워드 토큰에 한정 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-n1j-01 | Availability | keywords.py KEYWORD_MAP | mitigate | 블로그별 키워드 수 하한 8개 safety valve (apply 스크립트 내장); 소형 블로그(massage/car/homeappliance/golf/bike) 위험 키워드 전수 유지 분석 완료 — 제거 0건 |
| T-n1j-02 | Information Disclosure | curation.db products | mitigate | 스캔 스크립트는 sqlite3 read-only 조회만 (SELECT), DML 금지; Task 1 verify로 production 파일 무변경 확인 |
| T-n1j-03 | Spoofing | B1_REVIEW 판단 | mitigate | 유효 키워드 오제거 방지: 상품명 8개 샘플 + 결정 규칙(주제 의미 일치/데모그래픽 일치 → KEEP, 불확실 시 KEEP) + kw_b1_review.json 124건 전수 + kw_removal_log.json으로 589건 감사 가능 |
| T-n1j-04 | Tampering | keywords.py 토큰 삭제 | mitigate | ast 기반 블록 스코프 토큰 삭제 (다중 키워드 라인·주석·순서 보존, 타 블록 동일 키워드 무손상), 결합 아티팩트는 SKIP_ARTIFACT 규칙(파일 무변경+로그), 재-import 후 세트 diff로 대상 키워드만 제거 증명 (SET_DIFF OK, 0 collateral), py_compile + 테스트 실패 셋 불변 검증 |
| T-n1j-05 | Elevation of Privilege | 스케줄러 반영 | accept | 재시작 불필요 (subprocess fresh import 증명). auto_collector 캐시 스테일은 무해(미사용 products 행) — 잔존 위험으로 SUMMARY에 보고 |
| T-n1j-06 | Availability | 결합 아티팩트 키워드 | accept | '귀체온계카시트'/'노트북가을'은 파일에 소스 토큰 부재 → SKIP_ARTIFACT로 유지 (broken missing-comma 라인). 하드 게이트 261이 이를 흡수 (251 ≤ 261). 쉼표 포맷 복구는 범위 외 — 잔존 위험으로 SUMMARY 보고 |
| T-n1j-SC | Tampering | npm/pip/cargo installs | accept | 패키지 설치 0건 (표준 라이브러리 + 기존 venv만 사용) — 패키지 감사 불필요 |
</threat_model>

<verification>
- Task 1: 스캔 충실도 — 위험 고유쌍 589건 == kw_scan_pipeline.json 고유쌍 (중복 12건 문서화), 버킷 합계 A 256 / B0 83 / B1 124 / C 126, SKIP_ARTIFACT 후보 2건 출력
- Task 2: apply 스크립트 SET_DIFF OK (자동 제거 A+B0, 0 collateral) + py_compile + 테스트 실패 셋 불변 (4 failed, 3 passed 기준선)
- Task 3: kw_b1_review.json 124건 + kw_removal_log.json 589건 + 재스캔 고유쌍 ≤ 261 (하드 게이트)
- Task 4: fresh-import assert (제거 키워드 부재, 유지 키워드 존재, 총량 일치) + 커밋 1건 + SUMMARY 3분법 보고
- 범위 가드: pipeline.py / relevance_scorer.py / keyword_health.py / curation.db / 테스트 코드 — 전부 무변경
</verification>

<success_criteria>
- KEYWORD_MAP에서 589 고유 위험 키워드 중 규칙 기반으로 제거 완료, 전수 감사 로그(kw_removal_log.json, 중복 12건 + 아티팩트 2건 포함) 존재
- 유효 키워드 오제거 0건 (상품 샘플 절차 거친 결과, 세트 diff로 증명 가능 — 0 collateral)
- 재스캔 위험 수 589 → ≤ 261 고유쌍 감소 확인 (실측 보고)
- 기존 통과 테스트 불변, 파이프라인 로직/DB 무변경, 스케줄러 재시작 불필요 판단 증명
- 커밋 1건, SUMMARY.md에 3분법 + 잔존 위험 보고 완료
</success_criteria>

<output>
Create `.planning/quick/260805-n1j-keyword-map-low-relevance-601/260805-n1j-SUMMARY.md` when done
</output>

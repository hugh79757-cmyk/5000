---
phase: quick-260805-mo7
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - pipelines/curation/keywords.py
autonomous: true
requirements:
  - Q-20260805-camping-generic-keywords
user_setup: []

must_haves:
  truths:
    - "camping-hugo가 더 이상 '조명'/'선풍기' 일반명 키워드로 low_relevance 실패를 반복하지 않음"
    - "KEYWORD_MAP 총 키워드 수가 2985 → 2982로 정확히 3개 감소"
    - "appliance-hugo의 '선풍기'는 그대로 유지 (가전 블로그 정상 동작)"
    - "기존 실패 기록(keyword_health) 및 다른 블로그 키워드 목록 불변"
  artifacts:
    - path: "pipelines/curation/keywords.py"
      provides: "KEYWORD_MAP camping-hugo 목록에서 '조명'/'난로'/'선풍기' 3개 요소 제거"
      contains: "KEYWORD_MAP total = 2982, camping-hugo 조명/선풍기 부재"
  key_links:
    - from: "pipelines/curation/keywords.py KEYWORD_MAP['camping-hugo']"
      to: "get_keywords(blog_id)"
      via: "목록 반환 — 제거 후에도 시그니처/동작 불변"
      pattern: "get_keywords(blog_id)가 남은 248개 반환"
    - from: "camping-hugo 키워드 선택"
      to: "relevance_scorer.score_product()"
      via: "일반명 키워드 제거로 저점수 실패 경로 차단"
      pattern: "조명/난로/선풍기 미선택"
---

<objective>
camping-hugo의 KEYWORD_MAP에서 일반명 키워드 '조명', '난로', '선풍기' 3개를 제거하여, 오늘(8/5) 발생한 low_relevance 발행 실패가 다음 1회만 더 발생하면 90일 격리로 이어지는 상황을 차단한다.

Purpose: camping-hugo가 이 3개 일반명 키워드로 쿠팡 검색 시 "가정용 전기제품"(거실 스탠드/전기히터/가정용 선풍기)을 반환받아 relevance_scorer 평균 < 0.55 → low_relevance 실패. keyword_health에 consecutive_failures=2 기록 중 — 3회째 실패 시 90일 격리. 키워드를 목록에서 제거하면 재선택되지 않으므로 격리 위험이 사라짐. 대체 키워드 '캠핑 조명 추천'(발행성공 1), '캠핑 난로 추천'(발행성공 2), '캠핑 선풍기 추천'(발행성공 2)이 이미 KEYWORD_MAP에 존재해 커버리지 손실 없음.

Output: `pipelines/curation/keywords.py`의 camping-hugo 목록에서 3개 요소 제거 (KEYWORD_MAP 2985→2982). 다른 파일/데이터 무변경.
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md

# 판단 완료된 사실 (재조사 금지)
- 증상: camping-hugo가 8/5 16:10~16:16에 '조명'/'난로'/'선풍기' 키워드로 low_relevance 실패 3건. keyword_health consecutive_failures=2, quarantined_until 오늘 20:16. keyword_health.py:124 — low_relevance 3회 이상 시 90일 격리.
- 근본 원인: 쿠팡 검색이 "캠핑용"이 아닌 "가정용 전기제품"을 반환 (조명→거실 스탠드/LED 방등, 난로→전기히터, 선풍기→가정용 선풍기). relevance_scorer.py score_product() score 0.00~0.50 → avg < threshold(0.55) → low_relevance.
- KEYWORD_MAP 구조: dict[blog_id, list[str]]. camping-hugo 목록은 `pipelines/curation/keywords.py:1035`~1144.
- 제거 대상 위치: 1052행 `"선풍기",`, 1053행 `"난로",`, 1054행 `"조명",` — 인접 3행.
- '선풍기'는 appliance-hugo 목록(198행부터, 212행·263행)에도 존재 — **camping-hugo 목록에서만 제거, appliance-hugo는 유지** (가전 블로그라 정상 동작).
- '조명'/'난로'는 KEYWORD_MAP에서 camping-hugo에만 존재.
- 대체 키워드 확인 완료 (1112~1113행): "캠핑 선풍기 추천"(products 20, 발행성공 2), "캠핑 난로 추천"(products 26, 발행성공 2), "캠핑 조명 추천"(products 20, 발행성공 1) — 제거 후에도 커버리지 손실 없음.
- 키워드 제거 시 `get_keywords(blog_id)`(1151행) 동작 영향 없음 — 그냥 목록에서 빠질 뿐.
- keyword_health 기존 실패 기록은 그대로 둠 — 키워드가 목록에서 제거되면 재선택되지 않아 추가 실패/격리 없음.
- 파이프라인 코드/CATEGORY_FILTERS 수정 불필요 — 캠핑 접두어 키워드들은 CATEGORY_FILTERS allowed 목록과 매칭되어 정상 통과.
- KEYWORD_MAP 총 개수: 2985 → 2982 (정확히 3개 감소).

# ⚠ 잔존 관찰 (제거 범위 밖, 사용자 확인용)
- camping-hugo 목록 1108행(확장 다중 키워드 라인)에 `"난로"`가 1건 더 존재: `"그늘막", "글러브", ..., "낚시", "난로", "난방기구", ...`. 사용자 결정은 1052~1054행 3개 요소만 제거 (count 2985→2982 확정)이므로 **1108행은 건드리지 말 것**. 동일 문자열이므로 이론상 재선택 시 동일 low_relevance 위험은 남으나, 사용자 결정에 따라 범위 밖 — SUMMARY의 잔존 위험 섹션에 명시하고 사용자에게 보고할 것.

# 테스트 기준선 (수정 전 반드시 실행 — 기존 실패 구분용)
- `tests/curation/test_keywords.py`는 **현재 이미 3 failed + 1 deselected** 상태 (블로그 수 10→15, camping-hugo 251개로 상한 200 초과, 중복 키워드 등 — golf/bike 추가 등으로 인한 기존 실패).
- 이번 수정은 테스트 실패 셋을 늘리지 않아야 함. 수정 전후 동일 실패 셋이어야 통과.
- 기준선: `.venv/bin/python3 -m pytest tests/curation/test_keywords.py -q` → `3 failed, 3 passed, 1 deselected` (test_all_10_blogs_present deselected 시). 실패 항목: test_each_blog_has_60_to_200_keywords / test_no_generic_keywords_remain / test_get_keywords_all_blogs_have_unique_keywords.
- **테스트 코드를 수정하지 말 것** — 기존 실패는 이 작업의 범위 밖.

# 허용된 사용자 결정 (locked, 변경 금지)
1. 제거 대상: KEYWORD_MAP["camping-hugo"] 목록에서 '조명', '난로', '선풍기' **정확히 3개 요소** (1052~1054행)
2. appliance-hugo '선풍기' 유지, keyword_health 기록 불변, 다른 파일 무수정
3. 기존 테스트 실패는 이번 작업과 무관 — 수정 전후 동일해야 함 (새 실패 금지)
</context>

<tasks>

<task type="auto">
  <name>Task 1: camping-hugo KEYWORD_MAP에서 일반명 키워드 '조명'/'난로'/'선풍기' 3개 제거</name>
  <files>pipelines/curation/keywords.py</files>
  <action>`pipelines/curation/keywords.py`의 KEYWORD_MAP["camping-hugo"] 목록(1035~1144행)에서 정확히 다음 3개 요소 라인만 삭제한다:

  - 1052행: `        "선풍기",`
  - 1053행: `        "난로",`
  - 1054행: `        "조명",`

  삭제 후 해당 인접 라인(1051행 "모기장" ↔ 1055행 "카트")이 바로 이어지도록 한다. 3개 요소 제거로 KEYWORD_MAP 총 개수는 2985 → 2982.

  절대 금지 (비파괴 원칙 — 범위 밖 항목):
  - 1108행 확장 라인의 `"난로"`는 건드리지 말 것 (사용자 결정: 1052~1054행 3개만, count 2982 확정. 잔존 위험으로 SUMMARY에 기록)
  - appliance-hugo 목록(198행 시작)의 '선풍기'(212행·263행)는 유지할 것
  - keyword_health의 기존 실패 기록(DB) 수정 금지 — 키워드가 목록에서 제거되면 재선택되지 않아 추가 격리 없음
  - CATEGORY_FILTERS, 파이프라인 코드, 다른 블로그 키워드 목록, 테스트 코드 전부 무수정
  - 키워드 추가/이름 변경/정렬 변경 금지 — 순수 삭제 3건만

  참고: 수정 전 기준선으로 테스트를 먼저 실행하고 기록할 것 (기존 실패 3건 + deselected 1건이 수정 후에도 동일해야 새 회귀 없음이 증명됨). 파일 수정 후 `py_compile`으로 문법 검증. `git diff`로 변경이 정확히 3라인 삭제뿐인지 확인할 것.</action>
  <verify>
    <automated>.venv/bin/python3 -c "
from pipelines.curation.keywords import KEYWORD_MAP
total = sum(len(v) for v in KEYWORD_MAP.values())
c = KEYWORD_MAP['camping-hugo']
a = KEYWORD_MAP['appliance-hugo']
assert total == 2982, f'total={total} (기대 2982)'
assert '조명' not in c and '선풍기' not in c, '조명/선풍기 제거 실패'
assert c.count('난로') == 1, f\"camping 난로 count={c.count('난로')} (기대 1 — 1108행 확장 잔존)\"
assert '선풍기' in a, 'appliance-hugo 선풍기 유지 필요'
print(f'OK total={total} camping(len={len(c)}) 난로잔존={c.count(\"난로\")} appliance선풍기={a.count(\"선풍기\")}')
"</automated>
  </verify>
  <verify>
    <automated>.venv/bin/python3 -m py_compile pipelines/curation/keywords.py && echo "py_compile OK"</automated>
  </verify>
  <verify>
    <automated>git diff --stat pipelines/curation/keywords.py && git diff pipelines/curation/keywords.py | rg -c '^-' && echo "삭제 라인 수(위) — 기대 3"</automated>
  </verify>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/curation/test_keywords.py -q 2>&1 | tail -2</automated>
  </verify>
  <done>KEYWORD_MAP 총 2982개 (2985−3). camping-hugo 목록에 '조명'/'선풍기' 부재, '난로' 1108행 잔존 1건(범위 밖으로 명시 보고). appliance-hugo '선풍기' 유지. py_compile 통과. git diff = 순수 3라인 삭제. test_keywords.py 실패 셋이 수정 전(3 failed + 1 deselected)과 동일 — 신규 회귀 0건</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keywords.py KEYWORD_MAP → get_keywords() | 단일 데이터 파일 편집 — 잘못된 라인 삭제 시 다른 블로그 키워드/시그니처 영향 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260805-mo7-01 | Tampering | keywords.py 편집 범위 | mitigate | 삭제 대상 1052~1054행 3라인으로 한정. git diff로 3라인 삭제만 확인. 1108행 '난로'/appliance '선풍기'는 명시적 금지 + 자동 검증(assert) |
| T-260805-mo7-02 | Tampering | test_keywords.py 기존 실패 오인 | mitigate | 수정 전 기준선 기록(3 failed + 1 deselected), 수정 후 동일 셋 확인 — 기존 실패를 이번 변경으로 위장/수정 금지 |
| T-260805-mo7-03 | Tampering | keyword_health 데이터 | accept | 기존 실패 기록 유지가 사용자 결정 — 키워드 미선택으로 격리 위험 소멸 (1108행 '난로'는 잔존 위험으로 보고) |
| T-260805-mo7-SC | Tampering | npm/pip installs | accept | 신규 패키지 설치 없음 — 공급망 표면 없음 |
</threat_model>

<verification>
- Task 1: KEYWORD_MAP total 2982 + camping-hugo 조명/선풍기 부재 + 난로 1건(1108행 잔존) + appliance 선풍기 유지 (자동 assert)
- py_compile 통과, git diff 3라인 삭제만
- test_keywords.py: 수정 전(3 failed + 1 deselected)과 동일 실패 셋 — 신규 회귀 0
- AGENTS.md 보고 규칙: SUMMARY는 3분법([검증됨]/[부분검증]/[검증불가]) + 산출 근거 + 잔존 위험 섹션 필수 (1108행 '난로' 잔존 명시)
</verification>

<success_criteria>
- [ ] KEYWORD_MAP 총 키워드 수 2982 (2985 → 2982, 정확히 3개 감소 — 산출: 2985−3)
- [ ] camping-hugo 목록에 '조명'/'선풍기' 없음, '난로'는 1108행 확장 라인 1건만 잔존 (범위 밖, 보고 대상)
- [ ] appliance-hugo '선풍기'(212·263행) 유지
- [ ] git diff가 keywords.py의 3라인 삭제뿐 — 다른 파일/데이터/테스트 무변경
- [ ] test_keywords.py 실패 셋 수정 전후 동일 (신규 실패 0)
- [ ] keyword_health DB 기록 불변
</success_criteria>

<output>
작업 완료 시 `.planning/quick/260805-mo7-root-title-remove-generic-camping-keywor/260805-mo7-SUMMARY.md` 작성
</output>

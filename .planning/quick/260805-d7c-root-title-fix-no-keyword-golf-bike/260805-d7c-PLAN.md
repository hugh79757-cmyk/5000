---
phase: quick-260805-d7c
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - scheduler.py
  - data/curation.db
autonomous: true
requirements:
  - Q-20260805-golf-bike-no-keyword
user_setup: []

must_haves:
  truths:
    - "golf-hugo/bike-hugo 키워드 24개 전부 curation.db products 테이블에 3건 이상 보유"
    - "재시작된 스케줄러가 auto_collector에서 전체 2985개 키워드를 로드 (stale 2924 소멸)"
    - "catchup 루프가 malformed 시각(600/840)에서 ValueError로 중단되지 않음"
    - "golf-hugo/bike-hugo 발행이 no_keyword 실패 없이 성공"
  artifacts:
    - path: "data/curation.db"
      provides: "golf/bike 키워드 상품 데이터 (products 테이블)"
      contains: "24개 키워드 각각 ≥3행"
    - path: "scheduler.py"
      provides: "catchup 시각 파싱 방어 가드 (_catchup_missed_inner, ~line 420)"
      contains: "split(':') 길이 가드 + ValueError 예외 처리"
    - path: "/tmp/5000-scheduler.error.log"
      provides: "재시작 후 auto_collector 실행 증거"
      contains: "전체 2985개"
  key_links:
    - from: "pipelines/curation/auto_collector.py"
      to: "pipelines/curation/keywords.py KEYWORD_MAP"
      via: "fresh 프로세스 import (stale 캐시 제거)"
      pattern: "total_kw = 2985"
    - from: "scheduler.py _catchup_missed_inner"
      to: "config/blogs.d/tap.yaml schedule.times"
      via: "방어 가드로 malformed 값 skip + warning"
      pattern: "split(':') len 가드"
---

<objective>
golf-hugo/bike-hugo의 `no_keyword` 발행 실패를 해소하고, 동일 실패를 재발시키는 두 가지 시스템 결함(스케줄러 stale 키워드 캐시, catchup 시각 파싱 크래시)을 제거한다.

Purpose: 8/5 생성된 golf/bike 블로그가 `_select_keyword()` → None (키워드 상품 0건)으로 발행 불가 상태. 근본 원인은 8/4 15:36 커밋 b81598725로 KEYWORD_MAP에 추가된 golf/bike 키워드(24개)가, 실행 중인 스케줄러(PID 54024, 8/3 시작)의 stale 모듈 캐시(2924개) 때문에 수집된 적 없음. 추가로 scheduler.py:420의 `h, m = map(int, str(t).split(":"))`이 tap-blogger의 YAML sexagesimal 시각(600/840 int)에서 ValueError로 catchup을 매번 중단시킴.

Output: golf/bike 키워드 24개 상품 수집 완료 / 재시작된 스케줄러(2985개 로드) / 방어 가드가 적용된 scheduler.py / golf-hugo·bike-hugo 발행 성공
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md

# 판단 완료된 사실 (재조사 금지)
- root cause: `pipelines/curation/pipeline.py:884` `_select_keyword()` → 키워드 상품 0건 → None → no_keyword
- golf/bike 키워드는 KEYWORD_MAP 맨 끝에 추가됨 (커밋 b81598725). 실행 중 스케줄러의 stale 모듈 캐시(2924)가 신규 키워드를 모름
- 현재 KEYWORD_MAP: 2985개 (golf-hugo 12, bike-hugo 12 — 데이터 확인 완료)
- curation.db: golf/bike 24개 키워드 전부 products 0건 (확인 완료)
- scheduler.py:420 크래시 원인: `config/blogs.d/tap.yaml` tap-blogger `schedule.times` = `['06:00', 600, 840]` — unquoted `10:00`/`14:00`이 PyYAML sexagesimal int로 파싱됨. `str(600).split(":")` → `["600"]` → ValueError. catchup은 5분마다 실행되고 main 루프(line 761-763)에서 except로 잡힘 — 프로세스는 살아있지만 catchup은 매번 실패
- lock 파일은 flock 기반(`_fcntl.flock LOCK_EX | LOCK_NB`) — 파일 존재는 정상, 활성 hold 여부만 확인 필요
- launchd: com.5000.scheduler, KeepAlive=true, `.venv/bin/python scheduler.py`, stderr → /tmp/5000-scheduler.error.log
- auto_collector는 매시간 :50에 실행 (scheduler.py:648-657), MAX_PER_RUN=5

# 허용된 사용자 결정 (locked, 변경 금지)
1. golf/bike 키워드 수동 수집 — auto_collector를 FRESH Python 프로세스로 실행
2. 스케줄러 재시작: `launchctl kickstart -k gui/$(id -u)/com.5000.scheduler`
3. scheduler.py:420 catchup split 버그 방어 가드 수정 (유일한 production 코드 변경)
</context>

<tasks>

<task type="auto">
  <name>Task 1: golf/bike 키워드 수동 수집 (fresh 프로세스)</name>
  <files>data/curation.db</files>
  <action>사용자 승인 액션 ① 실행. 저장소 루트에서 FRESH 프로세스로 `.venv/bin/python3 pipelines/curation/auto_collector.py` 실행 — 새 프로세스는 현재 KEYWORD_MAP(2985개)을 로드하므로 stale 스케줄러(2924)와 달리 golf/bike 24개 키워드를 인지함. 실행 전 `.venv/bin/python3 -c "from pipelines.curation.keywords import KEYWORD_MAP; print(sum(len(v) for v in KEYWORD_MAP.values()))"`로 2985 출력 확인 (fresh 로드 증거). auto_collector stdout에서 `[auto_collector] 전체 2985개` 라인을 확인할 것.

  제약: auto_collector는 1회 실행당 MAX_PER_RUN=5개 키워드만 수집하고 KEYWORD_MAP dict 순서상 golf/bike가 맨 끝이라 한 번으로 미달할 수 있음. 실행 후 골프/바이크 24개 키워드 중 products < 3건인 키워드가 남으면, 결정적 보충 수집을 inline 파이썬으로 수행: pipelines.curation.collector의 `_check_rate_limit()`을 매 키워드 호출 전에 검사(False면 즉시 중단 후 부분 상태 보고 — 쿠팡 파트너스 RATE LIMIT 제재 재발 방지가 최우선), 통과 시 `collect_keyword(kw)` 호출, 호출 간 0.5~1초 sleep (auto_collect.sh 패턴). 보충 대상 키워드 리스트는 정확히 이 24개: golf-hugo 12개(골프클럽, 골프드라이버, 아이언세트, 골프백, 골프거리측정기, 골프화, 퍼터, 골프공, 골프장갑, 골프의류, 골프우산, 스윙연습기), bike-hugo 12개(전기자전거, 접이식자전거, 로드자전거, MTB자전거, 미니벨로, 하이브리드자전거, 자전거헬멧, 전동킥보드, 자전거자물쇠, 자전거라이트, 아동자전거, 자전거거치대). 각 키워드가 products ≥ 3건이 될 때까지 반복하되 최대 2회 통과로 제한, 미달 시 상세 보고.

  비파괴 원칙: 이 작업은 products 테이블에 INSERT OR REPLACE만 수행 — 스키마/설정/다른 테이블 불변. 쿠팡 API 제재 시 자동 재시도 금지(수동으로 해제 후 재시도 필요함을 보고).</action>
  <verify>
    <automated>.venv/bin/python3 -c "
import sqlite3
conn = sqlite3.connect('data/curation.db')
kws = ['골프클럽','골프드라이버','아이언세트','골프백','골프거리측정기','골프화','퍼터','골프공','골프장갑','골프의류','골프우산','스윙연습기','전기자전거','접이식자전거','로드자전거','MTB자전거','미니벨로','하이브리드자전거','자전거헬멧','전동킥보드','자전거자물쇠','자전거라이트','아동자전거','자전거거치대']
bad = [(k, conn.execute('SELECT COUNT(*) FROM products WHERE keyword=?', (k,)).fetchone()[0]) for k in kws if conn.execute('SELECT COUNT(*) FROM products WHERE keyword=?', (k,)).fetchone()[0] < 3]
print('미달 키워드:', bad if bad else '없음 — 24/24 통과')
raise SystemExit(1 if bad else 0)
"</automated>
  </verify>
  <done>24개 키워드 전부 products ≥ 3건. auto_collector 출력에 `전체 2985개` 포함. 쿠팡 API 제재 없음(부분 수집 시 상태 명확히 보고)</done>
</task>

<task type="auto">
  <name>Task 2: scheduler.py:420 catchup 시각 파싱 방어 가드 수정</name>
  <files>scheduler.py</files>
  <action>사용자 승인 액션 ③ — 유일한 production 코드 변경. `scheduler.py`의 `_catchup_missed_inner()` 시각 파싱 블록(417~422행)에서 무방비인 `h, m = map(int, str(t).split(":"))`을 방어 가드로 교체:

  1. `parts = str(t).split(":")`로 분리 후 `len(parts) != 2`이면 `logger.warning(f"CATCHUP: 잘못된 스케줄 시각 무시 — {blog_id} time={t!r}")` 로그 후 해당 time만 `continue` (전체 블로그 루프는 계속)
  2. `int()` 변환을 try/except ValueError로 감싸 동일한 warning + `continue`
  3. 유효한 "HH:MM" 값은 기존 시맨틱을 그대로 유지: `if h < now.hour or (h == now.hour and m <= now.minute): expected += 1`

  참고: malformed 값의 출처는 `config/blogs.d/tap.yaml`의 tap-blogger times `['06:00', 600, 840]` — unquoted `10:00`/`14:00`이 PyYAML sexagesimal int(600/840)로 로드됨. **tap.yaml은 수정하지 말 것** (사용자 결정: 가드 수정이 승인된 해법. config 수정은 catchup 외 다른 스케줄 경로 동작을 바꿀 수 있어 범위 밖).

  비파괴 원칙: 해당 블록 외 다른 라인/시그니처/동작 불변. 추가되는 것은 warning 로그와 skip뿐. 수정 후 수동으로 `catchup_missed()`나 `_catchup_missed_inner()`를 직접 실행하지 말 것 — 실제 발행을 트리거하므로 금지. end-to-end 검증은 Task 3의 재시작 후 로그 관찰로 수행.</action>
  <verify>
    <automated>.venv/bin/python3 -m py_compile scheduler.py && .venv/bin/python3 -c "import scheduler" && echo "import OK"</automated>
  </verify>
  <verify>
    <automated>.venv/bin/python3 -c "
import yaml
# 가드 시맨틱 repro — 실제 config 데이터로 예외 없음 + 정상값만 카운트 확인 [부분검증: 로직 미러링, 실제 함수 실행은 Task 3에서]
data = yaml.safe_load(open('config/blogs.d/tap.yaml'))
times = [b['schedule']['times'] for b in data['blogs'] if b.get('id') == 'tap-blogger'][0]
expected = 0
for t in times:
    parts = str(t).split(':')
    if len(parts) != 2:
        continue
    try:
        h, m = map(int, parts)
    except ValueError:
        continue
    if h < 10 or (h == 10 and m <= 24):
        expected += 1
assert expected == 1, expected  # '06:00'만 카운트, 600/840 int는 skip
print(f'가드 repro OK — tap-blogger expected={expected} (10:24 기준, malformed skip 확인)')
"</automated>
  </verify>
  <done>py_compile/import 통과. 가드 repro가 tap-blogger 실제 데이터(`['06:00', 600, 840]`)에서 예외 없이 '06:00'만 카운트. 다른 파일/라인 변경 없음 (git diff 확인)</done>
</task>

<task type="auto">
  <name>Task 3: 스케줄러 재시작 + end-to-end 검증</name>
  <files></files>
  <action>사용자 승인 액션 ② 실행. Task 2(가드)가 적용된 상태에서만 재시작할 것 — 그래야 재시작된 프로세스가 수정된 코드를 로드함.

  1. 재시작 전 현재 PID 기록: `launchctl list | rg com.5000.scheduler` (현재 54024)
  2. 재시작: `launchctl kickstart -k gui/$(id -u)/com.5000.scheduler` (launchd KeepAlive=true → 자동 재실행)
  3. 새 PID ≠ 기존 PID 확인
  4. 약 5~6분 대기 후 `/tmp/5000-scheduler.error.log`에서 재시작 시각 이후의 라인을 확인: (a) "ValueError"/"not enough values to unpack" 신규 발생 없음, (b) "CATCHUP:" 라인이 정상 흐름으로 나타남 — 가드가 tap-blogger malformed 시각을 skip하고 다른 블로그 catchup은 계속됨을 의미
  5. 매시간 :50 auto_collector 사이클이 관찰 창에 걸리면 해당 로그 라인에 `[auto_collector] 전체 2985개` 포함 확인 — 재시작된 스케줄러가 최신 KEYWORD_MAP 로드의 결정적 증거. 사이클이 안 걸렸으면 [부분검증]으로 기록 (2985 로드 자체는 Task 1의 fresh 실행 + KEYWORD_MAP 직접 확인으로 입증됨)
  6. stale lock 확인: `lsof data/.lock_golf-hugo` / `lsof data/.lock_bike-hugo` — flock 기반이라 파일 존재는 정상, 활성 홀더 프로세스가 있는지만 확인. 좀비 프로세스가 홀더면 강제 종료하지 말고 보고
  7. 최종 스모크 발행 (no_keyword 해소의 결정적 증거): `.venv/bin/python3 dispatcher.py golf-hugo` 후 `.venv/bin/python3 dispatcher.py bike-hugo` — 각각 success true 반환. 주의: 이는 실제 콘텐츠 1건씩을 정상 발행+배포하며, 스케줄러가 어차피 다음 슬롯에 수행할 동일 작업을 앞당기는 것 (AGENTS.md 배포 규칙 준수 — 반드시 dispatcher.py 경유, 수동 wrangler 금지). 실패 시 실패 사유가 no_keyword가 아닌지 로그 확인</action>
  <verify>
    <automated>launchctl list | rg com.5000.scheduler</automated>
  </verify>
  <verify>
    <automated>sleep 10; rg -n "ValueError|not enough values to unpack" /tmp/5000-scheduler.error.log | tail -3; echo "---"; rg -c "CATCHUP:" /tmp/5000-scheduler.error.log</automated>
  </verify>
  <verify>
    <automated>.venv/bin/python3 dispatcher.py golf-hugo 2>&1 | tail -5; echo "==="; .venv/bin/python3 dispatcher.py bike-hugo 2>&1 | tail -5</automated>
  </verify>
  <done>새 PID로 스케줄러 재시작. 재시작 후 catchup 라인 정상, ValueError 신규 0건. golf-hugo/bike-hugo 각각 success true (no_keyword 아님). auto_collector 로그에 전체 2985개 (사이클 미관찰 시 [부분검증] 명시)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coupang Partners API → auto_collector | 외부 API 응답/rate limit — 수동 수집 작업에서 제재 트리거 가능 |
| config YAML → scheduler catchup | 신뢰하지 않는 시각 포맷(mixed str/int)이 파싱 크래시 유발 |
| dispatcher.py → Cloudflare 배포 | 실제 발행 스모크 테스트 — 배포 규칙(AGENTS.md) 준수 필요 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260805-01 | DoS | Coupang API rate limit | mitigate | `_check_rate_limit()` 사전 검사 + 키워드 간 sleep + 제재 시 자동 재시도 금지, 부분 상태 보고 |
| T-260805-02 | Tampering | scheduler.py catchup 파싱 | mitigate | 방어 가드(split 길이 + ValueError try/except)로 malformed 값 skip, 유효값 시맨틱 불변, py_compile+import+repro 검증 |
| T-260805-03 | Tampering | config/blogs.d/tap.yaml | accept | 사용자 결정으로 가드만 수정 — config 수정은 catchup 외 경로 동작 변경 위험, 범위 밖 |
| T-260805-SC | Tampering | npm/pip installs | accept | 신규 패키지 설치 없음 — 공급망 표면 없음 |
</threat_model>

<verification>
- Task 1: golf/bike 24개 키워드 products ≥ 3건 (DB 쿼리, 자동 검증)
- Task 2: py_compile + import + 가드 repro (실제 config 데이터, 부분검증)
- Task 3: 새 PID, 재시작 후 ValueError 0건 + CATCHUP 정상, dispatcher 스모크 발행 success, auto_collector 전체 2985개
- AGENTS.md 보고 규칙: 완료 보고는 3분법([검증됨]/[부분검증]/[검증불가]) + 산출 근거 + 잔존 위험 섹션 필수
</verification>

<success_criteria>
- [ ] curation.db products에 golf-hugo 12개 + bike-hugo 12개 키워드 전부 ≥ 3건
- [ ] 스케줄러가 새 PID로 가동 중, catchup 크래시(ValueError) 재발 0건
- [ ] 재시작된 스케줄러의 auto_collector가 2985개 키워드 로드
- [ ] golf-hugo/bike-hugo dispatcher 발행 success (no_keyword 소멸)
- [ ] 변경 파일: scheduler.py만 (data/curation.db는 데이터 변경)
</success_criteria>

<output>
작업 완료 시 `.planning/quick/260805-d7c-root-title-fix-no-keyword-golf-bike/260805-d7c-SUMMARY.md` 작성
</output>

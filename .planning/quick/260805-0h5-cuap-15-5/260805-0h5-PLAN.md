---
quick_id: 260805-0h5
slug: cuap-15-5
task: cuap 15개 블로그중 오늘 만든 5개 블로그가 클라우드 플레어 설정이 완료가 안됐다. 배포까지 완료해줘.
---

<objective>
CUAP 신규 5개 블로그(massage/car/homeappliance/golf/bike-hugo)의 Cloudflare 설정을 완료하고 배포한다.

- massage/car-hugo: Worker는 이미 배포됨(2026-08-04) — custom domain 바인딩 + DNS 레코드만 누락 → wrangler.toml에 routes 추가 후 재배포
- homeappliance/golf/bike-hugo: 전혀 설정 안 됨(Worker/Pages/DNS 모두 없음, public/ 비어 있음) — Hugo 빌드 + Worker 배포 + custom domain 바인딩 전체 수행
- 검증된 레퍼런스: baby-hugo의 `routes = [{ pattern = "{domain}", custom_domain = true }]` 패턴 — 배포 시 custom domain 바인딩 + DNS 레코드를 자동 생성 (유일하게 custom_domain=true를 쓰는 기존 블로그, 동작 확인됨)
- 배포는 반드시 OAuth profile(hugh79757) 사용: `env -u CLOUDFLARE_API_TOKEN wrangler deploy --config wrangler.toml` — CLOUDFLARE_API_TOKEN 설정 절대 금지
- 사이트 디렉토리: `/Users/twinssn/Projects/CUAP/{blog}-hugo` (cuap.yaml의 site_path `/Users/twinssn/Projects/cuap/...` 소문자는 대소문자 무시 FS라 동일 디렉토리)
- 모든 Hugo 빌드: `HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes hugo --gc --minify`
- 완료 보고는 AGENTS.md Section 2 3분법([검증됨]/[부분검증]/[검증불가] + 근거)으로 작성
</objective>

<tasks>

<task type="auto">
  <name>Task 1: massage/car-hugo custom domain 바인딩 추가 + 재배포</name>
  <files>/Users/twinssn/Projects/CUAP/massage-hugo/wrangler.toml, /Users/twinssn/Projects/CUAP/car-hugo/wrangler.toml</files>
  <action>
  massage-hugo(도메인: massage.informationhot.kr)와 car-hugo(도메인: car.informationhot.kr) 각각에 대해:

  1. wrangler.toml 편집: `compatibility_date = "2025-07-31"` 줄과 `[assets]` 블록 사이에 routes 블록 삽입 — baby-hugo와 정확히 동일한 형식:
     ```
     routes = [
       { pattern = "massage.informationhot.kr", custom_domain = true }
     ]
     ```
     (car는 "car.informationhot.kr" 사용. name/compatibility_date/[assets]는 변경하지 않는다.)
  2. Hugo 재빌드: `HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes hugo --gc --minify` (workdir = 사이트 디렉토리). public/가 이미 존재하나 idempotent 재빌드로 현재 config와의 일치를 보장한다.
  3. 배포: `env -u CLOUDFLARE_API_TOKEN wrangler deploy --config wrangler.toml` (workdir = 사이트 디렉토리). 두 블로그는 순차 실행 — wrangler 동시 실행 금지. 배포 시 custom domain 바인딩 + DNS 레코드가 자동 생성된다.
  4. 실패 시: `Authentication error code: 10000`이면 `env -u CLOUDFLARE_API_TOKEN wrangler whoami`로 hugh79757 profile 확인 후 재시도 (cuap 디렉토리는 hugh79757에 이미 바인딩되어 있어야 정상 — 바인딩이 없으면 `env -u CLOUDFLARE_API_TOKEN wrangler auth activate hugh79757 /Users/twinssn/Projects/CUAP/{blog}-hugo`로 추가 후 재시도).
  </action>
  <verify>
  dig +short massage.informationhot.kr → Cloudflare IP (104.21.x.x 또는 172.67.x.x) 반환
  dig +short car.informationhot.kr → Cloudflare IP 반환
  curl -sI https://massage.informationhot.kr/ → 유효한 HTTP 응답 (200 또는 404 — Worker가 응답하면 OK. 000/연결실패는 실패)
  curl -sI https://car.informationhot.kr/ → 동일
  </verify>
  <done>두 도메인이 DNS에서 Cloudflare IP로 해석되고 HTTPS 요청에 Worker가 응답함. wrangler 배포 출력에 custom domain 생성 확인.</done>
</task>

<task type="auto">
  <name>Task 2: homeappliance/golf/bike-hugo 전체 설정 (빌드 + 배포)</name>
  <files>/Users/twinssn/Projects/CUAP/homeappliance-hugo/wrangler.toml, /Users/twinssn/Projects/CUAP/golf-hugo/wrangler.toml, /Users/twinssn/Projects/CUAP/bike-hugo/wrangler.toml</files>
  <action>
  homeappliance-hugo(→ homeappliance.informationhot.kr), golf-hugo(→ golf.informationhot.kr), bike-hugo(→ bike.informationhot.kr) 각각에 대해 — public/가 비어 있으므로 빌드부터 전체 수행:

  1. wrangler.toml 편집: Task 1과 동일하게 routes 블록 삽입 (`{ pattern = "{도메인}", custom_domain = true }`)
  2. Hugo 빌드: `HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes hugo --gc --minify` (workdir = 사이트 디렉토리) — public/ 생성 확인
  3. 배포: `env -u CLOUDFLARE_API_TOKEN wrangler deploy --config wrangler.toml` (workdir = 사이트 디렉토리). 세 블로그 순차 실행 — wrangler 동시 실행 금지.
  4. 빌드 또는 배포 실패 시 Task 1의 4번과 동일한 인증 처리 후 재시도. 조용한 실패 금지 — 실패는 명확한 에러와 함께 보고.
  </action>
  <verify>
  dig +short homeappliance.informationhot.kr → Cloudflare IP
  dig +short golf.informationhot.kr → Cloudflare IP
  dig +short bike.informationhot.kr → Cloudflare IP
  curl -sI https://homeappliance.informationhot.kr/ (및 golf/bike) → 각각 유효한 HTTP 응답 (000/연결실패 아님)
  </verify>
  <done>세 도메인이 DNS 해석되고 HTTPS 응답함. homeappliance/golf/bike-hugo Worker가 Cloudflare에 존재하고 custom domain 바인딩 + DNS 레코드가 자동 생성됨.</done>
</task>

<task type="auto">
  <name>Task 3: dispatcher.py WORKERS_BLOGS에 5개 신규 블로그 추가</name>
  <files>/Users/twinssn/Projects/5000/dispatcher.py</files>
  <action>
  dispatcher.py의 WORKERS_BLOGS set(약 line 524-530, 현재 6개: health/pet/kitchen/beauty/camping/baby)에 "massage-hugo", "car-hugo", "homeappliance-hugo", "golf-hugo", "bike-hugo" 5개를 추가한다.

  이유: 이 5개는 Worker static assets 형식(wrangler.toml `[assets]`) 사이트로 이번에 Worker 경로로 배포된다. WORKERS_BLOGS에 없으면 향후 dispatcher가 Pages 경로(`wrangler pages deploy public --project-name`)로 잘못 배포하여 Pages 프로젝트가 생성되거나 배포가 실패한다. 추가 시 `_build_and_deploy_central()` → `wrangler deploy --config wrangler.toml`(Worker 경로, CLOUDFLARE_API_TOKEN 자동 제거) 경로를 타게 된다.

  수정 범위: WORKERS_BLOGS set에 5개 추가만 — 기존 엔트리, 상수명, 주변 로직은 건드리지 않는다 (additive, non-destructive).
  </action>
  <verify>
  python3 -m py_compile dispatcher.py → 컴파일 통과
  grep -n '"massage-hugo"\|"car-hugo"\|"homeappliance-hugo"\|"golf-hugo"\|"bike-hugo"' dispatcher.py → 5개 ID 모두 발견, 위치는 WORKERS_BLOGS 블록(약 line 524-532) 내
  최종 5개 도메인 일괄 확인: for d in massage car homeappliance golf bike; do curl -s -o /dev/null -w "$d:%{http_code} " https://$d.informationhot.kr/; done → 5개 모두 유효한 HTTP 상태 코드
  </verify>
  <done>WORKERS_BLOGS에 5개 신규 블로그 ID가 포함되고 dispatcher.py 컴파일이 통과한다. 5개 도메인(massage/car/homeappliance/golf/bike.informationhot.kr) 전부 HTTPS로 응답한다.</done>
</task>

</tasks>

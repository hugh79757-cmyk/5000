---
phase: 11-coupang-import-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/coupang_car.py
  - shared/coupang_senior.py
autonomous: true
requirements:
  - BUG-01
must_haves:
  truths:
    - "content_enhancer.py imports without ImportError for both coupang_car and coupang_senior"
    - "Coupang product injection works for rank-hugo (car pipeline) at runtime"
    - "Coupang product injection works for rap-* blogs (senior pipeline) at runtime"
  artifacts:
    - path: shared/coupang_car.py
      provides: "inject_coupang() standalone function"
      contains: "def inject_coupang"
    - path: shared/coupang_senior.py
      provides: "inject_coupang() standalone function"
      contains: "def inject_coupang"
  key_links:
    - from: shared/publishers/content_enhancer.py
      to: shared/coupang_car.py
      via: "from shared.coupang_car import inject_coupang"
      pattern: "from shared.coupang_car import inject_coupang"
    - from: shared/publishers/content_enhancer.py
      to: shared/coupang_senior.py
      via: "from shared.coupang_senior import inject_coupang"
      pattern: "from shared.coupang_senior import inject_coupang"
---

<objective>
Fix `ImportError: cannot import name 'inject_coupang' from 'shared.coupang_car'` in content_enhancer.py.

Purpose: `content_enhancer.py:_insert_coupang()` tries to import `inject_coupang` from `shared.coupang_car` (and `shared.coupang_senior`), but neither module had a standalone function of that name — only a class `CoupangCar`/`CoupangSenior` with instance methods.

This caused every `rank-hugo` publish to fail at the content enhancement stage, with catchup retries amplifying the same error 4x via Telegram.

Output: Working import path + verified runtime injection.
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@shared/coupang_car.py
@shared/coupang_senior.py
@shared/publishers/content_enhancer.py

**Current state:** Both files already have `inject_coupang` added. Plan verifies + commits.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify inject_coupang exists and imports correctly in both modules</name>
  <files>shared/coupang_car.py, shared/coupang_senior.py</files>
  <action>
    Confirm both files have a standalone `inject_coupang(body_md, segment="", fuel_type="", blog_cfg=None)` function that:
    - Returns `body_md` unchanged when `body_md` is falsy or no products found
    - Creates a `CoupangCar`/`CoupangSenior` instance
    - Calls `get_car_product_links(segment, fuel_type)` / `get_senior_product_links(category)`
    - Appends product markdown to `body_md` with `\n\n` separator
    - Gracefully returns `body_md` if Coupang API is unconfigured (no crash)
  </action>
  <verify>
    <automated>.venv/bin/python3 -c "from shared.coupang_car import inject_coupang; from shared.coupang_senior import inject_coupang; print('OK')"</automated>
  </verify>
  <done>Both imports succeed, function signatures match content_enhancer call sites</done>
</task>

<task type="auto">
  <name>Task 2: Verify content_enhancer integration works end-to-end</name>
  <files>shared/publishers/content_enhancer.py</files>
  <action>
    Test that `_insert_coupang` can be called with both the car and senior code paths without ImportError:
    - `blog_cfg={'id': 'rank-hugo'}` with `segment='SUV'` → uses coupang_car
    - `blog_cfg={'id': 'rap-senior'}` → uses coupang_senior
    Both should return gracefully (Coupang may not be configured in test env).
  </action>
  <verify>
    <automated>
      .venv/bin/python3 -c "
from shared.publishers.content_enhancer import _insert_coupang
r1 = _insert_coupang('test', segment='SUV', blog_cfg={'id': 'rank-hugo'})
assert r1 == 'test' or '차량' in r1, f'car path failed: {r1}'
r2 = _insert_coupang('test', blog_cfg={'id': 'rap-senior'})
assert r2 == 'test' or '어르신' in r2, f'senior path failed: {r2}'
print('All paths OK')
"
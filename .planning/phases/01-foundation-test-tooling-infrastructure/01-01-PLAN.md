# Plan 01-01: Tooling & Test Infrastructure

**Phase:** 1 — Foundation: Test & Tooling
**Goal:** Set up pytest, ruff, mypy, and CI pipeline
**Mode:** mvp

## Objective

Create the tooling foundation: `pyproject.toml` with all configs, `tests/` directory structure, CI workflow, and verify Python 3.14 compatibility. This enables all subsequent test work.

## Requirements

- STB-04: ruff + mypy configured and passing
- STB-05: Python 3.14 compat verified
- STB-06: CI pipeline with test + lint steps

## Tasks

### 1.1 Create `pyproject.toml`
- Add `[tool.pytest.ini_options]` with test paths and coverage config
- Add `[tool.ruff]` with all rules enabled, unsafe-fix, per-file ignores
- Add `[tool.mypy]` with strict mode, target modules
- Add `[tool.coverage.run]` with per-module thresholds (70% minimum)

### 1.2 Update `requirements.txt`
- Add `pytest>=8.0`, `pytest-cov>=5.0`

### 1.3 Create `tests/` directory
- `tests/__init__.py` (empty)
- `tests/conftest.py` with shared fixtures if needed
- `tests/shared/__init__.py` (empty)
- `tests/shared/test_validators.py` (placeholder)
- `tests/shared/test_humanizer.py` (placeholder)
- `tests/shared/test_telegram_notifier.py` (placeholder)

### 1.4 Create CI workflow (`.github/workflows/ci.yml`)
- Trigger: push to main
- Steps: ruff check → mypy check → pytest with coverage
- Coverage enforcement: fail if below per-module 70% threshold
- Run on ubuntu-latest with Python 3.14 setup

### 1.5 Remove startup compat check
- Remove `check_package_imports()` from `dispatcher.py:31`
- Verify `dispatcher.py` imports still work

### 1.6 Run initial lint + fix
- `ruff check --fix --unsafe-fixes .`
- Fix remaining issues
- `mypy shared/ dispatcher.py scheduler.py` — add `# type: ignore` where needed

### 1.7 Verify CI passes
- Push to trigger CI
- Review results, fix any issues

## Verification

- [ ] `pytest tests/` runs and passes
- [ ] `ruff check .` passes with no errors
- [ ] `mypy shared/ dispatcher.py scheduler.py` passes
- [ ] `.github/workflows/ci.yml` runs on push
- [ ] `check_package_imports()` removed from dispatcher.py

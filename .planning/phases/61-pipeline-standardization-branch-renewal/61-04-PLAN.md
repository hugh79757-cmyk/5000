---
phase: 61-pipeline-standardization-branch-renewal
plan: 04
type: execute
wave: 4
depends_on: [61-03]
files_modified:
  - "/Users/twinssn/Projects/5000/pipelines/car/fetcher.py"
  - "/Users/twinssn/Projects/5000/pipelines/car/enrich.py"
  - "/Users/twinssn/Projects/5000/pipelines/car/validator.py"
  - "/Users/twinssn/Projects/5000/pipelines/car/writer.py"
  - "/Users/twinssn/Projects/5000/pipelines/car/pipeline.py"
  - "/Users/twinssn/Projects/5000/tests/shared/test_skeleton_car.py"
autonomous: true
requirements: [61-D, D-01, D-06]
user_setup: []
must_haves:
  truths:
    - "car (CAP, 8 blogs) exposes the standard 6-module skeleton (pipeline/fetcher/topic_manager/writer/enrich/validator)"
    - "car's run(cfg) return contract is unchanged (dict, behavior preserved)"
    - "car DB path wired through shared/db.py (no data moved)"
    - "All existing car tests and full suite stay green"
  artifacts:
    - path: "pipelines/car/fetcher.py"
      provides: "Thin fetcher wrapper (NEW — car currently has no fetcher module)"
    - path: "pipelines/car/enrich.py"
      provides: "Thin enrich wrapper"
    - path: "pipelines/car/validator.py"
      provides: "Thin validator wrapper"
    - path: "pipelines/car/writer.py"
      provides: "Thin writer wrapper (delegates to existing writing logic)"
  key_links:
    - from: "pipelines/car/pipeline.py"
      to: "pipelines/car/topic_manager.py"
      via: "existing topic_manager preserved and wired"
    - from: "pipelines/car/pipeline.py"
      to: "shared/db.py"
      via: "DB path centralized"
---

<objective>
Stage D1 — **CAP(car, 8개) 전환.** rollout의 첫 단계. car 분기를 표준 6-모듈 골격(D-01)에 정렬한다. car는 현재 `pipeline.py`+`topic_manager.py`만 있어 fetcher/writer/enrich/validator가 부재 → 얇은 wrapper로 추가.

Purpose: pilot(61-03)에서 검증된 패턴을 첫 rollout 분기에 적용. 구조 변경과 동작 변경 분리 커밋 원칙 유지.

Output: car 6-모듈 골격 완비, DB path `shared/db.py` 배선.
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/twinssn/Projects/5000/.planning/STATE.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-CONTEXT.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-RESEARCH.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-03-SUMMARY.md
@/Users/twinssn/Projects/5000/docs/PIPELINE-STANDARD.md
@/Users/twinssn/Projects/5000/pipelines/car/pipeline.py
@/Users/twinssn/Projects/5000/pipelines/car/topic_manager.py
@/Users/twinssn/Projects/5000/pipelines/car/title_engine.py
@/Users/twinssn/Projects/5000/pipelines/car/data_builder.py

All changes additive. **Existing pipeline.py run(blog_cfg) behavior must NOT change** — only wrapper additions + DB path wiring.

<interfaces>
From `pipelines/car/` (verified):
- `pipeline.py`: `def run(blog_cfg)` at L73 — dict-returning
- `topic_manager.py`: exists (present in skeleton)
- `title_engine.py`, `data_builder.py`, `daily_refresh.py` exist
- MISSING: `fetcher.py`, `writer.py`, `enrich.py`, `validator.py`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 61-04-1: Add thin fetcher/writer/enrich/validator wrappers to car</name>
  <files>
    /Users/twinssn/Projects/5000/pipelines/car/fetcher.py
    /Users/twinssn/Projects/5000/pipelines/car/writer.py
    /Users/twinssn/Projects/5000/pipelines/car/enrich.py
    /Users/twinssn/Projects/5000/pipelines/car/validator.py
  </files>
  <action>
    **Pure additive wrapper creation (D-01).** Add four thin wrapper modules to `pipelines/car/`. Each must DELEGATE to existing car logic or shared/ code — do NOT rewrite generation logic. Behavior unchanged.

    **`pipelines/car/fetcher.py`** (NEW): data fetching. Inspect `pipelines/car/data_builder.py` / `daily_refresh.py` for the data-source logic. Create `fetch_data(cfg)` delegating to the existing builder/refresh code (import + passthrough). If data fetching is embedded in pipeline.py, delegate to that code path.

    **`pipelines/car/writer.py`** (NEW): content writing. car uses `title_engine.py` + shared writing. Create `write_article(cfg, topic) -> dict` delegating to the existing generation path (title_engine + shared/ai_writer or pipeline inline). Import and delegate — do NOT duplicate.

    **`pipelines/car/enrich.py`** (NEW): enrichment. Create `enrich_content(cfg, body_md) -> body_md` delegating to existing shared/entity_linker or a passthrough if car has no enrichment step.

    **`pipelines/car/validator.py`** (NEW): validation. Create `validate_post(cfg, article) -> (ok: bool, errors: list[str])` delegating to `shared/post_validator.py` / `shared/validators.py`, or passthrough `(True, [])` if car uses no standalone validator.

    Each wrapper: module docstring noting Phase 61 standard skeleton membership.

    **Commit separation (D-08):** STRUCTURE addition only. Commit as:
    `feat(phase-61): add car standard-skeleton wrappers (fetcher/writer/enrich/validator)`
  </action>
  <verify>
    <automated>python3 -c "import ast; [ast.parse(open('/Users/twinssn/Projects/5000/pipelines/car/'+f).read()) for f in ['fetcher.py','writer.py','enrich.py','validator.py']]; print('AST OK')"</automated>
    <automated>python3 -c "import sys; sys.path.insert(0,'/Users/twinssn/Projects/5000'); from pipelines.car import fetcher, writer, enrich, validator; print('IMPORT OK', hasattr(fetcher,'fetch_data'), hasattr(writer,'write_article'), hasattr(enrich,'enrich_content'), hasattr(validator,'validate_post'))"</automated>
  </verify>
  <done>pipelines/car/{fetcher,writer,enrich,validator}.py exist as thin wrappers delegating to existing logic. car now exposes the 6-module skeleton. Behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 61-04-2: Wire car DB path to shared/db.py + extend skeleton test</name>
  <files>
    /Users/twinssn/Projects/5000/pipelines/car/pipeline.py
    /Users/twinssn/Projects/5000/tests/shared/test_skeleton_car.py
  </files>
  <action>
    **Part A — DB-path wiring (D-06, additive):**
    In `pipelines/car/pipeline.py`, replace the inline `*_DB_PATH` constant usage with `shared.db.get_db_path(...)`:
    - Add `from shared.db import get_db_path` at import site.
    - Replace the inline path constant reference with `get_db_path('car')`.
    - **Do NOT move/delete DB files.** If the path resolves identically, publish behavior is identical.
    - Keep the original constant as a fallback if replacement risks behavior change (non-destructive overlay).

    **Commit separation (D-08):** STRUCTURE/utility change. Commit:
    `refactor(phase-61): centralize car DB path via shared/db.py`

    **Part B — extend skeleton test (D-01):**
    Add `test_car_skeleton` to `tests/shared/test_skeleton_car.py` asserting `pipelines.car` has pipeline/fetcher/topic_manager/writer/enrich/validator module files (via pkgutil.iter_modules). Do not invoke run() (may need live services).

    **Commit:** `test(phase-61): add car skeleton presence test`
  </action>
  <verify>
    <automated>pytest tests/shared/test_skeleton_car.py -x --no-cov 2>&1 | tail -5</automated>
    <automated>cd /Users/twinssn/Projects/5000 && python3 -c "import sys; sys.path.insert(0,'.'); from shared.db import get_db_path; print('car db:', get_db_path('car'))"</automated>
  </verify>
  <done>car pipeline.py uses shared/db.py for DB path (files unmoved, behavior unchanged). test_skeleton_car.py includes car and passes.</done>
</task>

<task type="auto">
  <name>Task 61-04-3: Rollout regression gate</name>
  <files>
    /Users/twinssn/Projects/5000/pipelines/car/
  </files>
  <action>
    Run full suite to confirm zero regression:
    `pytest -q`

    Confirm git log shows the car commits (wrapper additions + db wiring + skeleton test) as separate commits. Confirm no `data/*.db` changed.
  </action>
  <verify>
    <automated>pytest -q 2>&1 | tail -8</automated>
    <automated>git log --oneline -5 | grep "phase-61"</automated>
  </verify>
  <done>Full suite green after car conversion. CAP branch standardized skeleton complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wrapper module → existing car logic | Thin delegation |
| shared/db.py → car DB | Path resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-04-01 | Tampering | wrapper delegation | mitigate | Wrappers import+delegate existing functions; import smoke test |
| T-61-04-02 | Denial of Service | DB path | mitigate | shared/db.py resolves identical existing path; no data move |
| T-61-04-SC | Tampering | npm/pip installs | accept | No new package installs |
</threat_model>

<verification>
### Plan 61-04 verification

1. [ ] car has 6 modules — `ls pipelines/car/*.py` includes fetcher/writer/enrich/validator
2. [ ] Wrappers import cleanly — `python3 -c "from pipelines.car import fetcher,writer,enrich,validator"`
3. [ ] DB path centralized — `grep -rn "get_db_path" pipelines/car/pipeline.py`
4. [ ] No data files changed — `git status --porcelain | grep "data/"` empty
5. [ ] Skeleton test passes — `pytest tests/shared/test_skeleton_car.py -q`
6. [ ] Full suite green — `pytest -q`
</verification>

<success_criteria>
1. ✅ car(CAP, 8) 6-모듈 골격 완비
2. ✅ 부재 모듈 얇은 wrapper로 추가, 기존 로직 위임
3. ✅ DB path `shared/db.py` 배선 (무이동, 무동작변경)
4. ✅ 구조/동작 분리 커밋
5. ✅ 기존 테스트 + skeleton 테스트 green
</success_criteria>

<output>
Create `.planning/phases/61-pipeline-standardization-branch-renewal/61-04-SUMMARY.md` when done
</output>

---
phase: 61-pipeline-standardization-branch-renewal
plan: 08
type: execute
wave: 6
depends_on: [61-02]
files_modified:
  - "/Users/twinssn/Projects/5000/dispatcher.py"
  - "/Users/twinssn/Projects/5000/tests/integration/test_subprocess_routing.py"
autonomous: true
requirements: [61-E, D-03, D-04]
user_setup: []
must_haves:
  truths:
    - "dispatcher.py's _run_stap and _run_tap_subprocess delegate to shared/subprocess_runner.run_subprocess (deduplicated ~400 lines)"
    - "STAP stock and TAP run_publish still return the same dict contract as before (external projects stay subprocess-isolated)"
    - "External TAP/STAP repos are NOT merged into 5000 (contract alignment only, D-04)"
    - "CLOUDFLARE_API_TOKEN removal behavior preserved in the runner"
    - "dispatcher registry tests and full suite stay green"
  artifacts:
    - path: "dispatcher.py"
      provides: "Thin STAP/TAP subprocess callers via subprocess_runner"
      contains: "run_subprocess"
    - path: "tests/integration/test_subprocess_routing.py"
      provides: "Verifies dispatcher routes STAP/TAP through subprocess_runner"
  key_links:
    - from: "dispatcher.py:_run_stap"
      to: "shared/subprocess_runner.py:run_subprocess"
      via: "module delegated to shared runner"
    - from: "dispatcher.py:_run_tap_subprocess"
      to: "shared/subprocess_runner.py:run_subprocess"
      via: "module delegated to shared runner"
---

<objective>
Stage E — **외부 분기 정합 (STAP/TAP).** `dispatcher.py`의 `_run_stap`(L380)과 `_run_tap_subprocess`(L504)가 공유하는 ~400줄 subprocess 격리 패턴을 `shared/subprocess_runner.run_subprocess`(61-02 생성)로 위임하도록 재작성한다.

**중요 (D-04):** STAP/TAP 저장소는 **물리적 병합하지 않는다.** 계약 정합(subprocess 격리 + 동일 dict 반환)만 수행. 외부 프로젝트는 계속 subprocess로 격리 실행된다.

Purpose: 400줄 중복 제거 + 외부 분기가 5000 표준 계약(D-02)에 정합. 모든 실행 방식이 `shared/subprocess_runner`로 통일됨.

Output: `_run_stap`/`_run_tap_subprocess` → `run_subprocess` 위임 (thin caller), 라우팅 통합 테스트.
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/twinssn/Projects/5000/.planning/STATE.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-CONTEXT.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-RESEARCH.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-02-SUMMARY.md
@/Users/twinssn/Projects/5000/docs/PIPELINE-STANDARD.md
@/Users/twinssn/Projects/5000/dispatcher.py
@/Users/twinssn/Projects/5000/shared/subprocess_runner.py

**CRITICAL — deploy path safety:** `shared/subprocess_runner.py` MUST preserve the CLOUDFLARE_API_TOKEN-removal behavior (pop from env before any wrangler subprocess). Verify this was implemented in 61-02; if not, this plan completes it. Do NOT hand-run wrangler anywhere (AGENTS.md §3).

<interfaces>
From `dispatcher.py` (verified):
- `_run_stap(stap_name, cfg)` at L380-440 — STAP subprocess runner
- `_run_tap_subprocess(cfg)` at L504-566 — TAP subprocess runner
- `STAP_PIPELINE_MAP` at L81 — blog→STAP pipeline mapping
- Both build a tempfile runner + venv python + JSON-parse — the ~400-line duplicated pattern

From `shared/subprocess_runner.py` (created in 61-02):
- `run_subprocess(project_root, venv_python, module_spec, run_callable='run', cfg=None, timeout=600, prefix='subproc') -> dict`
- Handles venv resolution, tempfile runner, inspect-bridged run()/run(cfg)/run_publish(), JSON parse, error/timeout/no-output dicts, CLOUDFLARE_API_TOKEN pop

External project entry points (verified):
- STAP: `pipelines.{stap_name}.pipeline.run(cfg)` — `STAP/pipelines/stock/pipeline.py:365`
- TAP: `app.run_publish()` — returns tuple `(bool, result, error)` — runner must convert to dict
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 61-08-1: Refactor _run_stap and _run_tap_subprocess to delegate to run_subprocess</name>
  <files>/Users/twinssn/Projects/5000/dispatcher.py</files>
  <action>
    **Deduplicate the ~400-line subprocess pattern (D-03).** Rewrite the bodies of `_run_stap` (L380-440) and `_run_tap_subprocess` (L504-566) in `dispatcher.py` to delegate to `shared/subprocess_runner.run_subprocess`. Keep the SAME external behavior (same dict return contract) — only the implementation is centralized.

    **`_run_stap(stap_name, cfg)` → delegate:**
    - Keep the `STAP_ROOT` existence check + `{success: False, reason: "stap_not_found"}` early return (or let run_subprocess handle root-missing).
    - Resolve `stap_root = STAP_ROOT`, `stap_python = os.path.join(stap_root, ".venv", "bin", "python3")` (fallback `sys.executable` if missing).
    - Call `run_subprocess(stap_root, stap_python, module_spec=f"pipelines.{stap_name}.pipeline", run_callable="run", cfg=cfg, timeout=600, prefix="stap")`.
    - Return the dict result. Preserve the STAP-specific reason prefixes (`stap_not_found`, `stap_timeout`, `stap_no_output`, `stap_error`, `stap_subprocess_error`) — ensure run_subprocess's `prefix` param yields these exact keys, or map them explicitly.
    - **Do NOT change the STAP_PIPELINE_MAP or how STAP is dispatched.**

    **`_run_tap_subprocess(cfg)` → delegate:**
    - Keep the `TAP_ROOT` existence check + `{success: False, reason: "tap_not_found"}` early return.
    - Resolve `tap_root = TAP_ROOT`, `tap_python = os.path.join(tap_root, "venv", "bin", "python3")` (fallback `sys.executable`).
    - Call `run_subprocess(tap_root, tap_python, module_spec="app", run_callable="run_publish", cfg=None, timeout=600, prefix="tap")`.
    - **TAP return is a tuple `(bool, result, error)`** (verified): the runner OR this caller must convert tuple → dict. Prefer handling tuple→dict inside `run_subprocess` (if not already) OR in a thin wrapper here. Result must be `{"success": bool, ...}`. Ensure the TAP-specific reason prefixes (`tap_not_found`, `tap_timeout`, `tap_error`) are preserved.
    - **Do NOT change TAP_ROOT resolution or TAP dispatch.**

    **Preserve:** All error/timeout/no-output dict keys that dispatcher and downstream rely on. The `CLOUDFLARE_API_TOKEN` removal MUST be active inside run_subprocess (verify; complete if missing).

    **Commit separation (D-08):** This is a BEHAVIOR-PRESERVING refactor (structure centralization, same contract). Commit:
    `refactor(phase-61): centralize STAP/TAP subprocess execution via shared/subprocess_runner`
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('/Users/twinssn/Projects/5000/dispatcher.py').read()); print('AST OK')"</automated>
    <automated>cd /Users/twinssn/Projects/5000 && python3 -c "
import inspect
src = inspect.getsource(__import__('dispatcher', fromlist=['_run_stap']))
" 2>/dev/null; grep -n "run_subprocess" /Users/twinssn/Projects/5000/dispatcher.py | head</automated>
    <automated>grep -n "_run_stap\|_run_tap_subprocess" /Users/twinssn/Projects/5000/dispatcher.py</automated>
  </verify>
  <done>_run_stap and _run_tap_subprocess bodies now delegate to run_subprocess. Same dict contract preserved. STAP/TAP reason prefixes preserved. No external repo merged.</done>
</task>

<task type="auto">
  <name>Task 61-08-2: Add STAP/TAP routing integration test + regression gate</name>
  <files>
    /Users/twinssn/Projects/5000/tests/integration/test_subprocess_routing.py
  </files>
  <action>
    **Part A — routing integration test (NEW):**
    Create `tests/integration/test_subprocess_routing.py` that verifies dispatcher's STAP/TAP routing through `shared/subprocess_runner` WITHOUT invoking live external services (mock the subprocess):

    - `test_stap_delegates_to_run_subprocess`: patch `shared.subprocess_runner.run_subprocess` and call `dispatcher._run_stap('stock', {...})` — assert it calls `run_subprocess` with module_spec `pipelines.stock.pipeline`, run_callable `run`, prefix `stap`.
    - `test_tap_delegates_to_run_subprocess`: patch `run_subprocess` and call `dispatcher._run_tap_subprocess({...})` — assert module_spec `app`, run_callable `run_publish`, prefix `tap`.
    - `test_tap_tuple_result_converted_to_dict`: with `run_subprocess` returning a TAP tuple `(True, {...}, None)`, assert `_run_tap_subprocess` (or the runner) returns a dict `{"success": True, ...}` (verify tuple→dict normalization path).
    - `test_run_subprocess_strips_cf_token`: assert the runner pops `CLOUDFLARE_API_TOKEN` from env (mock env + assert it's removed before subprocess).
    - `test_stap_not_found_reason`: missing STAP_ROOT → `{success: False, reason: "stap_not_found"}`.

    **Part B — regression gate:**
    Run the new test + the dispatcher registry test + full suite:
    `pytest tests/integration/test_subprocess_routing.py tests/test_dispatcher_registry.py -x --no-cov`
    `pytest -q`

    Confirm git log shows the refactor + test as separate commits.
  </action>
  <verify>
    <automated>pytest tests/integration/test_subprocess_routing.py -x --no-cov 2>&1 | tail -6</automated>
    <automated>pytest tests/test_dispatcher_registry.py -x --no-cov 2>&1 | tail -4</automated>
    <automated>pytest -q 2>&1 | tail -8</automated>
  </verify>
  <done>test_subprocess_routing.py added and passes (STAP/TAP delegate to run_subprocess, tuple→dict, token strip, not_found). Dispatcher registry + full suite green. Stage E complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatcher → shared/subprocess_runner → external STAP/TAP venv subprocess | Untrusted external code runs isolated; stdout parsed for dict |
| subprocess → wrangler deploy path | CLOUDFLARE_API_TOKEN hazard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-08-01 | Information Disclosure | run_subprocess env | mitigate | CLOUDFLARE_API_TOKEN popped before any wrangler subprocess (mirror deploy.py); test asserts strip |
| T-61-08-02 | Tampering | module_spec injection | mitigate | module_spec from controlled STAP_PIPELINE_MAP / TAP constant, not raw blog_id concat |
| T-61-08-03 | Tampering | TAP tuple→dict | mitigate | Tuple converted deterministically; routing test asserts dict shape |
| T-61-08-04 | Denial of Service | subprocess timeout | mitigate | 600s timeout + runner unlink in finally; reason prefix preserved |
| T-61-08-SC | Tampering | npm/pip installs | accept | No new package installs |
</threat_model>

<verification>
### Plan 61-08 verification

1. [ ] dispatcher AST parses — `python3 -c "import ast; ast.parse(open('dispatcher.py').read())"`
2. [ ] `_run_stap`/`_run_tap_subprocess` contain `run_subprocess` — `grep "run_subprocess" dispatcher.py`
3. [ ] STAP/TAP reason prefixes preserved — `grep "stap_not_found\|tap_not_found" dispatcher.py`
4. [ ] External repos NOT merged — `git status` shows no new files under any external project path
5. [ ] Routing test passes — `pytest tests/integration/test_subprocess_routing.py -q`
6. [ ] Full suite green — `pytest -q`
</verification>

<success_criteria>
1. ✅ `_run_stap`/`_run_tap_subprocess` → `run_subprocess` 위임 (400줄 중복 제거)
2. ✅ STAP/TAP 동일 dict 계약 유지 (external repos 미병합)
3. ✅ CLOUDFLARE_API_TOKEN 제거 동작 보존
4. ✅ 라우팅 통합 테스트 green
5. ✅ 전체 스위트 green, 구조/동작 분리 커밋
</success_criteria>

<output>
Create `.planning/phases/61-pipeline-standardization-branch-renewal/61-08-SUMMARY.md` when done
</output>

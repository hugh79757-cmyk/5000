---
phase: 61-pipeline-standardization-branch-renewal
plan: 05
type: execute
wave: 4
depends_on: [61-03]
files_modified:
  - "/Users/twinssn/Projects/5000/pipelines/travel/topic_manager.py"
  - "/Users/twinssn/Projects/5000/pipelines/travel/enrich.py"
  - "/Users/twinssn/Projects/5000/pipelines/travel/validator.py"
  - "/Users/twinssn/Projects/5000/pipelines/travel/pipeline.py"
  - "/Users/twinssn/Projects/5000/tests/shared/test_skeleton_travel.py"
autonomous: true
requirements: [61-D, D-01, D-06]
user_setup: []
must_haves:
  truths:
    - "travel (8 blogs) exposes the standard 6-module skeleton (pipeline/fetcher/topic_manager/writer/enrich/validator)"
    - "travel run(cfg) now returns a dict, never None — _run_single None result normalized to {success:False, reason:'no_result'} in a wrapper (behavior preserved via dispatcher fallback)"
    - "travel DB path wired through shared/db.py (no data moved)"
    - "All existing travel tests and full suite stay green"
  artifacts:
    - path: "pipelines/travel/topic_manager.py"
      provides: "Thin topic_manager wrapper (NEW — travel lacks topic_manager)"
    - path: "pipelines/travel/enrich.py"
      provides: "Thin enrich wrapper"
    - path: "pipelines/travel/validator.py"
      provides: "Thin validator wrapper"
  key_links:
    - from: "pipelines/travel/pipeline.py"
      to: "pipelines/travel/_run_single"
      via: "None result wrapped to no_result dict in run(cfg)"
    - from: "pipelines/travel/pipeline.py"
      to: "shared/db.py"
      via: "DB path centralized"
---

<objective>
Stage D2 — **travel(8개) 전환.** travel 분기를 표준 골격(D-01)에 정렬한다. travel은 `run(cfg)->None` 가능(연구 Pitfall 3)하므로, **표준 dict 계약**에 맞게 None을 `{success:False, reason:'no_result'}`로 정규화하는 wrapper를 추가한다.

Purpose: None-반환 레거시를 표준 계약으로 정규화하는 패턴을 적용. dispatcher의 기존 `or {"success": False}` fallback(L407)은 유지하되, 모듈 계약은 dict-only로 통일.

Output: travel 6-모듈 골격 완비, run(cfg) None→dict 정규화, DB path `shared/db.py` 배선.
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/twinssn/Projects/5000/.planning/STATE.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-CONTEXT.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-RESEARCH.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-03-SUMMARY.md
@/Users/twinssn/Projects/5000/docs/PIPELINE-STANDARD.md
@/Users/twinssn/Projects/5000/pipelines/travel/pipeline.py
@/Users/twinssn/Projects/5000/pipelines/travel/fetcher.py
@/Users/twinssn/Projects/5000/pipelines/travel/writer.py

All changes additive. **Existing travel run()/run(cfg) behavior preserved** — the None→dict normalization is a wrapper that matches what dispatcher already does (fallback `or {"success": False}`), so live behavior is unchanged.

<interfaces>
From `pipelines/travel/` (verified):
- `pipeline.py`: `def run(cfg)` at L413 — delegates to `_run_single` which returns None on quota/no-data (RESEARCH Pitfall 3)
- `fetcher.py`, `writer.py`, `area_codes.py` exist
- MISSING: `topic_manager.py`, `enrich.py`, `validator.py`
- travel DB file: `data/travel-en.db` (per db_paths.py / pipeline)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 61-05-1: Add thin topic_manager/enrich/validator wrappers to travel</name>
  <files>
    /Users/twinssn/Projects/5000/pipelines/travel/topic_manager.py
    /Users/twinssn/Projects/5000/pipelines/travel/enrich.py
    /Users/twinssn/Projects/5000/pipelines/travel/validator.py
  </files>
  <action>
    **Pure additive wrapper creation (D-01).** Add three thin wrapper modules to `pipelines/travel/`. Each must DELEGATE to existing travel logic or shared/ code — do NOT rewrite. Behavior unchanged.

    **`pipelines/travel/topic_manager.py`** (NEW): topic selection. Inspect `pipelines/travel/pipeline.py` / `fetcher.py` for the topic/region selection logic (e.g., heritage/topic fetch + sigungu guard). Create `select_topic(cfg)` delegating to the existing selection path.

    **`pipelines/travel/enrich.py`** (NEW): enrichment. Create `enrich_content(cfg, body_md) -> body_md` delegating to existing shared/entity_linker or a passthrough if travel has no enrichment step.

    **`pipelines/travel/validator.py`** (NEW): validation. Create `validate_post(cfg, article) -> (ok, errors)` delegating to `shared/post_validator.py` / `shared/validators.py`, or passthrough `(True, [])`.

    Each wrapper: module docstring noting Phase 61 standard skeleton membership.

    **Commit separation (D-08):** STRUCTURE addition only. Commit:
    `feat(phase-61): add travel standard-skeleton wrappers (topic_manager/enrich/validator)`
  </action>
  <verify>
    <automated>python3 -c "import ast; [ast.parse(open('/Users/twinssn/Projects/5000/pipelines/travel/'+f).read()) for f in ['topic_manager.py','enrich.py','validator.py']]; print('AST OK')"</automated>
    <automated>python3 -c "import sys; sys.path.insert(0,'/Users/twinssn/Projects/5000'); from pipelines.travel import topic_manager, enrich, validator; print('IMPORT OK', hasattr(topic_manager,'select_topic'), hasattr(enrich,'enrich_content'), hasattr(validator,'validate_post'))"</automated>
  </verify>
  <done>pipelines/travel/{topic_manager,enrich,validator}.py exist as thin wrappers delegating to existing logic. travel now exposes the 6-module skeleton. Behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 61-05-2: Normalize travel run(cfg) None→dict + wire DB path + extend skeleton test</name>
  <files>
    /Users/twinssn/Projects/5000/pipelines/travel/pipeline.py
    /Users/twinssn/Projects/5000/tests/shared/test_skeleton_travel.py
  </files>
  <action>
    **Part A — run(cfg) None→dict normalization (standard contract, additive):**
    In `pipelines/travel/pipeline.py`'s `run(cfg)` (L413), wrap the `_run_single` result so it never returns None:
    - After obtaining the result from `_run_single` (or the current delegation), add:
      `result = result if isinstance(result, dict) else {"success": False, "reason": "no_result"}`
    - This is a **behavior-consistent** change: dispatcher already falls back to `{"success": False, "reason": "no_result"}` when run returns None (L407). Normalizing at the module level makes the module contract dict-only per PIPELINE-STANDARD.md Section 3, while live behavior is identical.
    - **Do NOT change `_run_single` itself** — only the `run(cfg)` return normalization.
    - **Commit separation (D-08):** This is a BEHAVIOR-consistent contract normalization. Commit SEPARATELY from the structure commits:
      `refactor(phase-61): normalize travel run(cfg) None to no_result dict`

    **Part B — DB-path wiring (D-06, additive):**
    In `pipelines/travel/pipeline.py`, replace inline DB path constant usage with `shared.db.get_db_path('travel')` (resolves `data/travel-en.db`). Do NOT move/delete DB files. Keep original constant as fallback if replacement risks behavior change.

    **Commit:** `refactor(phase-61): centralize travel DB path via shared/db.py`

    **Part C — extend skeleton test (D-01):**
    Add `test_travel_skeleton` to `tests/shared/test_skeleton_travel.py` asserting `pipelines.travel` has the 6 module files. Do not invoke run().

    **Commit:** `test(phase-61): add travel skeleton presence test`
  </action>
  <verify>
    <automated>pytest tests/shared/test_skeleton_travel.py -x --no-cov 2>&1 | tail -5</automated>
    <automated>cd /Users/twinssn/Projects/5000 && python3 -c "
import sys; sys.path.insert(0,'.')
import inspect, importlib
# Verify run(cfg) normalizes None without invoking network
from pipelines.travel import pipeline as tp
src = inspect.getsource(tp.run)
assert 'no_result' in src, 'run() lacks no_result normalization'
print('RUN NORMALIZES None->no_result')
"</automated>
  </verify>
  <done>travel run(cfg) returns dict, never None (no_result normalization). DB path centralized via shared/db.py. test_skeleton_travel includes travel and passes.</done>
</task>

<task type="auto">
  <name>Task 61-05-3: Rollout regression gate</name>
  <files>
    /Users/twinssn/Projects/5000/pipelines/travel/
  </files>
  <action>
    Run full suite to confirm zero regression:
    `pytest -q`

    Confirm git log shows the travel commits as separate structure/behavior commits (D-08). Confirm no `data/*.db` changed.
  </action>
  <verify>
    <automated>pytest -q 2>&1 | tail -8</automated>
    <automated>git log --oneline -6 | grep "phase-61"</automated>
  </verify>
  <done>Full suite green after travel conversion. travel run contract now dict-only (None normalized), skeleton complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wrapper module → existing travel logic | Thin delegation |
| run(cfg) normalization → dispatcher | None→dict contract consistency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-05-01 | Tampering | None→dict normalization | mitigate | Matches dispatcher L407 fallback exactly; `_run_single` untouched; inspect-source assert confirms |
| T-61-05-02 | Denial of Service | DB path | mitigate | shared/db.py resolves identical existing path; no data move |
| T-61-05-SC | Tampering | npm/pip installs | accept | No new package installs |
</threat_model>

<verification>
### Plan 61-05 verification

1. [ ] travel has 6 modules — `ls pipelines/travel/*.py` includes topic_manager/enrich/validator
2. [ ] Wrappers import cleanly — `python3 -c "from pipelines.travel import topic_manager,enrich,validator"`
3. [ ] run(cfg) normalizes None — `grep -n "no_result" pipelines/travel/pipeline.py`
4. [ ] DB path centralized — `grep -rn "get_db_path" pipelines/travel/pipeline.py`
5. [ ] No data files changed — `git status --porcelain | grep "data/"` empty
6. [ ] Skeleton test passes — `pytest tests/shared/test_skeleton_travel.py -q`
7. [ ] Full suite green — `pytest -q`
</verification>

<success_criteria>
1. ✅ travel(8) 6-모듈 골격 완비
2. ✅ run(cfg) None→`{success:False, reason:'no_result'}` 정규화 (표준 dict 계약)
3. ✅ DB path `shared/db.py` 배선 (무이동)
4. ✅ 구조/동작 분리 커밋
5. ✅ 기존 테스트 + skeleton 테스트 green
</success_criteria>

<output>
Create `.planning/phases/61-pipeline-standardization-branch-renewal/61-05-SUMMARY.md` when done
</output>

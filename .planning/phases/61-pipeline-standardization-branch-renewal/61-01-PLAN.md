---
phase: 61-pipeline-standardization-branch-renewal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "/Users/twinssn/Projects/5000/docs/PIPELINE-STANDARD.md"
autonomous: true
requirements: [61-A, D-02, D-04]
user_setup: []
must_haves:
  truths:
    - "docs/PIPELINE-STANDARD.md exists and is the canonical reference for the pipeline contract, module skeleton, config schema, and reason-key vocabulary"
    - "The standard defines a single run(cfg) -> dict return contract (success/reason/slug/title/body_md/thumbnail_url/deploy_error)"
    - "The standard enumerates a closed reason-key vocabulary that includes the 11 unregistered reason strings"
    - "The standard defines the unified blogs.d/*.yaml config schema with optional/extension fields preserved"
    - "The standard defines the 6-module skeleton (pipeline/fetcher/topic_manager/writer/enrich/validator) and the additive wrapper rule"
  artifacts:
    - path: "docs/PIPELINE-STANDARD.md"
      provides: "Canonical standardization reference"
      contains: "run(cfg)"
  key_links:
    - from: "docs/PIPELINE-STANDARD.md"
      to: "shared/problem_registry.py"
      via: "reason vocabulary enumerated in standard feeds registry extension (Plan 02)"
    - from: "docs/PIPELINE-STANDARD.md"
      to: "shared/config_validator.py"
      via: "schema documented here is enforced there (Plan 02)"
---

<objective>
Stage A — 표준 정의 및 문서화. 7개 분기를 한 골격으로 묶을 **canonical reference** `docs/PIPELINE-STANDARD.md`를 작성한다.

Purpose: 분기별 표준을 단일 문서로 고정해, 이후 모든 전환 플랜(61-02..61-09)이 이 문서를 근거로 구현하게 한다. 표준 없이는 각 분기가 제각각 다시 갈라진다.

Output: `docs/PIPELINE-STANDARD.md` — 파이프라인 계약·모듈 골격·config 스키마·reason 어휘·DB 접근 규칙.
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/twinssn/Projects/5000/.planning/STATE.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-CONTEXT.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-RESEARCH.md

This is a documentation-only plan. All contracts written here are code-verified against the current working tree (see RESEARCH.md). No source code is modified — only `docs/PIPELINE-STANDARD.md` is created. This satisfies D-02 (written standard as canonical reference) and D-04 (config schema documentation).

<interfaces>
From `shared/problem_registry.py`:
- P01..P24 + `unknown_failure` problem specs, each with `reason_keys: tuple[str, ...]`
- reason strings that currently fall through to `unknown_failure` (verified in RESEARCH Pitfall 2): `daily_quota_reached`, `expired_service`, `generation_failed`, `language_error`, `no_subscription_data`, `no_topic`, `no_topics`, `no_trade_data`, `prompt_not_found`, `publish_failed`, `write_failed`

From `shared/config_validator.py` (existing, to be extended in Plan 02):
- `REQUIRED_BLOG_FIELDS = ["id", "pipeline", "platform"]`
- `OPTIONAL_BLOG_FIELDS = ["daily_quota", "site_path", "repo", "theme", "status"]`
- `VALID_PLATFORMS = ["hugo", "blogger", "wordpress"]`

From `dispatcher.py`:
- `dispatch()` normalization at L740: None→no_result, str→quota_met/fetch_error/etc, bool→{success:bool}
- Standard run contract dict keys: `success`, `reason`, plus optional `slug`, `title`, `body_md`, `thumbnail_url`, `deploy_error`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 61-01-1: Write PIPELINE-STANDARD.md core sections (module skeleton + run contract)</name>
  <files>/Users/twinssn/Projects/5000/docs/PIPELINE-STANDARD.md</files>
  <action>
    Create `docs/PIPELINE-STANDARD.md` (NEW file, additive — does not touch any source). This is the canonical standard that Plans 02-09 implement against.

    Write the following sections:

    **Section 1 — 목적 및 경계 (Purpose & Boundary)**
    - 7개 분기(car/curation/etap/rap/senior/travel/stock)를 한 골격으로.
    - 외부 프로젝트(TAP/STAP)는 계약 정합만 (물리 병합 아님).
    - 새 콘텐츠 기능/신규 third-party 패키지/DB 데이터 통합은 범위 밖.

    **Section 2 — 표준 모듈 골격 (D-01)**
    - 6개 모듈: `pipeline.py` / `fetcher` / `topic_manager` / `writer` / `enrich` / `validator`
    - **추가 규칙 (additive rule):** 이미 존재하는 모듈은 유지하고 골격에 배선. 부재 모듈은 **얇은 wrapper**로 추가해 기존 shared/ 코드로 위임. 기존 코드는 제거하지 않는다.
    - 골격 구성원별 책임 정의 (pipeline=run(cfg) 진입점, fetcher=데이터 수집, topic_manager=토픽 선택, writer=글 생성, enrich=엔리치먼트, validator=검증)
    - 각 분기별 현재 모듈 → 표준 골격 매핑 테이블 (senior/rap/car/travel/curation/etap/stock, RESEARCH `Recommended Project Structure` 참조)
    - **스코프 명시 (D-04 계약 정합):** `stock`(STAP) 분기는 **독립 저장소 물리 병합 금지**이므로 표준 골격 전체(fetcher/topic_manager/enrich/validator wrapper 추가)가 아닌 **`run(cfg)` 반환 계약 + subprocess 라우팅만** 적용한다. 매핑 테이블에 stock의 골격 적용 범위를 "계약만"으로 표기하고, 나머지 6개 인-트리 분기(car/curation/etap/rap/senior/travel)만 완전 골격을 보장함을 명시. (이로써 ROADMAP 수용 기준 #1 "7개 분기 동일 골격"은 6개 인-트리 + stock 계약정합으로 해석)

    **Section 3 — 실행 계약 `run(cfg) -> dict` (D-01/D-02)**
    - 표준 진입점 시그니처: `def run(cfg: dict) -> dict` (단일 인자)
    - 표준 반환 dict 키: `success`(bool, 필수), `reason`(str, 필수 — Section 5 어휘), `slug`/`title`/`body_md`/`thumbnail_url`/`deploy_error`(선택)
    - **반환 정규화 규칙:** dict가 아닌 반환은 허용하지 않음. `None` → `{"success": False, "reason": "no_result"}`, `bool` → `{"success": bool}`, `str` → dispatcher L740 매핑. 단, 기존 dispatcher `dispatch()` 정규화는 유지하고 모듈 계약만 통일.
    - ETAP 하위 토픽 모듈은 `run()`/`run(cfg)` 혼합 — 표준은 `run(cfg)` adapter wrapper로 위임하고 `_ETAP_BLOG_EXCEPTIONS` + `inspect.signature` 브리지는 유지.

    **Section 4 — 통합 config 스키마 (D-04)**
    - `config/blogs.d/*.yaml` 각 blog entry의 표준 필드 정의:
      - **필수(required):** `id`, `pipeline`, `platform`
      - **표준(standard, 위 공통 필드):** `name`, `domain`, `daily_quota`, `schedule`, `status`, `managed_by`
      - **확장/선택(optional, 분기별 보존):** `cf_project`, `repo`, `site_path`, `theme`, `funnel_stage`, `depth_next`, `bridge_to`, `post_type`, `prompt`, `ga4_property`, `gsc_site`, `blogger_blog_id`, `force_draft`, `language`
    - `managed_by: pipeline` 을 표준 관리 모드로 명시.
    - 분기별 확장 키는 optional 로 보존 (삭제 금지) — 기존 config 무중단.
    - 이 스키마는 Plan 02의 `config_validator.py` 확장의 근거.

    **Section 5 — 표준 reason-key 어휘 (closed vocabulary)**
    - `shared/problem_registry.py`의 P01..P24 + `unknown_failure`를 표준 어휘로 선언.
    - **미등록 11+ reason 추가 예정 목록** (Plan 02에서 registry에 반영): `daily_quota_reached`, `expired_service`, `generation_failed`, `language_error`, `no_subscription_data`, `no_topic`, `no_topics`, `no_trade_data`, `prompt_not_found`, `publish_failed`, `write_failed`
    - 규칙: 신규 reason은 반드시 registry에 등록하거나 가장 가까운 P-code로 매핑. `unknown_failure`로의 소실 금지.
    - 분기별 현재 사용 reason ↔ P-code 매핑 표 (RESEARCH Pitfall 2 참조).

    **Section 6 — DB 접근 규칙 (D-06)**
    - DB 파일은 분기별로 유지 (데이터 통합 금지). `shared/db.py`(Plan 02)가 path+connection을 중앙화.
    - `shared/db_paths.py`는 확장해 사용 (replace 금지).

    **Section 7 — 배포 및 보존 규칙**
    - 배포는 `dispatcher.py`/`deploy.py` 경유만. `CLOUDFLARE_API_TOKEN` 제거 규칙.
    - AdSense Publisher ID 매핑(8772/6677/5938) 불변 — 광고 파일 표준화 시에도 위반 금지.
    - 구조 변경과 동작 변경은 분리 커밋.

    파일 작성 시 위 `Section 2`의 분기별 매핑 테이블과 `Section 4`의 필드 목록은 RESEARCH.md와 `config/blogs.d/*.yaml` 실측값을 근거로 한다. 표준은 규범적(normative)이어야 하며 모호한 표현("등", "기타")은 피한다.
  </action>
  <verify>
    <automated>grep -c "run(cfg)" /Users/twinssn/Projects/5000/docs/PIPELINE-STANDARD.md && grep -c "no_result" /Users/twinssn/Projects/5000/docs/PIPELINE-STANDARD.md && grep -c "topic_manager" /Users/twinssn/Projects/5000/docs/PIPELINE-STANDARD.md</automated>
    <automated>grep -c "daily_quota_reached\|expired_service\|generation_failed\|language_error\|no_subscription_data\|no_topic\|no_trade_data\|prompt_not_found\|publish_failed\|write_failed" /Users/twinssn/Projects/5000/docs/PIPELINE-STANDARD.md</automated>
  </verify>
  <done>PIPELINE-STANDARD.md exists with Sections 1-7. It defines: single run(cfg)->dict contract, 6-module skeleton with additive rule, unified config schema with optional extension fields, closed reason vocabulary including the 11 unregistered keys, DB path rule, and preservation rules.</done>
</task>

<task type="auto">
  <name>Task 61-01-2: Commit documentation and run baseline tests</name>
  <files>/Users/twinssn/Projects/5000/docs/PIPELINE-STANDARD.md</files>
  <action>
    This is a doc-only commit (structure/documentation change — no behavior change).

    1. Run baseline regression to confirm no source was touched:
       `pytest tests/test_dispatcher_registry.py -x`
    2. Stage and commit ONLY the new doc file:
       `git add docs/PIPELINE-STANDARD.md`
       `git commit -m "docs(phase-61): add PIPELINE-STANDARD.md (canonical pipeline/config/run-contract standard)"`
    3. Do NOT commit any other file. No source code is modified in this plan.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -i "PIPELINE-STANDARD"</automated>
    <automated>cd /Users/twinssn/Projects/5000 && git status --porcelain | grep -v "docs/PIPELINE-STANDARD.md" | grep -v "^$" || echo "no-other-changes"</automated>
  </verify>
  <done>PIPELINE-STANDARD.md committed. Only that file changed. Baseline test `test_dispatcher_registry.py` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → downstream implementation | Written standard consumed as normative spec by Plans 02-09 — if inaccurate, cascades incorrect implementations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | PIPELINE-STANDARD.md contracts | mitigate | All contracts code-verified against working tree (RESEARCH.md HIGH confidence); doc is normative, downstream plans verify against actual code |
| T-61-01-SC | Tampering | npm/pip installs | accept | No new package installs in this phase (doc-only) |
</threat_model>

<verification>
### Plan 61-01 verification

1. [ ] `docs/PIPELINE-STANDARD.md` exists — `ls docs/PIPELINE-STANDARD.md`
2. [ ] All 7 sections present — `grep -E "^#+ Section [0-9]" docs/PIPELINE-STANDARD.md`
3. [ ] run contract + reason vocabulary present — `grep -E "run\(cfg\)|daily_quota_reached|generation_failed|write_failed" docs/PIPELINE-STANDARD.md`
4. [ ] No source file modified — `git status --porcelain` shows only the doc file
5. [ ] Baseline test green — `pytest tests/test_dispatcher_registry.py -x`
</verification>

<success_criteria>
1. ✅ `docs/PIPELINE-STANDARD.md` 생성 — Section 1-7 완비
2. ✅ 단일 `run(cfg) -> dict` 계약 정의
3. ✅ 6-모듈 골격 + additive wrapper 규칙 정의
4. ✅ 통합 config 스키마 (필수/표준/선택 필드) 정의
5. ✅ 표준 reason-key 어휘 + 미등록 11+ 반영 예정 목록
6. ✅ DB 접근 규칙 + 배포/보존 규칙
7. ✅ 소스 코드 0 변경, baseline 테스트 green
</success_criteria>

<output>
Create `.planning/phases/61-pipeline-standardization-branch-renewal/61-01-SUMMARY.md` when done
</output>

---
phase: 61-pipeline-standardization-branch-renewal
plan: 09
type: execute
wave: 6
depends_on: [61-01, 61-02]
files_modified:
  - "/Users/twinssn/Projects/5000/scripts/scaffold_branch.py"
  - "/Users/twinssn/Projects/5000/tests/shared/test_scaffold_branch.py"
  - "/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-F-LEGACY-CLEANUP-REPORT.md"
  - "/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-F-DEAD-CODE-REPORT.md"
autonomous: true
requirements: [61-F, D-05]
user_setup: []
must_haves:
  truths:
    - "scripts/scaffold_branch.py generates a new branch with the standard 6-module skeleton + config template + DB schema scaffolding (no manual duplication)"
    - "scaffold_branch.py validates the branch name (path/module injection safe) and produces a runnable pipeline skeleton"
    - "A scaffolded branch can be instantiated and imported without error (demonstrated in test)"
    - ".bak files are inventoried (59 total) but NOT deleted — actual deletion deferred pending separate approval (destructive-ops rules)"
    - "Dead code report lists per-branch inline DB constants now superseded by shared/db.py, without deleting anything"
  artifacts:
    - path: "scripts/scaffold_branch.py"
      provides: "New-branch scaffolding from standard skeleton"
      contains: "def scaffold"
    - path: "tests/shared/test_scaffold_branch.py"
      provides: "Scaffold validation + skeleton generation test"
    - path: "61-F-LEGACY-CLEANUP-REPORT.md"
      provides: ".bak inventory + cleanup recommendation (deletion deferred)"
    - path: "61-F-DEAD-CODE-REPORT.md"
      provides: "Inline DB-path constants superseded by shared/db.py (report only)"
  key_links:
    - from: "scripts/scaffold_branch.py"
      to: "docs/PIPELINE-STANDARD.md"
      via: "generates skeleton per the canonical standard"
    - from: "scripts/scaffold_branch.py"
      to: "shared/db.py"
      via: "scaffold wires DB path via get_db_path"
---

<objective>
Stage F — **온보딩 도구 + 레거시 정리.** 새 분기 생성 도구 `scripts/scaffold_branch.py`(D-05)를 만들어 표준 골격 + config 템플릿 + DB 스키마를 자동 생성한다. ETAP 35쌍 같은 수동 복제를 근절한다. 레거시(`.bak` 파일, 인라인 DB 상수)는 **보고서로만 식별** — 실제 삭제는 별도 승인 필요(파괴적 작업 규칙).

Purpose: 새 분기가 처음부터 표준 골격으로 시작하게 하여 미래 발산 방지. 온보딩을 코드-생성으로 자동화.

Output: `scripts/scaffold_branch.py` + 테스트, `.bak` 정리 보고서, dead-code 보고서 (둘 다 삭제 없음).
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/twinssn/Projects/5000/.planning/STATE.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-CONTEXT.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-RESEARCH.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-01-SUMMARY.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-02-SUMMARY.md
@/Users/twinssn/Projects/5000/docs/PIPELINE-STANDARD.md
@/Users/twinssn/Projects/5000/shared/db.py
@/Users/twinssn/Projects/5000/scripts/

**Destructive-ops rules (AGENTS.md §파괴적 작업):** This plan does NOT delete `.bak` files or any code. It only (a) creates a scaffolding script, (b) inventories `.bak` files, (c) reports dead code. Actual deletion requires separate approval + 4-step protocol. Confirm: no `rm`, no DB DELETE/UPDATE, no wrangler deploy in this plan.

<interfaces>
From `docs/PIPELINE-STANDARD.md` (Plan 01):
- 6-module skeleton: pipeline/fetcher/topic_manager/writer/enrich/validator
- run(cfg)->dict contract
- unified config schema (required/standard/optional fields)
- managed_by: pipeline

From `shared/db.py` (Plan 02):
- `get_db_path(branch)` — resolves per-branch DB path
- `get_connection(path)` — connection helper

From RESEARCH D-05:
- Target: prevent ETAP 35-pair-style manual duplication
- scaffold must generate: standard skeleton + config template (`blogs.d/{branch}.yaml` blog entry) + DB schema scaffolding
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 61-09-1: Write Wave-0 test for scaffold_branch.py (RED)</name>
  <files>/Users/twinssn/Projects/5000/tests/shared/test_scaffold_branch.py</files>
  <action>
    Create `tests/shared/test_scaffold_branch.py` (NEW, Wave-0) defining scaffold behavior BEFORE implementation. Tests must FAIL (RED) until `scripts/scaffold_branch.py` exists.

    Behavior tests:
    - `test_scaffold_creates_skeleton`: calling `scaffold_branch("testbranch")` into a temp dir creates the 6 standard module files (`pipeline.py`, `fetcher.py`, `topic_manager.py`, `writer.py`, `enrich.py`, `validator.py`)
    - `test_scaffold_creates_config_template`: creates a `config/blogs.d/{branch}.yaml` template with the required standard fields (`id`, `pipeline`, `platform`, `name`, `domain`, `daily_quota`, `schedule`, `status`, `managed_by: pipeline`)
    - `test_scaffold_wires_db_path`: the generated `pipeline.py` imports `get_db_path` and uses it for the branch DB
    - `test_scaffold_validates_branch_name`: invalid names (e.g., `"../evil"`, `"a b"`, `"with.dot"`) are rejected — validates `^[a-z][a-z0-9_]*$` (path/module injection safety)
    - `test_scaffolded_pipeline_imports`: the generated skeleton module imports without error (importlib on the temp-generated package)
    - `test_scaffold_generates_run_contract`: generated `pipeline.py` has `def run(cfg)` returning a dict

    Design the API as: `scripts/scaffold_branch.py` with a function `scaffold_branch(branch_name, base_dir=None, config_dir=None) -> Path` (targets a temp base_dir in tests; defaults to real project layout). Provide a `__main__` CLI with `--branch` and `--base-dir` args.

    Run to confirm RED:
    `pytest tests/shared/test_scaffold_branch.py -x --no-cov` (fails: module missing)
  </action>
  <verify>
    <automated>pytest tests/shared/test_scaffold_branch.py -x --no-cov 2>&1 | tail -4</automated>
  </verify>
  <done>test_scaffold_branch.py created, currently FAILING (RED) as expected — scaffold_branch.py does not exist yet.</done>
</task>

<task type="auto">
  <name>Task 61-09-2: Implement scripts/scaffold_branch.py</name>
  <files>/Users/twinssn/Projects/5000/scripts/scaffold_branch.py</files>
  <action>
    Implement `scripts/scaffold_branch.py` (NEW, D-05) to satisfy the Wave-0 test. **Additive — creates new files only, never modifies existing branches.**

    **Public API:**
    - `def scaffold_branch(branch_name: str, base_dir: str | None = None, config_dir: str | None = None) -> Path`
      - `branch_name`: validated against `^[a-z][a-z0-9_]*$` (reject otherwise — module-path injection / path-traversal safety per security domain V5). Raise `ValueError` on invalid.
      - `base_dir`: where to create `pipelines/{branch}/` (default: project root). `config_dir`: where to write `blogs.d/{branch}.yaml` template (default: project `config/`).
      - Returns the created pipeline dir path.
    - `def main()` / `__main__` CLI: `python scripts/scaffold_branch.py --branch <name> [--base-dir <path>] [--config-dir <path>] [--dry-run]`
      - `--dry-run`: print what would be created without writing files.
      - On real run, refuse to overwrite an existing branch directory (raise if `pipelines/{branch}/` already exists).

    **Generated content (per PIPELINE-STANDARD.md D-01):**
    - `pipelines/{branch}/__init__.py` — empty or docstring.
    - `pipelines/{branch}/pipeline.py` — `def run(cfg) -> dict` skeleton returning `{"success": False, "reason": "no_topic"}` placeholder, importing `get_db_path` from `shared.db`, delegating to `fetcher/topic_manager/writer/enrich/validator` in the standard order.
    - `pipelines/{branch}/fetcher.py` — `def fetch_data(cfg) -> list` (returns `[]` placeholder).
    - `pipelines/{branch}/topic_manager.py` — `def select_topic(cfg)` placeholder.
    - `pipelines/{branch}/writer.py` — `def write_article(cfg, topic)` placeholder.
    - `pipelines/{branch}/enrich.py` — `def enrich_content(cfg, body_md)` passthrough placeholder.
    - `pipelines/{branch}/validator.py` — `def validate_post(cfg, article) -> (bool, list)` returning `(True, [])` placeholder.
    - **DB path wiring:** generated `pipeline.py` uses `from shared.db import get_db_path` + `get_db_path('{branch}')` (no inline hardcoded path).
    - **Config template:** write `{config_dir}/blogs.d/{branch}.yaml` with a single blog entry containing the standard required fields + `managed_by: pipeline` (placeholder values, e.g., `id: {branch}-hugo`, `pipeline: {branch}`, `platform: hugo`, `name: {Branch}`, `domain: "{branch}.example.com"`, `daily_quota: 1`, `schedule: {times: []}`, `status: inactive`, `managed_by: pipeline`).

    **Commit separation (D-08):** Single feature commit:
    `feat(phase-61): add scripts/scaffold_branch.py (standard-skeleton branch generator)`
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('/Users/twinssn/Projects/5000/scripts/scaffold_branch.py').read()); print('AST OK')"</automated>
    <automated>pytest tests/shared/test_scaffold_branch.py -x --no-cov 2>&1 | tail -5</automated>
    <automated>cd /Users/twinssn/Projects/5000 && python3 -c "
import tempfile, os, importlib.util
spec = importlib.util.spec_from_file_location('scaffold_branch', 'scripts/scaffold_branch.py')
m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
with tempfile.TemporaryDirectory() as d:
    p = m.scaffold_branch('demobranch', base_dir=d, config_dir=d)
    print('created:', sorted(os.listdir(p)))
"
"  </verify>
  <done>scripts/scaffold_branch.py exists — validates branch name (^[a-z][a-z0-9_]*$), generates 6-module skeleton + config template + DB path wiring. test_scaffold_branch.py passes (GREEN).</done>
</task>

<task type="auto">
  <name>Task 61-09-3: Inventory .bak files + write legacy cleanup report (report-only, no deletion)</name>
  <files>/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-F-LEGACY-CLEANUP-REPORT.md</files>
  <action>
    Create `.planning/phases/61-pipeline-standardization-branch-renewal/61-F-LEGACY-CLEANUP-REPORT.md` (NEW, report-only). **Do NOT delete or modify any `.bak` file.**

    Inventory the `.bak` files (59 total verified) via:
    `find /Users/twinssn/Projects/5000 -name "*.bak*" -not -path "./.git/*" 2>/dev/null`

    Report contents:
    - Total `.bak` count + full list grouped by directory (config/blogs.d/*.yaml.bak*, etc.)
    - For each `.bak`, note: size, date, and whether it's a git-tracked backup or manual snapshot
    - **Recommended cleanup:** a proposed deletion list with rationale — but explicitly marked **DEFERRED / requires separate approval** per destructive-ops rules (AGENTS.md §파괴적 작업: 4-step protocol + mandatory user confirmation + `logs/destructive_*.log` + worklog). Do NOT perform deletion.
    - **Preservation note:** `content.db` `source=''` real-publish rows must never be deleted (permanent preservation rule).
    - State clearly: "실제 삭제는 별도 Phase/승인 후 4단계 프로토콜로 수행. 본 보고서는 식별만."

    **Commit:** `docs(phase-61): add .bak legacy cleanup inventory report (deletion deferred)`
  </action>
  <verify>
    <automated>test -f /Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-F-LEGACY-CLEANUP-REPORT.md && grep -c "DEFERRED\|별도" /Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-F-LEGACY-CLEANUP-REPORT.md
    </automated>
  </verify>
  <done>61-F-LEGACY-CLEANUP-REPORT.md created — inventories all .bak files (59), recommends cleanup but marks deletion as DEFERRED/requires approval. No .bak file deleted or modified.</done>
</task>

<task type="auto">
  <name>Task 61-09-4: Write dead-code report (inline DB constants superseded) + full regression gate</name>
  <files>
    /Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-F-DEAD-CODE-REPORT.md
  </files>
  <action>
    **Part A — dead-code report (report-only, no deletion):**
    Create `.planning/phases/61-pipeline-standardization-branch-renewal/61-F-DEAD-CODE-REPORT.md` (NEW) documenting code now superseded by Phase 61 centralization — WITHOUT deleting anything.

    Inventory inline DB-path constants that were superseded by `shared/db.py` (D-06) via:
    `grep -rn "_DB_PATH\s*=\|DB_PATH\s*=" /Users/twinssn/Projects/5000/pipelines/ /Users/twinssn/Projects/5000/shared/ 2>/dev/null`

    Report contents:
    - List each inline `*_DB_PATH` constant in pipelines/{car,stock,rap,senior,curation,etap} that is now redundant with `shared.db.get_db_path`
    - For each: file:line, constant name, the branch it serves, and the `shared/db.py` resolution that supersedes it
    - **Explicitly mark:** "삭제는 별도 Phase/승인 필요. 본 보고서는 식별만." (destructive-ops rules apply to any future deletion)
    - Note `shared/db_paths.py` is preserved (extended, not replaced per D-08/D-06)

    **Commit:** `docs(phase-61): add dead-code report for inline DB constants superseded by shared/db.py`

    **Part B — full regression gate (Phase F / Phase completion):**
    Run the complete test suite:
    `pytest -q`

    Confirm git log shows all Phase F commits (scaffold + tests + 2 reports) as separate commits. Confirm no source branch was modified by this plan (`git status` shows only scripts/, tests/, .planning reports).
  </action>
  <verify>
    <automated>test -f /Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-F-DEAD-CODE-REPORT.md && grep -c "get_db_path\|shared/db.py" /Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-F-DEAD-CODE-REPORT.md
    </automated>
  </verify>
  <done>61-F-DEAD-CODE-REPORT.md created — lists inline *_DB_PATH constants superseded by shared/db.py (report-only, no deletion). Full suite green. Phase F complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scaffold_branch.py CLI → filesystem | Creates new pipeline dir + config from user-provided branch name |
| scaffold generated code → importlib | Generated module imported for validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-09-01 | Tampering | scaffold branch_name | mitigate | Validate `^[a-z][a-z0-9_]*$` before path join / module import (path traversal + module injection) |
| T-61-09-02 | Tampering | scaffold overwrite | mitigate | Refuse overwrite of existing branch dir; dry-run mode |
| T-61-09-03 | Denial of Service | .bak inventory | accept | Report-only; no deletion, no modification |
| T-61-09-SC | Tampering | npm/pip installs | accept | No new package installs |
</threat_model>

<verification>
### Plan 61-09 verification

1. [ ] `scripts/scaffold_branch.py` exists — `ls scripts/scaffold_branch.py`
2. [ ] scaffold validates branch name — `grep "re.match\|fullmatch" scripts/scaffold_branch.py`
3. [ ] scaffold generates 6-module skeleton — `python3` smoke test (temp dir) creates pipeline/fetcher/topic_manager/writer/enrich/validator
4. [ ] Wave-0 test green — `pytest tests/shared/test_scaffold_branch.py -q`
5. [ ] Legacy report exists — `ls .../61-F-LEGACY-CLEANUP-REPORT.md`
6. [ ] Dead-code report exists — `ls .../61-F-DEAD-CODE-REPORT.md`
7. [ ] No .bak deleted — `git status --porcelain` shows no `.bak` deletions
8. [ ] Full suite green — `pytest -q`
</verification>

<success_criteria>
1. ✅ `scripts/scaffold_branch.py` — 표준 골격 + config 템플릿 + DB path 배선 자동 생성 (D-05)
2. ✅ 분기명 검증 (`^[a-z][a-z0-9_]*$`) — 경로/모듈 주입 방지
3. ✅ scaffold된 분기가 import/실행 가능 (테스트로 입증)
4. ✅ `.bak` 59건 인벤토리 보고서 — 삭제 DEFERRED (별도 승인, 파괴적 작업 규칙)
5. ✅ dead-code 보고서 — 인라인 DB 상수 식별 (삭제 없음)
6. ✅ 전체 스위트 green, 구조/동작 분리 커밋
</success_criteria>

<output>
Create `.planning/phases/61-pipeline-standardization-branch-renewal/61-09-SUMMARY.md` when done
</output>
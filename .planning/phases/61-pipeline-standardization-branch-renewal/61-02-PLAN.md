---
phase: 61-pipeline-standardization-branch-renewal
plan: 02
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - "/Users/twinssn/Projects/5000/shared/db.py"
  - "/Users/twinssn/Projects/5000/shared/subprocess_runner.py"
  - "/Users/twinssn/Projects/5000/shared/config_validator.py"
  - "/Users/twinssn/Projects/5000/shared/problem_registry.py"
  - "/Users/twinssn/Projects/5000/tests/shared/test_db.py"
  - "/Users/twinssn/Projects/5000/tests/shared/test_subprocess_runner.py"
  - "/Users/twinssn/Projects/5000/tests/shared/test_config_schema.py"
  - "/Users/twinssn/Projects/5000/tests/shared/test_problem_registry.py"
autonomous: true
requirements: [61-B, D-03, D-04, D-06]
user_setup: []
must_haves:
  truths:
    - "shared/db.py resolves per-branch DB path via existing db_paths.py without moving any data file"
    - "shared/subprocess_runner.py runs an external project module in an isolated subprocess and returns a dict"
    - "shared/config_validator.py validates blogs.d/*.yaml against the unified schema (required/standard/optional fields)"
    - "shared/problem_registry.py recognizes the 11+ previously unregistered reason keys instead of unknown_failure"
    - "All existing tests stay green; new Wave-0 tests pass"
  artifacts:
    - path: "shared/db.py"
      provides: "Central DB path+connection helper"
      contains: "def "
    - path: "shared/subprocess_runner.py"
      provides: "STAP/TAP subprocess isolation runner"
      contains: "def run_subprocess"
    - path: "shared/config_validator.py"
      provides: "Unified config schema validation"
      contains: "REQUIRED_BLOG_FIELDS"
    - path: "shared/problem_registry.py"
      provides: "Extended reason-key registry"
      contains: "generation_failed"
  key_links:
    - from: "shared/subprocess_runner.py"
      to: "shared/db.py"
      via: "both import from shared/paths.py, no circular import"
    - from: "shared/config_validator.py"
      to: "docs/PIPELINE-STANDARD.md"
      via: "implements the schema documented in Section 4 (Plan 01)"
---

<objective>
Stage B — 공용 기반 구축. 분기 표준화가 의존할 **shared 모듈 4개**를 추가한다. 전부 **순수 추가(additive)** — 기존 소스는 수정하지 않고 신규 모듈 생성 + 기존 모듈 확장만.

Purpose: subprocess 격리(400줄 중복)와 DB path 해석(6+ 분기 인라인)을 중앙화하고, config 스키마 검증과 reason-key 어휘를 표준화한다. 이후 pilot(61-03)과 rollout(61-04..07)이 이 모듈들을 소비한다.

Output: `shared/db.py`, `shared/subprocess_runner.py`, `shared/config_validator.py` 확장, `shared/problem_registry.py` reason-key 보강, + 신규 테스트.
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/twinssn/Projects/5000/.planning/STATE.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-CONTEXT.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-RESEARCH.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-01-SUMMARY.md
@/Users/twinssn/Projects/5000/shared/db_paths.py
@/Users/twinssn/Projects/5000/shared/config_validator.py
@/Users/twinssn/Projects/5000/shared/problem_registry.py
@/Users/twinssn/Projects/5000/shared/paths.py
@/Users/twinssn/Projects/5000/dispatcher.py

This plan creates shared modules. **No new third-party packages** — stdlib only (json, subprocess, tempfile, sqlite3, inspect, importlib). All changes additive.

<interfaces>
From `shared/paths.py` (verified):
- `STAP_ROOT`, `TAP_ROOT`, `ETAP_ROOT`, `CUAP_ROOT`, `RAP_ROOT` — project root resolution

From `shared/db_paths.py` (verified, to be extended not replaced):
- `PUBLISH_LEDGER_DB = data/content.db`, `ARTICLES_DB = data/stap_content.db`, plus TAP/FESTIVAL/TRAVEL_DB constants
- Note: branches (car/stock/rap/senior/curation/etap) each inline their own `*_DB_PATH` — these are the consolidation targets, but `shared/db.py` ADDS central resolution; per-branch inlines are rewired in Plans 03-07, not here.

From `dispatcher.py:_run_stap` (L380-440) and `_run_tap_subprocess` (L504-566) — the duplicated pattern to centralize into `shared/subprocess_runner.py`:
- resolve project root + venv python (`.venv/bin/python3` for STAP, `venv/bin/python3` for TAP), fallback `sys.executable`
- write tempfile runner script (imports target module, calls run/run_publish, prints JSON)
- run with `timeout=600`, `cwd=project_root`, `capture_output`
- parse last JSON line from stdout (STAP scans reversed lines for first line starting with `{`; TAP takes last line)
- return dict; error/timeout/no-output → `{success: False, reason: "<prefix>_error"}`

From `shared/problem_registry.py` (verified):
- Problem spec: `problem_id`, `reason_keys: tuple[str,...]`. Match function checks `if reason in spec.reason_keys`. 24 problems + `unknown_failure`.
- Missing reason keys to add (from RESEARCH Pitfall 2): `daily_quota_reached`, `expired_service`, `generation_failed`, `language_error`, `no_subscription_data`, `no_topic`, `no_topics`, `no_trade_data`, `prompt_not_found`, `publish_failed`, `write_failed`

From `shared/config_validator.py` (verified, to extend):
- `REQUIRED_BLOG_FIELDS = ["id", "pipeline", "platform"]`, `OPTIONAL_BLOG_FIELDS = [...]`, `VALID_PLATFORMS = [...]`
- Functions: `validate_configs()`, `_validate_blog_entry(blog, source, idx)`
- Extend with the unified schema (Section 4 of PIPELINE-STANDARD.md): standard fields `name/domain/daily_quota/schedule/status/managed_by`, optional extension fields preserved, and `managed_by: pipeline` accepted.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 61-02-1: Wave-0 test scaffolding (test_db.py, test_subprocess_runner.py, test_config_schema.py, test_problem_registry.py)</name>
  <files>
    /Users/twinssn/Projects/5000/tests/shared/test_db.py
    /Users/twinssn/Projects/5000/tests/shared/test_subprocess_runner.py
    /Users/twinssn/Projects/5000/tests/shared/test_config_schema.py
    /Users/twinssn/Projects/5000/tests/shared/test_problem_registry.py
  </files>
  <action>
    Create Wave-0 test files (NEW, additive). These define the expected behavior of the shared modules. Write the tests FIRST so they fail (RED), then implement in the next tasks.

    **`tests/shared/test_db.py`** (covers D-06):
    - `test_db_path_resolves_existing`: importing `shared.db` must resolve `data/content.db` and `data/rap.db`/`data/senior.db` paths via `db_paths.py` (no new files created)
    - `test_get_connection_readonly`: `shared.db.get_connection(db_path)` returns a working `sqlite3.Connection` for an existing DB file without altering it
    - `test_db_paths_preserved`: existing `shared.db_paths` symbols (PUBLISH_LEDGER_DB, ARTICLES_DB) remain importable and unchanged
    - Design `shared.db` to be a thin module over `db_paths.py` + a `get_connection(path)` helper. DO NOT move/delete data files — this is path/connection centralization only (D-06).

    **`tests/shared/test_subprocess_runner.py`** (covers D-03):
    - `test_run_subprocess_returns_dict`: `run_subprocess(root, venv_python, module_path, run_callable_name='run', cfg=None, timeout=600)` invokes the target module's callable in a subprocess and returns its dict result
    - `test_run_subprocess_timeout`: a runner that sleeps past `timeout` returns `{success: False, reason: "<prefix>_timeout"}`
    - `test_run_subprocess_no_output`: a runner printing no JSON returns `{success: False, reason: "<prefix>_no_output"}`
    - `test_run_subprocess_nonzero`: a runner that exits 1 returns `{success: False, reason: "<prefix>_subprocess_error"}`
    - Signature MUST support both STAP (`from pipelines.{name}.pipeline import run; run(cfg)`) and TAP (`from app import run_publish; run_publish()`) call shapes. Use `inspect` to bridge run()/run(cfg) like dispatcher L463-465.

    **`tests/shared/test_config_schema.py`** (covers D-04):
    - `test_required_fields`: a blog entry missing `id`/`pipeline`/`platform` is flagged
    - `test_standard_fields_accepted`: entries with `name/domain/daily_quota/schedule/status/managed_by` validate cleanly
    - `test_optional_extension_fields_preserved`: entries with branch-specific keys (`cf_project`, `ga4_property`, `blogger_blog_id`, `funnel_stage`, `language`, etc.) are accepted (optional, not rejected)
    - `test_managed_by_pipeline_accepted`: `managed_by: pipeline` is a valid value

    **`tests/shared/test_problem_registry.py`** (regression + extension):
    - `test_existing_24_unchanged`: existing P01..P24 reason mappings still resolve (no regression)
    - `test_missing_keys_now_recognized`: each of the 11 unregistered reasons (`daily_quota_reached`, `expired_service`, `generation_failed`, `language_error`, `no_subscription_data`, `no_topic`, `no_topics`, `no_trade_data`, `prompt_not_found`, `publish_failed`, `write_failed`) now maps to a concrete problem_id, NOT `unknown_failure`

    Run tests to confirm they FAIL (RED) before implementing:
    `pytest tests/shared/test_db.py tests/shared/test_subprocess_runner.py tests/shared/test_config_schema.py tests/shared/test_problem_registry.py -x --no-cov`
    (Expected: failures for the new behaviors since modules not yet implemented.)
  </action>
  <verify>
    <automated>pytest tests/shared/test_db.py tests/shared/test_subprocess_runner.py tests/shared/test_config_schema.py tests/shared/test_problem_registry.py -x --no-cov 2>&1 | tail -5</automated>
  </verify>
  <done>Four Wave-0 test files created. They define D-03/D-04/D-06/reason-key behaviors. Tests currently fail (RED) as expected.</done>
</task>

<task type="auto">
  <name>Task 61-02-2: Implement shared/db.py + extend shared/config_validator.py</name>
  <files>
    /Users/twinssn/Projects/5000/shared/db.py
    /Users/twinssn/Projects/5000/shared/config_validator.py
  </files>
  <action>
    **Part A — `shared/db.py` (NEW, D-06):**
    Create a thin central DB-path + connection helper. Use `shared/db_paths.py` as the underlying source of paths — DO NOT replace it, DO NOT rename its symbols, DO NOT move any data file. Keep DB files per-branch (data consolidation is out of scope).

    Expose:
    - `get_db_path(branch: str) -> str` — resolve a per-branch DB path (map `car`→`data/car.db`, `rap`→`data/rap.db`, `senior`→`data/senior.db`, `travel`→`data/travel-en.db`, `curation`→`data/curation.db`, `stock`→`data/stap_content.db`, `etap`→ per-branch map). Reuse existing constants from `db_paths.py` where they exist; add missing branch constants as additions (do not change existing ones).
    - `get_connection(db_path: str) -> sqlite3.Connection` — open a connection with `sqlite3.connect(db_path)`; return the connection.
    - Preserve `shared/db_paths.py` as-is (import + extend, never replace).

    **Part B — extend `shared/config_validator.py` (D-04):**
    Additive extension only. Do NOT change existing required-field/error behavior (existing tests must stay green).
    - Add `STANDARD_BLOG_FIELDS = ["name", "domain", "daily_quota", "schedule", "status", "managed_by"]` and `EXTENSION_BLOG_FIELDS = ["cf_project", "repo", "site_path", "theme", "funnel_stage", "depth_next", "bridge_to", "post_type", "prompt", "ga4_property", "gsc_site", "blogger_blog_id", "force_draft", "language"]` — these are the optional/extension fields per PIPELINE-STANDARD.md Section 4.
    - Add `validate_blog_entry_standard(blog)` that checks standard fields are present/valid AND that any branch-specific key is in the allowed extension set (unknown keys → warning, NOT error, to preserve backward compat).
    - Add `validate_managed_mode(blog)` asserting `managed_by` value is in `("pipeline",)` (allow future values with warning).
    - Wire these into `validate_configs()` so a full `blogs.d/*.yaml` scan emits the standard-schema report WITHOUT rejecting existing configs (non-blocking — report only, per D-08 incremental compat).

    **Commit separation (D-08):**
    - This task = structure/utility addition (new module + config schema validation). Commit as: `feat(phase-61): add shared/db.py and extend config_validator with unified schema`
    - Keep behavior (existing validation paths) unchanged.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('/Users/twinssn/Projects/5000/shared/db.py').read()); ast.parse(open('/Users/twinssn/Projects/5000/shared/config_validator.py').read()); print('AST OK')"</automated>
    <automated>pytest tests/shared/test_db.py tests/shared/test_config_schema.py -x --no-cov 2>&1 | tail -5</automated>
  </verify>
  <done>shared/db.py exists (get_db_path + get_connection), no data moved. config_validator.py extended with standard-schema validation (non-blocking). test_db.py + test_config_schema.py pass.</done>
</task>

<task type="auto">
  <name>Task 61-02-3: Implement shared/subprocess_runner.py + extend shared/problem_registry.py</name>
  <files>
    /Users/twinssn/Projects/5000/shared/subprocess_runner.py
    /Users/twinssn/Projects/5000/shared/problem_registry.py
  </files>
  <action>
    **Part A — `shared/subprocess_runner.py` (NEW, D-03):**
    Centralize the duplicated STAP/TAP subprocess isolation pattern. Extract exactly what `dispatcher.py:_run_stap` (L380-440) and `_run_tap_subprocess` (L504-566) do, into one module with a generalized signature:

    ```python
    def run_subprocess(project_root: str, venv_python: str, module_spec: str,
                       run_callable: str = 'run', cfg: dict | None = None,
                       timeout: int = 600, prefix: str = 'subproc') -> dict
    ```
    - `module_spec` is the dotted import path (e.g., `pipelines.stock.pipeline` or `app`).
    - `run_callable` is the callable name inside the module (`run` for STAP, `run_publish` for TAP).
    - Resolve venv python: use `venv_python` if `os.path.exists`, else `sys.executable`.
    - Build a tempfile runner that: inserts project_root into sys.path, chdirs to project_root, loads dotenv (`.env` in project_root with override, plus optionally the 5000 `.env`), imports the module, and uses `inspect` to bridge `run()`/`run(cfg)`/`run_publish()` per dispatcher L463-465, then prints `json.dumps(result or {"success": False, "reason": "no_result"})`.
    - Run with `subprocess.run([venv, runner_path], capture_output=True, text=True, timeout=timeout, cwd=project_root)`, unlink runner file.
    - Return dict: on returncode != 0 → `{success: False, reason: f"{prefix}_subprocess_error"}`; on no JSON → `{success: False, reason: f"{prefix}_no_output"}`; on TimeoutExpired → `{success: False, reason: f"{prefix}_timeout"}`; on exception → `{success: False, reason: f"{prefix}_error"}`.
    - **CLOUDFLARE_API_TOKEN safety:** before any wrangler invocation inside the runner subprocess, the runner MUST pop `CLOUDFLARE_API_TOKEN` from env (mirror `dispatcher._build_and_deploy_central` and `deploy.py:_deploy_site_inner`). Document this in the module docstring. (Runner only strips it; no wrangler call made in this plan.)
    - Reuse `shared/db.py`/`paths.py` if needed; do NOT create circular imports (subprocess_runner must not import dispatcher).

    **Part B — extend `shared/problem_registry.py` (reason-key vocabulary):**
    Additive: add the 11 unregistered reason keys to the appropriate existing problem specs' `reason_keys`, OR add new problem specs if none fits. Map them to the nearest P-code (per registry header comment). Target mapping (adjust to nearest P-code if needed):
    - `daily_quota_reached`, `quota_met` → the quota problem spec
    - `expired_service`, `no_subscription_data`, `prompt_not_found` → P02 (llm/config related)
    - `generation_failed`, `language_error`, `no_topic`, `no_topics` → the generation/problem spec
    - `no_trade_data` → no-data spec
    - `publish_failed`, `write_failed` → P04/P01 publish/write spec
    - Verify with the test that none of these falls through to `unknown_failure`.
    Do NOT remove or alter existing reason_keys (existing tests must stay green).

    **Commit separation (D-08):** Commit Part A and Part B as SEPARATE commits:
    1. `feat(phase-61): add shared/subprocess_runner.py (subprocess isolation runner)`
    2. `feat(phase-61): extend problem_registry with unregistered reason keys`
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('/Users/twinssn/Projects/5000/shared/subprocess_runner.py').read()); ast.parse(open('/Users/twinssn/Projects/5000/shared/problem_registry.py').read()); print('AST OK')"</automated>
    <automated>pytest tests/shared/test_subprocess_runner.py tests/shared/test_problem_registry.py -x --no-cov 2>&1 | tail -5</automated>
    <automated>cd /Users/twinssn/Projects/5000 && python3 -c "
from shared.problem_registry import lookup_reason
for r in ['daily_quota_reached','expired_service','generation_failed','language_error','no_subscription_data','no_topic','no_topics','no_trade_data','prompt_not_found','publish_failed','write_failed']:
    p = lookup_reason(r)
    assert p is not None and p.problem_id != 'unknown_failure', f'{r} still unknown_failure'
print('ALL 11 REASONS RECOGNIZED')
"</automated>
  </verify>
  <done>shared/subprocess_runner.py exists (run_subprocess, signature-bridged, token-safe). problem_registry.py extended so all 11 previously unregistered reasons map to concrete P-codes. Both test files pass. Existing registry tests stay green.</done>
</task>

<task type="auto">
  <name>Task 61-02-4: Full regression gate + wave merge</name>
  <files>
    /Users/twinssn/Projects/5000/shared/db.py
    /Users/twinssn/Projects/5000/shared/subprocess_runner.py
    /Users/twinssn/Projects/5000/shared/config_validator.py
    /Users/twinssn/Projects/5000/shared/problem_registry.py
  </files>
  <action>
    Run the full test suite to confirm zero regression across the whole project (per wave-merge gate):
    `pytest -q`

    Ensure the four previously committed files are all staged and the working tree reflects the completed shared-base build. Run the plan's regression tests:
    `pytest tests/test_dispatcher_registry.py -x tests/shared/ -q`

    Confirm git log shows the 4 commits from Tasks 61-02-2 and 61-02-3 (structure additions + registry extension as separate commits).
  </action>
  <verify>
    <automated>pytest -q 2>&1 | tail -8</automated>
    <automated>git log --oneline -5 | grep "phase-61"</automated>
  </verify>
  <done>Full suite green. Four shared-module commits present. Wave 2 complete — shared base ready for pilot consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared/subprocess_runner.py → external project subprocess | Untrusted external module runs in isolated subprocess; stdout parsed for dict |
| shared/config_validator.py → blogs.d/*.yaml | Config parsing of branch-specific fields |
| shared/db.py → SQLite files | Path/connection resolution over production DBs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-02-01 | Tampering | `run_subprocess` module_spec | mitigate | `module_spec` is an internal dotted path from dispatcher's controlled registry, not user input; still validate it against `^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$` |
| T-61-02-02 | Information Disclosure | subprocess env | mitigate | Runner pops `CLOUDFLARE_API_TOKEN` before any wrangler subprocess (mirrors deploy.py) |
| T-61-02-03 | Tampering | `config_validator` unknown keys | accept | Unknown keys are warnings (not errors) — non-blocking, preserves backward compat per D-08 |
| T-61-02-04 | Denial of Service | subprocess timeout | mitigate | 600s timeout with `{success:False}` return; runner file always unlinked (try/finally) |
| T-61-02-SC | Tampering | npm/pip installs | accept | No new package installs in this phase (stdlib only) |
</threat_model>

<verification>
### Plan 61-02 verification

1. [ ] `shared/db.py` exists — `ls shared/db.py`
2. [ ] `shared/subprocess_runner.py` exists — `ls shared/subprocess_runner.py`
3. [ ] `config_validator.py` has `STANDARD_BLOG_FIELDS` — `grep "STANDARD_BLOG_FIELDS" shared/config_validator.py`
4. [ ] `problem_registry.py` has `generation_failed` — `grep "generation_failed" shared/problem_registry.py`
5. [ ] All 11 reason keys map to concrete P-codes — `python3 -c` check above
6. [ ] Wave-0 tests pass — `pytest tests/shared/ -q`
7. [ ] Full suite green — `pytest -q`
8. [ ] No data files moved — `git status --porcelain` shows no `data/*.db` changes
</verification>

<success_criteria>
1. ✅ `shared/db.py` — get_db_path + get_connection, DB 파일 무이동
2. ✅ `shared/subprocess_runner.py` — run_subprocess, inspect 브리지, timeout, token-safe
3. ✅ `shared/config_validator.py` — 통합 스키마 검증 (비차단, backward-compat)
4. ✅ `shared/problem_registry.py` — 미등록 11+ reason 인식
5. ✅ 신규 테스트 green + 기존 스위트 green
6. ✅ 구조/동작 분리 커밋, no new packages
</success_criteria>

<output>
Create `.planning/phases/61-pipeline-standardization-branch-renewal/61-02-SUMMARY.md` when done
</output>

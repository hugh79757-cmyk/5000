---
phase: 61-pipeline-standardization-branch-renewal
plan: 06
type: execute
wave: 4
depends_on: [61-03]
files_modified:
  - "/Users/twinssn/Projects/5000/pipelines/curation/fetcher.py"
  - "/Users/twinssn/Projects/5000/pipelines/curation/topic_manager.py"
  - "/Users/twinssn/Projects/5000/pipelines/curation/validator.py"
  - "/Users/twinssn/Projects/5000/pipelines/curation/pipeline.py"
  - "/Users/twinssn/Projects/5000/tests/shared/test_skeleton_curation.py"
autonomous: true
requirements: [61-D, D-01, D-06]
user_setup: []
must_haves:
  truths:
    - "curation (15 blogs, CUAP) exposes the standard 6-module skeleton (pipeline/fetcher/topic_manager/writer/enrich/validator)"
    - "curation run(cfg) return contract unchanged (dict, behavior preserved)"
    - "curation DB path wired through shared/db.py (no data moved)"
    - "All existing curation tests (large suite) and full suite stay green"
  artifacts:
    - path: "pipelines/curation/fetcher.py"
      provides: "Thin fetcher wrapper (NEW — curation lacks fetcher module)"
    - path: "pipelines/curation/topic_manager.py"
      provides: "Thin topic_manager wrapper"
    - path: "pipelines/curation/validator.py"
      provides: "Thin validator wrapper"
  key_links:
    - from: "pipelines/curation/pipeline.py"
      to: "pipelines/curation/enricher.py"
      via: "existing enricher preserved and wired to enrich module"
    - from: "pipelines/curation/pipeline.py"
      to: "shared/db.py"
      via: "DB path centralized"
---

<objective>
Stage D3 — **curation(15개, CUAP) 전환.** curation은 67KB로 프로젝트에서 가장 큰 pipeline.py이며 CUAP 15개 블로그를 담당한다. 표준 골격(D-01)에 정렬한다.

Purpose: 대형 분기에서의 전환 안전성 확인. curation은 이미 `pipeline.py`(run(cfg) dict) + `writer.py` + `enricher.py` + `collector.py`가 있어, 부재 모듈(fetcher/topic_manager/validator)만 얇은 wrapper로 추가.

Output: curation 6-모듈 골격 완비, DB path `shared/db.py` 배선.
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/twinssn/Projects/5000/.planning/STATE.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-CONTEXT.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-RESEARCH.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-03-SUMMARY.md
@/Users/twinssn/Projects/5000/docs/PIPELINE-STANDARD.md
@/Users/twinssn/Projects/5000/pipelines/curation/pipeline.py
@/Users/twinssn/Projects/5000/pipelines/curation/enricher.py
@/Users/twinssn/Projects/5000/pipelines/curation/writer.py
@/Users/twinssn/Projects/5000/pipelines/curation/collector.py

All changes additive. **Existing curation pipeline.py run(cfg) behavior must NOT change** — only wrapper additions + DB path wiring. curation has the largest test coverage (pyproject addopts covers `pipelines/curation`), so regression is critical.

<interfaces>
From `pipelines/curation/` (verified):
- `pipeline.py`: `def run(cfg)` at L836 — dict-returning
- `enricher.py`: exists — content enrichment (entity linking, cross-sell)
- `writer.py`: exists — content writing
- `collector.py`, `keywords.py`, `auto_collector.py`, `keyword_expander.py`, `naver_datalab_sync.py` exist
- MISSING: `fetcher.py`, `topic_manager.py`, `validator.py`
- curation DB file: `data/curation.db`

Note: pyproject `addopts = --cov=shared,pipelines/curation` — this branch is heavily covered. Any change risks coverage/regression breakage. Be minimal and additive.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 61-06-1: Add thin fetcher/topic_manager/validator wrappers to curation</name>
  <files>
    /Users/twinssn/Projects/5000/pipelines/curation/fetcher.py
    /Users/twinssn/Projects/5000/pipelines/curation/topic_manager.py
    /Users/twinssn/Projects/5000/pipelines/curation/validator.py
  </files>
  <action>
    **Pure additive wrapper creation (D-01).** Add three thin wrapper modules to `pipelines/curation/`. Each must DELEGATE to existing curation logic or shared/ code — do NOT rewrite. Behavior unchanged.

    **`pipelines/curation/fetcher.py`** (NEW): data fetching. Inspect `pipelines/curation/collector.py` / `auto_collector.py` for the product/data-source collection logic. Create `fetch_data(cfg) -> list` delegating to the existing collector path (import + passthrough). Do NOT duplicate collection logic.

    **`pipelines/curation/topic_manager.py`** (NEW): topic/keyword selection. Inspect `pipelines/curation/keywords.py` / `keyword_expander.py` for keyword selection. Create `select_topic(cfg)` delegating to the existing keyword selection logic.

    **`pipelines/curation/validator.py`** (NEW): validation. Delegate to `shared/post_validator.py` / `shared/validators.py`. Create `validate_post(cfg, article) -> (ok, errors)` calling the existing shared validator (curation uses validators), or passthrough `(True, [])` if the actual validation lives inline in pipeline.py — in which case delegate to that code path.

    Each wrapper: module docstring noting Phase 61 standard skeleton membership.

    **Commit separation (D-08):** STRUCTURE addition only. Commit:
    `feat(phase-61): add curation standard-skeleton wrappers (fetcher/topic_manager/validator)`
  </action>
  <verify>
    <automated>python3 -c "import ast; [ast.parse(open('/Users/twinssn/Projects/5000/pipelines/curation/'+f).read()) for f in ['fetcher.py','topic_manager.py','validator.py']]; print('AST OK')"</automated>
    <automated>python3 -c "import sys; sys.path.insert(0,'/Users/twinssn/Projects/5000'); from pipelines.curation import fetcher, topic_manager, validator; print('IMPORT OK', hasattr(fetcher,'fetch_data'), hasattr(topic_manager,'select_topic'), hasattr(validator,'validate_post'))"</automated>
  </verify>
  <done>pipelines/curation/{fetcher,topic_manager,validator}.py exist as thin wrappers delegating to existing logic. curation now exposes the 6-module skeleton. Behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 61-06-2: Wire curation DB path to shared/db.py + extend skeleton test</name>
  <files>
    /Users/twinssn/Projects/5000/pipelines/curation/pipeline.py
    /Users/twinssn/Projects/5000/tests/shared/test_skeleton_curation.py
  </files>
  <action>
    **Part A — DB-path wiring (D-06, additive):**
    In `pipelines/curation/pipeline.py`, replace the inline DB path constant usage with `shared.db.get_db_path('curation')` (resolves `data/curation.db`):
    - Add `from shared.db import get_db_path` at import site.
    - Replace the inline path constant reference with `get_db_path('curation')`.
    - **Do NOT move/delete DB files.** If the path resolves identically, publish behavior is identical.
    - Keep the original constant as a fallback if replacement risks behavior change (non-destructive overlay).

    **Commit separation (D-08):** STRUCTURE/utility change. Commit:
    `refactor(phase-61): centralize curation DB path via shared/db.py`

    **Part B — extend skeleton test (D-01):**
    Add `test_curation_skeleton` to `tests/shared/test_skeleton_curation.py` asserting `pipelines.curation` has the 6 module files. Do not invoke run().

    **Commit:** `test(phase-61): add curation skeleton presence test`
  </action>
  <verify>
    <automated>pytest tests/shared/test_skeleton_curation.py -x --no-cov 2>&1 | tail -5</automated>
    <automated>cd /Users/twinssn/Projects/5000 && python3 -c "import sys; sys.path.insert(0,'.'); from shared.db import get_db_path; print('curation db:', get_db_path('curation'))"</automated>
  </verify>
  <done>curation pipeline.py uses shared/db.py for DB path (files unmoved, behavior unchanged). test_skeleton_curation includes curation and passes.</done>
</task>

<task type="auto">
  <name>Task 61-06-3: Rollout regression gate (curation coverage critical)</name>
  <files>
    /Users/twinssn/Projects/5000/pipelines/curation/
  </files>
  <action>
    curation is the most-tested branch (pyproject addopts covers `pipelines/curation`). Run the full suite to confirm zero regression:
    `pytest -q`

    Specifically confirm no curation test regressed:
    `pytest tests/curation/ -q` (or the curation coverage subset).

    Confirm git log shows the curation commits as separate structure/utility commits. Confirm no `data/*.db` changed.
  </action>
  <verify>
    <automated>pytest -q 2>&1 | tail -8</automated>
    <automated>git log --oneline -5 | grep "phase-61"</automated>
  </verify>
  <done>Full suite green after curation conversion. CUAP branch standardized skeleton complete with zero regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wrapper module → existing curation logic | Thin delegation over heavily-tested branch |
| shared/db.py → curation DB | Path resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-06-01 | Tampering | wrapper delegation | mitigate | Wrappers import+delegate existing functions; import smoke test; curation coverage gate |
| T-61-06-02 | Denial of Service | DB path | mitigate | shared/db.py resolves identical existing path; no data move |
| T-61-06-SC | Tampering | npm/pip installs | accept | No new package installs |
</threat_model>

<verification>
### Plan 61-06 verification

1. [ ] curation has 6 modules — `ls pipelines/curation/*.py` includes fetcher/topic_manager/validator
2. [ ] Wrappers import cleanly — `python3 -c "from pipelines.curation import fetcher,topic_manager,validator"`
3. [ ] DB path centralized — `grep -rn "get_db_path" pipelines/curation/pipeline.py`
4. [ ] No data files changed — `git status --porcelain | grep "data/"` empty
5. [ ] Skeleton test passes — `pytest tests/shared/test_skeleton_curation.py -q`
6. [ ] Full suite green — `pytest -q` (curation coverage subset green)
</verification>

<success_criteria>
1. ✅ curation(CUAP, 15) 6-모듈 골격 완비
2. ✅ 부재 모듈 얇은 wrapper로 추가, 기존 로직 위임
3. ✅ DB path `shared/db.py` 배선 (무이동, 무동작변경)
4. ✅ 구조/동작 분리 커밋
5. ✅ curation 커버리지 포함 전체 스위트 green (회귀 0)
</success_criteria>

<output>
Create `.planning/phases/61-pipeline-standardization-branch-renewal/61-06-SUMMARY.md` when done
</output>

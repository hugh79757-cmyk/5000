---
phase: 61-pipeline-standardization-branch-renewal
plan: 03
type: execute
wave: 3
depends_on: [61-02]
files_modified:
  - "/Users/twinssn/Projects/5000/pipelines/senior/topic_manager.py"
  - "/Users/twinssn/Projects/5000/pipelines/senior/enrich.py"
  - "/Users/twinssn/Projects/5000/pipelines/senior/validator.py"
  - "/Users/twinssn/Projects/5000/pipelines/senior/pipeline.py"
  - "/Users/twinssn/Projects/5000/pipelines/rap/topic_manager.py"
  - "/Users/twinssn/Projects/5000/pipelines/rap/enrich.py"
  - "/Users/twinssn/Projects/5000/pipelines/rap/validator.py"
  - "/Users/twinssn/Projects/5000/pipelines/rap/pipeline.py"
  - "/Users/twinssn/Projects/5000/tests/shared/test_pipeline_skeleton.py"
autonomous: true
requirements: [61-C, D-01, D-06]
user_setup: []
must_haves:
  truths:
    - "senior (SEAP, 2 blogs) and rap (RAP, 5 blogs) each expose the standard 6-module skeleton (pipeline/fetcher/topic_manager/writer/enrich/validator)"
    - "Existing modules are preserved and wired to the skeleton; absent modules are added as thin wrappers delegating to existing code"
    - "Both branches still return the same run(cfg) dict as before (behavior unchanged)"
    - "All existing senior/rap tests and the full suite stay green"
    - "Pilot validates the conversion pattern before rollout to car/travel/curation/ETAP"
  artifacts:
    - path: "pipelines/senior/topic_manager.py"
      provides: "Thin topic_manager wrapper (delegates to existing selection logic)"
    - path: "pipelines/senior/enrich.py"
      provides: "Thin enrich wrapper"
    - path: "pipelines/senior/validator.py"
      provides: "Thin validator wrapper"
    - path: "pipelines/rap/topic_manager.py"
      provides: "Thin topic_manager wrapper"
    - path: "tests/shared/test_pipeline_skeleton.py"
      provides: "Skeleton presence smoke test (D-01)"
  key_links:
    - from: "pipelines/senior/pipeline.py"
      to: "pipelines/senior/fetcher.py"
      via: "existing fetch path preserved"
    - from: "pipelines/rap/pipeline.py"
      to: "shared/db.py"
      via: "DB path resolution centralized (D-06)"
---

<objective>
Stage C — **Pilot 전환.** 가장 작은 분기 SEAP(senior, 2개)와 RAP(rap, 5개)를 표준 골격(D-01)에 정렬한다. 두 분기 모두 이미 `run(cfg)->dict` 네이티브이고 `managed_by: pipeline` 이므로, 골격 정렬이 안전한 pilot 대상이다.

Purpose: 전환 패턴을 저위험 분기에서 검증해, rollout(61-04..07)에서 동일 패턴을 안전하게 적용. 구조 변경과 동작 변경을 분리 커밋으로 실습.

Output: senior·rap에 6-모듈 골격 완비 (부재 모듈은 얇은 wrapper로 추가), DB path는 `shared/db.py`로 배선.
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/twinssn/Projects/5000/.planning/STATE.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-CONTEXT.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-RESEARCH.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-01-SUMMARY.md
@/Users/twinssn/Projects/5000/.planning/phases/61-pipeline-standardization-branch-renewal/61-02-SUMMARY.md
@/Users/twinssn/Projects/5000/docs/PIPELINE-STANDARD.md
@/Users/twinssn/Projects/5000/pipelines/senior/pipeline.py
@/Users/twinssn/Projects/5000/pipelines/senior/fetcher.py
@/Users/twinssn/Projects/5000/pipelines/senior/writer.py
@/Users/twinssn/Projects/5000/pipelines/rap/pipeline.py
@/Users/twinssn/Projects/5000/pipelines/rap/fetcher.py
@/Users/twinssn/Projects/5000/pipelines/rap/writer.py

This is the pilot. All changes are additive. **Existing pipeline.py run(cfg) behavior must NOT change** — only structure (new wrapper modules) and DB-path wiring are added.

<interfaces>
From `pipelines/senior/` (verified):
- `pipeline.py`: `def run(cfg)` at L116 — already dict-returning, `managed_by: pipeline`
- `fetcher.py`: exists — data fetching
- `writer.py`: exists — content writing
- MISSING: `topic_manager.py`, `enrich.py`, `validator.py`

From `pipelines/rap/` (verified):
- `pipeline.py`: `def run(blog_cfg)` at L962 — already dict-returning
- `fetcher.py`, `writer.py`, `thumbnail.py`, `keyword_generator.py` exist
- MISSING: `topic_manager.py`, `enrich.py`, `validator.py`

Standard skeleton (D-01): `pipeline.py` / `fetcher` / `topic_manager` / `writer` / `enrich` / `validator`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 61-03-1: Add thin topic_manager/enrich/validator wrappers to senior (SEAP)</name>
  <files>
    /Users/twinssn/Projects/5000/pipelines/senior/topic_manager.py
    /Users/twinssn/Projects/5000/pipelines/senior/enrich.py
    /Users/twinssn/Projects/5000/pipelines/senior/validator.py
  </files>
  <action>
    **Pure additive wrapper creation (D-01).** Add three thin wrapper modules to `pipelines/senior/`. Each wrapper must:
    - DELEGATE to existing senior logic or shared/ code — do NOT rewrite generation logic.
    - NOT change any existing module's behavior.
    - Have a module docstring stating it's part of the Phase 61 standard skeleton.

    **`pipelines/senior/topic_manager.py`** (NEW): topic selection responsibility. Inspect `pipelines/senior/pipeline.py` for the current topic-selection logic (likely a function that picks a topic/fetch source). Create a `select_topic(cfg)` that either:
    - re-uses the existing selection function (import + re-export / thin wrapper), or
    - if selection is embedded inline in pipeline.py, delegate to it via a small passthrough that calls the existing code path.
    Do NOT duplicate the selection logic — import and delegate.

    **`pipelines/senior/enrich.py`** (NEW): enrichment responsibility. If senior has any content enrichment (entity linking, keyword injection), create `enrich_content(cfg, body_md) -> body_md` delegating to the existing shared/entity_linker or pipeline inline step. If senior has no enrichment, create a passthrough `enrich_content(cfg, body_md) -> body_md` that returns body_md unchanged (documenting "senior has no enrichment step").

    **`pipelines/senior/validator.py`** (NEW): validation responsibility. Delegate to existing `shared/post_validator.py` / `shared/validators.py`. Create `validate_post(cfg, article) -> (ok: bool, errors: list[str])` that calls the existing shared validator, or a passthrough returning `(True, [])` if senior uses no validator.

    **Commit separation (D-08):** These are STRUCTURE additions only (new wrapper modules, no behavior change). Commit as:
    `feat(phase-61): add senior standard-skeleton wrappers (topic_manager/enrich/validator)`
  </action>
  <verify>
    <automated>python3 -c "import ast; [ast.parse(open('/Users/twinssn/Projects/5000/pipelines/senior/'+f).read()) for f in ['topic_manager.py','enrich.py','validator.py']]; print('AST OK')"</automated>
    <automated>python3 -c "import sys; sys.path.insert(0,'/Users/twinssn/Projects/5000'); from pipelines.senior import topic_manager, enrich, validator; print('IMPORT OK', hasattr(topic_manager,'select_topic'), hasattr(enrich,'enrich_content'), hasattr(validator,'validate_post'))"</automated>
  </verify>
  <done>pipelines/senior/{topic_manager,enrich,validator}.py exist as thin wrappers delegating to existing logic. Senior now exposes the 6-module skeleton. Behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 61-03-2: Add thin topic_manager/enrich/validator wrappers to rap (RAP)</name>
  <files>
    /Users/twinssn/Projects/5000/pipelines/rap/topic_manager.py
    /Users/twinssn/Projects/5000/pipelines/rap/enrich.py
    /Users/twinssn/Projects/5000/pipelines/rap/validator.py
  </files>
  <action>
    **Pure additive wrapper creation (D-01).** Same pattern as Task 61-03-1 for `pipelines/rap/`. Each wrapper must delegate to existing rap logic or shared/ code — do NOT rewrite. Behavior must not change.

    **`pipelines/rap/topic_manager.py`** (NEW): topic selection. Inspect `pipelines/rap/pipeline.py` / `keyword_generator.py` for the current topic/keyword selection. Create `select_topic(cfg)` delegating to the existing logic.

    **`pipelines/rap/enrich.py`** (NEW): enrichment. RAP uses `thumbnail.py` + entity linking. Create `enrich_content(cfg, body_md) -> body_md` delegating to existing enrichment steps; passthrough if none.

    **`pipelines/rap/validator.py`** (NEW): validation. Delegate to `shared/post_validator.py` / `shared/validators.py` or a passthrough returning `(True, [])` if rap uses no standalone validator.

    **Commit separation (D-08):** STRUCTURE addition only. Commit as:
    `feat(phase-61): add rap standard-skeleton wrappers (topic_manager/enrich/validator)`
  </action>
  <verify>
    <automated>python3 -c "import ast; [ast.parse(open('/Users/twinssn/Projects/5000/pipelines/rap/'+f).read()) for f in ['topic_manager.py','enrich.py','validator.py']]; print('AST OK')"</automated>
    <automated>python3 -c "import sys; sys.path.insert(0,'/Users/twinssn/Projects/5000'); from pipelines.rap import topic_manager, enrich, validator; print('IMPORT OK', hasattr(topic_manager,'select_topic'), hasattr(enrich,'enrich_content'), hasattr(validator,'validate_post'))"</automated>
  </verify>
  <done>pipelines/rap/{topic_manager,enrich,validator}.py exist as thin wrappers delegating to existing logic. RAP now exposes the 6-module skeleton. Behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 61-03-3: Wire senior/rap DB path to shared/db.py + add skeleton smoke test</name>
  <files>
    /Users/twinssn/Projects/5000/pipelines/senior/pipeline.py
    /Users/twinssn/Projects/5000/pipelines/rap/pipeline.py
    /Users/twinssn/Projects/5000/tests/shared/test_pipeline_skeleton.py
  </files>
  <action>
    **Part A — DB-path wiring (D-06, additive):**
    In `pipelines/senior/pipeline.py` and `pipelines/rap/pipeline.py`, replace the inline `*_DB_PATH` constant usage with `shared.db.get_db_path(...)`:
    - Add `from shared.db import get_db_path` (or `get_connection`) at the module import site.
    - Replace the inline path constant reference with `get_db_path('senior')` / `get_db_path('rap')`.
    - **Do NOT move/delete the DB files.** `shared/db.py` resolves the SAME path — this is pure centralization.
    - **Do NOT change any other pipeline.py behavior.** If the path resolves identically, publish behavior is identical.
    - If replacing the constant risks behavior change, keep the constant as a fallback and add the `shared/db.py` resolution alongside, documenting the wiring. Prefer a non-destructive overlay: use `get_db_path()` value but preserve the original constant for compatibility.

    **Commit separation (D-08):** This is a structure/utility change (path resolution). Commit separately from any behavior change:
    `refactor(phase-61): centralize senior/rap DB path via shared/db.py`

    **Part B — skeleton smoke test (D-01):**
    Create `tests/shared/test_pipeline_skeleton.py` (NEW) that asserts each standardized branch exposes the 6 standard skeleton modules:
    - `test_senior_skeleton`: `pipelines.senior` has pipeline/fetcher/topic_manager/writer/enrich/validator (via importlib + hasattr on the package's module files, or `pkgutil.iter_modules` on `pipelines.senior.__path__`)
    - `test_rap_skeleton`: same for `pipelines.rap`
    - `test_skeleton_run_contract`: `pipelines.senior.pipeline.run` and `pipelines.rap.pipeline.run` are callable (smoke — do not invoke, may need network/DB)
    - The test should verify module-file existence, not invoke run() (which may require live services).

    **Commit:** `test(phase-61): add pipeline skeleton presence test`
  </action>
  <verify>
    <automated>pytest tests/shared/test_pipeline_skeleton.py -x --no-cov 2>&1 | tail -5</automated>
    <automated>cd /Users/twinssn/Projects/5000 && python3 -c "
import sys; sys.path.insert(0,'.')
from shared.db import get_db_path
print('senior db:', get_db_path('senior'))
print('rap db:', get_db_path('rap'))
"</automated>
  </verify>
  <done>senior/rap pipeline.py use shared/db.py for DB path (files unmoved, behavior unchanged). test_pipeline_skeleton.py added and passes for senior+rap.</done>
</task>

<task type="auto">
  <name>Task 61-03-4: Pilot regression gate (full suite)</name>
  <files>
    /Users/twinssn/Projects/5000/pipelines/senior/
    /Users/twinssn/Projects/5000/pipelines/rap/
  </files>
  <action>
    Run the full test suite to confirm the pilot introduced zero regression:
    `pytest -q`

    Confirm git log shows the pilot commits (wrapper additions + db wiring + skeleton test) as separate commits per D-08. Confirm no `data/*.db` files were changed (`git status --porcelain` clean of data files).
  </action>
  <verify>
    <automated>pytest -q 2>&1 | tail -8</automated>
    <automated>git log --oneline -6 | grep "phase-61"</automated>
  </verify>
  <done>Full suite green after pilot. SEAP/RAP standardized skeleton validated. Pilot pattern confirmed for rollout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wrapper module → existing pipeline logic | Thin delegation — if wrapper mis-delegates, behavior could change |
| shared/db.py → production DB | Path resolution over live DB files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-03-01 | Tampering | wrapper delegation | mitigate | Wrappers import+delegate existing functions; smoke test confirms imports resolve; no logic duplicated |
| T-61-03-02 | Denial of Service | DB path mis-resolution | mitigate | shared/db.py resolves identical existing paths (no data move); test_db.py asserts path resolution |
| T-61-03-SC | Tampering | npm/pip installs | accept | No new package installs |
</threat_model>

<verification>
### Plan 61-03 verification

1. [ ] senior has 6 modules — `ls pipelines/senior/*.py` includes topic_manager/enrich/validator
2. [ ] rap has 6 modules — `ls pipelines/rap/*.py` includes topic_manager/enrich/validator
3. [ ] Wrappers import cleanly — `python3 -c "from pipelines.senior import topic_manager,enrich,validator; from pipelines.rap import topic_manager,enrich,validator"`
4. [ ] DB path centralized — `grep -rn "get_db_path" pipelines/senior/pipeline.py pipelines/rap/pipeline.py`
5. [ ] No data files changed — `git status --porcelain | grep "data/"` empty
6. [ ] Skeleton test passes — `pytest tests/shared/test_pipeline_skeleton.py -q`
7. [ ] Full suite green — `pytest -q`
</verification>

<success_criteria>
1. ✅ senior(SEAP)·rap(RAP) 6-모듈 골격 완비
2. ✅ 부재 모듈은 얇은 wrapper로 추가, 기존 로직 위임
3. ✅ DB path `shared/db.py` 배선 (파일 무이동, 동작 무변경)
4. ✅ 구조/동작 분리 커밋
5. ✅ 기존 테스트 + 신규 skeleton 테스트 green
6. ✅ Pilot 패턴 검증 완료 → rollout 확신
</success_criteria>

<output>
Create `.planning/phases/61-pipeline-standardization-branch-renewal/61-03-SUMMARY.md` when done
</output>

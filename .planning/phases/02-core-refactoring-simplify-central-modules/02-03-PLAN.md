# Plan 02-03: Backup File Cleanup

**Phase:** 2 — Core Refactoring: Simplify Central Modules
**Goal:** Delete backup files and harden .gitignore
**Mode:** mvp
**Depends on:** none

## Objective

Delete all `.bak*` variants from source directories and harden `.gitignore` to prevent future backup file pollution.

## Requirements

- STB-03: `.bak*` files cleaned, gitignore covers all variants

## Tasks

### 3.1 Create cleanup script (`scripts/cleanup_bak.sh`)
- Dry-run mode: find and list all .bak files (preview before deletion)
- Execute mode: remove them
- Target patterns: `*.bak*`, `*.bak_*`, `*.v1_bak`, `*.fix_bak`, `*-bak`, `*.feat-bak`, `*.dedup-bak`, `*.final_bak`, `*.topic-bak`, `*_bak`, `*.ev-bak`, `*.map-bak`

### 3.2 Run dry-run and review
- Execute `scripts/cleanup_bak.sh --dry-run` to preview
- Verify no important files are caught in the net

### 3.3 Execute cleanup
- Run `scripts/cleanup_bak.sh`
- Verify only .bak* / backup files were removed

### 3.4 Harden `.gitignore`
- Add backup patterns currently not covered
- Verify with `git check-ignore` test

### 3.5 Verify
- No .bak* files remain in source directories
- .gitignore covers all backup variants
- All existing tests pass
- CI passes

## Verification

- [ ] Zero `.bak*` files in source directories
- [ ] `.gitignore` covers all backup variants
- [ ] Existing tests still pass
- [ ] CI passes

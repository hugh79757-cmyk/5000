# Plan 02-02: Publisher Decomposition

**Phase:** 2 — Core Refactoring: Simplify Central Modules
**Goal:** Split 952-line publisher.py into focused modules
**Mode:** mvp
**Depends on:** Plan 02-01 (same phase, independent — no block)

## Objective

Decompose `shared/publisher.py` (952 lines, 20+ functions) into a `shared/publishers/` package with modules organized by concern. Keep original `publisher.py` as re-export hub for backward compatibility.

## Requirements

- STB-08: publisher.py split into focused modules

## Tasks

### 2.1 Create `shared/publishers/` package
- Create `shared/publishers/__init__.py`

### 2.2 Extract `shared/publishers/hugo_writer.py`
- Move: `_build_frontmatter_congo`, `_build_frontmatter_papermod`, `_build_frontmatter_blowfish`, `_write_hugo_post`
- Keep imports working

### 2.3 Extract `shared/publishers/deploy.py`
- Move: `deploy_site`, `_deploy_site_inner`
- Include deploy lock logic from dispatcher.py? (deferred — keep in dispatcher.py per D-02)

### 2.4 Extract `shared/publishers/content_enhancer.py`
- Move: `_insert_coupang`, `_insert_internal_links`, `_get_related_posts`, `_inject_related_cards_midpoint`, `_inject_related_cards`, `_extract_first_image`, `_extract_description`
- Keep imports consistent

### 2.5 Update `shared/publisher.py` as re-export hub
- Import all functions from submodules
- Re-export everything so existing imports continue working
- Mark as deprecated in docstring

### 2.6 Verify
- Run all existing tests
- Verify imports work: `from shared.publisher import deploy_site` still works
- `ruff check .` and `mypy .` pass

## Verification

- [ ] `shared/publishers/` package exists with 4 modules
- [ ] `from shared.publisher import X` still works for all exports
- [ ] All existing tests pass
- [ ] CI passes

# Plan 03-03: External Dependency Isolation

**Phase:** 3 — Hardening: Stability & Decoupling
**Goal:** Isolate TAP/STAP/ETAP dependencies behind clear interfaces
**Mode:** mvp
**Depends on:** Plan 03-01 (paths must be env-driven first)

## Objective

Wrap TAP/STAP/ETAP subprocess calls with try/except at call site. On failure: log clear error message with setup instructions, skip the affected blog. The rest of 5000 continues running.

## Requirements

- STB-10: External project coupling isolated

## Tasks

### 3.1 Add wrapper functions
- `_try_run_tap()` in dispatcher.py — catches missing TAP, logs help message
- `_try_run_stap()` — wraps `_run_stap()`, handles missing STAP gracefully
- Keep existing subprocess logic, just add try/except at call sites

### 3.2 Update entity linker imports
- `shared/publisher.py`: TAP entity manager import (lines 44-48) — already wrapped, verify message clarity
- `shared/publishers/content_enhancer.py`: STAP/TAP paths — already ref'd to env vars via Plan 03-01

### 3.3 Verify
- Remove TAP/STAP access → dispatcher logs clear error, skips, continues
- All tests pass

## Verification

- [ ] Missing TAP/STAP produces clear error with setup instructions
- [ ] Missing TAP/STAP doesn't crash dispatcher
- [ ] Non-TAP/STAP pipelines run normally when TAP/STAP missing

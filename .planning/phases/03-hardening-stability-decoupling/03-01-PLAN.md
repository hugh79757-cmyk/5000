# Plan 03-01: Path Configuration — Env-Driven Project Roots

**Phase:** 3 — Hardening: Stability & Decoupling
**Goal:** Replace 133 hardcoded paths with environment variables
**Mode:** mvp

## Objective

Replace all `/Users/twinssn/...` hardcoded paths with env vars: `5000_ROOT`, `STAP_ROOT`, `TAP_ROOT`, `ETAP_ROOT`, `LAP_ROOT`, `CUAP_ROOT`. Binary paths (`HUGO_PATH`, `WRANGLER_PATH`) also env-driven with `which` fallback. Create central path resolver.

## Requirements

- STB-02: Hardcoded paths eliminated

## Tasks

### 1.1 Create `shared/paths.py`
- `project_root(name)` function: checks `{NAME}_ROOT` env var, falls back to common env
- Path constants: `5000_ROOT`, `STAP_ROOT`, `TAP_ROOT`, `ETAP_ROOT`
- Binary resolvers: `hugo_path()`, `wrangler_path()` (env → `which` → hardcoded fallback)

### 1.2 Update `~/.env.common`
- Add `5000_ROOT`, `STAP_ROOT`, `TAP_ROOT`, `ETAP_ROOT`, `LAP_ROOT`, `CUAP_ROOT`
- Add `HUGO_PATH`, `WRANGLER_PATH` (optional — `which` handles it)

### 1.3 Migrate all hardcoded paths
- `dispatcher.py` — TAP path, tap_root, shared-themes path
- `shared/publisher.py` — STAP paths, PROJECT_DIR
- `shared/publishers/deploy.py` — hugo/wrangler paths, deploy log path, shared-themes path
- `shared/publishers/content_enhancer.py` — STAP DB path
- `scheduler.py` — 5000 path references

### 1.4 Verify
- `ruff check .` and `mypy .` pass
- All 43 tests pass
- Zero `/Users/twinssn` hardcoded paths remain in source code

## Verification

- [ ] Zero `/Users/twinssn` references in `shared/`, `dispatcher.py`, `scheduler.py`
- [ ] All paths use `shared/paths.py` resolver or env vars
- [ ] `$ which hugo` works without hardcoded path
- [ ] All tests pass

---
phase: 10-blowfish-engagement-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: 
  - config/prompts/travel.yaml
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "AI-generated camping articles include `{{< alert icon="info" >}}` callout boxes for pet policy and facility tips"
    - "AI-generated camping articles include `{{< badge >}}캠핑타입{{< /badge >}}` inline badge for camping type (e.g., 글램핑, 카라반, 일반야영장)"
    - "The badges reflect the actual `induty` field value from API data"
  artifacts:
    - path: "config/prompts/travel.yaml"
      provides: "tour1_camping prompt with alert + badge shortcode instructions"
      contains: "alert"
    - path: "config/prompts/travel.yaml"
      provides: "tour1_camping prompt with badge shortcode instructions"
      contains: "badge"
  key_links:
    - from: "AI system prompt"
      to: "generated body_md"
      via: "tour1_camping prompt instructions"
      pattern: "alert.*badge"
---

<objective>
Add P1 shortcode (alert + badge) instructions to the AI prompt for tour1_camping.

**Purpose:** Instruct the AI to generate `{{< alert icon="info" >}}` callout boxes for pet policy and facility tips, and `{{< badge >}}캠핑타입{{< /badge >}}` badges for camping type (글램핑, 카라반, 일반야영장).

**Output:**
- `config/prompts/travel.yaml` — tour1_camping system and user prompt updated with shortcode instructions
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-blowfish-engagement-optimization/10-CONTEXT.md

@config/prompts/travel.yaml (lines 1-180: tour1_camping system prompt, lines 94-180: tour1_camping user prompt)

### Key Data Fields Available to AI via `_build_data_block()`

From `pipelines/travel/writer.py` (lines 176-268), camping items are passed with these fields:
- `induty`: 캠핑장 업종 ("글램핑", "카라반", "일반야영장", "자동차야영장" etc.)
- `animalCmgCl`: 반려동물 ("가능" / "불가능")
- `sbrsCl`: 부대시설 ("전기,무선인터넷,장작,온수,놀이터,산책로,..." comma-separated)
- `gnrlSiteCo`, `autoSiteCo`, `glampSiteCo`, `caravSiteCo`: 사이트 수
- `toiletCo`, `swrmCo`, `wtrplCo`: 편의시설 개수
- `facltNm`: 캠핑장명 (item name)

### Current Prompt Structure

**System prompt** (line 1-93): Sections for [톤], [ABSOLUTE BAN], [ANTI-HALLUCINATION], [숫자 환각 금지], [TITLE-BODY CONSISTENCY], [문장 완성], [분량 규칙]

**User prompt** (line 94-180): Section structure — 도입부, `## {region} {theme} {count}곳 한눈에 비교`, `## 캠핑장별 상세 정보`, `## {theme} 선택 시 체크포인트`, `## 방문 전 준비사항`, 마무리

### Blowfish Shortcodes to Instruct

- **alert:** `{{< alert icon="info" >}}반려동물 동반이 가능한 캠핑장입니다. 다만, 목줄과 배변봉투는 필수로 준비하세요.{{< /alert >}}` — renders info-type callout box
- **badge:** `이 캠핑장의 유형은 {{< badge >}}글램핑{{< /badge >}}입니다.` — renders inline badge/pill
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add alert shortcode instructions to tour1_camping system prompt</name>
  <files>config/prompts/travel.yaml</files>
  <action>
    Add a new section `[SHORTCODE — alert]` to the tour1_camping system prompt (between `[분량 규칙]` and the final closing `'` at line 93).

    The section should instruct the AI to use `{{< alert icon="info" >}}...{{< /alert >}}` for:
    1. **반려동물 정리** — When `animalCmgCl` field says "가능" or "불가능", wrap the pet policy info in an alert callout box
    2. **시설 팁** — When a facility (from `sbrsCl`) is noteworthy (e.g., "온수" available, "놀이터" for kids), wrap the tip in an alert box
    3. **예약 참고** — When reservation tips are mentioned (e.g., weekend booking, seasonal operation)

    Added text:
    ```yaml
      
    [SHORTCODE — alert]
    
    캠핑장 정보 중 반려동물 정리(animalCmgCl), 시설 팁, 예약 참고사항을 전달할 때
    아래 alert shortcode를 사용하세요:
    
    {{< alert icon="info" >}}반려동물 동반이 가능한 캠핑장입니다. 목줄과 배변봉투는 필수로 준비하세요.{{< /alert >}}
    
    사용 규칙:
    - 각 글에서 최대 2회까지 사용 (1회 권장)
    - 문장 중간이 아닌, 단락 시작이나 끝에 배치
    - 중첩 사용 금지 (alert 안에 alert 넣지 않음)
    ```
  </action>
  <verify>
    <automated>python3 -c "
import yaml
with open('config/prompts/travel.yaml') as f:
    prompts = yaml.safe_load(f)
system = prompts['tour1_camping']['system']
assert 'alert' in system, 'alert shortcode instruction missing'
assert '{{< alert' in system, 'alert syntax missing'
assert 'animalCmgCl' in system, 'animalCmgCl reference in alert missing'
print('[PASS] tour1_camping system prompt contains alert shortcode')
"</automated>
  </verify>
  <done>
    tour1_camping system prompt includes alert shortcode section with usage rules and examples
  </done>
</task>

<task type="auto">
  <name>Task 2: Add badge shortcode instructions to tour1_camping user prompt</name>
  <files>config/prompts/travel.yaml</files>
  <action>
    Add badge shortcode usage instruction to the tour1_camping user prompt. The badge should be applied to the camping type (`induty` field).

    Insert in the "H2 section structure" area of the user prompt. The best placement is in the `## 캠핑장별 상세 정보` section (around line 126-138), where per-camp H3 content is described.

    Add to the list of items to include per camp:
    ```yaml
    
       - 업종(업종: induty) 정보를 캠핑장명 첫 줄에 badge shortcode로 표시:
         ### {캠핑장명} {{< badge >}}글램핑{{< /badge >}}
         - induty 값이 "글램핑"이면 {{< badge >}}글램핑{{< /badge >}}
         - induty 값이 "카라반"이면 {{< badge >}}카라반{{< /badge >}}
         - induty 값이 "일반야영장"이면 {{< badge >}}일반야영장{{< /badge >}}
         - induty 값이 "자동차야영장"이면 {{< badge >}}오토캠핑{{< /badge >}}
    ```
    
    Placement: Insert after line 128 (`- 포함해야 할 정보: 위치/접근성, 핵심 시설, 주변 환경, 예약 참고사항`) and before line 129 (`- 단, 모든 캠핑장을 동일한 순서로 서술 금지`).

    Also add the induty→badge mapping as a general rule in the system prompt's `[SHORTCODE — badge]` section:
    ```yaml
    
    [SHORTCODE — badge]
    
    캠핑장 업종(induty) 정보를 표시할 때 badge shortcode를 캠핑장명 옆에 표시하세요:
    
    ### {캠핑장명} {{< badge >}}글램핑{{< /badge >}}
    
    induty 매핑:
    - "글램핑" → {{< badge >}}글램핑{{< /badge >}}
    - "카라반" → {{< badge >}}카라반{{< /badge >}}
    - "일반야영장" → {{< badge >}}일반야영장{{< /badge >}}
    - "자동차야영장" → {{< badge >}}오토캠핑{{< /badge >}}
    ```
  </action>
  <verify>
    <automated>python3 -c "
import yaml
with open('config/prompts/travel.yaml') as f:
    prompts = yaml.safe_load(f)
system = prompts['tour1_camping']['system']
user = prompts['tour1_camping']['user']
assert 'badge' in system or 'badge' in user, 'badge shortcode instruction missing'
assert '{{< badge' in system or '{{< badge' in user, 'badge syntax missing'
assert 'induty' in system or 'induty' in user, 'induty mapping missing'
print('[PASS] tour1_camping prompts contain badge shortcode')
"</automated>
  </verify>
  <done>
    tour1_camping user prompt includes badge shortcode instructions in the per-camp H3 section, system prompt includes induty→badge mapping
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI prompt → AI output → Hugo build | Prompt instructions influence AI output; shortcodes must be syntactically valid for Hugo build to succeed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | AI prompt injection via data | accept | Data fields (`induty`, `animalCmgCl`) are structured API values, not free text. Low risk of closing-tag injection. |
| T-10-06 | Denial of Service | Malformed shortcode breaks Hugo build | mitigate | `_clean_body()` regex preserves `{{< ... >}}` but `{{< /if >}}` etc. are not valid Blowfish shortcodes. Post-processor guard in `_write_hugo_post()` can strip unknown shortcodes. |
| T-10-07 | Spoofing | AI generates wrong badge mapping | accept | Badge text mirrors `induty` field value. If AI maps incorrectly, the rendered badge shows wrong type. Low impact (cosmetic). |
</threat_model>

<verification>
## Verification

- [ ] `python3 -c "import yaml; yaml.safe_load(open('config/prompts/travel.yaml')); print('YAML OK')"` — YAML syntax valid
- [ ] `python3 -c "from shared.prompt_builder import build; p=build('tour1_camping', 'test'); print('OK')"` — prompt builds without error
- [ ] System prompt contains `[SHORTCODE — alert]` section with example
- [ ] System prompt contains `[SHORTCODE — badge]` section with induty→badge mapping
- [ ] User prompt contains badge instruction in `## 캠핑장별 상세 정보` H3 structure
- [ ] `_clean_body()` regex preserves `{{< alert ... >}}` and `{{< badge >}}` in generated content
- [ ] Other blog prompts (tour2_food, tour3_course, etc.) are NOT modified
- [ ] Safety: Hugo build with generated shortcodes does not produce build errors
</verification>

<success_criteria>
Measurable completion:
1. tour1_camping system prompt includes alert + badge shortcode sections with examples
2. tour1_camping user prompt includes badge placement instructions in per-camp H3 structure
3. `prompt_builder.build('tour1_camping', ...)` loads without YAML error
4. Only travel.yaml is modified — other prompt files untouched
</success_criteria>

<output>
Create `.planning/phases/10-blowfish-engagement-optimization/10-02-SUMMARY.md` when done
</output>

# 블로그 분기별 안정화 & 재발행 계획

## 0. 원칙
- "중단"은 목적이 아니라 수단. 최종 목표는 (1) 잘못된 글 숨김/수정 후 재발행, (2) 글쓰기 로직 정비.
- cuap 분기(10개 안정화)에서 검증된 패턴을 다른 분기로 확산한다.
- 분기 단위로 격리(isolate)하여 작업 → 한 분기가 안정화될 때까지 다른 분기는 건드리지 않는다.

## 1. 현황 파악 (Discovery) — 반나절
목표: 각 분기별 발행 상태, 글 품질, 파이프라인 구조를 한눈에 본다.

- [ ] BLOG_STATUS.md / STATE.md 최신화: 분기별(cuap, food, coding 등) 상태 표로 정리
      | 분기 | 발행 수 | 정상 | 문제 | 상태(안정/중단/작업중) |
- [ ] dispatcher.py / scheduler.py 에서 "발행 스케줄"을 분기별로 켜고 끄는 플래그 확인
- [ ] content/ 하위 디렉토리 구조로 분기 매핑 (분기별 글 개수 카운트)
- [ ] cuap 안정화 때 무엇을 고쳤는지 diff/커밋 로그로 회고 → "체크리스트화"

## 2. 발행 일시중단 (Freeze) — 분기별 스위치
목표: 코드 삭제 없이, 스케줄러 레벨에서 분기별 발행만 끈다.

- [ ] scheduler.py / config 에 분기별 enable 플래그 도입 (예: PUBLISH_ENABLED = {cuap: true, food: false, ...})
- [ ] 크론/워커가 flag=false 인 분기는 발행 스킵하도록 가드 추가
- [ ] cuap 은 계속 발행 유지, 나머지는 false
- 주의: 이미 발행된 글은 그대로 둔다(숨김은 3단계에서 개별 판단).

## 3. 잘못된 글 처리 (Triage) — 분기별 반복
목표: 발행된 글을 3분류하여 조치한다.

분류 기준:
- KEEP: 문제 없음 → 유지
- FIX: 사실오류/제목-본문 불일치/포맷 깨짐 → 수정 후 재발행
- HIDE: 심각(중복, 저품질, 정책 위반) → 즉시 숨김(draft=true 또는 플랫폼 unpublish)

- [ ] 분기별 글 목록 CSV/스프레드시트로 export (URL, 제목, 발행일, 분류, 사유)
- [ ] HIDE 우선 처리: draft: true 로 전환 후 hugo 재빌드 or 플랫폼 API로 unpublish
- [ ] FIX: cuap 체크리스트 기준으로 일괄 점검 → 수정 → 재발행
- [ ] 조치 결과를 BLOG_STATUS.md 에 반영

## 4. 글쓰기 로직 정비 (Root Cause) — 가장 중요
목표: 같은 문제가 재발하지 않도록 프롬프트/파이프라인을 고친다.

- [ ] prompts/ 안의 분기별 프롬프트 점검: cuap 에서 통한 프롬프트 구조를 템플릿화
- [ ] pipelines/ 글 생성 → 검증 → 발행 단계에서 "검증(validation) 단계" 강화
      - 제목/본문 일치 검사
      - 최소 길이/구조(H2, 요약) 검사
      - 중복 주제 검사
      - 사실성/링크 유효성 검사
- [ ] 검증 실패 시 자동 발행 금지 → draft 로 보류 → 사람이 리뷰
- [ ] 분기별 차이는 config 로 분리, 공통 로직은 shared/ 로 통합

## 5. 재개 (Rollout) — 분기별 순차
- [ ] 한 분기 안정화 완료 → 소량(2~3개)만 시험 발행 → 24h 모니터링
- [ ] 이상 없으면 해당 분기 PUBLISH_ENABLED = true
- [ ] 다음 분기로 이동 (2~5단계 반복)

## 6. 재발 방지 (Guardrails)
- [ ] logs/ 기반 발행 후 자동 QA 리포트 (매일)
- [ ] "새 글 발행 전 검증 통과 필수" 를 파이프라인 기본값으로
- [ ] STATE.md 를 단일 진실 소스(single source of truth)로 유지

## 작업 순서 요약
1) 현황표 작성 → 2) 분기별 발행 OFF(cuap 제외)
→ 3) 분기 하나 골라 Triage(HIDE/FIX) → 4) 프롬프트·검증 로직 정비
→ 5) 시험발행 후 재개 → 다음 분기 반복

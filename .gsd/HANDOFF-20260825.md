# 핸드오프 문서 — 2026-08-25 Resume Work

> 이 문서를 읽고 순서대로 실행하세요. `resume work` 시 첫 번째로 참조.

---

## 즉시 실행 (06:00+)

### 1. CUAP keyword_pool 최종 검증
```bash
ls /Users/twinssn/Projects/5000/logs/harvest_20260825.log && echo "✅" || echo "❌"
grep -c "INSERT\|saved" /Users/twinssn/Projects/5000/logs/harvest_20260825.log 2>/dev/null
sqlite3 /Users/twinssn/Projects/5000/data/curation.db "SELECT COUNT(*) FROM keyword_pool WHERE used=0;" 2>/dev/null || echo "table missing"
grep "keyword_expander.*completed" /Users/twinssn/Projects/5000/logs/scheduler.log | grep "2026-08-25" | tail -3
grep "no_keyword" /Users/twinssn/Projects/5000/logs/scheduler.log | grep "2026-08-25"
pgrep -f scheduler.py && echo "ALIVE" || echo "DEAD"
```
판정: 5개 모두 ✅ → CUAP closure 커밋. 하나라도 ❌ → scheduler.log 03:00 구간 확인.

### 2. CUAP closure 커밋 (검증 통과 시)
```bash
cd /Users/twinssn/Projects/5000
mkdir -p .planning/worklog
cat > .planning/worklog/WL-20260825-cuap-final-closure.md << 'WLEOF'
# CUAP Final Closure
- keyword_pool created: ✅
- harvest INSERT >0: ✅
- expander <1800s: ✅
- no_keyword=0: ✅
- api_call_log ≤8/hr: ✅
WLEOF
git add -A && git commit -m "docs: CUAP project closure — keyword_pool verified"
```

## 오전 작업 (우선순위순)

### 3. runner.py 연동 — health check 대시보드 반영
```bash
cd /Users/twinssn/Projects/5000
# 기존 체크 등록 패턴 확인
find ops_dashboard -name "*.py" | xargs grep -l "register\|CHECKS\|run_all" | head -5
# 패턴에 맞게 topic_pool_health 등록
# 등록 후 테스트: .venv/bin/python -m ops_dashboard.runner (또는 해당 진입점)
```

### 4. placeholder 이미지 19건 교체
```bash
# 실제 R2 버킷: pub-2f5c7af1c303419a933069212bc25874.r2.dev
# 각 slug별 cover.webp 업로드 후 교체
for dir in /Users/twinssn/Projects/CAP/pick-hugo/content /Users/twinssn/Projects/RAP/rap2-hugo/content /Users/twinssn/Projects/RAP/rap4-hugo/content; do
  find "$dir" -name "*.md" -exec grep -l "default-thumbnail" {} \;
done
# 교체: sed "s|default-thumbnail.webp|https://pub-2f5c7af1c303419a933069212bc25874.r2.dev/<branch>/<slug>/cover.webp|"
# → 각 파일의 slug 추출하여 Pexels fetch + R2 upload + sed 교체 + build + deploy
```

### 5. STAP stock_issue 활용 촉진
```bash
# ipo 파이프라인에서 source 우선순위 확인
grep -n "source\|priority\|ORDER BY" /Users/twinssn/Projects/STAP/pipelines/ipo*.py | head -10
# etf 우선 → stock_issue 포함하도록 수정 (또는 round-robin)
```

## 오후 작업

### 6. expander 전분기 확대 (sustainability 구현 시작)
```bash
# 현재 CUAP만 적용된 expander를 ETAP/CAP에 확장
grep -rn "expander\|keyword_expander" /Users/twinssn/Projects/5000/config/ | head -10
# config/expander.yaml에 ETAP/CAP 블로그 추가
```

### 7. deals body image 검증
```bash
curl -sL "https://deals.techpawz.com/posts/flight-deals-from-los-angeles-v2/" | grep -oP 'src="[^"]*r2\.dev[^"]*"' | head -5
# body 이미지도 destination별로 다른지 확인
```

## 상태 요약

| 항목 | 상태 | 차단 여부 |
|------|------|-----------|
| Scheduler | PID 56215 ALIVE | - |
| CUAP keyword_pool | 03:00 대기 | 내일 06:00 확인 |
| 규격 통일 | 38/42 배포 완료 (stock SKIP, pick/rap2/rap4 이미지 주입 완료) | - |
| 토픽 고갈 | ETAP/CAP 긴급 리셋 완료, MAN-004 작성 | health check runner 미연동 |
| deals 썸네일 | dest 기반 수정 완료, 4건 regenerate 완료 | body image 확인 필요 |
| .gsd/SESSION.md | .gitignore 제거 필요 | git add -f |

## 참조 파일

- .planning/designs/topic-pool-sustainability.md — 영구 해결 설계
- docs/manuals/MAN-004-topic-exhaustion.md — 토픽 고갈 매뉴얼
- /tmp/golden_unification_report.md — 규격 통일 diff 보고서
- ops_dashboard/checks/topic_pool_health.py — health check 코드
- .gsd/SESSION.md — 전체 세션 이력

## 커밋 이력 (오늘)

| Hash | 내용 |
|------|------|
| a3a3e7b2c | SESSION CUAP awaiting |
| 3d70d614a | golden unification live 38 blogs |
| a20735725 | topic-pool-sustainability + SESSION + deals_writer |
| 78d9dfac8 | SESSION + design + W5 |
| 15db174e7 | airports +200, W5 R2 image |
| b176b8549 | deals cover dest기반 |
| cc652d517 | watersports 200 + W5 exempt |
| d739d9561 | MAN-004 + health check |

- ops_dashboard/checks/run_all_checks.py 생성 완료 — cron 또는 scheduler 연동 필요 (check_results_custom 테이블 사용)
